# Decision Log

The append-only ledger of architectural decisions (ADRs) and implementation
decisions, one per `Decision:` line below (numbered by entry). The design these
decisions produced is specified in **[SPEC.md](SPEC.md)**; the values they obey
are in **[AGENTS.md](AGENTS.md)**. References to "FEATURES §N" in the reasoning
below are historical (FEATURES.md is retired into SPEC.md) and are kept verbatim
as the record of what each decision overturned.

Decision: Kill the separate visibility/hide axis; row existence is governed by `read`, while other capabilities (write/admin/subscribe) remain runtime grants. Reason: "invisible" was a third outcome that had to be derived and reconciled against grant/deny, and "visible be different to editable feels arbitrary" — the second axis shouldn't exist. A denied read removes the row from the result set; absent-vs-forbidden is recovered by a prod server-side log + a dev-mode "this exists, but you wouldn't know that in production" error.
Decision: Drop the `admits(...)` check wrapper; `scope(...)` declares read intent by calling plain-function checks. Reason: `admits` was a magic marker serving the compiler, not developer intent, and hand-applying what the framework can derive is a leak. Crucially, "can compile to SQL" must not auto-admit a read (an `archived` fact compiles but must not be world-readable) — read intent is declared, never derived from compilability. A check used in `scope` that cannot compile to SQL is a load-time error — never a silent JS fallback, never a warning.
Decision: Field access is always runtime `.can`, never a compiled SQL `scope`. Reason: the row is already materialized when a field rule runs, so a field cannot filter rows in a WHERE. Field read-denial returns a typed `withheld` marker (not silent absence, not a row-failing 403) in production, and a dev-mode diagnostic carrying the field path + deny reason; field edit-denial is a hard write reject. This is the deliberate asymmetry with the row level, where read IS the compiled scope.
Decision: A field with no `.can` strong-inherits the row grant — readable exactly when the row is readable; edit floor = the row grant's write capabilities. Reason: one field floor — strong inheritance from the row grant by default (a private row stays private; this is row-grant-inherit-by-default, not public-open); a stricter field-read is an explicit `.can` or `fieldAccess:{default:ownerOnly}` override for security-sensitive entities, never a competing default. "If it has both, it should error" applies.
Decision: Live event delivery is NOT a third grant method; `grant` stays `scope(...).can(...)` (exactly two halves). Reason: "delivery" was two concerns fused — (1) re-authorization, which is the existing scope+can engine re-run at emit (the "no second auth path" gate, latched/cached for scale), and (2) subscriber interest, which is not authorization at all — it is a connection-transient narrowing filter (e.g. viewport) whose input is neither principal nor row, so it cannot live on grant without breaking scope's SQL-compilable purity. Interest runs only after re-authorization, is structurally narrowing-only (keep/drop, no fetch, no OR), is data-not-code (a typed constraint expression over plugin-published coordinates, validated at subscribe time), and is indexable. The IMPLEMENTATION-PLAN's "delivery is a THIRD grant axis" is overturned.
Decision: Declarative `effects` are bounded, in-transaction, effect-principal reentrancy — not `afterSave` reborn, not unbounded reactive fixpoint. Reason: a cross-entity effect is a compiled mutation re-entering the one pipeline, so the project's "one batch / one transaction / one composed event" forces it into the SAME transaction as the triggering mutation (target grant/validation failure rolls back the origin; a separate saga boundary would be a second pipeline with different failure semantics, forbidden). Reentrancy is BOUNDED: a declared-effect graph is statically knowable because effects use typed handles (target = entity handle, trigger = typed event handle — this is the third reason to kill magic strings, since without typed handles static cycle detection is impossible), so a structural cycle is a LOAD-TIME error (same discipline as a non-compilable `scope`), with a runtime depth cap as a fail-closed backstop that aborts the whole batch on overflow. The triggering mutation's effect runs as an EFFECT PRINCIPAL, not the triggering user (which would force widening the target's grant to admit the source's actors) and not the ambient SYSTEM god-principal (which, with interpolated `data:{recipient:delta.member}`, could write any field of any entity for an arbitrary recipient — an integrity/confidentiality risk) — its capability is bounded to exactly the declared (target entity + template fields), it is authorized against the TARGET's own grant (target stays sovereign; a target deny rolls back the batch), and its data is interpolated only from the trigger delta + origin row. A composed event is atomic for COMMIT but not for DELIVERY: re-auth-at-emit (ADR #5) runs per subscriber per source-entity-tagged fragment, so a Doc-only subscriber sees the Doc fragment and an Inbox-only subscriber sees the Inbox fragment — no new rule, no all-or-nothing delivery (which would leak). A cross-store effect target is a load-time error (cannot share one DB transaction; never silently degrade to a separate boundary). The grammar collapses `{set}`(self) and `{create}`(cross-entity) into one primitive `{ mutate: <target>, with: <data-template> }`; the engine decides set vs create from whether the target row exists.
Decision: An entity that declares no `grant` is a load-time error — never silently private-by-creator, never world-readable. Reason: authorization is never magic and never guessed; the developer must name *who* reads. The grilled "fail-closed / private-by-default" and "no-magic-owner / authorization is always a function, never a static value" principles collide at the zero-state: auto-injecting an `owner` ref + a creator principal would reify "owner" as a universal concept (the very universalizing the predicates model rejects) and hide authorization behind a magic default; defaulting to "creator-only via a hidden `createdBy`" is still an injected column the app didn't declare. The honest resolution is fail-closed at its purest — refuse to load an entity with no grant, same discipline as a non-compilable check in `scope`. The cost is the ~3-line zero-floor promised in FEATURES §6 ("no grant + no access → framework default = owner=all, else hide()"); that promise is overturned — `hide()` is dead (ADR #1) and the zero-to-one default is dead (this ADR). The floor is now "declare an entity + declare a grant, however short."
Decision: Out-of-band side effects (webhooks, emails, external HTTP) are projections over the committed event log, NOT a new effect primitive. Reason: FEATURES §7 left this open — the grilled `effects` primitive covers in-transaction DB mutations only. The scope workbench (a shipped event-sourced framework; see SCOPE-FINDINGS.md) answers it without a new primitive: after a transaction commits, the framework fans its events out to projection consumers, each independently durable, so a projection failure never rolls back the committed action. The two effect kinds split exactly on the atomicity boundary. An in-transaction effect (`{ mutate, with }`) MUST be atomic with its origin — it re-enters the one pipeline in the same transaction, and a target deny rolls the origin back (ADR #6). An out-of-band effect MUST NOT be atomic with its origin — it leaves the process and cannot join the DB transaction, so forcing it into the transaction would make an unreachable webhook roll back a committed mutation. Therefore it runs as a post-commit consumer of the committed log: it fires after commit, is retried on its own schedule, and never rolls back the origin. This reuses the committed log the durable pipeline already maintains (no new primitive — subtract before add) and keeps the "one reconciliation path" invariant intact (a projection reads the log; it is not a second write path). FEATURES §7's "Out-of-band side effects … NOT yet designed" is overturned.
Decision: Field types are an open registry of named-whole plugin contracts, not a hardcoded catalog. Reason: the v2 stress test (minecraft chunk = ~4913 internally-keyed sub-records, photo-editor raster.crdt, drawing-canvas polyline.crdt, 5 apps needing ordered lists) showed the baked-in `text/number/boolean/state/map/set/log/ref` catalog plus the privileged `text.crdt()` special case could not express real field shapes without each becoming its own ad-hoc mechanism. One registry, FOUR named-whole contracts distinguished by genuinely distinct diff+index+inverse machinery (a `kind` enum that could grow optional surfaces would re-form the flag lattice the "pipeline variants are named wholes" rule forbids): `fieldType.value` (single stored value), `fieldType.store` (internally-keyed owned collection with per-key diff + index + range-query — what `map` could not be, since map is keyed-UNORDERED), `fieldType.crdt` (custom merge with per-element deltas — `text.crdt()` becomes one instance of this, deleting the baked-in special case), and `fieldType.ordered` (fractional-index keyspace for atomic `insertAt`/`move`/`reorder` without renumbering — earned by the deletion test: delete it and atomic reorder has nowhere to attach, leaving the "map-as-ordered-array fake" 5 apps currently hand-roll). `blob`/`bytes`, `json(shape)` (typed sub-object, path-queryable via opt-in index), and `list` ship as built-ins. **Coordinates are CONSTRUCTED FROM declared indexes** — a `coordinate.box3(...)` takes the `index.range([...])` as its constructor argument, so a coordinate with no backing index is *unrepresentable* (a load-time type error), not detected-after-the-fact; this is fail-closed one level earlier than "validate every coordinate has an index," and it is what keeps the plugin out of hand-SQL (the plugin declares index *capability*; the entity/field instantiation selects the backing index; the engine owns materializing + compiling SQL — no plugin ever emits SQL). Custom CRDT-authoring *toolkit* is deferred (ship `text.crdt`/`polyline`/`raster` as the proof; proactive ≠ exhaustive).
Decision: Time-driven mutations are a typed **source** feeding the one pipeline, with two public nouns because a deadline and a recurring loop are different things (the naming rule: distinguish similar-but-different concepts). `schedule.at(dateField)` / `schedule.after(anchor, delay)` are one-shot deadlines (library overdue at `dueAt`, blog scheduled-publish at `publishedAt`, todo reminders — the gap `state.auto` could not express, since `state.auto` is fixed-duration from state-entry, not a per-row date field); `tick.hz(n)` / `tick.every(...)` are recurring loops (the 20–30Hz game loop space-invaders/minecraft need). `state.auto` and entity-TTL demote to *sugar* over `schedule.after` / `schedule.at` — three reserved roadmap items collapse into one time-source system with two sugars. A tick/schedule runs as a **bounded scheduler/tick principal** (not the ambient SYSTEM god), admitted by the target's own grant via an `effectSource(handle)`/`principalFrom(handle)` check the developer declares — the clock is a new *trigger*, never a new *authority* (no second auth path). The scheduler MUST index its `when`/`while` discovery predicates (a non-compilable `while` is a load-time error, the tick-layer analog of a non-compilable `scope` — otherwise due-row discovery is a full-table scan every pass, the `findAll` leak at scale); a non-compilable `while` is allowed only as a post-discovery runtime guard when some other indexed discovery source exists. An empty `while` is forbidden for row-set ticks (the "run on ALL rows forever" foot-gun) and legal only for singleton/explicit-finite handles. Tick durability is a field-type persistence-strategy decision (ephemeral = no durable events; coalesced-snapshot-with-seq-span for high-frequency persisted fields so sequence-cursor replay stays correct; per-event for the rare case) — the tick is the clock, what persists is the field plugin's call.
Decision: Domain identities (library Patron, blog Reader, game Player) are sub-account entities owned by `User` via a typed FK, NOT new principal types — the principal-type union stays closed (`user | link | system | anonymous`). Reason: a Patron is a User wearing a domain hat, not a fourth kind of authentication; reopening the union ad hoc per app is the "universalizing" ADR #7 rejects. Every domain identity HAS an account — there is no account-less "email user" pseudo-identity for authenticated actors (a blog reader who is only an email subscriber still has an account; it just has no password and logs in via an email link or similar passwordless flow). A `User` may own MULTIPLE sub-account entities (one-to-many: a RuneScape-style clone has one sign-in but many character-accounts, each with its own team permissions); the principal resolves to the active sub-account, and `is.<role>()` checks traverse the typed FK principal→SubAccount→role in one compiled JOIN (G2.1). This MUST be easy for the developer (declare the sub-account entity + the identity FK; the framework hydrates the binding — not app middleware, which would be the inline-ownership debt relocated). `anonymous` is a SEPARATE first-class principal type for UNAUTHENTICATED public-read (reddit front page, blog published posts) — also `{type:'anonymous',id:null}`, capability-bounded, admitted only by checks that don't reference identity (`published`), never a `publicRead` flag. `everyone()` is a compiled SQL `TRUE` constant (NULL-safe, symmetric to `never()`=FALSE), a value the developer wrote — never the NULL-unsafe `entity.id.is(entity.id)` tautology, never a derived admission. Per-verb route-gate opt-out (`r.resource({ gate: { list: allowAnonymous(), create: requireUser() } })`) relaxes ONLY the route gate (session→principal) for named verbs; the row grant runs on every verb regardless — two default-on layers intact, no second auth path, fail-closed default-empty.
Decision: Stored computed fields have TWO named modes, split on the same atomicity boundary as effects — collapsing to "all post-commit with staleness" is a false economy that would force a vote-count to be eventually-consistent for no reason. `projected.inline` updates inside the originating transaction when the dependency is in the same batch and the compute is cheap+synchronous (a post's `hotRank` from in-row `score`/`commentCount`/`createdAt` — transactionally consistent so the visible-page sort key never mis-ranks; it is just an in-transaction effect targeting a derived field, covered by ADR #6). `projected.async` is a post-commit projection over the committed log with a sequence watermark and explicit staleness — for expensive/async/external compute that MUST NOT join the DB transaction: thumbnails, rendered images, and **embeddings for a search index** (scope requires this now, so `projected.async` is build-now, not deferred — it is the motivating case). Both projection writes go through a **bounded projection principal admitted by the target's own grant** — a "framework-reserved internal write path" is exactly the second write path AGENTS.md forbids and is rejected. `derived` stays strictly for pure synchronous read-time pull (a `wordCount`); `projected` ≠ `derived` (distinct names for distinct concepts). Out-of-band side effects (webhooks/emails) remain ADR #8 projections over the same committed log — `projected.async` is the in-framework read-model case of the same primitive.
Decision: Effects grow by typed field-plugin operators and bounded fan-out, NOT by a new effect grammar or general callbacks. Reason: `inc`/`dec`/`append`/`push`/`insertAt`/`move` are FIELD-PLUGIN OPERATORS (G0.1) the effect names in `with` — the effect layer borrows the field layer's operators instead of inventing its own mutation language (key deletion-test win: deletes a bespoke effect-mutation grammar). `inc(delta.direction)` references the target's own current value, which the effect principal already has authority over (it's mutating that field) — acceptable as read-modify-write owned by the field operator, NOT generalized into arbitrary target reads (which would widen beyond ADR #6's "data interpolated only from trigger delta + origin row"). `when` guards are typed predicates over delta+origin (not I/O; non-compilable = load-time error). The effect-principal admission is made EXPLICIT and verified at LOAD TIME: every declared cross-entity effect must have a matching `effectSource(handle)`/`principalFrom(handle)` admitting check on its TARGET (the load-time cycle-detector already walks the typed effect graph, so it verifies the handshake — a missing admit is a load-time error, never a silent runtime rollback). Owned-collection fan-out (`mutate: many(Target, { over: Origin.collection })`) is build-now — it traverses typed FKs from the origin row, staying inside ADR #6's bound with structurally-knowable, depth-capped cardinality (blog subscriber-notify). Typed compound trigger groups `effect.anyOf(handleA, handleB, handleC)` are build-now — finite, statically-known typed triggers, fully cycle-detectable (NOT the wildcard "any field on any child" string-glob, which reintroduces unbounded-fixpoint risk and is rejected). DEFERRED: arbitrary indexed-query fan-out and `recomputeFrom(query)` inside in-transaction effects — the fan-out cardinality becomes a runtime query result (not a structurally-bounded traversal), widening beyond ADR #6's bound; deferred until an app proves it needs it AND a safety case extends the ADR. General arithmetic beyond `inc`/`dec` deferred (no app needs it).
Decision: Subscribe interest is field-keyed, AND-only, over plugin-published coordinates — ADR #5's concept now has an invocation surface. `subscribe(Entity, id, { fields, pace })` is the client export; the interest object is keyed by *field handle*, and a field NOT listed is pass-through (a `name:changed` event is not tested against a `chunks` viewport interest). Interest grammar, constrained by indexability (the named law): AND across dimensions; per dimension, ONE continuous `range(lo,hi)` OR ONE finite set `.in([a,b,c])` (compiles to indexed IN — categorically NOT the forbidden cross-dimension OR, which is a Cartesian union no composite index answers) OR `.is(v)`; NO cross-dimension OR, NO closures (data-not-code, may not read principal/row, narrowing-only). Subscribe-time validation with a subscribe-time error on any unpublished/non-indexed coordinate (same discipline as `scope`). Reuses the sequence-cursor replay core (bootstrap-snapshot-then-stream ordering, gap/duplicate/next unchanged). Backpressure is TWO-LAYER: the field-type plugin publishes a lawful coalescer + sequence-span reducer + named pace-profiles (it knows position is loss-tolerant and block-edits aren't — the *data semantics* decide what's lawful to drop); the subscriber selects `pace.coalesce({window,by})` or a profile WITHIN the plugin-permitted bounds (data-not-code, runs after re-auth+interest, narrowing-only — a spectator and an in-game player may legitimately want different temporal policy for the same field without reopening "interest is a closure"). Scalar fields default included; high-volume coordinate-published collection fields are OPT-IN (omission = not-subscribed, never unfiltered firehose — the foot-gun).
Decision: Query predicates share the compiler with scope-predicates but never auto-admit a read (compilability ≠ read intent — ADR #2 leak guard). `findTree` compiles to a recursive CTE over the declared self-FK (FK-aware, with row `scope` in the WHERE) — deletes the fetch-all-then-build-tree-in-JS pattern (reddit 3000 comments, todo subtasks). Cursor pagination is `range` over a compound key (reuses the interest `range`). `.isNull()` is a query predicate (read half) compiling to SQL `IS NULL` — DISTINCT from `.is(undefined)` (a scope predicate, authz half, = FALSE) per the "distinguish similar-but-different concepts" rule; they live in different halves and do not collide. `unique([f1,f2]).where(...)` covers the genuine compound-with-partial-predicate case a keyed field cannot dissolve (library: one active Checkout per item+patron). Geo (`.near()`) and full-text (`.match()`) are index-gated predicate PLUGINS — the seam ships NOW so a query for them never degrades to raw SQL (no second query path); the actual rtree/FTS engines are deferred until google-photos is the active spine (build the seam, not the subsystem). `projected` field naming per its own ADR; `projected.inline` sort keys are cursor-paginable because they are materialized + indexed.
Decision: The async `is.*` guard catches an un-awaited check at the GRANT-DECISION boundary (a runtime backstop) plus load-time static analysis of `.can`/`scope` bodies (the primary guard) — NOT by intercepting boolean coercion of the check's return value, which JavaScript makes impossible. Reason: the first RED test for Phase 0 proved the originally-imagined mechanism cannot exist. `ToBoolean(object)` is always `true` and invokes NO hook — `||`, `&&`, `!`, `if (...)`, and the ternary test position all use `ToBoolean`, none of them call `Symbol.toPrimitive`/`valueOf`/`toString`. So a check that returns a guarded thenable cannot make `is.author() || is.blogOwner()` throw at the moment of coercion; the un-awaited `||` still yields the first (truthy) promise. The achievable guard has two layers, fail-closed at each: (1) RUNTIME BACKSTOP — the grant engine inspects the value a `.can(fn)`/`scope` body resolves to; if that value is itself a thenable (the body did `return is.owner()` or `return is.a() || is.b()` without `await`), it is an `UnawaitedCheckError`, never a silent grant. This catches every case where the un-awaited check escapes as the decision. (2) LOAD-TIME STATIC ANALYSIS (primary, the "~1 week Phase 0" of SPEC §13) — parse `.can`/`scope` function bodies at entity-load and reject any `is.*` call not lexically inside an `await`, same discipline as a non-compilable `scope` (load-time error, never a runtime surprise). The static pass is the real guard because it catches the foot-gun mid-expression before any request runs; the runtime backstop covers dynamically-constructed predicates the static pass cannot see. This corrects the Phase 0 framing in SPEC §13 ("the async is.* thenable foot-gun ... is guarded") with the mechanism that is actually possible. The token coercion-throw mechanism (Symbol.toPrimitive on the thenable) is REJECTED — it gives false confidence (passes a `!x` test, silently fails the `x || y` case).
Decision: `inherit(Parent, { via })` takes the compiled parent ENTITY OBJECT, not a string name — `inherit(Doc, { via: 'doc' })`, never `inherit('Doc', ...)`. No entity registry, no global name namespace. Reason: resolving a string `'Doc'` to the real compiled `Doc` record requires a global registry (every `entity()` self-registers by name), which introduces one shared namespace where two entities named `'Doc'` collide, and forces lazy/deferred resolution because entities are defined in arbitrary order. Passing the object directly makes the dependency explicit and order-enforced (the parent MUST be defined above the child — a clean, honest dependency edge), eliminates the registry concept entirely (subtract before add), and lets the inherit compiler reach the parent's harvested scope AST synchronously at the child's load time. Cost accepted: the string form every `projects/*` exemplar currently writes (`inherit('Doc', {via})`) is now wrong and must be migrated to the object form; circular parent/child grant inheritance becomes unrepresentable (acceptable — grant inheritance is a DAG by nature). The mechanism: a child with `grant: inherit(Parent, {via})` compiles to a correlated `EXISTS (SELECT 1 FROM Parent AS jN WHERE jN.id = t0.<via> AND (<parent scope re-lowered under jN>))`. The parent's scope is re-lowered (not string-substituted) under a fresh subquery alias by retaining the parent's harvested scope AST on its compiled record as `scopeAst` — the AST is the durable artifact, the SQL one rendering of it. The single `principalId` placeholder threads through the shared param state so `bindReadScope` fills it once for the whole join tree regardless of depth. Deletion-test PASS: `compileInheritScope` reuses `lowerToSql` + one new `join` AST node — it concentrates (the same lowering serves own-scope and inherited-scope), it does not relocate behind a config name.
Decision: A field's persistence strategy ({validate, apply, diff}) is resolved by `kind` from ONE framework-owned table (`STRATEGIES` in src/field-strategy.mjs), NOT embedded as a strategy object on each descriptor. A descriptor carries only its `kind` string (as before); the mutation pipeline calls `resolveStrategy(descriptor.kind)` at runtime. Reason: the four named-whole contracts (value/store/crdt/ordered, ADR #9) own genuinely distinct diff/apply machinery, but that machinery is shared across every instance of a kind — every value-kind field (text/ref/boolean/date) uses the same whole-value diff. Embedding the strategy object inline would duplicate identical functions on every descriptor and RELOCATE the machinery into a per-field config object (the AGENTS deletion-test failure mode). Keying by kind makes the kind ABSORB the strategy (deletion-test PASS: kind concentrates the machinery, the app declaration stays clean — you write text()/text.crdt(), never name a strategy). Matches SPEC §7.2 "the field-type plugin owns the persistence strategy". An unknown kind fails closed in resolveStrategy (no silent default strategy). Phase 1 scope: `value` is fully implemented (apply replaces whole value; diff is {set} or null when Object.is-unchanged) — the blog spine's text/ref/boolean/date. crdt/store/ordered register as named wholes with a working structural `validate` but their `apply`/`diff` merge throws a loud Phase-2 error (per-element deltas / fractional index land with live delta broadcast) — fail closed loudly, never a silent mis-merge, when the merge seam is engaged before its machinery ships.
Decision: validate is pipeline stage 1 (SPEC §7), implemented as `validateMutation(entityRecord, payload)` throwing a typed `ValidationError` (named, extends Error) that names `Entity.field` + the reason, BEFORE any apply/persist/emit — a bad payload never proceeds (fail closed). Reason: stage 1 must abort the whole pipeline on a bad payload, and a throw is the same fail-closed shape every other load/dispatch guard uses (no second non-throwing reject path). validateMutation runs, per payload key: (1) the kind's structural validator (the type the kind can store — a boolean field given 'yes' is rejected), then (2) the descriptor's declared `validate` fn if present (returns true | error-string). A payload key that is not a declared field is rejected ("not a declared field" — fail closed, a mutation may only touch declared fields). Fields ABSENT from the payload are untouched (partial update — a payload touching only `published` does not trip `title`'s validate). Returns the payload unchanged on success so the stage composes left-to-right. Exported from src/index.mjs (resolveStrategy, validateMutation, ValidationError). Suite 66→76 (+10, test/field-strategy.test.mjs), all GREEN.
Decision: The per-verb route gate (SPEC §6.2, ADR #20) is built from authorization FUNCTIONS — `requireUser()` and `allowAnonymous()` each return a `(principal) => boolean`, never a magic word (AGENTS: authorization is always functions). `requireUser()` admits any non-anonymous principal (user/link/system are authed) and rejects `anonymous`; `allowAnonymous()` admits everyone. `resolveRouteGate({verb: gateFn})` normalizes a declared partial map into a full per-verb gate over the five standard verbs (list/read/create/update/remove = ROUTE_VERBS), filling every UNLISTED verb with the default-on `requireUser()` — the route gate is default-on, opt-out is per-verb and names the capability that GRANTS access (allowlist, not denylist). A non-function gate value (a magic word like 'public') and an unknown verb name (typo guard) are both load-time throws — fail closed. `routeGateFor(resolved, verb, principal)` is the admission decision the dispatcher calls per request; an unknown verb cannot be admitted (no route to admit it to) — fail closed. Reason: this is the FIRST of the two default-on auth layers; it decides route admission (session→principal), NOT row visibility — the row grant (SQL scope + .can) still runs on EVERY verb regardless, so relaxing `list` to allowAnonymous() admits anonymous to the list ROUTE but the grant still filters which rows it returns. No second auth path: the gate never substitutes for the grant. This is the mechanism that replaces the dead `publicRead` flag (anonymous + everyone() + per-verb gate). Boundary note: src/route-gate.mjs ships the gate resolution + admission PRIMITIVES (Todo B); WIRING a declared `r.resource({gate})` from an entity's `routes` into a mounted route belongs to Todo C (the workbench app + router + route builder `r`), where the gate composes with mount/dispatch. Suite 76→85 (+9, test/route-gate.test.mjs), all GREEN.
Decision: Todo C (the `workbench` app + `router` + route builder `r`) lands as the RESOLUTION layer only — `app.mount(path, Entity)` resolves a declared entity into an INSPECTABLE routing table (a list of frozen `{ method, path, verb, entity, gate }` entries), NOT a live HTTP server. `.listen(port)` records the port and returns the app (chainable) so the exemplar call shape `workbench().mount('/notes', Note).listen(3000)` works; the real node:http socket + req/res dispatch + the baked-in middleware stack (sessions, body-parse, error handler, SPEC §3) are deferred to Phase 2 when a live transport is needed anyway. Reason (user decision via question tool, hobbyist A/B framing): the architecturally interesting risk in Todo C is the WIRING — does a declared entity + its `routes:(r)=>r.resource({gate})` resolve into a table that maps each of the five CRUD verbs to {its resolved gate, its entity, its method+path}, with unlisted verbs defaulting to requireUser() and the Todo B per-verb gate wired in — and that is fully provable against principals without standing up a socket (keeps the suite zero-dep + fast). Standing up real HTTP now would pull session hydration, req.user, and DB-backed CRUD handlers (much of it Phase-2 transport work) into Phase 1 prematurely. Mechanism (src/app.mjs, NEW ~140 lines): `workbench()` (default export) and `router(opts)` share a `makeMountable` core exposing `.mount(path, target)` + an accumulating `.routes` array; `mount` resolves a compiled entity via `buildResourceRoutes(entity, base)` (invokes `entity.routes(r)` if a function, else auto-CRUDs via a default `r.resource()`), or re-bases an already-resolved router mini-app's routes under the parent path (`rebaseRoute`). The route builder `r.resource({gate})` expands the five verbs (RESOURCE_VERBS: list GET /base, create POST /base, read GET /base/:id, update PATCH /base/:id, remove DELETE /base/:id) and runs `options.gate` through `resolveRouteGate` (Todo B) so each route carries its resolved `(principal)=>boolean`. `router({mergeParams:true})` carries `mergeParams` so a child mounted under a parametric parent path (`/:docId/notes`) can read the parent param; a router is mounted bare into a parent app with `app.mount(path, router)` (re-bases every child route). Default export + `router` re-exported from src/index.mjs. Deletion-test PASS: the route builder ABSORBS the per-verb gate resolution (one `resolveRouteGate` call per resource concentrates the five-verb wiring) — it does not relocate gate machinery behind a config name; the gate stays an authorization function on every route. Suite 85→93 (+8, test/app.test.mjs), all GREEN.
31: Decision: A value-kind field's value→stored-cell mapping is owned by the field-type STRATEGY as a `serialize(value, descriptor)` step, NOT coerced inline by the scope compiler. The `value` strategy serializes: boolean→1/0 (integer; SQLite has no boolean type and node:sqlite REFUSES to bind a JS boolean — it throws ERR_INVALID_ARG_TYPE), Date→epoch millis, text/ref/already-stored→passthrough, null/undefined→untouched. A new exported helper `serializeField(descriptor, value)` resolves the descriptor's strategy and defers to its serialize (a kind without serialize passes through). The scope compiler's `makeFieldsProxy` calls serializeField when baking a value literal (`fields.published.is(true)` mints an `eq` node whose value is already the stored integer 1) and for every element of `.in([...])`; `bindReadScope` is now EXPORTED from src/index.mjs so the read path can bind a principal into a compiled scope and run it against real node:sqlite. Reason (user decision via question tool, hobbyist A/B framing): the blog acceptance spine surfaced a real Phase-1 gap — `fields.published.is(true)` put a raw JS boolean into the SQL params and node:sqlite blew up. Something must convert true→1/false→0 at the boundary. Putting it in the field-type strategy (SPEC §7.2 "the field-type plugin owns the persistence strategy") means ONE place decides "how a value-kind field becomes a stored cell," and BOTH the scope-literal baker (reads) and the future write path call the SAME serialize — the singular-system / one-reconciliation-path rule. The rejected alternative (coerce inline in the SQL lowerer) splits that decision across the read-literal site and the future write path, a drift-prone second path. Mechanism deletion-test PASS: serialize ABSORBS into the existing strategy table keyed by kind (no new config object); the scope proxy simply routes its literal through the kind that already owns the field. Suite 93→98 (+4 serializeField tests in field-strategy.test.mjs: boolean→1/0, text/ref passthrough, Date→epoch, null/undefined untouched; +1 scope-sql.test.mjs: published.is(true) baked as integer 1 AND executed against node:sqlite selecting published-only for anonymous, published+own-draft for the author), all GREEN.
32: Decision: The Phase-1 ACCEPTANCE test is the blog-platform spine (test/blog-acceptance.test.mjs) — a real Blog → Post → Comment chain declared with ONLY built constructs and exercised end-to-end against a real node:sqlite DatabaseSync DB, asserting that each entity resolves AND its bound SQL scope selects EXACTLY the right rows per principal (anonymous vs author vs other). Blog uses scope(()=>everyone()) (public metadata, owner writes); Post uses the user-chosen published-flag scope anyOf(fields.published.is(true), is.author()) (published OR author can read, author writes); Comment uses inherit(Post, {via:post}) OBJECT form (parent declared above child, DECISIONLOG #26) so its readability is its parent Post's — a comment on a draft is hidden, a comment on a published post is visible. The app mounts the spine (workbench().mount('/blogs',Blog).mount('/posts',Post).mount('/posts/:postId/comments',router-of-Comment).listen(3000)) and asserts the resolved routing table keeps both default-on auth layers: Post relaxes ONLY its list verb to allowAnonymous() (public index) while create/read/update/remove stay default-on, and Comment routes re-base under the parent post path. Reason: the spine is the deletion-test for the WHOLE Phase-1 foundation at once — it proves entity compile + field strategies + serialize + grant SQL scope + inherit JOIN + bindReadScope + app route resolution compose into a working app declared the way an app author actually writes it (the note.mjs/comment.mjs exemplar shape), with zero unbuilt constructs. It passed GREEN on first run after the #31 serialize fix, confirming the framework is "done enough" for Phase 1. It is ALSO the migration reference for the inherit object form (the 81 string-form inherit('Post',...) matches elsewhere in projects/ stay stale for now; this file shows the correct inherit(Post,{via}) form). Suite 98→103 (+5 blog-acceptance tests: whole-spine compiles, Blog public, Post published-or-author per principal, Comment inherited per principal, app routing table with both auth layers), all GREEN.
33: Decision: A grant derives EXACTLY ONE read-scope: a second scope().can() clause in one grant is a LOAD-TIME ERROR (entity.mjs), never a silent first-wins. Reason: the entity compiler previously compiled only the first clause's scope predicate into the row-filtering SQL and silently dropped any further scope predicate (the `readScope === undefined` first-wins guard). A developer who wrote a second clause expecting it to RESTRICT reads would get every row exposed — a latent FAIL-OPEN in the authorization core, invisible to the suite (every test used exactly one scope clause or inherit). Phase 1 has no union/intersection-of-scopes read semantics, and no known use case needs one; if additive read scopes are ever required they arrive as an explicit NAMED construct, not inferred from "more than one element in the clause array". So the compiler now counts scope-bearing clauses and throws if >1, telling the author to combine conditions inside a single scope predicate (anyOf(...)/.and(...)) or inherit a parent's scope. Every clause's .can body is still statically guarded regardless of count. Fail-closed + singular-system. Confirmed by GPT-5.5 consult (the right call vs defining union semantics). Suite +1 (test/entity.test.mjs "two scope clauses is a load-time error"). Surfaced by the pre-commit Phase-1 review.
34: Decision: createServer({ authorize }) REQUIRES an explicit authorize function — omitting it is a LOAD-TIME ERROR (pipeline.mjs), replacing the former `authorize = () => true` default. Reason: a default `() => true` admits every action (FAIL OPEN), the exact opposite of the route gate's default-on requireUser(); a default-open auth stub is a planted footgun even while the pipeline kernel is latent (not yet wired to a request path). Authorization is always an explicit function (AGENTS: never a magic default), so the absence of one fails closed at construction rather than producing a silently-permissive server. When Phase 2 wires this kernel to the real HTTP request path, authorize is precisely where the route gate + grant engine compose — it is NOT a second, looser auth path (no second auth path). DEFERRED to Phase 2 (not done now, per GPT-5.5 consult): unifying the pipeline's bare `{id}` principal with the typed closed-union principal() — that is genuine request-path wiring with nothing currently consuming it, so unifying it now would be speculative (build wiring only when its consumer exists). Suite +1 (test/pipeline-server.test.mjs "no authorize is a load-time error"). Surfaced by the pre-commit Phase-1 review.
35: Decision: Field options are enforced at the SMALLEST SEAM that has enough information to decide the rule correctly (not "Phase 1 vs Phase 2" but "which seam owns the invariant" — GPT-5.5 framing). validateMutation (the untrusted-payload validate seam, stage 1) now enforces: (a) READONLY — a payload that names a readonly field is rejected outright (a client may not set/change it; the framework assigns it server-side, e.g. owner from the authenticated principal); (b) REQUIRED-CLEAR — a payload may not explicitly set a required field to null/undefined. Both are pure payload-shape rules needing no DB and no live path, so they are enforced NOW even though the write path that calls validateMutation is itself Phase 2 — enforcing the contract of an already-declared surface at an already-existing seam is NOT speculative wiring (contrast DECISIONLOG #34's deferred principal unification, which has no consumer yet). DEFERRED to the Phase-2 write/materialization seam: (c) DEFAULT application (materialization, not validation — turning an accepted create into a row; the validator accepts/rejects, it never mints values), and (d) FINAL requiredness on create (was the field supplied by payload OR route OR principal OR default — only decidable once all sources are merged). The blog-spine declarations (owner readonly, blog/parent required, createdAt default) are KEPT, not stripped: they are real known use cases and stripping them would hide a requirement rather than remove complexity; Phase 1 now enforces the payload-visible part and Phase 2 owns the rest — a staged contract, not inert surface. No field.mjs change (readonly/required already ride the frozen descriptor via ...options). Suite +3 (test/field-strategy.test.mjs: readonly reject, required-clear reject, partial-update still allowed). Surfaced by the pre-commit Phase-1 review; resolution per GPT-5.5 consult.
36: Decision: Phase 2 slice 1 — `.listen(port, options)` opens a REAL node:http server that serves the Phase-1 routing table; the transport lives in a DISTINCT module (src/serve.mjs), not in app.mjs (which stays the resolution layer). The request flow is fail-closed at each step: (1) derive the principal via `options.principalOf(req)` which DEFAULTS to `() => anonymous`; (2) match {method, url.pathname} to a route (literal + `:param` segments, segment-count-exact) — no path match → 404, path matched but wrong method → 405; (3) run the matched route's gate(principal) — the FIRST default-on auth layer — deny → 401; (4) dispatch the admitted request (slice 1: a 200 JSON stub echoing {verb, entity} to prove route+gate wiring over the socket; DB-backed CRUD is the next slice). The server is exposed on `app.httpServer` and `.listen` still returns the app (chainable). Reason: standing up the socket is the user-chosen Phase-2 start (real HTTP transport + baked-in middleware before WS live-sync). The principal source defaults to anonymous so an UNCONFIGURED server is fail-closed — the default-on route gate (requireUser) denies every request until session→req.user hydration (a later slice) REPLACES principalOf; that is the SAME admission path, never a second auth path. The row grant (SQL scope + .can) still runs downstream in dispatch on every admitted verb — this layer decides route admission, never row visibility. matchRoute checks path before method so 404 (unknown path) is distinguishable from 405 (known path, wrong method). Two Phase-1 tests (app.test.mjs, blog-acceptance.test.mjs) that called `.listen(3000)` to assert chainability/port were updated to `.listen(0)` (ephemeral port, no collision) + app.httpServer.close() — `.listen` now genuinely opens a socket, so a table-only test must close its handle or the node:test process hangs on the open handle. Suite 108→113 (+5 test/http-transport.test.mjs: socket opens, anonymous→401, allowAnonymous verb admits + sibling verb stays 401, admitted→200 verb echoed, undeclared path→404), all GREEN ~259ms.

37: Decision: Phase 2 slice 2 — DB-backed CRUD dispatch replaces slice 1's 200 stub in src/serve.mjs, running BOTH default-on auth layers end to end against a real node:sqlite DatabaseSync handle. The DB handle is an APP-LEVEL resource supplied once at construction — `workbench({ db })` stores it as `app.db`; `.listen` reads it and threads it to the dispatcher (`makeRequestHandler(routes, { principalOf, db })`). It is NOT a per-transport `.listen({ db })` option: persistence is shared infrastructure that the future WS /events transport must read from the SAME place (one DB, no per-transport divergence; "prefer a singular system"). This does NOT violate "persistence is opt-in by engaged seam, not a class field" — that rule governs per-ACTION durability (a route is durable because it engages the CRUD dispatch seam), orthogonal to where the shared connection handle lives (GPT-5.5 consult confirmed Option B with the sharpening: read by transports, never let both workbench({db}) and listen({db}) exist). Per-verb dispatch: list → bindReadScope(entity.readScope, principal) then `SELECT * FROM <Entity> AS t0 WHERE <bound.sql>` .all(params) (the SQL-scope half decides VISIBILITY); read/update/remove → same scope AND `t0.id = :id` (single row; not visible OR absent → 404, do not distinguish — fail closed); create → validateMutation (ValidationError → 400) then INSERT with the owner field (a readonly role-ref) filled SERVER-SIDE from principal.id (SPEC §158 `from:'req.user.id'` is the explicit form; a readonly role-ref is the default). The SECOND auth layer — the row grant's `.can` CAPABILITY half — now runs at runtime for the first time (closing the Phase-1 gap where .can was declared + statically guarded but never executed against a materialized row): new src/row-grant.mjs builds the entity's `is` checks bound to { entity: row, principal } (each wrapped via check() so `await is.owner()` works + the un-awaited-check backstop applies), runs the grant clause's .can body through resolveDecision, and yields the conferred capability set; mayVerb(entity, verb, row, principal) maps verb→required capability (list/read→read, create/update/remove→write — an allowlist naming the capability that GRANTS, never a denial condition) and is consulted on every materialized-row verb. A non-owner who can SEE a public-read row (scope everyone()) is still denied write/remove with 403 — visibility and capability are distinct axes, both enforced. HTTP status map: list 200, read 200/404, create 201/400, update 200/404/403/400, remove 204/404/403, no-db-for-entity-CRUD 500 (fail closed), malformed/oversized body 400 (BodyError, ~1mb cap, distinct from ValidationError), last-resort catch 500 (the formal 4-arg error handler is slice 3). Minimal inline JSON body-read (readJsonBody, 1mb cap) was pulled forward for create/update; the formal baked-in middleware stack is slice 3. One slice-1 stub test (http-transport.test.mjs "an admitted request reaches dispatch") asserted the removed {verb,entity} echo — updated to its new fail-closed truth (admitted + no db → 500, proving the request crossed the gate INTO dispatch; a denied request stops at 401 and never reaches the db check); per the TDD rule, an assertion describing obsolete behavior is fixed in the TEST. Suite 113→124 (+4 test/row-grant.test.mjs, +7 test/http-crud.test.mjs), all GREEN ~299ms.

38: Decision: Phase 2 slice 3 — the baked-in middleware stack lands as the structurally load-bearing, fail-closed SUBSET of SPEC §3 (security headers, a single error renderer, a capped body parser with a 413/400 split, graceful shutdown), NOT the full §3 list (rate limiting, CORS, sessions, view engine, static, request logging are deferred — sessions are slice 4, the rest are later cosmetic layers; building them now would be "build every conceivable knob" rather than "a real app in the set needs this shape"). New src/config.mjs exports a frozen `config` ({ port: PORT||3000, env: NODE_ENV||'development' }) — the env-override surface SPEC §3 names, re-exported from index.mjs; the app READS it (the gdocs-clone exemplar app.mjs does `app.listen(config.port, () => ...[${config.env}])`) and never re-implements the knobs. New src/middleware.mjs owns two baked-in defaults: (a) applySecurityHeaders(res) sets four fail-closed-without-per-app-knowledge headers on EVERY response (x-content-type-options:nosniff, x-frame-options:DENY, referrer-policy:no-referrer, x-dns-prefetch-control:off) — CSP/HSTS are deliberately EXCLUDED because they need per-app/per-deployment input (they are app declarations, not framework defaults; a wrong default breaks apps); (b) renderError(res, err, {env}) is the SPEC §3 "4-argument JSON error handler" realized as the ONE place an unexpected exception becomes a 500 response — dev includes {error, message, stack}, production is opaque {error:'internal error'} (no stack, no internal message leaked). serve.mjs wiring: applySecurityHeaders runs at the TOP of every request (before any writeHead, retained through it); the handler's catch now delegates to renderError instead of the inline `sendJson(500,{error:'internal error'})`; `env` is a SERVER-OWNED listen option threaded through makeRequestHandler (default config.env) — NEVER a client header, because a client must never be able to force a stack trace (security foot-gun explicitly rejected). Body parsing: the inline readJsonBody (1mb cap, pulled forward in slice 2) now distinguishes oversized (BodyError status 413 Payload Too Large) from malformed-JSON (status 400) via a status field on BodyError; the oversized path does NOT `req.destroy()` (an abrupt socket close races the response and the client sees a dropped connection, not the 413) — instead it sets an `aborted` flag, pauses, rejects, and resumes to drain the rest so the 413 flushes cleanly (empirical: undici/fetch errors with UND_ERR_SOCKET 'other side closed' if you destroy mid-upload). Graceful shutdown (framework-owned per SPEC §3 / AGENTS Defaults — an app mounting its own traps is a leak): installGracefulShutdown gives each app an `app.shutdown()` that closes app.httpServer (resolving when it stops listening) and unregisters the app from a module-level `liveApps` Set; process-level traps (SIGTERM/SIGINT close every registered app then process.exit(0); unhandledRejection/uncaughtException log to stderr without crashing) are installed ONCE per process behind a `processTrapsInstalled` flag — installing them per-app would accumulate listeners (a leak + MaxListeners warning), so they belong to the process, not the app. The `.listen` second arg is now overloaded the Express way: a FUNCTION is a listening callback (`app.listen(0, () => ...)`, the exemplar shape, wired via httpServer.once('listening', cb)); an OBJECT is server-owned listen options ({principalOf, env}); both are server-owned, neither client-controlled. TDD: RED first (test/middleware.test.mjs, 9 tests: config shape; security headers on a 404 and on a 401; oversized→413; malformed→400; prod renderer opaque/no-leak via a throwingDb() stub that throws on .prepare to force a genuine exception through the catch, not the deliberate no-db 500; dev renderer includes a stack; app.shutdown() leaves the server not-listening; listen(port,callback) fires the callback) — first RED was the missing `config` export, then GREEN piece by piece. Suite 124→133 (+9), node --test 133/133 GREEN ~308ms, no hang, no MaxListeners warning. Deletion-test PASS: config/middleware CONCENTRATE the §3 boilerplate into framework-owned single owners (one header set, one error renderer, one shutdown seam) the app never hand-mounts — they do not relocate app code behind a config name. Deferred to later slices: slice 4 (session→req.user→principal hydration, the requireAuth concept on HTTP, REPLACES the principalOf default — SAME admission path); then WS /events live-sync; the remaining §3 cosmetics (rate limit, CORS, logging, views, static) as a real app in the set needs each; the 'scope' overload disambiguation when the write path forces pipeline+grant to meet.

39: Decision: Phase 2 slice 4 — session→principal hydration is the default request-principal source, replacing the bare `() => anonymous` default whenever an app has a db. New src/session.mjs owns the auth wiring (SPEC §660): parseCookies (zero-dep — node:http exposes the raw `Cookie` header string and nothing parses it; url-decoded name→value map), sessionCookie(token, {secure=true}) (the Set-Cookie value with fail-closed defaults HttpOnly + SameSite=Lax + Path=/ + Secure; secure may drop for a non-TLS dev/test context, HttpOnly/SameSite never drop), and sessionPrincipalOf(db) → the `principalOf(req)` function the HTTP transport calls. The principal is built ENTIRELY SERVER-SIDE (SPEC §572): the cookie carries only an opaque session token; the framework looks the token up in the Session table (`SELECT principalType, principalId FROM Session WHERE token = ?`) and constructs principal({type, id}) from the stored row. The client cannot supply its own identity — there is no type/id in the cookie protocol, so a smuggled `principalId=root; type=system` is ignored and a forged/unknown/missing token yields `anonymous`, which the default-on route gate then denies (fail closed end-to-end: no cookie → anonymous → 401). This is the SAME admission path as the `principalOf` default it replaces (AGENTS "no second auth path"): hydration only changes WHERE the principal comes from, not how admission is decided. The lookup statement is prepared PER REQUEST inside the returned function rather than once at construction, so a broken db fails CLOSED (anonymous) on each request instead of throwing at listen time (this also keeps the throwingDb middleware test honest — its prepare-throw surfaces at dispatch as the intended unexpected-exception 500, not at startup). Wiring lives in serve.mjs listen(): `principalOf = options.principalOf ?? (app.db ? sessionPrincipalOf(app.db) : undefined)` — an explicit listen({principalOf}) still overrides (tests inject a fixed principal), and with no db the source stays the fail-closed `() => anonymous` default in makeRequestHandler. SCOPE: slice 4 is deliberately narrowed to the hydration CORE. The exemplar session.mjs imperative-router surface (s.post(path, ...handlers), app.use, the `open` route-gate opt-out marker, the User/Session entities, hash() password fields, the User.findOne/.select/.getOrFail query API) is a SEPARATE axis (SPEC §4 imperative routing) deferred to its own slice — it is not needed to prove session→principal hydration ("build for the known use cases, but no further"). NAMING: requireUser()/allowAnonymous() (the entity-verb gate vocabulary, 22+21 load-bearing refs across src/test) are KEPT; the SPEC's `requireAuth` term and the `open` marker (which appear only in exemplar prose, never in framework code/tests) are the imperative-router surface's vocabulary and arrive with that slice — renaming the proven core for a cosmetic SPEC-vocabulary match would relocate without concentrating (deletion test). Suite 133→145 (+12 test/session.test.mjs: cookie codec, server-side principal construction, client-cannot-inject-identity, and end-to-end cookie→hydrate→admit / no-cookie→401 / forged→401).

55: Decision: Milestone 1b-E2 end-to-end acceptance (COMPLETE) — the unified check registry is proven to drive BOTH authorization layers to the SAME verdict for every principal, closing the FULL fork opened in #54. Reason: #54 collapsed `owner`+`collaborator` onto one registry with two mechanically-derived faces (a harvest face → SQL AST, a run face → boolean), but recorded only the classification rule; "no second auth path … one check registry evaluated in both modes" is a CLAIM that is worthless unless the two faces are shown to AGREE on a real row against a real db — agreement by construction is the same "unverified luck" the unification was built to kill, just moved up a level. So acceptance is a behavioral proof, not a code-shape inspection. test/membership-acceptance.test.mjs stands up the exact todo.mjs TodoList exemplar (owner ref-role + `collaborators: map(ref('User'),{role:['viewer','editor']})` + declared `collaborator: ({TodoList,principal}) => TodoList.collaborators.has(principal.id)` + grant `scope(anyOf(is.owner(),is.collaborator())).can(...)` granting read+write+subscribe to the owner, read to a collaborator, deny otherwise) on a real `:memory:` db, and for four principal situations asserts that LAYER 1 (the SQL row-scope filter via `bindReadScope(TodoList.readScope, who)` → which row ids a scoped SELECT returns) and LAYER 2 (the runtime gate via `mayVerb(TodoList,'read',row,who)` → the `.can` boolean) reach the IDENTICAL admit/deny verdict: (1) the owner is admitted by both; (2) a collaborator added through the row write handle is admitted by both (the scope EXISTS subquery and the runtime `.has()` read the SAME `<Entity>_<field>` side-table row); (3) a non-member is denied by both (scoped ids empty AND `.can` false — they agree on denial, not just on grant); (4) removing the collaborator through the row handle revokes BOTH layers together. This also proves the E2-E write path is coherent with the read paths: collaborators are seeded via `row.collaborators.add(member, role)` (NOT raw SQL), so one table shape serves write, scope-compile, and runtime check. Owned rows are seeded with raw `INSERT` (established precedent scope-sql.test.mjs:138 / principal.test.mjs:101) because `owner` is `readonly:true` and `create({owner})` is correctly rejected by validateMutation — the readonly owner is assigned server-side from the principal at the HTTP create path (serve.mjs:194), never client-supplied. TDD: RED-first, 4 tests, full suite 250→254 (+4), node --test 254/254 / 0 fail (lead-verified raw). Deletion-test N/A (pure test addition, no new production abstraction). Milestone 1b-E2 (FULL fork: `roleFieldMap`+`deriveRoleChecks` retired onto one registry, map membership compilable in scope + runnable at request time + writable through the loaded-row handle) is now COMPLETE and behaviorally verified.

54: Decision: Milestone 1b-E2 (FULL fork) — a check's HARVESTABILITY (whether `is.<name>()` may appear in a `scope(...)` predicate that compiles to a SQL row-filter) is classified by the check's SOURCE CATEGORY, NOT by a developer-applied wrapper, and the existing `owner` double-path is collapsed onto ONE check registry consulted by BOTH evaluation modes. Reason: AGENTS "a new general mechanism retires the special-case it generalizes, in the same change" + "no second auth path … within the engine: one check registry evaluated in both modes" — `owner` today is defined TWICE independently (compile: scope-sql.mjs `roleFieldMap` → eq node; runtime: entity.mjs `deriveRoleChecks` → `row[field]===principal.id`), a working second path whose agreement is unverified luck. The user fork chose FULL: one registry entry per check, with up to two mechanically-derived FACES (a harvest face → AST node, a run face → boolean), so `roleFieldMap` and `deriveRoleChecks` are DELETED as independent authorities and both `makeIsProxy` (compile) and `makeIs` (runtime) ask the registry. A check arises from exactly THREE sources, and the source — not a wrapper — fixes its faces: (1) a ref field carrying `role` (e.g. `owner: ref('User',{role:'owner'})`) → registry entry with BOTH faces auto-derived from the one field name (harvest `{node:'eq',field,param:principalId}`, run `row[field]===principal.id`); (2) a declared `checks: { <name>: ({ <EntityName>, principal }) => … }` whose body composes only framework field handles (e.g. `TodoList.collaborators.has(principal.id)`) → harvestable (its harvest face is obtained by invoking the body once under a COMPILE self-handle that returns AST nodes; its run face by invoking the SAME body under a RUNTIME self-handle that returns booleans) AND runtime; (3) a `map(of,{role:[…]})` field's role names (e.g. `editor`/`viewer`) → RUNTIME-ONLY checks (the member's stored role is a per-row payload value the SQL filter deliberately never inspects), so using `is.editor()`/`is.viewer()` inside `scope(...)` is a LOAD-TIME error. The user (a hobbyist, communicate at level) explicitly chose "plain functions, framework figures it out" over the gpt-5.5 consult's proposed `harvestableCheck(…)`/`runtimeCheck(…)` wrappers — the binding exemplars (todo.mjs/doc.mjs) write BARE functions and must stay bare. FAIL-CLOSED CORRECTION adopted from the same consult (overriding my initial "attempt-harvest-then-silently-downgrade" idea, which was a fail-OPEN hole): a check from a HARVESTABLE source that is used in `scope` but whose body cannot actually lower to SQL is a HARD load-time error, NEVER a silent downgrade to runtime-only (a silent downgrade would drop the row-filter from the SQL and admit all rows to the `.can` body — exactly the fail-open the project forbids). A raw boolean returned in harvest mode is likewise rejected (no implicit `true`⇒`everyone()`); admin-style "see everything" must be written as an explicit compilable expression. CONSULT MODIFICATION REJECTED: the wrapper API (it contradicts the bare-function exemplars); CONSULT SEQUENCING ADOPTED: land runtime collaborator self-handle (E2-C) and registry unification as one architectural change but in safe sub-steps, and do NOT leave the repo in a stable state where owner-runtime goes through the registry while owner-compile still goes through `roleFieldMap` (delete both old paths before E2 is called complete). Owner's two faces deriving from the ONE ref-role field declaration PASSES the deletion test (CONCENTRATE not RELOCATE) only if `roleFieldMap` + `deriveRoleChecks` are deleted and the registry-entry generator is the SOLE place owner-ness is interpreted. This entry records the classification + fail-closed rule; the end-to-end acceptance (todo.mjs/doc.mjs collaborator scope compiles AND runs) lands under the NEXT entry at E2 completion.

53: Decision: Milestone 1b-E1 — doc.mjs's "ambient" declaration symbols (`now`, `delta`, `entity`, `collaborators`) become honest imported/parameter handles, NOT framework-injected free identifiers in a sandbox. Reason: doc.mjs used `now`/`delta`/`entity`/`collaborators` as bare free identifiers in declaration position, so plain `import('./doc.mjs')` threw ReferenceError before any framework code ran; the user fork chose "make them imported handles" over "framework evaluates declarations in an injected scope" (declarations stay plain importable ESM modules — no Function-constructor/with-block sandbox, free-variable typos stay import-time errors not runtime surprises). Resolution: (a) `now` is an exported deferred-value token `Object.freeze({ kind:'deferred', resolve:'commit-instant', toString(){return 'now:commit'} })` in src/deferred.mjs, exported from index.mjs and imported into doc.mjs — the engine resolves it to the commit instant later (mirrors the typed-handle, never-a-string discipline of state.transition); (b) per-mutation runtime values `delta`/`entity` CANNOT be imported, so the effect `with` becomes a FUNCTION receiving them as parameters: `with: ({ delta, entity }) => ({ recipient: delta.member, doc: entity.id, kind: 'invite' })` (honest closure params, not module-level magic); (c) `collaborators` is hoisted to a top-level `const` so the sibling `effects:` key can reference `collaborators.onAdded`, and the `map` descriptor gains a frozen typed event handle `onAdded: Object.freeze({ event:'added', toString(){return 'map:onAdded'} })` — a stringifiable computed key (same precedent as state.transition), preserved across `.can()`. SCOPE = import-surface only (same doctrine as map/log/presence/state): the top-level `effects:` key is currently DEAD declarative surface — entity() reads only fields/grant/checks/routes/create/inherit and IGNORES `effects` — so this delivers an import-EVALUABLE doc.mjs (now/delta/entity ReferenceErrors gone) plus the `now` token and `onAdded` handle, while effect FIRING is deferred to the live-sync milestone. KNOWN-DEFERRED: a doc.mjs<->comment.mjs circular-import TDZ (comment.mjs uses `inherit(Doc,…)` at module-eval) is independent of E1, surfaced only when doc.mjs is the import entry, and is left to route-wiring time. TDD: test/deferred-now.test.mjs (2 tests: now exported+frozen, kind/resolve correct, String(now)==='now:commit' usable as computed key) + test/map-field.test.mjs (+3 tests: frozen onAdded stable toString, computed-key roundtrip, preservation through .can()). Full suite 231→236 (+5), node --test 236/236 / 0 fail (lead-verified raw). Deletion-test PASS: `now` is one small token module reusing the frozen-handle pattern; `onAdded` rides the existing makeDescriptor freeze/.can-spread — no new injection mechanism, no sandbox, no loader.
48: Decision: Slice A2 'continue all' sub-piece 2 — `number()` is a `value` KIND field typed `number` (not a new kind), stored as-is, carrying a `derived` option on the descriptor whose write-path materialization is deferred. Reason: a numeric scalar has the SAME persistence/diff/compare semantics as text/date — one stored cell, whole-value diff, ordinary `.is/.in` comparison — so it is one more `value`-type instance (`makeDescriptor({ kind:'value', type:'number', ...options })`), NOT a new strategy (deletion test: a `number` strategy entry would duplicate the value strategy; node:sqlite binds a JS number directly, no serialize coercion needed, so value's passthrough already serves it). The gdocs exemplar declares `wordCount: number({ derived: row => row.body ? wordcount(row.body) : 0 })`; `derived` (a recompute-from-row function) rides the frozen descriptor via `...options` exactly like date's `default` and `touch` — its MATERIALIZATION (recomputing the cell from sibling fields on every write) belongs to the write/materialization seam (the same Phase-2 seam that owns default application, ADR #35), not to this declaration, so this piece delivers only the import-time surface (a valid value-kind descriptor the entity compiler accepts and the scope compiler compares like any value field). `number` exported from index.mjs alongside text/boolean/date/ref/hash/link. TDD: test/number-field.test.mjs RED-first (no `number` export) → GREEN, 3 tests (kind 'value'/type 'number'/frozen; derived fn carried on descriptor; compiles into an entity and exposes a value query handle score.is(5)). Full suite 203→206 (+3), node --test 206/206 / 0 fail (lead-verified raw). Deletion-test PASS: number ABSORBS into the existing value kind + existing value strategy — zero new strategy, comparator, or serialize path.
47: Decision: Slice A2 'continue all' sub-piece 1 — `admin` is a distinct typed capability token (`capability('admin')` in grant.mjs), exported from workbench alongside read/write/subscribe, NOT a derived super-set of the lesser capabilities. Reason: the gdocs exemplar doc.mjs confers an OWNER capability set `grant(read, write, subscribe, admin)` for management actions (rename, reshare, change owner, delete) beyond plain write; authorization is always typed tokens compared by IDENTITY, never strings and never an implied hierarchy (AGENTS: no magic strings; allowlist the capability that grants access), so an OWNER grant lists `admin` EXPLICITLY rather than the engine inferring "owner ⇒ admin". Making admin its own frozen singleton (same shape as read/write/subscribe) means a capability check stays a single `.includes(token)` identity match with no precedence lattice to half-apply (fail closed: a row that needs admin gets it only when admin is explicitly conferred). This is the import-time binding surface doc.mjs needs (the `admin` symbol existing); the row-grant ENFORCEMENT that maps a verb to a required capability (rename/delete ⇒ admin) is unchanged (row-grant.mjs VERB_CAPABILITY already keys verbs to tokens and simply gains admin-keyed verbs when those routes land). TDD: test/admin-capability.test.mjs RED-first (no `admin` export) → GREEN, 2 tests (admin is a distinct frozen token ≠ read/write/subscribe; grant(read,write,subscribe,admin) carries admin by identity). Full suite 201→203 (+2), node --test 203/203 / 0 fail (lead-verified raw). Deletion-test PASS: admin ABSORBS into the existing one-line `capability()` factory + the existing grant capability-set — no new precedence code, no new check path, just one more typed token in the same allowlist mechanism.
45: Decision: Slice A2 piece 4 — the `link` field is the framework's first STRUCTURED field (`struct` KIND), a NAMESPACE of named value sub-cells each backed by its own flat SQL column, so a nested handle `Doc.linkShare.token.is(token)` reuses the ORDINARY value-handle `.is()` rather than a second comparison path. Reason: four exemplars (Doc/Album/Photo/Canvas) declare share-by-link as ONE field `link({ tiers, tier, token })` and query it nested (`Doc.findOne(Doc.linkShare.token.is(token))` in session.mjs, `Doc.linkShare.token.is(...)` in the doc.mjs scope) and read it nested (`entity.linkShare.tier`), yet a field had been exactly one flat column named by the field name. The contended question — how a structured field stores sub-cells and exposes the nested compilable handle — was taken to a different-perspective consult (openai-gpt-5.5-expensive) AND a four-named-perspective roleplay (general-think: end-user/inheriting-dev/systems-engineer/downstream-dev); BOTH converged decisively on: each STORED sub-cell becomes its own flat column; the structured field is a namespace of value sub-handles; `tiers` is declared CONFIG, never a per-row column. Rejected alternatives (both consults): a JSON blob (a second `.is()` path via json_extract, not cleanly indexable, write-amplifying — violates "one way to do a thing"); separate top-level `token`/`tier` fields (leaks storage shape into the app declaration, loses the link's cohesion, collides when an entity needs two link fields); a 1:1 join entity (violates AGENTS "an owned relation is a field on the entity, not a standalone table" + adds a join to the hottest lookup). RESOLUTION: (a) `link({tiers,tier,token})` returns a frozen descriptor `{ kind:'struct', type:'link', cells:{token:text(),tier:text()}, tiers:[...config], tokenIntent, tierDefault }` — `cells` is the persisted half, every other key is config, so the write path serializes only `cells` and the handle exposes a value sub-handle only for `cells` (`tiers` is not a queryable handle because it is not row data). (b) The generated column name is derived from declared shape (NO magic strings) by ONE authority `structCellColumn(field, cell) = \`${field}__${cell}\`` in field-strategy.mjs; a load-time guard in entity() forbids any declared field/sub-cell name containing `__`, so the generated namespace and the declared namespace can never alias (fail closed). (c) `fieldHandle` gains a `struct` branch: the struct's OWN `.is/.in/.isNull` THROW (you compare a sub-cell, not the whole struct — like any non-value kind), and each sub-cell resolves to `fieldHandle(structCellColumn(name,cell), cellDescriptor)` — an ORDINARY value handle whose `fieldName` IS the generated column, so `linkShare.token.is(x)` lowers to `t0.linkShare__token = :p` through the EXISTING value path with ZERO new AST/lowering (the consult's heavier "stored-cell registry + rewrite every AST leaf" was trimmed as a relocation; reusing the flat value handle is the concentration). `makeFieldsProxy` delegates `struct` to `fieldHandle` too (one handle definition, both the scope builder and the entity Proxy reach it). (d) The write path flattens via `flattenStruct(field, descriptor, value)` (serialize each supplied sub-cell to its `__` column); hydration reconstructs the namespace (`row.linkShare = { token, tier }`) and DELETES the raw `__` columns so a handler sees the declared shape, not the storage shape. `.is(undefined)` stays the deliberate SQL FALSE through the unchanged value-handle rule (fail closed, never IS NULL) — centralized, not link-specific. DEFERRED (out of this piece): the compiled-SCOPE use `checks.linkHolder` (`Doc.linkShare.token.is(principal.attributes?.token)`) needs a symbolic principal-attribute bind param (today only `principalId` is symbolic) — that lands when the whole Doc grant compiles (the Inbox/full-doc-load milestone), not for this nested handle which the runtime query API exercises with a concrete value. TDD: test/link-field.test.mjs RED-first (no `link` export) → GREEN, 6 tests (descriptor shape incl. tiers-config-not-cell; struct handle exposes sub-handles + own .is throws; token.is lowers to `linkShare__token`; .is(undefined)→FALSE; entity-Proxy nested handle; write-flatten + read-reconstruct round-trip via Doc.findOne). `link` exported from index.mjs alongside text/boolean/date/ref/hash. Full suite 191→197 (+6), node --test 197/197 / 0 fail (lead-verified raw). Deletion-test PASS: `struct` ABSORBS the nested-handle into the existing flat value handle (sub-handle == a value handle bound to a generated column) and the existing serializeField/hydrate seams — no second comparator, no second serialize path, no per-field config DSL; flat field == the degenerate one-cell case of the same namespace rule.
52: Decision: Slice A2 'continue all' sub-piece 6 (FINAL) — `state({ values, transitions, effects, auto })` is the framework's finite-state-machine field, its own new KIND (`kind: 'state'`, `type: 'state'`): a CLOSED `values` domain plus a declared legal-transition graph, and `state.transition(from, to)` is a STATIC method on the `state` export returning a typed, stable, stringifiable transition handle usable as a COMPUTED OBJECT KEY in the `effects` map. Reason: doc.mjs declares `status: state({ values:['draft','shared','archived'], transitions:{draft:['shared'],shared:['archived','draft'],archived:['draft']}, effects:{ [state.transition('shared','archived')]:{ with:{ archivedAt: now } } }, auto:{ when:'shared', after:'90d', to:'archived' } }).can(...)` — a Doc lifecycle whose value is one of a closed set, whose only legal moves are graph edges (fail closed: a move not in the graph is rejected; the value is NOT a free-form column the app may set to anything), with an in-transaction effect on the shared→archived edge and a 90-day auto-archive. The `effects` map is keyed by `state.transition('shared','archived')` — this MUST be a typed handle, not a magic string (AGENTS: identifiers are never magic strings), so `state.transition` is implemented as a static method (the `text.crdt` precedent of a static on a constructor function) returning a frozen `{ from, to, toString() }` whose `toString()` yields a STABLE identifier (`transition:<from>-><to>`): two calls for the same pair stringify to the SAME computed key, so the effect map is keyed by a DERIVED identifier, never a hand-authored literal. The descriptor carries `values` (frozen closed domain), `transitions` (frozen legal-move graph), `effects` (frozen, keyed by transition handles), and `auto` (the time-driven auto-transition, sugar over the schedule time-source ADR #19). SCOPE = import-surface only (mirrors map/log/presence/Inbox import-now-wiring-later): this delivers the `state` symbol + `state.transition` + a descriptor the entity compiler accepts at load. The transition ENFORCEMENT (reject illegal moves), the effect WIRING, and the `auto` SCHEDULER are the state kind's deferred behavior. No strategy/handle/entity code was added (entity compile does not eagerly resolve a field's strategy; the Proxy mints a handle only on access; the non-value fieldHandle gate already fails closed — a state handle throws "is a state field and cannot be compared in scope"). Deletion-test PASS: `state` CONCENTRATES — it adds one constructor + one static method + one export and rides every existing seam (makeDescriptor/.can, the strategy table's absence-means-not-yet-serialized, the fail-closed handle gate); the transition handle reuses the computed-object-key coercion the language already provides (no new registry, no new key mechanism). TDD: test/state-field.test.mjs RED-first (no `state` export) → GREEN, 7 tests (frozen state/state descriptor; carries values/transitions/auto frozen; `state.transition` returns a stable stringifiable handle that encodes the from/to pair and differs per pair; the handle works as a computed key in an effects map; `.can` returns a new frozen state descriptor carrying the access fn; compiles into an entity at import under a `fields:` block + explicit grant; a state handle throws /state field and cannot be compared/ in scope). Full suite 224→231 (+7), node --test 231/231 / 0 fail (lead-verified raw). ACCEPTANCE GATE for 'continue all': all 22 symbols in doc.mjs's import list are now present and the `Doc` entity itself compiles fully; `import('./doc.mjs')` now progresses past all field imports and Doc compilation, failing only DOWNSTREAM at comment.mjs:18 `inherit('Doc', { via: 'doc' })` — a PRE-EXISTING exemplar staleness (DECISIONLOG #26 ruled `inherit` takes the compiled parent OBJECT not a string; the repo-root comment.mjs was never migrated, only test/blog-acceptance.test.mjs uses the correct form), which is outside the 'continue all' field-constructor scope and is part of the SEPARATE deferred doc.mjs grant-compilation milestone. 'continue all' (admin, number, map, log, presence, state) is COMPLETE.
47: Decision: Slice A2 'continue all' sub-piece 5 — `presence(cells)` is the framework's first NON-PERSISTING field: its own new KIND (`kind: 'presence'`, `type: 'presence'`), a namespace of named live sub-cells (doc.mjs: `presence: presence({ cursor: true, selection: true })`, comment "ephemeral, per-connection"). Reason: presence is per-connection live state (cursors, selections, typing indicators) that must NOT be durably stored, and SPEC §7.2 / AGENTS mandate that an action's class (durable/ephemeral/volatile) is EMERGENT from which seams it engages, NEVER a label it carries. So presence carries NO `persisted: false` flag; its ephemerality is the ABSENCE of the persistence seam — there is no `presence` entry in the field-strategy STRATEGIES table, so it never serializes (the absent seam IS the ephemerality), and a presence handle is correctly NOT whole-value comparable in scope (the existing fieldHandle non-value gate throws "is a presence field and cannot be compared in scope" — fail closed, no new code). It is a DISTINCT kind from `struct` (link), NOT a reuse: `struct` flattens its `cells` to real flat columns (it persists); reusing `struct` for presence would force the struct strategy to special-case `type:'presence'` to skip persistence — a label flipping a gate, the exact thing the emergent-seam rule forbids. Decision taken via the question tool (hobbyist A/B framing: new `presence` kind vs reuse `struct`); user chose the new kind. The descriptor carries `cells` (the frozen declared live sub-cell shape, e.g. `{ cursor:true, selection:true }` — config, not stored columns). SCOPE = import-surface only (mirrors map/log/Inbox import-now-wiring-later): this delivers the `presence` symbol + a descriptor the entity compiler accepts at load. The per-connection live broadcast and volatile coalescing are the presence kind's deferred live behavior. No strategy/handle/entity code was added (entity compile does not eagerly resolve a field's strategy; the Proxy mints a handle only on access; the non-value fieldHandle gate already fails closed for any non-value/non-struct kind). Deletion-test PASS: `presence` CONCENTRATES — it adds one constructor + one export and rides every existing seam (makeDescriptor/.can, the strategy table's absence-means-ephemeral, the fail-closed handle gate); it introduces no persistence-skipping flag, no new gate, no second path. TDD: test/presence-field.test.mjs RED-first (no `presence` export) → GREEN, 6 tests (frozen presence/presence descriptor; carries declared `cells` frozen; NO `persisted` flag on the descriptor; `.can` returns a new frozen presence descriptor carrying the access fn; compiles into an entity at import under a `fields:` block + explicit grant; a presence handle throws /presence field and cannot be compared/ in scope). Full suite 218→224 (+6), node --test 224/224 / 0 fail (lead-verified raw).
50: Decision: Slice A2 'continue all' sub-piece 4 — `log(entry)` is the `store` KIND's append-only owned-collection instance (`type: 'log'`), an internally-keyed collection of STRUCTURED entries living ON the owning entity, declared with a per-entry sub-field descriptor map `entry` (e.g. `{ sender: ref('User'), body: text() }`). Reason: doc.mjs declares `chat: log({ sender: ref('User'), body: text() })` (comment "append-only; emits :appended:<id>") — a chat log whose entries each carry a User-FK sender and a text body — and AGENTS mandates an owned relation is a FIELD on the entity, not a standalone entries table, so chat is a field of kind `store` (the already-registered internally-keyed-owned-collection kind, SPEC §5.1/ADR #9), NOT a second entity. `log` is the same `store` kind as `map` (#49), distinguished only as `type: 'log'`: map is keyed membership (each member a value of `of`), log is append-only structured entries (each entry the shape `entry`). The descriptor carries `entry` (the frozen per-entry sub-field descriptor map — each sub-field a value/ref/text descriptor; a declared shape, immutable). SCOPE = import-surface only (mirrors map/Inbox import-now-wiring-later): this delivers the `log` symbol + a descriptor the entity compiler accepts at load. The append MUTATION, the `:appended:<id>` EVENT HANDLE (doc.mjs's chat emits on append), and any per-entry query are the `store` kind's Phase-2 merge/event behavior, deferred — the store strategy's apply/diff already fail closed with a loud Phase-2 throw, and a `store`-kind field is correctly NOT whole-value comparable in scope (the existing fieldHandle non-value gate throws "is a store field and cannot be compared in scope" — fail closed, no new code). No strategy/handle code was added (same as #49: entity compile does not eagerly resolve a field's strategy; the Proxy mints a handle only on access). Deletion-test PASS: `log` ABSORBS into the existing `store` kind + makeDescriptor/.can + the non-value fieldHandle gate — one more named instance of an existing kind, no new mechanism (it does not even need a new options shape: `entry` mirrors link's `cells` / map's `of`). TDD: test/log-field.test.mjs RED-first (no `log` export) → GREEN, 6 tests (frozen store/log descriptor; carries `entry` sub-descriptors with sender ref→User + body text; entry shape frozen; `.can` returns new frozen descriptor; compiles into an entity at import; not whole-value comparable). Full suite 212→218 (+6), node --test 218/218 / 0 fail (lead-verified raw).
49: Decision: Slice A2 'continue all' sub-piece 3 — `map(of, { role, default })` is the `store` KIND's owned-collection instance (`type: 'map'`), an internally-keyed membership collection living ON the owning entity, declared with a per-member value descriptor `of` (e.g. `ref('User')`) and a `role` domain. Reason: doc.mjs declares `collaborators: map(ref('User'), { role:['viewer','editor'], default:{} })` — a set of collaborators each carrying a role — and AGENTS mandates an owned relation is a FIELD on the entity, not a standalone join table, so collaborators is a field of kind `store` (the already-registered internally-keyed-owned-collection kind, SPEC §5.1/ADR #9), NOT a second entity. The descriptor carries `of` (the per-member value descriptor, so the membership's element type is a typed FK, not opaque sugar), `roles` (the frozen role-name domain — declared CONFIG that feeds the role-derived checks is.viewer()/is.editor() when the grant is compiled, NEVER a per-row column, same config/data split as link's `tiers`), and `default`. SCOPE = import-surface only (mirrors Inbox/number import-now-wiring-later): this delivers the `map` symbol + a descriptor the entity compiler accepts at load. The membership MUTATION (`collaborators.set`), the `collaborators.onAdded` EVENT HANDLE (doc.mjs uses it as an effects key), and the viewer/editor role-derived CHECKS are the `store` kind's Phase-2 merge/event behavior, deferred — the store strategy's apply/diff already fail closed with a loud Phase-2 throw, and a `store`-kind field is correctly NOT whole-value comparable in scope (the existing fieldHandle non-value gate throws "is a store field and cannot be compared in scope" — fail closed, no new code). No strategy/handle code was added: entity compile does not eagerly resolve a field's strategy (validate/serialize run only at insert/mutation), and the Proxy mints a handle only on access, so a store-kind field imports and compiles without a strategy entry beyond the one `store` already has. Deletion-test PASS: `map` ABSORBS into the existing `store` kind + the existing makeDescriptor/.can + the existing non-value fieldHandle gate — one more named instance of an existing kind, no new mechanism. TDD: test/map-field.test.mjs RED-first (no `map` export) → GREEN, 6 tests (frozen store/map descriptor; carries `of`; frozen `roles` list; `.can` returns new frozen descriptor; compiles into an entity at import; not whole-value comparable). Full suite 206→212 (+6), node --test 212/212 / 0 fail (lead-verified raw).
46: Decision: Slice A2 — the framework PROVIDES the `Inbox` entity (exported from workbench, imported by doc.mjs as the target of a declared effect `effects: { [collaborators.onAdded]: { mutate: Inbox, with: { recipient: delta.member, doc: entity.id, kind: 'invite' } } }`), a per-user notification store with a RECIPIENT-SCOPED grant (a user reads only their own inbox rows). Reason: a per-user notification/inbox shape is every collaborative app's concern, not each app's (the same way User/Session are framework-provided, ADR #43), so Inbox ships FROM the framework with fields `recipient: ref('User', { role: 'recipient' })`, `doc: ref('Doc')`, `kind: text()`. The `recipient` role-ref makes the entity compiler derive `is.recipient()` (deriveRoleChecks: `row.recipient === principal.id`), so the grant `scope(({is}) => is.recipient()).can(() => grant(read, subscribe))` compiles to the read filter `t0.recipient = :principalId` — a user sees ONLY their own inbox rows (recipient-scoped, fail-closed default-on, both default-on layers intact: the route gate admits the user, the row grant scopes to their own rows). The `.can` body confers read (one-shot REST fetch) + subscribe (sustained WS push of new notifications) on those already-recipient-scoped rows; it needs no `is.*` call (the scope already filtered to the principal's own rows), so it is trivially guard-clean (the scope predicate's `is.recipient()` compiles to SQL and never runs as JS, so it is correctly un-awaited). KEY CALL — `recipient` is NOT `readonly` (contrast Session's readonly stored cells and an owner role-ref): `readonly` means "an untrusted client request payload may not set this field", but Inbox is never client-written at all (its grant confers read+subscribe, never write; no request principal can POST to it), and its ONLY write path is the trusted effect projection which passes `{ recipient, doc, kind }` — keys that ARE exactly the stored cells, so the generic create + validateMutation accept them unchanged. A `create` POLICY was deliberately NOT added (deletion test: an Inbox policy would be `(payload, { insert }) => insert(payload)` — pure relocation that only skips validation, adding a named seam that does nothing; Session's policy earns its place because its payload keys differ from its stored cells and it mints server-side values, whereas Inbox's projection payload already equals its stored shape). The effects-projection WIRING that actually RUNS `mutate: Inbox` (re-enters the pipeline, authorizes against Inbox's own grant, folds into the originating transaction) is a LATER pipeline piece; the binding surface doc.mjs needs at IMPORT time is just the `Inbox` symbol existing and being a valid recipient-scoped entity, which this piece delivers. Inbox lives in src/auth-entities.mjs (the framework-entity family) alongside User/Session, exported from src/index.mjs. doc.mjs still does not fully LOAD because it also imports not-yet-built field constructors (number/map/presence/log/state) and the `admin` capability — Inbox is necessary-but-not-sufficient for full doc.mjs load, by design (each is its own piece). TDD: test/inbox-entity.test.mjs RED-first (no `Inbox` export) → GREEN, 4 tests (Inbox exported + recipient/doc/kind field shape incl. recipient role; grant compiles to `t0.recipient = :`; bound scope per principal selects ONLY that principal's own rows [alice vs bob]; create+findOne round-trip). Full suite 197→201 (+4), node --test 201/201 / 0 fail (lead-verified raw).
43: Decision: Slice A2 piece 3 — the framework PROVIDES the `User` and `Session` entities (exported from workbench, imported by the exemplar `session.mjs`, never app-declared), and the entity compiler gains a declared `create` POLICY seam + an extracted trusted `insert(cells)` core to host Session's bespoke session-minting without a second write path. Reason: authentication is the framework's concern (the app should not hand-roll a user table, a password digest, or the session→principal mapping the request pipeline already reads), so `User { username: text(), password: hash() }` and `Session` ship FROM the framework. User needs nothing new — the generic query API (#41) + hash() hydration (#42) already serve `User.findOne(User.username.is(name))` / `User.create({username,password})` / `user.password.verify(pw)`. Session is the contentious one: `sessionPrincipalOf` runs `SELECT principalType, principalId FROM Session WHERE token = ?`, so Session's STORED row is exactly `(token, principalType, principalId)` — yet the binding exemplar calls `Session.create` in TWO shapes whose payload keys are NOT stored columns and whose stored columns are ALL server-minted: `Session.create({ userId })` (a user session) and `Session.create({ kind:'link', token })` (a link session). The generic create + validateMutation cannot fit — it fails closed on the undeclared `userId`/`kind` keys. RESOLUTION (different-perspective consult, openai-gpt-5.5-expensive): (a) Session DECLARES its three stored cells as `readonly` framework-owned fields (`token/principalType/principalId`, each text({readonly:true})) so the table schema stays typed and inspectable and `Session.token.is(...)` is a first-class handle — NOT a fieldless bespoke entity; (b) entity() accepts an optional declared `create` policy (`createPolicy`) that REPLACES the generic create — declaration absorbs the imperative minting wiring (AGENTS) — and receives the call payload plus a trusted toolkit `{ insert, mintToken }`; (c) the generic create body was refactored to extract `insert(cells)` = serialize-each-declared-field → INSERT → hydrate-return (NO validateMutation), so BOTH write paths compose ONE insert core (generic create = validateMutation(untrusted payload) then insert; a policy mints server-side cells it owns then insert) — the deletion test PASSES because the policy adds intent, not a second INSERT/return mechanism. Session's policy `mintSession` treats the two intents as a CLOSED set of named wholes each mapped to the one canonical row: `{userId}`→`{token:mint, principalType:'user', principalId:String(userId)}`; `{kind:'link',token}`→`{token:mint, principalType:'link', principalId:String(shareToken)}`; anything else THROWS (fail closed). The minted `token` is a fresh unguessable SESSION token (randomBytes(24) hex via the toolkit's `mintToken`), DISTINCT from a link's incoming share token. For the link intent, `principalId` stores the incoming SHARE TOKEN (the grant identity — "which share granted this principal", preserving per-link revocation/audit), NOT the doc id (which would collapse every link session for one doc into the same principal). This bypass of validateMutation is the SAME trust class as the unscoped query API (#41): "no second auth path" governs UNTRUSTED REQUEST admission, and a framework minting a session server-side is TRUSTED code, not a request path. Both entities carry a fail-closed grant `scope(()=>never()).can(()=>deny(...))` (they are never mounted as request-facing entity-CRUD — the exemplar mounts /users and /sessions as imperative routers — so their read-scope grants nothing; the grant exists only to satisfy ADR #7's load-time requirement, and the auth layer reaches them only through the trusted unscoped query API). New src/auth-entities.mjs holds both, exported from src/index.mjs. node:crypto randomBytes only (zero deps). TDD: test/framework-entities.test.mjs RED-first (no Session export) → GREEN, 12 tests (User shape/create-digest/findOne-roundtrip/select-projection; Session readonly-fields/user-mint/distinct-tokens/link-mint-carries-share-token/closed-set-reject/user-resolves-through-sessionPrincipalOf/link-resolves/delete); full suite 179→191 (+12), all GREEN. The exemplar session.mjs+app.mjs still do not fully LOAD only because doc.mjs imports a not-yet-built `Inbox` framework entity (a future piece, out of scope); the piece-3 binding surface (User, Session) is complete and resolves under the `workbench` specifier.
44: Decision: Slice A2 piece 2 — `hash()` is its own field KIND (not `value`), digesting a plaintext password on write into a salted, one-way scrypt cell (`saltHex:digestHex`, random 16-byte salt + scrypt-64) and hydrating on read into a `{ verify(plaintext) → boolean }` handle on the row (the exemplar surface `user.password.verify(pw)`). Reason: a password has two properties no `value` field has — it must be one-way (never stored or returned in the clear) and it must NEVER be queryable by plaintext. Making `hash` a distinct kind gets the second property for FREE through the EXISTING fail-closed gate in `fieldHandle`: any non-`value` kind already throws "field X is a <kind> field and cannot be compared in scope", so `User.password.is(pw)` throws at predicate-build time — a plaintext password structurally cannot reach the SQL WHERE (fail closed, no new guard written). The kind also slots cleanly into the ONE framework-owned STRATEGIES table (ADR #27): `hash`'s `serialize(plaintext)` does the scrypt digest with a fresh per-write salt (so the same plaintext stores differently each time — passwords are write-only, never diffed for plaintext equality; `diff` compares STORED cells and reads a re-store as a change, which is correct), reusing the same `serializeField` seam the write path and scope-literal baker already call (no second serialize path). VERIFICATION is a one-way check, not a stored comparable, so `verifyHash(candidate, stored)` lives in field-strategy.mjs (recompute scrypt over the candidate with the stored salt, `timingSafeEqual` against the stored digest) and is exposed ONLY via row hydration — entity.mjs precomputes the hash-kind field names at compile time and a `hydrate(row)` wraps each hash cell into `{ verify }` on every read path (findOne/getOrFail/create + every findAll row incl. .select projections), so the digest string never leaves the field as a comparable value. verifyHash fails CLOSED: a non-string candidate, a missing/malformed `salt:digest`, or empty salt/digest returns `false` (never a thrown 500 on the login path, never a permissive default). node:crypto only (scryptSync/randomBytes/timingSafeEqual) — zero deps, consistent with the node:sqlite/node:http/node:test stack. `hash` exported from src/index.mjs alongside text/boolean/date/ref. TDD: test/hash-field.test.mjs RED-first (no `hash` export) → GREEN; suite 173→179 (+6), all GREEN. Deletion-test PASS: the `hash` kind ABSORBS its own digest+verify machinery into the existing strategy table and the existing fieldHandle non-value gate — no new comparator path, no new auth path, no per-field config object; the query-scope refusal of plaintext is a reused invariant, not new code.

41: Decision: Slice A2 piece 1 — the runtime query API is the SERVER's own trusted, UNSCOPED data-access primitive attached to every compiled entity (findOne(predicate)/findAll()[.select(...)]/getOrFail(id)/create(payload)/delete(id) + per-field typed handles Entity.<field>.is/.in/.isNull doubling as .select projection handles), reached over an AMBIENT db handle. Reason: the imperative-router handlers from slice A need to read/write entities directly (login is `User.findOne(User.username.is(name))`, a lookup that is inherently PRE-PRINCIPAL — it runs before any principal exists — which proves the query API cannot be principal-scoped); so the query API runs UNSCOPED/PRIVILEGED, NOT threading bindReadScope and NOT running mayVerb, exactly like dispatch()'s raw SQL and like Workbenchan-ORM. This is NOT a second auth path (AGENTS) because "no second auth path" governs UNTRUSTED REQUEST admission, and an admitted handler calling the query API is TRUSTED server code, the same status as dispatch's own SQL; a scoped variant, if ever needed, arrives as an explicit named method (e.g. `.scopedTo(principal)`), never the default (fail-closed: the privileged path is the explicit, named, non-default one... and here it is simply the only path because the caller is already trusted). The db handle is AMBIENT (new src/db.mjs setActiveDb/getActiveDb; workbench({db}) binds it; getActiveDb throws fail-closed if unbound) because an entity is declared INDEPENDENTLY of any app (doc.mjs is imported before the app exists) yet the exemplar calls `User.findOne(...)` with no db argument — the handle must therefore be reachable ambiently; it is the SAME app.db handle made reachable, one shared database (singular system), never a second persistence path. The per-field handle is ONE definition (`fieldHandle(name, descriptor)` in scope-sql.mjs): makeFieldsProxy's value branch was refactored to delegate to it, so the scope compiler and the query API mint handles through one rule (deletion-test CONCENTRATE — the handle logic was absorbed, not copied). getOrFail throws a numeric `status:404` error so middleware.renderError renders it through the deliberate-client-error path, not an opaque 500. NAMESPACE COLLISION RESOLUTION: field handles are reached as `Entity.<field>` but the compiled record also owns reserved metadata (name/fields/grant/checks/routes/readScope/scopeAst) and the query methods — a field literally named `name` (the blog exemplar has `name: text()`) would corrupt `entity.name` if attached as an own property. Resolved with a PROXY over the frozen record: reserved metadata + query methods are OWN properties (the Proxy passes them through unchanged, so every existing `entity.name`/`entity.fields`/`route.entity.name` read is untouched), and ONLY a NON-own string key resolves to a field handle (declared field → fieldHandle; the synthetic projection-only `id` handle; otherwise undefined). The OPEN field namespace and the FIXED reserved set thus never collide — a field named `name` keeps `entity.name` as its string metadata and is simply not reachable as a query handle (no exemplar needs `.name.is(...)`), zero blast radius, no metadata eviction. The Proxy CONCENTRATES (one rule: own-key→metadata/method, else field handle) rather than special-casing each reserved word. Rejected: eager per-field attach loop (collides + duplicated makeFieldsProxy), Symbol-eviction of all 7 metadata keys (correct but pervasive blast radius across serve.mjs/row-grant.mjs/tests, deferred until a real exemplar needs a colliding field name as a handle), namespaced `Entity.field.<name>` (breaks the binding exemplar `User.username.is(...)`).

56: Decision: Two-phase app assembly — route resolution moves from mount time to a deferred resolveRoutes()/listen() step, allowing an entity's `routes` thunk to be async (a parent entity lazily dynamic-importing a child module at wiring time to break a circular import). Reason: the doc.mjs (parent) <-> comment.mjs (child) exemplars form a circular ESM import where comment.mjs must `inherit(Doc, {via:'doc'})` at module-eval — an eager `import { commentRoutes } from './comment.mjs'` at doc.mjs:16 triggers the temporal dead zone (Doc not yet assigned). Dynamic `import()` at call time (when routes wire up) lets doc.mjs finish defining Doc before comment.mjs's `inherit(Doc,…)` evaluates, but `import()` is async and the framework's mount was synchronous and chainable. The GPT-5.5 consult endorsed a two-phase boundary: `mount`/`use` RECORD ordered intent synchronously (fluent chain preserved); `resolveRoutes()` (async, idempotent) drains declarations to build the concrete `routes` table, awaiting each entity's routes thunk so async thunks work; `listen()` creates the server synchronously (the handler awaits `app.ready` internally, so no request dispatches against a partial table), then sets `app.ready` via a background boot promise. The entity routes builder `r` becomes a FULL mountable surface (`.resource()` + imperative verbs + `.use`), UNIFYING the two separate builder concepts (the old `buildResourceRoutes` mini-builder that only had `.resource()` is RETIRED — singular-system, one-reconciliation-path). inherit stays STRICT: `inherit(Doc,…)` still receives the real compiled entity object, no deferred `()=>Doc` thunk (the lazy-thunk shape was explicitly rejected earlier). Collision-conformance DECISIONLOG #54/#55 edits (doc.mjs `checks.owner` removed, comment.mjs `checks.author` removed — the ref-role field is the single source of truth) are included in this commit. TDD: test/app.test.mjs updated for two-phase (all route reads go through `await resolved(app)` or `await r.resolveRoutes()`; imperative-record-time validation `assert.throws(()=>r.get('/nothing'))` stays synchronous); 3 new boot tests (mount does not invoke thunk eagerly, async thunk awaited, listen(0)+ready closes cleanly); blog-acceptance.test.mjs updated to `await app.resolveRoutes()` before route inspection. Full suite 258/258 / fail 0 (lead-verified raw). Deletion-test: PASS — `buildResourceRoutes` (the old mini-builder) is deleted entity-retired same-change; its route records, RESOURCE_VERBS expansion, and gate resolution are all absorbed into `resolveResource` + the unified buildEntityRoutes (net reduction: one fewer function, surface unified, no second resolution path).

40: Decision: Slice A — the imperative router surface coexists with entity-CRUD routes in ONE table + ONE dispatch spine via a DISCRIMINATED route record: a shared spine {method, path, gate} plus exactly one tail — an entity tail {entity, verb} OR an imperative tail {handlers:[...middleware, finalHandler]}. The discriminant is STRUCTURAL (`route.handlers` present → imperative; absent → entity), never a `kind` string tag. Dispatch keeps the unchanged shared spine (match → 404/405 → principalOf → route.gate(principal) → 401 → read body) and forks EXACTLY ONCE at the tail: `route.handlers ? runChain(...) : dispatch(...)` (dispatch = the unchanged entity-CRUD arm). Gates are BRANDED functions — a GATE_BRAND symbol stamped non-enumerably by `brand()`, tested by exported `isGate()`; requireUser()/allowAnonymous()/open() all return branded `(principal)=>boolean`, so the varargs `r.get(path, ...handlers)` peels LEADING branded gates deterministically into route.gate and the remainder is the handler chain (empty chain = load-time throw; a non-function handler = load-time throw). A gateless imperative route inherits requireUser() — the FAIL-CLOSED default (an undeclared-gate route 401s anonymous). `open()` is the imperative-facing admit-anonymous gate (behaviorally allowAnonymous but a distinct name for the distinct surface). `use` is a second NAME for `mount` (one impl) — the repo-root exemplars deliberately write app.use(path, router) for routers and app.mount(path, Entity) for entities, a binding naming convention (AGENTS: exemplars are binding naming direction). runChain builds an Express-like req {body, params, query} + res {status().json()/.json()/.send()/.sendStatus()} over the node req/res and threads next(err); it receives the ALREADY-ADMITTED principal and NEVER re-gates (no second auth path) — an imperative route has no entity, hence no row grant, so the route gate is its only framework-applied auth, by design. renderError now distinguishes a DELIBERATE client error (a thrown/next-ed value with a NUMERIC `status`, e.g. next({status:401,message:'bad credentials'}) → 401 {error:message} in every env) from an UNEXPECTED exception (no numeric status → opaque 500, prod hides message+stack) — distinguished strictly by `typeof err.status === 'number'`, so a raw thrown Error never leaks its message in production. Reason: this is the ONE legitimate fork (deletion-test PASS — the fork CONCENTRATES; the rejected alternative of generating entity-CRUD into a synthetic `handlers` chain so the spine always runChains RELOCATES the verb switch into a wrapper and adds chain machinery + an unused Express adapter for entity routes, net concept count rises — claude-opus-4-8-expensive consult). One reconciliation path / singular system, two default-on layers (imperative routes carry only the route gate), no second auth path. The framework User/Session entities + query API + hash() password field the gdocs-clone exemplars also need are a SEPARATE sub-axis (slice A2), deferred — the routing/handler surface is self-contained and proven without them. Suite 145→165 (+20: route-gate 9→12, app 8→16, new test/imperative-route.test.mjs 9), all GREEN, ~520ms, no hang.

57: Decision: Demo use-case completion — three features the binding exemplars (doc.mjs/gdoc.mjs) reference but that did not yet exist, landed together because they are mutually enabling. (1) Map handle .set(memberId,{role}) + .toArray(): the canonical map mutation is an UPSERT (idempotent re-share updates the role, fires the onAdded effect only on a genuinely new member — a role update is not a fresh invite), implemented as check-then-insert/update so it does NOT depend on a UNIQUE constraint being present on the side-table; .toArray() returns [member, role] pairs with members populated+hydrated through the declared of: ref(Target), so a share list returns hydrated User rows (a hash password stays a {verify} handle, not a raw digest leaking into the JSON) — this required an ambient ENTITY REGISTRY (db.mjs setActiveEntity/getActiveEntity, mirroring the ambient db) because a map(ref('User')) names its member by string and toArray must resolve that to the compiled entity to hydrate; an entity self-registers by name at construction (covers imported-but-unmounted entities like the built-in User). The owner/member ids are STRINGIFIED on bind (the side-table columns are TEXT and a numeric SQLite PK stored as text '10.0' broke the upsert existence probe — consistent with ref's String(value) storage). The old .add(memberId, role) is RETIRED same-change — .set generalizes it (singular system); tests migrated. findById added (hydrated-or-null), getOrFail refactored to compose it (one lookup mechanic, two call shapes). (2) req.<entity> auto-loading: an entity-bound route builder mounting under :<entityName>Id computes an autoLoad descriptor at declaration time; resolveRoutes stamps it onto every descendant route so a handler under /:docId/shares finds req.doc regardless of nesting depth; runChain loads the parent row via entity.findById before the chain, and a MISSING row renders 404 (the resource the handler operates on does not exist), not an opaque null-deref 500. The convention is SCOPED to an entity's own route subtree — a generic router's :userId does NOT auto-load (no entity context), so the share delete route's :userId is left as a raw param while :docId loads. Not a second auth path: the row grant still governs the entity's own CRUD verbs; auto-load is a read for the handler's benefit. (3) findAll(predicate) is a chainable, AWAITABLE query builder (.sort(field,dir)/.limit(n)/.select) that lowers the predicate to a WHERE and composes ORDER BY + LIMIT into one SELECT run on await/Promise.all (the exemplar feed shape: Doc.findAll(Doc.owner.is(me)).sort(Doc.updatedAt,'desc').limit(10) inside Promise.all); it is a THENABLE, not a plain array — the no-arg findAll() stays synchronous (plain array + .select) because existing tests rely on .map()/.length off a sync return, so the two call shapes are distinct (no-arg = trivial bulk read sync; predicate = deferred chainable query). ROUTING PRECEDENCE FIX: matchRoute was first-match-wins by declaration order, so a CRUD /:id from r.resource() (declared first) shadowed a hand-written /feed route mounted after it (doc.mjs /feed returned 404, matching /:id with id='feed'). Fixed by preferring the MOST SPECIFIC match (literal segments beat parametric) regardless of declaration order — Express-conventional specificity; ties (same literal count) fall back to declaration order (first wins), so equal-specificity overlapping routes stay stable. TDD: test/map-handle.test.mjs (3), test/autoload.test.mjs (3), test/query-builder.test.mjs (5), test/doc-share-e2e.test.mjs (2 — the real doc.mjs share routes + /feed over HTTP, the binding use-case tying all three features together). Full suite 303→316, all GREEN (lead-verified raw). Deletion-test: PASS — .add retired (not kept beside .set); getOrFail composes findById (one mechanic); the query builder is one thenable, not a config object.
58: Decision: Field-level capability authorization for map handles — the field's `.can(fn)` (stored as the descriptor's `access` fn, src/field.mjs makeDescriptor.can) was NEVER evaluated at runtime; `req.doc.collaborators.set/toArray/remove` did raw SQL with NO authz, so a non-owner could share a doc they don't own — contradicting the doc.mjs binding comment ('403s non-owners via collaborators.can, one auth engine, no second path'). Built the pipeline: `fieldCapabilities(entityRecord,fieldName,row,principal)` + `mayFieldOp(...,capability,...)` in src/row-grant.mjs REUSE the SAME `makeIs` check registry + `rowCapabilities` (the row grant's .can body) as `defaults` — NO second auth path, NO duplicated check map (AGENTS Authorization). A field WITH `.can` owns its rule (`access({is}, defaults) → grant(...)/deny(...)`); a field WITHOUT `.can` strong-inherits the row grant (returns `defaults`). Threading: `hydrate(row, principal=null)` + `makeMapHandle(...,row,principal)` + `record.findById(id,principal)`; serve.mjs runChain auto-load passes the request principal so `req.doc` is principal-stamped. `set`/`remove` (require `write`) and `toArray` (requires `read`) are async, guard `if (principal && !await mayFieldOp(...)) throw {status:403}` BEFORE mechanics; `has`/`get` stay SYNC + unguarded (read-primitives used inside check evaluation; the check registry builds its own `.has`/`.get`). Principal-present = request path = ENFORCED (fail-closed); principal-absent = trusted query API = BYPASSED — mirroring `mayVerb` which also runs ONLY in dispatch, never on the query API (DECISIONLOG #41); the only request-path hydrate is record.findById via runChain auto-load; findOne/getOrFail/create/toArray-member-hydrate stay principal-free (trusted). A thrown {status:403} → renderError (middleware.mjs: numeric err.status = deliberate client error) → HTTP 403. TDD: test/field-authz.test.mjs (5 tests — 3 RED-then-GREEN 403s for non-owner add/list/remove, owner regression, trusted-bypass mechanics); test/map-handle.test.mjs + test/map-write.test.mjs awaited set/remove/toArray (now async). Pre-existing exemplar/framework bugs fixed as REQUIRED dependencies (fieldCapabilities calls rowCapabilities which runs the Doc grant .can body exercising is.editor()/is.viewer()/is.linkHolder()): (1) doc.mjs editor/viewer checks destructured `{entity}` but the registry passes `{Doc:runtimeSelf,principal}` so `entity` was always undefined → aligned to `{Doc,...}` matching the file's own collaborator/linkHolder convention; (2) doc.mjs linkHolder check `: never()` returned a TRUTHY SQL-FALSE AST object at runtime, making is.linkHolder() enter the link arm and crash on entity.linkShare.tier inside rowCapabilities → discriminator `typeof principal.id !== 'string' ? never() : false` uses the codebase's OWN harvest signal (PRINCIPAL_ID_TOKEN is a Symbol at harvest, a string at runtime — scope-sql.mjs:33) to return the AST node at harvest and boolean false at runtime; (3) src/registry.mjs map runtime-self gained `.get(memberId)` (parallel to the existing `.has`) so editor/viewer role checks can read a member's role. Latent (deferred, out of scope — link-share/CRDT collaboration is a known demo gap): the Doc grant .can body still reads `entity.linkShare.tier` where `entity` is undefined (rowCapabilities passes only {is}); unreachable for non-link principals after the linkHolder fix; would crash for a real link principal. Suite 321/321 fail 0.

59: Decision: Opus + GPT-5.5 48-open-question consult (mutual cross-check, lead-synthesized). 5 user-resolved architectural calls + 3 cross-check refinements; the other ~40 items reaffirmed existing ADRs/deferrals (not restated here). USER DECISIONS: #34 general arithmetic beyond inc/dec in effects → DEFERRED, NOT EXCLUDED (needs a real use-case + an ADR cardinality/safety bound before it ships; lead leaned GPT-5.5 over Opus's 'permanently excluded' because the safety bar is IDENTICAL — both require an ADR first — and the posture differs only in whether DECISIONLOG reads 'deferred' vs 'excluded'; the one genuine cross-model disagreement). #16 slow-reconnect bootstrap gate → BOTH a held-events size cap AND a wait-time deadline, whichever trips first forces the client to hard-fail/re-bootstrap (eng-review spec #12; bound is an implementer tuning call, small + configurable). #26 share-by-link token → SERVER AUTO-GENERATES at create (token:'autogen' via the existing tokenIntent descriptor; apps never hand-make tokens — fail-closed unguessability, completes the create-time seam scope-sql.mjs already lowers via linkShare.token). #41 deleted-entity snapshot → a 'removed' NOTICE + the item VANISHES from the live view (no 404 hole, no endless spinner); soft-delete (row stays, flagged) only if an app needs deleted items visible/readable — leverages the existing _removed projection + removed events. #47 event naming → DOTS everywhere (Note.created / Doc.collaboratorAdded) + a load-time lint rejecting colon-form names — kills the colon-vs-dot drift SCOPE-FINDINGS §5 flagged (pipeline.mjs already parses on '.', so a colon-form store/field event would break the entity-name parser). CROSS-CHECK REFINEMENTS (confirm prior ADRs, settle open framing): #11 handler signature = { payload, principal } emit-only (entity-as-projection Fork A) + a read-only txn-scoped {db} for custom handlers needing in-txn QUERIES, NOT `now` — `now` resolves at commit in the kernel (ADR #24; GPT-5.5 retracted its round-1 `now` after Opus flagged it as the ad-hoc-clock foot-gun ADR #24 forbids; a handler needing the commit instant gets it stamped on the event after commit). #30 `derived` STAYS READ-TIME (never stored; SPEC §5.3); materialize-at-write is the SEPARATE `projected.inline` concept (ADR #12), authored as its own declaration — three distinct named concepts (derived / projected.inline / projected.async), NOT a knob/mode-toggle on `derived` (that would re-form the flag-lattice the 'pipeline variants are named wholes' rule forbids; Opus retracted its round-1 materialize-derived). #38 unify the kernel's bare {id} principal with the typed closed-union {type,id,attributes} AT the step-5 CRUD cutover — that cutover IS the request-path consumer whose absence justified DECISIONLOG #34's principal-unification deferral, so unifying there RESOLVES the deferral rather than violating it (both consultants converged; not a speculative build). Other notable cross-check outcomes: confidence raised M→H on #18 text.crdt (built-in named whole), #19 store pipeline actions, #20 ordered fractional-index, #25 presence ephemeral, #27 §8 live delivery, #33 CRDT toolkit deferred — these are settled decisions verified against ADRs, not judgment calls. Opus caught a real #14 constraint GPT-5.5 overlooked: VACUUM / CREATE UNIQUE INDEX can't run inside a sqlite txn → migrations need a separate pre-traffic non-transactional phase for those statements. Consensus metric: 37/48 full consensus · 10/48 direction-only (confidence split or exact-value = implementer call) · 1/48 genuine disagreement (the #34 above, resolved). The full per-item consult record (both models' Round-1 positions, the Round-2 mutual cross-check, per-item consensus tags, and retractions) is preserved durably at docs/reviews/consult-2026-06-30.md; the final-resolution register is entry #60. No code changed — design/posture decisions only.

60: Decision: [Opus + GPT-5.5 CONSULT — enumerated resolutions; mutual cross-check, lead-synthesized. Every line below tagged [C] = consult-sourced. Supplementing #59's summary with the full decision register; numbering matches the consult's 48-item list]. USER FINAL CALLS (user-decided on consult recommendation): #34 general arithmetic beyond inc/dec → DEFERRED-NOT-EXCLUDED [C] (needs real use-case + ADR cardinality/safety bound before ship) — the consult's one genuine cross-model disagreement (Opus=excluded, GPT-5.5=deferred; lead + user chose deferred, identical safety bar). #16 slow-reconnect bootstrap gate → BOTH a held-events size cap AND a wait-deadline, EITHER trips forces hard-fail/re-bootstrap [C]. #26 share-by-link token → SERVER AUTO-GENERATES at create (token:'autogen' via existing tokenIntent; apps never hand-make tokens) [C]. #41 deleted entity → a 'removed' NOTICE + item VANISHES from the live view (no 404 hole); soft-delete only if an app needs deleted items readable [C]. #47 event naming → DOTS everywhere (Note.created / Doc.collaboratorAdded) + load-time lint rejecting colon-form [C] (pipeline.mjs already parses on '.'). CROSS-CHECK REFINEMENTS (settled open framing): #11 handler signature = { payload, principal } emit-only (entity-as-projection Fork A) + read-only txn-scoped {db} for custom handlers needing in-txn QUERIES, NOT `now` [C] (`now` resolves at commit in the kernel, ADR #24; GPT-5.5 retracted round-1 `now` after Opus flagged the ad-hoc-clock foot-gun). #30 `derived` stays READ-TIME (never stored); materialize-at-write is the SEPARATE `projected.inline` concept (ADR #12) — three distinct named concepts (derived / projected.inline / projected.async), NOT a knob on `derived` [C] (Opus retracted round-1 materialize-derived). #38 unify the kernel's bare {id} principal → typed closed-union {type,id,attributes} AT the step-5 CRUD cutover [C] (that cutover IS the request-path consumer whose absence justified the #34 principal-unification deferral — resolves, not violates). P1-PATH RESOLUTIONS [C]: #1 fix linkShare.tier crash = thread materialized {entity:row} into the .can body (row-grant.mjs passes only {is}) + compile linkHolder through the structured linkShare.token handle (no hand-branch) — fixes the link-principal crash at doc.mjs:163. #2 step-5: delete raw SQL create/update/remove + live.emit in serve.mjs, route all CRUD through app.pipelineServer.dispatch, remove dead crudHandlers if unused. #3 step-6: retire live.mjs sequences/dispatchedActions Maps, read committed (scope,seq,actionId) off _Log. #4 land caller-owned TEXT ids (entity PKs + FK/member ids as strings, UUID minted-before-dispatch when absent, ~20 seed files numeric→string) BEFORE step 5 — entity-as-projection needs the id in the emitted event before row materialization, which INTEGER lastInsertRowid structurally cannot supply. #5 build app.pipelineServer in app.ready (two-phase boot), await app.ready in http tests, NO raw-SQL fallback (hard cutover — prior attempt failed on broken fallback, not kernel construction). #6 live = pure post-commit consumer of committed log events (no live-local seq/dedupe). #7 ONE post-commit fan-out registry: live=sync in-proc, jobs/webhooks/FTS/projected.async=async, all strictly after COMMIT (no socket I/O inside the txn). #8 snapshot reads row + Cursor.lastSeq in ONE sqlite txn + subscribe(since) replays _Log WHERE seq>since + stale-cursor HARD-FAIL (never silent truncate). #9 writeQueue single-writer async mutex over BEGIN IMMEDIATE→COMMIT + bounded wait (~5s)→503. #10 handler {db} = txn-scoped wrapper throwing on BEGIN/COMMIT/ROLLBACK/SAVEPOINT (framework owns the one txn). #12 projection writes re-enter server.dispatch (event→log→projection folds the row), never a direct UPDATE. #13 per-consumer durable watermark keyed (consumerId,scope,seq); derived-write + cursor-advance atomic OR replay-tolerant; job-enqueue keyed (consumerId,eventId). #14 migrations pre-traffic under writeQueue + meta-version bump in SAME txn where possible, BUT VACUUM / CREATE-UNIQUE-INDEX need a SEPARATE pre-traffic non-txn phase (sqlite can't run them in a txn) [Opus caught this; GPT-5.5 overlooked]. #15 re-auth NO-CACHE-FIRST (re-run mayVerb('subscribe') per emit on the captured row; add latched cache only after profiling). #17 reaper acquires the writeQueue mutex for BOTH pending-reap and adopted-refcount-reap. DEFERRED-MACHINERY RESOLUTIONS [C]: #18 text.crdt real merge (per-element deltas) in P6 — a built-in named whole, NOT the deferred toolkit (retire phase2Merge('crdt')). #19 store map/log mutations → pipeline actions/events in P6 (.set/.remove/append produce committed events; retire phase2Merge('store')). #20 ordered fractional-index contract in P6 (insertAt/move/reorder, not map/list sugar). #21 state runtime in P6 (transition guard rejects illegal edges + effects on edges + auto=sugar over schedule.after). #22 build src/schedule.mjs time-source (schedule.at/after, tick.hz/every, indexed-while guard) BEFORE state.auto/entity-TTL. #23 wire declarative effects {mutate,with} in entity() (currently IGNORES the effects key) as bounded in-txn effect-principal reentrancy + DELETE the fireMapEffects/direct mutate.create() special-case side path. #24 resolve the `now` deferred token at commit-instant IN THE KERNEL (pipeline.mjs:161 currently hardcodes new Date()). #25 presence = ephemeral/volatile live state + coalesced broadcast, no persistence seam engaged. #27 build §8 live delivery (field-keyed interest, two-layer pace, since) AFTER the fan-out spine; re-auth cache-less first. #28 client LiveChannel/LiveList in P7, structurally enforcing snapshot→cursor→stream (no public "open-WS-first" API), URLs derived from declarations. #29 apply default + final required check at the create/materialization seam (after payload/route/principal/generated-id merge; validateMutation only rejects, never mints). #36 projected.async = the SAME post-commit projection primitive as §9.3 OOB effects (one registry entry, NOT a separate subsystem). #39 retain-all log default (trimming = explicit named opt-in; hard-fail resync #8 is its safety net). #43 projection denial = LOAD-TIME error, never a silent runtime skip. #44 orphan handling (pending uploads + unreferenced adopted blobs) BEFORE blob support ships; content-hash dedup deferred/opt-in. #45 projected field writes through a bounded-projection-principal API, load-time-verified against the target grant (same mechanism as #12/#43). #46 fix stale inherit('Name',{via}) string-form comments/examples → object form inherit(Entity,{via}) (scope-sql.mjs already rejects string-form). REAFFIRMED existing ADRs/SPEC/deferrals (consult CONFIRMED, not new — listed for completeness): #31 recomputeFrom deferred (ADR #22), #32 spatial scoping deferred (SPEC §13), #33 CRDT-authoring toolkit deferred (SPEC §5.1), #35 batch-load + rate-negotiation deferred (SPEC §13), #37 .scopedTo unbuilt (DECISIONLOG #32/#41), #42 shared-disk blob access for single-process now (eng-review assumption), #48 baseline 345 canonical + stray await app.ready kept/reverted per whether step-5 makes it load-bearing (PLANS). CROSS-CHECK CONFIDENCE RAISES M→H (#18/#19/#20/#25/#27/#33) = settled decisions verified against ADRs, not judgment calls. Consensus metric: 37/48 full consensus · 10/48 direction-only (confidence split or exact-value = implementer call) · 1/48 genuine disagreement (#34, resolved by user as deferred-not-excluded). No code changed — design/posture decisions only; ALL entries above tagged [C] are Opus+GPT-5.5 consult-sourced (mutual cross-check; both models converged unless noted as a split).
61: Decision: ephemeral(cells) general NON-PERSISTING field kind; presence RETIRED to a thin wrapper returning ephemeral(cells) — ONE non-persisting kind, not two parallel kinds (AGENTS "a new general mechanism retires the special-case it generalizes, in the same change"; consult #25). Ephemerality is EMERGENT from the ABSENT persistence seam: NO STRATEGIES entry, NO main-table column, NO _Log row — both `presence` AND `ephemeral` throw on resolveStrategy (if an ephemeral field ever produces a _Log row, the seam LEAKED = bug). A per-connection side-table is ALLOWED (not the persistence seam; that's STRATEGIES/_Log). Subagent delivered 600/0 but chose "keep presence.kind='presence' for backward-compat" — TWO parallel constructors + a ddl OR-branch = the parallel-path smell the consult forbade; lead verified the only `kind==='presence'` dependents were 4 test assertions + the ddl OR-branch (no project/external API depends on the kind string), so a true retirement was clean + is the genuine migrate-behind-green-tests the rule demands. presence(cells) is now `return ephemeral(cells)`; ddl dispatches on `kind==='ephemeral'` ONLY; presence-field + ephemeral-field tests migrated (kind/type 'ephemeral'; parity presence(...) ≡ ephemeral(...) at kind/type/cells — deepEqual chokes on the `can` method descriptor attaches, so compared fields not whole-object; stale error-regex /presence field/ → /ephemeral field/ since scope-sql emits `${descriptor.kind} field`). Deletion-test PASS (presence constructor gone, one kind, one ddl branch). Per-connection broadcast deferred to Spine B7 (needs live.mjs subscription read; may overlap P6e pace/coalescer). Suite 559→600, 0 fail (lead-verified raw).
62: Decision: A4b scheduler admission wiring — Option A (dispatch-level `authorize`), MINIMAL staging (A1). GPT-5.5 consult (ses_0e852abd7f), lead-verified ALL load-bearing claims against code: (a) `authorize` (pipeline.mjs:304) runs OUTSIDE dispatch's write txn (BEGIN IMMEDIATE at :340) → TOCTOU real for scheduler (row may change between discovery + dispatch); (b) create authorization is ALREADY an in-txn re-check via `postHandlerAuthorize` (serve.mjs:1071) — re-reads row + mayVerb inside dispatch txn → the scheduler admission wants the SAME SHAPE (in-txn re-check), a SIBLING to postHandlerAuthorize, NOT a widened admitsEffects (GPT-5.5's admitsEffects-scope decision holds — effects admit committed-event re-entry; schedules admit time-fired self-dispatch; different sources, sibling functions, same dispatch spine); (c) `effectSource`/`principalFrom` use `attributes.source` (DELIBERATELY distinct from effects' `attributes.effect` per step-3 comment) — usable for the scheduler source, NOT the working effect-gate (effects pass `effect` as a separate arg to admitsEffects); do NOT assume they hook into effect admission. THE BINDING-RULE ARGUMENT (decisive): a reaper pre-call guard (Option B) leaves `dispatch` callable with a system principal + `authorize:()=>true` = STRUCTURALLY FAIL-OPEN — "no second auth path" requires admission at the CANONICAL SEAM (dispatch.authorize), not bolted onto one caller. A1 staging (user-chosen): `authorize` becomes a real fn branching on principal KIND — scheduler system principals run `admitScheduledMutation` IN-TXN (re-read current row, re-check due-field <= now AND compiled whileSql, recompute the declared `with` payload from the current row, COMPARE against dispatched payload — the scheduler principal can ONLY do what its declared schedule permits, NOT an arbitrary payload, else "due"="system write-anything hole"); USER principals stay pass-through (HTTP still pre-authorizes upstream in serve.mjs — NO regression, leaves the user-path unification to P1-path #2's own deferred cutover). Closes the fail-open gap FOR THE SCHEDULER; the TOCTOU is resolved in-txn (re-read + re-check inside dispatch's BEGIN IMMEDIATE). Different branches for different principal kinds inside ONE authorize fn = correct routing, NOT a second engine (the "second path" smell = two gateways, not two branches of one). handlers[type] is NOT CRUD-only (map/log/ordered field-mutation handlers also route through dispatch — `<Entity>.<field>.add/.setRole/.remove/.append/.insert` at entity.mjs:1029-1153) — my earlier "CRUD-only" framing was WRONG; the schedule CRUD restriction is genuinely narrower than the handler registry, and the scheduler admission must not break map/log field-mutation dispatches (they fire under the request principal, not the scheduler). payload-compare is security-critical (recompute `with` from current row, deny on mismatch). Scheduler can dispatch ONLY create/update/remove (entity.mjs:239-246 enforces); scheduled `create` semantics under-specified (discoverDueSchedules is row-based returning rowId → naturally fits update/remove) — lead defers scheduled create to when a real consumer needs it (fail-closed: if no schedule-create consumer exists, don't ship the create path).
62: Decision: A4b scheduler admission wiring — Option A (dispatch-level `authorize`), MINIMAL staging (A1). GPT-5.5 consult (ses_0e852abd7f), lead-verified ALL load-bearing claims against code: (a) `authorize` (pipeline.mjs:304) runs OUTSIDE dispatch's write txn (BEGIN IMMEDIATE at :340) → TOCTOU real for scheduler (row may change between discovery + dispatch); (b) create authorization is ALREADY an in-txn re-check via `postHandlerAuthorize` (serve.mjs:1071) — re-reads row + mayVerb inside dispatch txn → the scheduler admission wants the SAME SHAPE (in-txn re-check), a SIBLING to postHandlerAuthorize, NOT a widened admitsEffects; (c) `effectSource`/`principalFrom` use `attributes.source` (DELIBERATELY distinct from effects' `attributes.effect`). THE BINDING-RULE ARGUMENT: a reaper pre-call guard leaves `dispatch` callable with a system principal + `authorize:()=>true` = STRUCTURALLY FAIL-OPEN → admission at the CANONICAL SEAM (dispatch.authorize). A1 staging: `authorize` branches on principal KIND — scheduler system principals run `admitScheduledMutation` IN-TXN; USER principals stay pass-through (HTTP pre-authorizes upstream — NO regression; user-path unification deferred to P1-path #2). payloads security-critical (recompute `with` from current row, deny on mismatch). Scheduled `create` deferred (under-specified; discoverDueSchedules is row-based → fits update/remove).
63: Decision: A4b scheduler admission ORDERING + payload-compare correction (the seam #62 placed in `postHandlerAuthorize` was the WRONG PHASE; lead fixed). Subagent (general-prog, ses_0e83c5cd3f) implementing the 4 e2e tests BLOCKED on a real wiring bug: `applyEventsToTxn` runs projections (mutating the row) BEFORE `postHandlerAuthorize`, so `admitScheduledMutation`'s `while`/due/`with` re-checks saw the POST-PROJECTION row → falsely denied (the dispatch's OWN mutation made the row leave the `while` set it was being checked against). Subagent's proposed fix — "move the whole hook pre-projection" — was WRONG: create row-grant auth needs the POST-projection row (it authorizes against the freshly-created row), so one hook position can't serve both. Lead synthesis (A2 — split phases): scheduler admission and create row-grant have OPPOSITE row-visibility needs (pre-mutation vs post-mutation) → two distinct hook phases, not one. NEW `preProjectionAuthorize` hook in `applyEventsToTxn` (pipeline.mjs): runs IN-TXN, BEFORE _Log-append + projections, denial = clean 403 with ZERO footprint (nothing appended, nothing projected). Holds the scheduler admission branch (moved out of `postHandlerAuthorize`, which keeps create row-grant + effect-principal short-circuit, post-projection). This aligns with the GPT-5.5 consult's ORIGINAL Option A direction (admission in the dispatch spine, in-txn before mutation) — overloading `postHandlerAuthorize` (#62's refined seam) was the error. SECOND bug found while wiring (lead): `admitScheduledMutation` compared the FULL dispatched payload `{id, status:'published'}` against the declared `with:{status:'published'}` → always mismatches (the `update` handler REQUIRES `payload.id` to locate the row, but `id` is the rowId TARGET, not a field the schedule writes). Fixed by stripping `id` (the structural primary-key pointer) before comparison INSIDE `admitScheduledMutation` — the write-anything guard matches on the DECLARED FIELD SET, not the structural rowId. The earlier e2e harness MASKED this by stripping `id` in the hook; the real serve.mjs wiring passed `payload` unstripped, so the bug would have denied EVERY bound scheduler dispatch in production. Suite 610→614/0 (4 e2e: admitted-on-due+while / denied-undeclared-payload / denied-wrong-source / denied-while-fails). NO src/ scheduled-create path shipped (deferred per #62). Lead-verified raw.
64: Decision: dispatch ACTION-TYPE keys on the CRUD VERB (`${entityName}.${verb}` e.g. `Blog.update`), NOT the event-noun (`Blog.updated`). The handler registry (entity.mjs:1059-1069 crudHandlers) is keyed `${name}.create` / `${name}.update` / `${name}.remove` (the VERB), and `dispatch({type})` looks up `handlers[type]` DIRECTLY (pipeline.mjs:305 durable, :364 live) — so a dispatch CONSUMER emits `type: \`${e}.${verb}\``. The handler then EMITS the event-noun (`record.verbs.created.type` etc., entity.mjs:1065) which `VERB_FROM_EVENT` (pipeline.mjs:55 `{created:'create',updated:'update',removed:'remove'}`) maps BACK to the verb for projection/event routing. A4b + A5 (the scheduler/tick engines dispatching under system principals) initially INVERTED this — dispatching `type:\`${e}.updated\`` (event-noun) → "no handler registered" → the gate caught it. CONCENTRATION: one verb surface — the action-type IS the verb-noun; the event-noun is its emitted image mapped back through the inverse table; there is no second keying convention. The REAPER (next consumer) must dispatch `type:\`${entityName}.${verb}\`` (verb) with `payload:{id:rowId,...with}` + `actionId:randomUUID()`.
65: Decision: A5 Phase 2 — tick engine + wiring. `startTickEngine({db,entities,dispatch,now})` mirrors `startReaper` (job-queue.mjs:189-199): `setInterval`+`unref()`+idempotent start+`stop`. Computes the single FASTEST sweep interval across all tick triggers (`tick.hz(n)`→`1000/n` ms, `tick.every(d)`→`intervalMs`); returns a no-op `{stop(){}}` if no tick triggers exist (no empty-config timer). Each sweep: `discoverTickedRows(db,entityList,now())` → per row `dispatch({actionId:randomUUID(), type:\`${e}.${verb}\`, principal:principalFrom(tickSource(e,v)), payload:{id:rowId,...with}})` wrapped in PER-ROW try/catch (one row's deny/throw → stderr + CONTINUE — never aborts the sweep; an outer try/catch guards discovery-phase throws). The engine is a CLOCK TRIGGER, not an authority (DECISIONLOG #19): it dispatches under a system principal through the existing `kernel.dispatch` sink — ONE reconciliation path; `preProjectionAuthorize` admits in-txn via `admitTickedMutation` (kind-gated, no due-check, while+payload). `entities` normalized Map→array (`discoverTickedRows` iterates `for...of`). serve.mjs: tick-source branch in `preProjectionAuthorize` (exact 2-part `source===tickSource(e,v)` match → `admitTickedMutation`; scheduler's 3-part sources fall through). Engine started inside `app.ready` AFTER `app.kernel=buildKernel(app)` (it's undefined synchronously in `listen()`). USER DIRECTIVE applied: ds-v4-flash generates, lead self-reviews (lead is the reviewer for this risky slice). Lead self-review caught 3 real bugs: (1) missing `actionId` → ERR_INVALID_ARG_TYPE on undefined (pre-staged import wired by lead); (2) PRODUCTION ordering bug — engine startup placed in synchronous `listen()` body BEFORE `app.kernel` assigned at the async `app.ready` IIFE → `dispatch:undefined` in prod (e2e masked it, passing `server.dispatch` directly); (3) the #64 type-key inversion. Test 2 setup flaw (uncompiled-trigger swap) fixed. Suite 646→656/0 (10 new tick-engine tests). Off-limits held (pipeline/live/principal/effect-compiler/schedule.mjs/entity.mjs/field/ddl/scope-sql — reuse only). SUBAGENT DIAGNOSIS ACCURACY: ds-v4-flash ses_0e80f9ae9 returned DONE_WITH_CONCERNS — honestly found the actionId issue + flagged the `app.kernel.dispatch` handle, but MISSED (b) the production ordering bug, (c) the type-key inversion (shared the brief's inverted belief), and the test-2 uncompiled-trigger flaw. Lead self-review (per user directive) caught all three + fixed surgically (resume-interrupted-subagents: NOT re-dispatched — fixes were precise + small).

## #66 — State runtime: responsibility split + ambient-db seam (not pipeline widening)
- **Date:** 2026-06-30. **Slice:** Spine C8 state field runtime (`02e67b3`).
- **Decision:** Two separate checks for `state` fields. (a) STRUCTURAL value check in the `state` STRATEGIES entry (`validate` = membership in declared `values` domain) — runs on create AND update, via the existing `validateMutation` path. (b) SEMANTIC transition guard in the UPDATE CRUD handler (entity.mjs:1071) — pre-reads current row, compares current→new against `descriptor.transitions`, throws `ValidationError` status:400 on illegal move, runs AFTER validateMutation (structural-first diagnostic order: invalid target → domain error, not transition error). `validateMutation` stays a structural validator (current-value NOT threaded into its signature).
- **Rejected alternative: thread current-value into validateMutation.** Would conflate structural validation (declared/readonly/required/type-shape) with row-dependent state-machine legality — a semantic rule specific to state fields. The guard belongs alongside the state strategy + the update handler, not folded into the generic validator.
- **The ambient-db seam (lead synthesis overriding GPT-5.5 consult premise):** GPT-5.5 (ses_0e7e4a70b) flagged 'CRUD handler lacks db → must widen pipeline.mjs handler context to pass db' as the load-bearing risk + a 'move handler into the txn' TOCTOU fix. Lead VERIFIED against the code: WRONG for this codebase — the entity has an AMBIENT db handle via `getActiveDb()` (db.mjs:19, set at app construction app.mjs:308), and `record.findById` (entity.mjs:822) already uses it throughout (DECISIONLOG #41 convention, not a second path). The guard pre-reads via `record.findById(id)` with ZERO pipeline.mjs changes. The slice stayed 4 files (not 5+ risky-tier).
- **TOCTOU resolution:** the handler runs outside BEGIN IMMEDIATE (the consult's premise was correct here), but app writes serialize through `writeQueue.run` (serve.mjs:240) — no concurrent WRITE dispatch can mutate the row between the pre-read and this dispatch's txn-open. Reads don't mutate. So the write-path TOCTOU is closed by writeQueue serialization. The long-term 'move durable handler execution into the txn' fix is NOT needed for C8.
- **Create rule:** on CREATE, state value must be in `values` domain (structural); NO transition-graph check (no 'from' state). Only updates are transition-guarded.
- **Zero-footprint:** the guard throws before the handler emits the event → `applyEventsToTxn` never runs → nothing appended to _Log, no projection. This is the security property: an illegal move leaves NO trace.
- **Scope deferred (per user grill):** `state.effects` (in-txn effect keyed by `state.transition(from,to)` handle) + `state.auto` (desugar to `schedule.after`) NOT wired in C8 — separate slices. Transition-handle strings (for the effects map) NOT used by the guard; guard reads `transitions` graph directly.
- **Lesson:** a consult's load-bearing premise about the framework's db-access pattern must be verified against the actual code. GPT-5.5 assumed the standard 'pass db through handler context' pattern; this codebase uses an ambient module-level handle set at construction. The lead's verification collapsed a would-be 5-module risky slice into a 4-file standard slice.

## #67 — Reaper sweep: direct dispatch (Shape C) over _Job, + writeQueue serialization for timer-driven dispatch
- **Decision:** the reaper (`startReaper`, src/reaper.mjs) mirrors `startTickEngine` exactly (setInterval+unref+idempotent start+stop, per-row try/catch stderr+continue, no-op when no triggers). It calls `discoverDueSchedules` and dispatches each due row through `kernel.dispatch` under `principalFrom(schedulerSource(e,v,fieldName))`. Rejected _Job integration (the user's initial choice) in favor of direct dispatch (Shape C, GPT-5.5 consult ses_0e7bc9216f, user accepted after lead verified the consult's premise).
- **Why direct dispatch over _Job:** a schedule is DECLARED on the entity; `discoverDueSchedules` RE-DERIVES due rows from committed state every sweep. So a deadline is never a stored thing that can be "lost" — crash mid-dispatch → next sweep re-discovers the still-due row. `preProjectionAuthorize` already re-admits in-txn (re-reads row, re-checks due/while/payload), so stale candidates deny with zero footprint (TOCTOU-safe). _Job (ddl.mjs:179) is a remote-worker claim/lease/heartbeat pool; it buys cross-process execution, which isn't used. _Job would duplicate a derivable fact (due-ness) into a durable ledger — a second path beside the tick-engine shape (AGENTS: one mechanism, not two).
- **Invariant:** a scheduled action MUST make itself no-longer-due (flip the watched field or falsify `while`) or the reaper re-dispatches every sweep. Encoded in tests (the e2e ADMIT case flips status draft→published, so `while: status.is('draft')` becomes false).
- **writeQueue serialization (the load-bearing fix surfaced this slice):** timer-driven dispatch (tick engine + reaper) was calling `app.kernel.dispatch(args)` DIRECTLY, bypassing `app.writeQueue.run` — the same write-serialization whose TOCTOU-closure C8 (#66) relied on. If a tick/reaper fired while an HTTP write was mid-transaction, they could overlap. Fix (serve.mjs:1258): `const dispatchThroughWriteQueue = (args) => app.writeQueue.run(() => app.kernel.dispatch(args))` passed as `dispatch` to BOTH engines. This is the ESTABLISHED idiom in this codebase — byte-identical to serve.mjs:593 (custom-route re-entry) and HTTP CRUD (:241/:268/:297). Before: HTTP writes serialized but timer-driven writes raced them. After: ALL durable dispatch (HTTP + tick + reaper) serializes through ONE queue — AGENTS "one reconciliation path." `createWriteQueue()` runs at serve.mjs:1066 (before the app.ready IIFE at :1255, so the queue exists when the engines start).
- **e2e masking (the A5 Phase 2 lesson, recurred):** the e2e harnesses pass `server.dispatch` directly, bypassing `app.kernel.dispatch` AND `app.writeQueue`. So the RED→green test 'listen tick dispatch waits behind the app write queue' (test/tick-engine.test.mjs) exercises the PRODUCTION app.writeQueue path that the harness masked. LESSON reaffirmed: a green e2e that passes a direct dispatch handle does NOT exercise the production write-serialization path.
- **Lead synthesis on cross-session input:** the writeQueue fix was surfaced by a parallel agent session's audit (which I did not dispatch in this visible session); lead VERIFIED the finding against the code + integrated it lead-owned. The parallel session also wrote unrelated "pre-approved expensive-model review gates" content into PLANS.md — UNTRUSTED (no basis in this session's user messages) + out-of-scope — reverted, NOT committed. The fix itself is SOUND and belongs in this commit (same fix class for both engines).

## #68 — P6e-1a ephemeral event pathway: committed pipeline action over direct side-table write; _Log-append retained (refines #51)
- **Decision:** an `ephemeral(cells)` field was WRITE-IMMUNE before this slice (no mutation handle, no handler, no event, no projection — the descriptor was a dead value on the hydrated row). P6e-1a builds the foundation as the RAW VERBATIM pathway that P6e-1b's pace/coalescer will RETIRE: `makeEphemeralHandle` exposes `.set(cells)` which RE-ENTERS dispatch as `<Entity>.<field>.set` (own actionId + own txn, mirroring makeLogHandle/makeMapHandle/makeOrderedListHandle), the handler emits `<Entity>.<field>.set` carrying `{owner, client, cells}`, and the projection upserts the per-connection side-table row (`INSERT OR REPLACE INTO <Entity>_<field> (<Entity>_id, client_id, cells)` — a `cells TEXT` column added to `ephemeralTableDDL` for it). One emission type (whole-cells snapshot replace — no per-op split).
- **Committed pipeline action over direct side-table write** (AGENTS one-reconciliation-path): the .set is NOT a direct unlogged `UPDATE` of the side-table. It flows through `dispatch` → handler → emit → `applyEventsToTxn` → projection, so it is atomic with the txn, replayable from _Log, and authorized through the SAME field-auth engine (`mayFieldOp(record, field, write, ...)`) — no second auth path. Fail-closed: a handle hydrated without a `dispatch` ref throws on `.set` (no silent direct-SQL fallback); nothing is written on the thrown path.
- **_Log-append retained (refines #51):** pipeline.mjs:144 appends ALL events to _Log with NO kind filter. DECISIONLOG #51 framed ephemerality as "no _Log row," but that is NOT structurally enforced today. This slice REFINES #51: ephemerality = no STRATEGIES entry / no serialize handle (the descriptor carries no persistence seam), NOT no-_Log. The committed ephemeral event DOES append to _Log (verified by test) — that row drives the projection AND the post-commit fan-out (P6e-1a's raw verbatim delivery). This is deliberate: P6e-1b's pace coalesces DELIVERY, not the commit; the event still commits + persists its projection. P6e-4 (durable-coalesced-log, ADR #10) will revisit the replay-visible span for high-frequency persisted fields; ephemeral stays latest-snapshot (INSERT OR REPLACE), not append-history.
- **Cross-client visibility is the known 1a limitation 1b fixes:** subscribe is keyed by (entity,id) ONLY (no field-interest narrowing yet), so a `Canvas.activeStroke.set` event is fanned out VERBATIM to every subscriber of that Canvas, including the `client` field — a different client COULD read another client's in-progress stroke from the raw event. This is acceptable for 1a because 1a is the EXPLICIT-TO-BE-RETIRED raw path; P6e-1b's field-interest normalization + per-subscriber buffer (after re-auth) restricts delivery to the interested client's own stream. The side-table read (`handle.get()`) is self-scoped (client_id derived from the same principal) so the persisted projection does not leak across clients — only the live fan-out does, and only until 1b.
- **client_id resolution:** `String(principal?.id ?? 'anonymous')`. The `anonymous` principal carries `id:null` → 'anonymous' (all anon share one row). A user principal's id is the client key. The handle resolved client_id in the CALLER (not the handler) so the handler trusts the dispatch payload's `client` (the handle already did the write-grant check, same trust model as map/ordered/log handles).
- **id-strip (DECISIONLOG #62) does NOT apply:** that admission-gate id-strip concerns system-principal scheduled/tick dispatches whose payload is wrapped `{id:rowId, ...with}` for CRUD updates. An ephemeral .set is a USER-principal field-mutation dispatch whose payload is `{owner, client, cells}` (no `id` PK pointer), and it bypasses the scheduler/tick admission gates entirely (those intercept SYSTEM principals only). So no id-strip is needed.
- **Auth:** reuses `mayFieldOp(record, fieldName, write, row, principal)` — the SAME field-auth engine (row-grant.mjs). A field with no `.can` strong-inherits the row grant; the test entity uses `grant: () => [scope(() => everyone()).can(() => grant(read, write, subscribe))]` (a function-returning-array with an own `.can` body — `rowCapabilities` requires `typeof entityRecord.grant === 'function'`). Bare `grant(read,write,subscribe)` (no `.can`) denies everyone at runtime — the established model.
- **Mirror fidelity:** makeEphemeralHandle is byte-faithful to makeLogHandle — `membershipTable`/`membershipOwnerCol` for the side-table, `requireDispatch()` guard, `mayFieldOp` auth before mutation, dispatch re-entry with `randomUUID()` actionId, `result.granted` check → 403 on deny. The `ephemeralMutateHandlers` spread + projection apply branch + eventTypes registration mirror the ordered/log/map shape exactly. 685/0 (678 + 7 new), lead-verified raw.

## #69 — P6e-1b field-paced-live design resolution (GPT-5.5 consult ses_0e5c492aaf, lead-verified premises)
- **Direction source:** GPT-5.5 consult returned a decisive plan on the 5 architecture forks (not an empty return). Per AGENTS, lead VERIFIED every load-bearing premise against the code before adopting: HP-2 (serve.mjs:1041 postCommitConsumer parses `ev.scope` on `:` → entity:id, SELECTs the MAIN doc row, re-auth runs `mayVerb(canvas,'subscribe',docRow,...)` — correct: a subscriber's right to see strokes on canvas X is governed by their grant on canvas X, not the stroke payload; the invariant "one buffer never spans two scopes" is load-bearing); HP-4 (field-strategy.mjs:346 resolveStrategy error lists `ephemeral` as a valid kind while STRATEGIES has no entry + throws — cosmetic lie to reconcile); DECISIONLOG #15 (re-auth NO-CACHE-FIRST is the decided baseline, NOT a compromise — entry #60).
- **Fork 1 (subscribe surface, SPEC-decided):** wire message becomes `{type:'subscribe', entity, id, fields?, pace?}`. `fields` object keyed by field-handle → `{range:[lo,hi]} | {in:[...]} | {is:v} | true`. Subscribe-time validation: every field key a declared field; every coordinate plugin-published + indexable; closures rejected. Scalar/text/ref fields default INCLUDED; ephemeral (high-volume) is OPT-IN (omission = not-subscribed — never the 1a firehose). **Opaque cells for 1b:** interest at FIELD granularity only; per-coordinate narrowing within `cells` DEFERRED to P6e-3+ (a `cells TEXT` JSON blob is neither published-coord nor indexable — per-coordinate narrowing would be an unindexed scan, the exact "non-indexed coordinate is a subscribe-time validation error" SPEC rejects). subscribe-time validation accepts ONLY `true`/absent for an ephemeral field key in 1b; reject `range`/`in`/`is` on a non-indexed coordinate.
- **Fork 2 (registry shape, deletion-test decided):** `byEntity` restructured to `Map<entity, Map<id, Map<conn, SubSpec>>>` where `SubSpec = {fields, pace, latch}` (declaration, immutable per-subscription). CONCENTRATES (interest+pace+latch travel WITH the subscription) vs RELOCATES (a parallel `connInterest` map would drift). `connSubs` Set stays (O(subs) disconnect purge) — disconnect must now ALSO drop pending buffers/timers (leak + send-after-close). The per-(conn,scope,field) buffer is MUTABLE RUNTIME STATE in a separate flush-keyed structure — NOT folded into SubSpec (folding a draining timer/buffer into the registry value re-creates the "registry holds two lifetimes" smell). One SubSpec per (conn,entity,id); re-subscribe replaces.
- **Fork 3 (pace registry — SEPARATE seam, 3A):** a NEW framework-owned `PACE_STRATEGIES` registry keyed by field-KIND (mirrors STRATEGIES' shape) declaring per-kind `{coalescers, reduceSpan, profiles, bounds}`. 3B (a `.pace` sub-slot inside STRATEGIES) is REJECTED — it contradicts DECISIONLOG #61 + AGENTS "persistence is opt-in by engaged seam": ephemeral has NO STRATEGIES entry by design (the absent seam IS the ephemerality); a `.pace` inside STRATEGIES would force either re-coupling persist+pace (adding an ephemeral STRATEGIES entry) or a label-flipping special-case lookup. 3A keeps the seams ORTHOGONAL: a field engages the persist seam by having a STRATEGIES entry; it engages the pace seam by having a PACE_STRATEGIES entry; ephemeral has the second and not the first. Per-KIND not per-field-descriptor: the coalescer/seq-span/profiles/bounds are DATA SEMANTICS OF THE KIND ("a position is loss-tolerant" is a property of the kind, not one canvas's field); two canvases' strokes share latest-wins because they're the same kind. Per-*subscriber* window (the `window` in `pace.coalesce({window,by})`) is the subscriber's selection validated against the kind's `bounds` — windows-per-subscriber, profiles-and-lawfulness-per-kind. Do NOT add a default window to the field descriptor ("build no further"). The no-closures/data-not-code rule binds the SUBSCRIBER's selection, NOT the registry's reducer (the reducer is framework/plugin code, like STRATEGIES.validate/apply).
- **Fork 4a (buffer granularity):** per-(conn, scope, field). The field is free (already in `ev.eventType` `<Entity>.<field>.set` — the consumer extracts it without a lookup). Buffer key's `scope` component is the RE-AUTH UNIT — a (conn,scope,field) buffer can never mix scopes, so one cached re-auth decision per buffer is sound (this is the HP-2 invariant enforced structurally, not accidentally).
- **Fork 4b (re-auth signal, 3B-ii decided baseline):** UNCONDITIONAL re-run `mayVerb('subscribe')` at flush with the hydrated row cached PER FLUSH-TICK (hydrate once per (scope, flush tick), run the boolean mayVerb per conn). NOT latched; DECISIONLOG #15 names this the decided baseline (re-auth NO-CACHE-FIRST; add latched cache only after profiling). This is STRICTLY SAFER than a latch+dirty-set: re-running at flush against a freshly-hydrated row catches a revoked grant within one flush window (≤ pace window, ~66ms); a latch with a dirty-set is only as correct as the completeness of the dirty-set wiring — every share/role/roster/ownership mutation must mark the scope dirty, and any missed call site is a SECURITY HOLE (a revoked collaborator keeps seeing strokes). The dirty-set (i) is a DEFERRED perf optimization that must NOT ship until provably complete. Fail-closed wins over CPU.
- **Fork 4c (seq shape):** the coalesced event carries `{seq: lastSeq, seqSpan: [firstSeq, lastSeq]}`. The seq-span reducer is the kind's PUBLISHED function (lives in PACE_STRATEGIES per-kind, not a generic live.mjs helper) — "data semantics decide what is lawful to drop." For the ephemeral kind this is LOSS-SAFE because the projection is INSERT OR REPLACE (latest-wins, HP-1): `lastSeq`'s cells ARE the fully-reduced state, so a client that only ever sees `lastSeq` has correct state, and a reconnect bootstrap reads the same latest snapshot. The seq-span exists to keep the CURSOR PROTOCOL honest (treat any seq within the span as already-seen → no false gap-detection), NOT to recover dropped state. For a future non-loss-tolerant kind (block edits), the kind's `bounds` forbids a lossy coalescer (`by:'latest-wins'` not in `allowedBy`), so the span never drops recoverable state.
- **Fork 5 (retire-raw — 5B, ONE path):** ONE paced emit path; the `raw` verbatim branch is DELETED as a named alternative. No-pace ≡ a `pass-through` named profile = flush-on-receive + identity coalescer + inline re-auth — the SAME code path as paced with the window/timer steps as no-ops, NOT a separate verbatim branch. Observably identical to today's verbatim send (event delivered synchronously within the same emit call, re-authed against the freshly-hydrated row). 5A (keep `raw` as a no-pace branch) is REJECTED — a branch IS a path; the no-pace and paced branches would drift at the re-auth seam (paced caches hydrated row per flush-tick; raw hydrates per-emit) — exactly the bug class AGENTS "a working second path is still a second path" forbids. Kinds with NO PACE_STRATEGIES entry default to pass-through WITHOUT requiring an entry (absence of a pace entry = "pass-through, no coalescing is lawful"); only kinds that publish a coalescer can be paced. So a title-change delivers verbatim through the one path because its kind publishes no lawful coalescer, NOT because there's a second `raw` branch. This satisfies "pipeline variants are named wholes, not orthogonal flags": `pass-through` and `15fps` are NAMED PRE-VALIDATED PROFILES, never a `buffered:bool + window:n` flag lattice. SAFETY CAVEAT: if pass-through and paced cannot share the re-auth-row cache without a 1-tick delay on pass-through, STOP and re-consult — that would be the one fact resurrecting 5A.
- **Risks the implementation MUST preserve:** (1) buffer key includes scope — re-auth invariant rides on it; (2) re-auth+interest gate on the way IN, coalesce on the way OUT — never coalesce-then-authorize (SPEC §8 ordering; coalescing first collapses events that might individually pass/fail interest); (3) disconnect purges pending buffers + timers, not just registry entries (leak + send-after-close); (4) commit/projection UNCHANGED — 1b coalesces DELIVERY ONLY; the _Log append + INSERT-OR-REPLACE projection stay (DECISIONLOG #68); (5) `removed` events skip re-auth + bypass coalescing (the remove IS the revocation signal — flush immediately, never sit in a stroke buffer behind a window).
- **Implementation sequence (GPT-5.5 consult, lead-adopted):** Step 1 — new `src/field-pace.mjs` PACE_STRATEGIES registry per-kind {coalescers, reduceSpan, profiles, bounds} + ephemeral entry (latest-wins) + `resolvePace`/`validatePaceSelection` accessors; reconcile field-strategy.mjs:346 resolveStrategy error (drop `ephemeral` from the persist-kind list / throw a specific "no persist strategy" for ephemeral — the persist/pace seam split makes the old "unknown kind" lie wrong). Step 2 — subscribe message `{entity,id,fields?,pace?}` + interest validation + SubSpec stored in `Map<conn,SubSpec>`. Step 3 — `byEntity` registry reshape + add/remove/removeAll + disconnect-purges-buffers. Step 4 — ONE paced emit path (interest gate → re-auth per-flush-tick-cached row → enqueue per-(conn,scope,field) buffer → coalescer on flush → send `{seq:lastSeq, seqSpan}`); DELETE the raw verbatim branch as the same change (retire-in-same-change). Step 5 — DECISIONLOG refinement + full suite green. Generated by ds-v4-flash per lead brief, lead self-reviews per user directive.

## #70 — P6e-1b implementation: Slice A (pace registry) + Slice B1 (interest opt-in) + Slice B2 (paced emit path retires raw)
- **Outcome:** Slice A (committed c52b81e, +24 tests, 709/0): NEW `src/field-pace.mjs` `PACE_STRATEGIES` (frozen, ephemeral entry: `coalescers['latest-wins']=(_acc,e)=>e`, `reduceSpan=(events)=>({seq:events.at(-1).seq, seqSpan:[events[0].seq, events.at(-1).seq]})`, `profiles={'pass-through':{window:0,by:null},'15fps':{window:66,by:'latest-wins'}}`, `bounds={allowedBy:['latest-wins'],maxWindow:1000}`) + `resolvePace(kind)` (synthetic pass-through for absent kinds — absence = only verbatim lawful, NOT error) + `validatePaceSelection(kind,pace)` (subscribe-time fail-closed: rejects closures, profile must be in profiles, `by` must be in allowedBy, window positive ≤maxWindow, 400-shaped throws). field-strategy.mjs reconcile: `resolveStrategy('ephemeral')` now throws specific "engage the pace seam, not the persist seam" + unknown-kind error no longer lists `ephemeral` (persist/pace seam split).
- **Slice B1 (committed 4f06081, +11 tests, 720/0):** subscribe gains `fields` param (interest); `byEntity` reshaped `Map<entity,Map<id,Map<conn,SubSpec>>>` where `SubSpec={fields, latch:true}`; emit interest-filters EPHEMERAL events ONLY (3-part `<Entity>.<field>.set` w/ field-kind==='ephemeral' → deliver only if `subSpec.fields[field]===true`; re-auth FIRST then interest-filter per SPEC §8 ordering; all else incl `removed` = pass-through to all subs). This RETIRES 1a's firehose (ephemeral opt-in) — the interest-narrowing replaces the verbatim-to-everyone special-case in the same change (AGENTS retire-rule). `pace` param deferred (rejected `'pace not yet supported'`, forward-compat).
- **Slice B2 (committed f436878, +11 tests, 732/0):** the paced emit path. subscribe gains `pace` (validated via `validatePaceSelection('ephemeral', pace)` → resolved `{window,by}` stored in SubSpec.pace; one pace per subscriber applied to all their interested ephemeral fields). NEW `paceBuffers` Map (SEPARATE from SubSpec per F2 — mutable runtime state, not folded into the registry). ONE paced emit path: `window===0` (pass-through, the no-pace/default case) sends IMMEDIATELY inline with `seqSpan:[seq,seq]`; `window>0` (paced) enqueues into per-(conn,scope,field) buffer (`bufKey = ${conn.id}|${scope}|${field}` — scope IS the re-auth unit, F4a) + `setTimeout(()=>flushPacedBuffer(bufKey), window)`. `flushPacedBuffer`: clears buffer+timer FIRST (re-entrant safety), re-auth `mayVerb('subscribe')` fail-closed (throw/`!allowed` → drop silently, send nothing — DROP, NOT leak), coalesce via `PACE_STRATEGIES.ephemeral.coalescers[by]` (latest-wins → `events.reduce(c)` = last event), span via `reduceSpan(events)` → `{seq:lastSeq, seqSpan:[firstSeq,lastSeq]}`, send ONE coalesced envelope. This RETIRES raw verbatim-for-paced in the SAME change (AGENTS): pass-through IS the no-pace path on the SAME code (window/timer as no-ops), NOT a separate raw branch — the `5A` anti-pattern is rejected (F5). `removed` bypass is AUTOMATIC (2-part `<Entity>.removed` → `ephemeralField===null` → pass-through window=0 → inline, never buffered — no special-case); disconnect purges pending buffers+timers (`removeAll` :384-389 + `close` :523-526).
- **Lead self-review verified the 5 risks held:** (1) buffer key includes scope ✓; (2) re-auth+interest IN (emit :444/:461), coalesce OUT (flush :105) — never coalesce-then-authorize ✓; (3) disconnect/close purge buffers+timers ✓; (4) commit/projection UNCHANGED — B2 touches live.mjs DELIVERY ONLY, no pipeline.mjs/entity.mjs (DECISIONLOG #68 holds) ✓; (5) `removed` bypass is automatic ✓. SAFETY CAVEAT (F5) satisfied: pass-through sends synchronously inline (no setTimeout), so no 1-tick delay distinguishes pass-through from paced on the re-auth-row path — they SHARE the one path.
- **Noted NON-BLOCKING refinement (authzRow freshness):** #69 F4b's full flavor says "hydrate the row once per (scope, flush-tick)" at flush. The B2 impl refreshes `authzRow` PER-ENQUEUE (`entry.authzRow = authzRow` at each emit, live.mjs:503) rather than re-hydrating at flush. This is sound for high-frequency streams (60Hz strokes: each enqueue re-hydrates via `findById` → authzRow is ≤16ms stale at flush, so a revoked grant is caught within ~window+cadence ≈ 82ms). The only stale window is flush-fires-with-no-new-enqueue-after-revocation (idle stream) — and the buffered events it would deliver were ALREADY committed BEFORE revocation (bob HAD access when they committed), so delivering the already-buffered pre-revocation events is defensible; the NEXT emit's re-hydrate catches the revocation. The per-flush-tick re-hydrate (the consult's efficiency + tighter-revocation form) is a deferred followup — ship B2, tighten only if revocation latency proves insufficient. The revocation TEST (live-pace.test.mjs invariant 6) uses a stub `mayVerb` that ignores the row (returns `bobAllowed`), so it verifies the stub-revocation path not the production-row-staleness path — a test gap noted, not a code bug.
- **Decomposition rationale:** B was split B1 (interest/registry, retires 1a firehose, no pace) → B2 (pace/coalescing, adds capability). Because B1's emit was verbatim-with-interest-filter (ONE path) and B2 adds the paced path + retires verbatim-for-paced (pass-through IS the no-pace case), each slice retires/adds cleanly with NO intermediate "pace-accepted-but-ignored" state. Per user directive "generate code with ds-v4-flash + review yourself": ds-v4-flash generated all three slices (A: DONE clean 709/0; B1: landed work but return TRUNCATED — lead checked tree + re-gated 720/0; B2: returned `BLOCKED` with substantial landed work 729/2 — the 2 failures were TEST-construction bugs, not production: (a) B1's obsolete `pace not yet supported` assertion that B2 supersedes, (b) the revocation test's raw-createLiveServer db missing `executeFrameworkDDL` → `currentSeq` queried `_Cursor` which didn't exist → threw → outer `#authorizeAndSubscribe.catch(()=>this.error('forbidden'))` → `'forbidden'` at subscribe). Lead fixed both test bugs directly (compiled Canvas + setActiveDb + executeFrameworkDDL; updated B1 assertion to B2-acceptance + invalid-pace-rejection). Resume-discipline reaffirmed: a `BLOCKED`/truncated return with a dirty tree usually has recoverable work — check the tree + gate independently BEFORE re-dispatching.
- **Pacing model detail:** `pace = {profile:'15fps'}` (or `{coalesce:{window,by}}`). Validated once against the ephemeral kind (`validatePaceSelection('ephemeral', pace)`) → `{window, by}`. If pace present but no ephemeral field in `fields`, pace is unused (no error — harmless). Non-ephemeral fields are ALWAYS pass-through regardless of pace (only ephemeral publishes a coalescer in 1b). One SubSpec.pace per subscriber, applied to all their interested ephemeral fields.

## #71 — P6e-2 `durable-field-delta-live`: delivery-layer prev-shadow (no pipeline change); kinds split (compute value/state/crdt/struct, normalize store/ordered); cold→set-from-empty delta (NOT whole-state); pass-through only (P6e-4 stays separate)
- **Context:** the field-type `diff(previous, next, descriptor)` functions EXIST (field-strategy.mjs STRATEGIES: value/hash→`{set}`/null, state→`{from,to}`, crdt→crdtTextDiff insert/delete, store→storeMapDiff `{added,removed,changed}`, ordered→orderedListDiff by-KEY, struct→structDiff per-sub-cell) but are NOT connected to the live stream — subscribers receive whole NEW state, not deltas. The committed event carries NEW state only + is frozen BEFORE projection (pipeline.mjs:151), and the `.updated` projection apply does NOT read prev (entity.mjs:1111-1126 UPDATEs straight from event.data). So PREV is not captured anywhere at commit today. P6e-2 wires the diffs to the live stream so persisted high-frequency fields (collaborative crdt text) deliver DELTAS not whole-state. Snapshot/bootstrap is ALREADY handled (live.mjs:322 currentSeq race-detection; SPEC §12 LiveList boots from REST snapshot) — P6e-2 is DELTA DELIVERY ON THE LIVE STREAM ONLY, out of scope for bootstrap.
- **GPT-5.5 consult (ses_0e5380901) returned DECISIVE + corrected two load-bearing premises (lead VERIFIED both against code, AGENTS consult=plan-only):**
  - **PREMISE 1 (Fork 3, scope narrowed):** store/ordered do NOT ride `<Entity>.updated` — they emit their OWN native delta events on side-table scopes (`.added`/`.roleChanged`/`.removed`, `.inserted`/`.moved`/`.reordered` via the handles → mapMutateHandlers/orderedMutateHandlers) whose `event.data` IS already the structural delta. VERIFIED: the update handler builds `data = {...rest, id}` (entity.mjs:1189) so struct/store payloads land in `data`, but the `.updated` apply SKIPS store+struct (entity.mjs:1118-1119 `continue`). store/ordered persist via their native events, not `.updated`. So delta COMPUTATION is needed only for the `.updated`-borne kinds: **value, state, crdt, struct**. store/ordered are DELTA-NATIVE — P6e-2 only NORMALIZES the envelope key (puts their `data` under `delta`), no diff computed (computing storeMapDiff over a re-read would diff the wrong rows + is redundant). hash EXCLUDED (re-salt churn would broadcast a meaningless `{set:digest}` on every update; no live use case).
  - **PREMISE 2 (Fork 1, cold-shadow):** a cold shadow MUST NOT emit whole-state as a bootstrap seed — that double-delivers against the REST snapshot (dual reconciliation path, AGENTS-forbidden). Instead: cold shadow → diff against the field's empty/initial value → the diff fns already return a SET-FROM-EMPTY delta (`value.diff(undefined, next)→{set:next}`; `state.diff(undefined,next)→{from:undefined,to:next}`; `crdtTextDiff('',next)→insert-from-empty`; `structDiff(undefined,next)→all cells set`). Seed the shadow from the re-read row (serve.mjs:1051 `SELECT *`), emit nothing extra. The shadow is COMMITTED-state, never client-state — do NOT seed from the REST snapshot (would make the server's diff baseline depend on a client read = second source of truth). currentSeq already closes the snapshot-vs-live gap; the shadow doesn't.
- **Fork 1 — PREV source = (C) delivery-layer prev-shadow.** An in-memory `Map<scope, Map<field, value>>` per live server holding the last committed value for the diff-bearing fields. On each `.updated` event: `prev = shadow.getPrev(scope)` (cold→empty per field), `next` = the re-read row (serve.mjs:1051 already passes it to emit), compute `computeDelta(...)` per changed field, broadcast, then `shadow.seed(scope, row)`. NO pipeline/entity change; delta is delivery-only by construction (matches #68/#70 "delivery coalesces/projects, commit unchanged" + AGENTS "out-of-band effects are projections over the committed log"). live.emit is ALREADY a post-commit projection consumer (live.mjs:404-406). Option (A) pipeline-restructure (capture prev in the update projection apply via SELECT-before-UPDATE, attach delta post-projection-pre-fanout) REJECTED — would push a delivery concern into the commit/projection path, contradicting #68/#70 + forcing a pipeline-order change. The delta is a derivable projection; storing it duplicates a derivable fact.
- **Fork 2 — delta is DELIVERY-ONLY, never in _Log.** The delta is derivable from the in-memory shadow (always one hop, O(1)); the reconnect story is REST-snapshot + currentSeq, NOT _Log delta-replay (so the "prev=N events back, expensive" cost never arises). _Log stays the new-state log.
- **Fork 3 — granularity = ONE envelope per committed event carrying `delta: {[fieldName]: deltaObj}`** (a multi-field `.updated` emits a multi-entry delta MAP, not N envelopes). ONE committed event = ONE seq = ONE envelope (the client dedupe invariant live.mjs:402-403). Splitting one seq across N envelopes would break dedupe. This DIVERGES from 1b's per-(conn,scope,field) buffer keys — correct, because 1b's per-field keys exist for COALESCING ephemeral fields, and P6e-2 persisted deltas are pass-through (no buffering).
- **Fork 4 — PASS-THROUGH ONLY for P6e-2 (P6e-4 stays separate).** Persisted deltas resolve to `window:0` via field-pace.mjs resolvePace's synthetic pass-through descriptor for non-ephemeral kinds (the structure ALREADY forbids requesting coalescing — `validatePaceSelection` rejects any `by` for a kind whose `allowedBy` is empty). So persisted deltas hit the existing pass-through branch (live.mjs ~window=0, `seqSpan:[seq,seq]`, synchronous inline) + the `delta` field is attached there. NO new coalescing branch. P6e-4 = persisted delta coalescing with structural composers (crdt op-composition) + replay-visible spans (ADR #10). DO NOT MERGE — P6e-2's driver (collaborative crdt text) needs ORDERED delivery (which pass-through provides), not coalescing; human edit rate (~5-10Hz) is far below 60Hz, and each edit is a durable commit through the write queue (serve.mjs:1066) which paces it anyway. Merging would force crdt-op-composition prematurely.
- **Fork 5 — message shape: carry `delta` ALONGSIDE `event.data` (whole state preserved).** The envelope always carries `delta`; `event.data` (whole state) stays as today. A delta-aware client applies `delta`; a delta-unaware client ignores `delta` + uses `event.data` — ONE PATH, backward-compatible. The `mode`-flag alternative (delta-mode vs whole-mode per subscription) REJECTED — a per-sub toggle is a second delivery path (AGENTS "a working second path"). The redundancy for value/state (`{set:next}` ≈ whole value) is the price of one-path compat + bounded (single field); for the bandwidth-critical kinds (crdt/struct/store/ordered) the delta is MUCH smaller than whole-state. `created` events: OMIT `delta` (already carries whole state, no prev semantics). `removed` events: EVICT the shadow, no delta.
- **Risks the implementation MUST preserve:** (1) ONE seq = ONE envelope = ONE dedupe — a multi-field `.updated` MUST stay one envelope with a delta map, never split per-field; (2) shadow is COMMITTED-state, never client-state — never seed from REST snapshot, never emit a whole-state "bootstrap" event from live (dual-reconciliation-path trap); (3) evict the shadow on `removed` (live.mjs:415 `removed = row === undefined`) AND on disconnect/idle (mirror removeAll's pace-buffer purge live.mjs:383-389) — a stale shadow surviving delete-then-recreate diffs against a dead prev → corrupt crdt/struct delta; (4) crdt/struct diffs need FULL prev (whole strings/whole struct), not the partial `.updated` event.data — the shadow holds the full committed field value from the re-read row, NOT the partial event payload; (5) NO commit/projection change — no SELECT-before-UPDATE at entity.mjs:1111, no delta in _Log (#68/#70 delivery-only; pipeline freeze-before-projection stays); (6) hash fields EXCLUDED from delta; (7) store/ordered are delta-native — do NOT compute a diff, only normalize the envelope key (their `data` IS the delta); (8) pass-through invariance — persisted deltas resolve to window=0 via the existing field-pace.mjs synthetic descriptor; do NOT add a coalescing branch for persisted fields (P6e-4).
- **Implementation sequence (consult, lead-adopted, ds-v4-flash generates, lead self-reviews):** Step 1 — NEW `src/field-delta.mjs` pure `computeDelta(entityRecord, prevRow, nextRow, changedFieldNames) → {[field]:deltaObj}`: for each changed field resolve strategy → `strategy.diff(prev[field], next[field], descriptor)`, drop nulls (unchanged), skip hash, skip store/ordered (don't ride this path); + unit tests. Step 2 — prev-shadow store owned by the live server: `Map<scope,Map<field,value>>`, API `getPrev/seed/evict`, evict on `removed` + disconnect purge. Step 3 — wire shadow+delta into emit for `.updated` events only: read prev (cold→empty), compute delta over `committedEvent.data` fields, `shadow.seed`, attach `delta` to pass-through envelope; `removed`→evict+no delta; `created`→omit delta. Step 4 — normalize store/ordered native events: present `committedEvent.data` under `delta` keyed by the field parsed from the event type (3-part). Step 5 — confirm pass-through invariance (persisted coalesce request 400s; delta envelopes carry `seqSpan:[seq,seq]`). Decomp via 1b's Slice A/B pattern: Slice A = Step 1 (isolated module + tests); Slice B = Steps 2-5 (live.mjs wiring, the risky half).

## #72 — P6e-2 Slice A (field-delta module) + Slice B1 (`live.mjs` shadow + `.updated` delta wiring) implementation
- **Slice A (committed 24eaa47 + docs 19e8dd5, 748/0, +16):** NEW `src/field-delta.mjs` (53 lines) pure `computeDelta(entityRecord, prevRow, nextRow, changedFieldNames) → {[field]:deltaObj}`. `DIFF_ELIGIBLE = new Set(['value','state','crdt','struct'])` — only the `.updated`-borne kinds; skips non-eligible BEFORE `resolveStrategy` (so never hits the `resolveStrategy('ephemeral')` throw). Imports `resolveStrategy` from `./field-strategy.mjs` (does not re-declare STRATEGIES). Drops null deltas. Passes descriptor as 3rd arg uniformly (struct needs it; value/state/crdt ignore). Cold prev: `const prev = prevRow ?? {}` → missing field → undefined → diff fns return set-from-empty. `changedFieldNames != null ? changedFieldNames : Object.keys(fields)`. Pure: no I/O, no arg mutation. index.mjs re-exports `computeDelta`. test/field-delta.test.mjs (16 tests). ds-v4-flash generated (DONE clean 748/0); lead Gate-Function-verified independently.
- **Slice B1 (committed <this>, 756/0, +8):** live.mjs wiring — the risky half. Decomposed B into B1 (Steps 2-3: shadow + `.updated` delta, the P6e-2 driver) + B2 (Steps 4-5: store/ordered native-event normalization + pass-through invariance), mirroring 1b's B1/B2 split.
  - **Shadow store (Step 2):** closure-level `const prevState = new Map()` (live.mjs ~:76) mirroring `paceBuffers` (~:73) — SEPARATE from SubSpec (#69 F2: mutable runtime state belongs in its own structure). Helpers `prevGet(scope)=prevState.get(scope)??{}`, `prevSeed(scope,row)=prevState.set`, `prevEvict(scope)=prevState.delete`. Per-SCOPE (`${entity}:${id}`) NOT per-conn — committed state shared across subs. NOT purged on disconnect (other subs may share the scope; the shadow is committed-state not conn-state). Evicted on `removed` + cleared on `close()`.
  - **Delta block (Step 3, live.mjs ~:449-472):** computed ONCE per emit (per-(scope,event), same for all subs), AFTER authzRow resolved (~:422) + BEFORE the sub loop (~:474). `const scope = ${name}:${String(id)}; let delta = undefined;`. If `removed` → `prevEvict(scope)`. Else split event type on `.`: 2-part + `parts[1]==='updated'` → `prev=prevGet(scope)`, `changed=Object.keys(committedEvent.data??{}).filter(k=>k!=='id')`, `delta=computeDelta(entityRecord, prev, authzRow, changed)`, `prevSeed(scope, authzRow)`. 2-part + `parts[1]==='created'` → `prevSeed(scope, authzRow)` (seed so first update has prev; NO delta on create, #71 F5). 3-part/ephemeral/store/ordered → delta stays undefined (B2 normalizes store/ordered). `next` is `authzRow` (the hydrated re-read from `findById` post-commit) — full prev per #71 risk #4.
  - **Envelope (Step 3, live.mjs ~:511-519):** pass-through (window=0) branch builds `envelope = {type:'event', entity, id, seq, seqSpan:[seq,seq], event:committedEvent}; if (delta !== undefined) envelope.delta = delta; conn.send(envelope);`. Paced (window>0) branch UNTOUCHED — ephemeral events have delta===undefined by construction (computeDelta excludes ephemeral; 3-part events skip the delta block). `close()` (~:564): `prevState.clear();`.
  - **5 test-construction bugs lead caught + fixed (NOT production — lead found via running the gate + tracing):** (1) `subscribe` helper name-collided with the imported `subscribe` grant-capability → renamed to `wsSubscribe` (declaration + 8 call sites). (2-4) tests 3/6 (state/multi-field) expected warm-shadow diffs `{from:'draft',...}` but never emitted `.created` to seed the shadow → cold `prev` → `from:undefined` DROPPED by JSON serialization → client received `{to:'published'}`. Fix: emit `.created` (seq=1) to seed the shadow before `.updated` (seq=2), mirroring production (.create dispatches .created → post-commit consumer seeds shadow). (5) test 4's `emitRemoved` helper passed `row=undefined` (simulating removed) but didn't DELETE the DB row → re-INSERT failed UNIQUE. Fix: `DELETE FROM Canvas WHERE id=?` after emitRemoved (mirror production: the .remove handler DELETEs before the post-commit consumer re-reads). (6) test 8 emitted `.updated` with `{title:'b2'}` but never UPDATEd the DB → `authzRow` re-read `'b1'` → delta `{set:'b1'}`. Fix: UPDATE the DB before emit (mirror production: the .update handler applies the mutation BEFORE the post-commit consumer re-reads). LESSON reaffirmed: the raw-WS direct-emit test harness must replicate the FULL production post-commit sequence (mutation applied to DB → re-read hydrated row → emit) — the helpers `emitUpdated`/`emitCreated`/`emitRemoved` re-read from DB, so the DB MUST reflect the committed state BEFORE emit.
  - **Lead self-review verified all 5 #71 risks hold:** (1) ONE seq=ONE envelope (delta computed once, attached per envelope; test 6 verifies multi-key single-envelope) ✓; (2) shadow=committed-state (prevSeed uses authzRow re-read) ✓; (3) removed evicts shadow (prevEvict :461 + close clears :564; test 4 verifies delete-then-recreate→set-from-empty) ✓; (4) crdt/struct full prev (authzRow=hydrated re-read) ✓; (5) NO commit/projection change (only live.mjs) ✓; (6) hash excluded (computeDelta) ✓; (7) store/ordered untouched (3-part events skip delta block — B2 normalizes) ✓; (8) pass-through invariance (.updated is 2-part non-ephemeral → window=0) ✓.
  - **struct-persistence side-question (verified by reasoning, NOT a code test — pre-existing gap, #71 flagged it):** struct on `.updated` is DORMANT-but-correct on the 2nd+ update — `authzRow.share` = the OLD create-time struct (entity.mjs:1119 projection `continue` skips writing struct `<field>__<cell>` columns on `.updated`, so the re-read returns create-time struct), so `struct.diff(old, old)` = null → dropped. The 1st-update-after-cold-shadow produces a set-from-empty delta reflecting the PERSISTED (old) struct — a manifestation of the pre-existing struct-`.updated`-doesn't-persist gap (entity.mjs:1119), NOT P6e-2's to fix. No test exercises struct `.update` (the test entity declares `share: link()` but no test updates it), so no test fails. struct delta ACCURACY depends on wiring struct persistence on `.updated` (separate fix, flagged in PLANS). A test asserting the BROKEN behavior (struct .update doesn't persist) was NOT added — anti-pattern; the struct-persistence fix will add a test asserting it persists.
- **NEXT: B2 (Steps 4-5)** — normalize store/ordered native events (present `committedEvent.data` under `delta` keyed by field parsed from the 3-part event type) + confirm pass-through invariance (persisted coalesce request 400s; delta envelopes carry `seqSpan:[seq,seq]`). Then P6e-3 (ordered-list-identity-reconcile, key→id — orderedListDiff matches by KEY not identity), P6e-4 (durable-coalesced-log, ADR #10 replay-visible spans for persisted deltas), then P7 (client lib).

## #73 — P6e-2 B2 implementation: normalize store/ordered native events under `delta` (delta-native, no diff); pass-through invariance confirmed
- **Outcome:** committed 7e5cd6a-then-B2. ONE `else if` branch added to B1's delta block in live.mjs (~:471-477). Baseline 756 → 763 (+7 tests). Lead-verified raw gate 763/0. ds-v4-flash generated (returned DONE 763/0, ACCURATE — lead re-gated independently per Gate Function: tree `M src/live.mjs` + `?? test/live-delta-native.test.mjs`, diff = exactly the 6 specified lines, no overreach).
- **The edit (src/live.mjs ~:471-477):** after B1's `else if (parts?.length===2 && parts[1]==='created') prevSeed(...)`, added: `else if (parts?.length===3 && ephemeralField===null) { delta = { [parts[1]]: committedEvent.data }; }`. store/ordered native events are 3-part (`<Entity>.<field>.added`/`.roleChanged`/`.removed`/`.inserted`/`.moved`/`.reordered`); `ephemeralField===null` excludes the paced ephemeral `.set` (1b's path, its delta stays undefined). The existing pass-through envelope (~:511-519) ALREADY does `if (delta!==undefined) envelope.delta = delta` — so once `delta` is set, the envelope picks it up. NO envelope change, NO paced-branch change, NO pipeline/entity/ddl change.
- **Delta-native (no diff):** a store/ordered event's `committedEvent.data` IS already the structural delta (mapMutateHandlers emit `{owner,member,role}` for `.added`/`.roleChanged`, `{owner,member}` for `.removed`; orderedMutateHandlers emit `{owner,id,key,value}` for `.inserted`, `{owner,id,key}` for `.moved`). So B2 does NOT call a diff fn — it normalizes the envelope KEY only: presents `data` under `delta:{[fieldName]:data}` so a client dispatches ONE uniform delta shape regardless of kind (#71 F3). `log()` is kind 'store' (field.mjs:151), so its `.appended` events are normalized too — consistent (a log append IS a delta).
- **Pass-through invariance confirmed (#71 Step 5):** store/ordered events have `ephemeralField===null` → pace stays `{window:0,by:null}` → pass-through branch → delivered synchronously inline with `seqSpan:[seq,seq]`. Test 6 verifies: a subscriber with `pace:{profile:'15fps'}` gets a non-ephemeral `.added` event IMMEDIATELY (pass-through, NOT coalesced) within 400ms, with `seqSpan:[1,1]` + `delta` present. (Pace is accepted at subscribe-time because `validatePaceSelection` validates against the ephemeral kind, which allows '15fps' — but Doc has no ephemeral field, so pace is unused; non-ephemeral events bypass coalescing at the EMIT level. The structural forbid — a kind whose `allowedBy` is empty rejecting `by` — is enforced at subscribe for ephemeral-field requests in field-pace.mjs Slice A; non-ephemeral kinds resolve to window=0 via resolvePace's synthetic pass-through descriptor.)
- **Lead self-review verified all 8 #71 risks hold:** (1) ONE seq=ONE envelope (store/ordered native event = ONE committed event = ONE envelope with delta map) ✓; (2) shadow=committed-state (store/ordered do NOT touch prev-shadow — no prevSeed; delta=event.data directly) ✓; (3) removed evicts shadow (unchanged B1 branch) ✓; (4) crdt/struct full prev (N/A — no diff) ✓; (5) NO commit/projection change (only live.mjs one else-if) ✓; (6) hash excluded (hash is kind 'value', rides 2-part `.updated` → computeDelta excludes it; B2's else-if is 3-part only) ✓; (7) store/ordered delta-native NO diff (`delta={[field]:event.data}`, no strategy.diff call) ✓; (8) pass-through invariance (window=0, seqSpan:[seq,seq]) ✓.
- **NEW test/live-delta-native.test.mjs (7 tests):** map `.added`/`.roleChanged`/`.removed` delta shape + event.data + seqSpan; ordered `.inserted`/`.moved` delta shape; pass-through invariance (pace subscriber gets non-ephemeral inline + seqSpan:[1,1] + delta); backward-compat (envelope has BOTH `delta` AND `event`/`event.data`/`event.type`). Entity `Doc{title:text(), collaborators:map(ref('User'),{role}), items:list(text())}` grant `[scope(()=>everyone()).can(()=>grant(read,write,subscribe))]`. Raw-WS harness mirrors live-delta.test.mjs (openRawWS verbatim + bootServer with setActiveDb+executeFrameworkDDL+generateDDL(User)+generateDDL(Doc) + wsSubscribe named to avoid `subscribe` collision + emitNative helper). LESSON reaffirmed: the B1 raw-WS harness rules (wsSubscribe naming, framework DDL, setActiveDb) applied cleanly here — 0 test-construction bugs (B2 is simpler: no shadow, no prev, direct emit of native events).

## P6e-2 COMPLETE (`durable-field-delta-live`). Persisted high-frequency fields deliver DELTAS not whole-state.
- Slice A (field-delta module, 24eaa47) + Slice B1 (shadow + `.updated` delta, 7e5cd6a) + Slice B2 (store/ordered normalization, this slice). Baseline 732 → 748 → 756 → 763. The 5 consult forks (#71) + 8 risks all verified in code. The collaborative-crdt-text driver (value/state/crdt/struct deltas on `.updated`) + the store/ordered delta-native normalization are both live. struct-`.updated`-persistence gap flagged (pre-existing, entity.mjs:1119, NOT P6e-2's fix). P6e-4 (durable-coalesced-log, ADR #10 replay-visible spans) is the SEPARATE persisted-delta-coalescing slice — do NOT merge with P6e-2 (#71 F4).
- NEXT: P6e-3 (ordered-list-identity-reconcile — orderedListDiff matches by KEY not identity; reconcile key→id), then P6e-4 (durable-coalesced-log ADR #10 replay-visible spans for persisted deltas), then P7 (client lib).

## #74 — P6e-3 `ordered-list-identity-reconcile` — DIRECTION: VESTIGIAL (deleted orderedListDiff); ordered's "diff machinery" = the native per-op identity-keyed events, NOT a whole-list snapshot diff
- **Context:** the recorded plan framed P6e-3 as "reconcile orderedListDiff key→id." Lead gathering of verbatim facts surfaced a genuine architecture fork (AGENTS hard-gate: don't let the agent drive architecture on tech lead is unsure of): `orderedListDiff` (field-strategy.mjs:85-106, deleted) was (a) broken-by-design — it matched list elements by `e.key` (volatile fractional-index sort position) NOT `e.id` (stable identity), so its `added`/`removed`/`moved` outputs were keyed by position; (b) DEAD CODE in the runtime — its only caller was `field-delta.mjs:45` (`strategy.diff(...)`), but `field-delta.mjs` EXCLUDES the ordered kind via `DIFF_ELIGIBLE = new Set(['value','state','crdt','struct'])` BEFORE `resolveStrategy`, so the ordered `strategy.diff` was never reached (zero production callers; the only references were 2 unit tests asserting the broken-by-key behavior in field-merge.test.mjs); (c) superseded for the live path by the delta-native per-op events (P6e-1b B2 normalized store/ordered native events under `delta:{[field]:event.data}` in live.mjs ~:471-477).
- **Lead's load-bearing observation (consult CONFIRMED it):** `orderedListDiff`'s INPUT SHAPE was itself wrong, not just its match key. It took arrays of `{key, ...item}` and built maps keyed on `e.key`. But the ordered side-table stores `(id, key, item)` ROWS (ddl.mjs:102-113, PK=`(ownerCol, id)`) — a faithful ordered snapshot to diff would be `[{id, key, item}]` keyed by `id`. So it was not just "match by id not key" — the whole snapshot-diff framing is wrong for ordered: the per-op identity-keyed events (`.inserted→{owner,id,key,value}`, `.moved→{owner,id,key}`, `.reordered→{owner,entries:[{id,key}]}`, `.removed→{owner,id}`, minted by orderedMutateHandlers entity.mjs:1322-1377) ARE ordered's correct delta contract. A whole-list snapshot diff is a category error for a fractional-index keyspace (like diffing two JSON dumps of a database: you'd reinvent the per-row events).
- **GPT-5.5 consult (ses_0e4e6c7bb) ruled B — VESTIGIAL, with a P6e-4 follow-up decision** (lead VERIFIED the one load-bearing premise the consult flagged — "no public export exposes orderedListDiff/strategy.diff for ordered" — against code: index.mjs exports only `resolveStrategy, validateMutation, ValidationError` from field-strategy.mjs; `orderedListDiff` was internal + unreferenced by production runtime; only the 2 unit tests referenced it). The consult confirmed the lead's observation + ruled ordered's SPEC §5.3 "diff + index + inverse machinery" IS satisfied by: stable element identity (`id`), sortable position (`key`), operation API (`insertAt`/`move`/`reorder`/`remove`), the native deltas emitted from those operations, and the side-table projection keyed by `(owner, id)`. A separate `orderedListDiff(previous, next)` would create a SECOND reconciliation path (forbidden by AGENTS "one reconciliation path" / "a working second path is still a second path") — native ops produce identity-correct deltas; a snapshot diff lossily reconstructs deltas from the wrong input shape, unable to preserve operation intent, coalescing boundaries, inverse semantics, or conflict behavior without inventing rules P6e-4 has not chosen (AGENTS "build no further than the known use case").
- **Option A (fix orderedListDiff to match by `id`) REJECTED** — would preserve a second, unused reconciliation shape before any consumer has proven it is the right one; correcting dead code into plausible-looking dead code.
- **Option C (defer P6e-3, leave orderedListDiff in place) REJECTED** — preserving a broken contract-shaped artifact that future agents may trust is WORSE than having no abstraction. Pure C (defer) would leave the broken-by-key function + its 2 broken tests as a latent trap.
- **The deletion (lead implemented directly — mechanical, resume-discipline: ds-v4-flash empties on heavy slices was not worth the round-trip for a deletion):**
  - DELETED `orderedListDiff` function (field-strategy.mjs, was :85-106, ~22 lines).
  - REMOVED the `diff` property from the ordered strategy (field-strategy.mjs ordered strategy now `{validate, apply}` only — the ABSENT `.diff` is the structural declaration of "ordered is per-op, not snapshot"; NOT a throwing `.diff` — a throwing function would be imperative ceremony restating what the absence already declares, AGENTS "declaration absorbs imperative wiring").
  - DELETED the 2 field-merge.test.mjs ordered-diff tests (asserting the broken-by-key behavior) + the unused `ordered` const.
  - REWROTE field-strategy.test.mjs shape-assertion loop: `value`/`store`/`crdt` assert `{validate, apply, diff}`; `ordered` asserts `{validate, apply}` + `diff === undefined` (the test ASSERTS the new contract — structural absence IS the positive assertion, deletion-test applied to the test suite).
  - UPDATED 4 comments (field-strategy.mjs ordered strategy, field-merge.test.mjs header, field-strategy.test.mjs header, list-field.test.mjs header) to reference #74 + explain the native-event contract.
- **Verification:** the only `strategy.diff(` caller in src/ is `field-delta.mjs:45`, guarded by `DIFF_ELIGIBLE` (excludes ordered) → ordered's (now-absent) `.diff` is never reached. Net deletion: -2 tests (763 → 761), -~25 lines. Suite 761/761/0, lead-verified raw gate.
- **P6e-4 deferral ruling (consult):** P6e-4 (durable-coalesced-log, ADR #10 replay-visible spans for persisted deltas) is the prospective consumer of an ordered reconcile. P6e-4 decides the required ordered replay/coalescing shape from its ACTUAL consumer: (1) replay unit (native ops as-is vs coalesced spans); (2) coalescing semantics (can moves of the same `id` collapse? does insert+remove disappear? remove+insert = replacement/resurrection/two-identities? is `reordered` canonical or expanded to per-id moves?); (3) inverse semantics (does undo need exact prior positions? computed at write or replay time?); (4) snapshot-reconcile need (is there a real path with only two ordered snapshots + no operation log? if yes, what's the authority model when they disagree?). ONLY if P6e-4 proves "we have two authoritative snapshots + must compute a delta between them" should a new ordered snapshot reconciler be introduced — and if introduced, named as snapshot reconciliation, NOT as ordered's primary diff contract. P6e-3 is REMOVED from the roadmap as a standalone implementation slice (it was a vestige, not a real slice).
- NEXT: P6e-4 (durable-coalesced-log ADR #10 replay-visible spans for persisted deltas — the prospective consumer that decides whether ordered snapshot-reconciliation is ever needed), then P7 (client lib).

## #75 — P6e-4 DEFERRED (split into durability-side + delivery-side, both consumer-gated); proceed to P7
- **Context:** P6e-4 was the last recorded P6e slice. ADR #10 (DECISIONLOG #10 + SPEC §10:526-529) specifies "Tick durability is a field-type persistence-strategy decision (ephemeral = no durable events; a **coalesced-snapshot-with-sequence-span for high-frequency persisted fields so cursor replay stays correct**; per-event for the rare case)." The P6e-2 consult (#71 F4) bundled P6e-4 as "persisted delta coalescing with structural composers (crdt op-composition) + replay-visible spans (ADR #10)" + ruled DO NOT MERGE with P6e-2 (collaborative crdt text is human-rate ~5-10Hz, needs ordered delivery not coalescing). P6e-3's deferral ruling (#74) named P6e-4 as the prospective consumer deciding IF ordered snapshot-reconciliation is ever needed.
- **GPT-5.5 consult (ses_0e4d42a5d) ruled C — SPLIT + DEFER both sub-slices until a real high-frequency persisted consumer exists; proceed to P7.** Lead VERIFIED all 5 load-bearing premises against code (AGENTS consult = plan only). The two concerns the #71 consult bundled are DISTINCT SEAMS:
  - **DURABILITY-side (commit/_Log):** coalesce high-frequency persisted commits into snapshot+seqSpan _Log rows (fewer _Log rows; cursor treats any seq in [first,last] as already-seen; resync reads latest snapshot). Touches pipeline.mjs:144-152 + field-type persistence strategy + replay/resync.
  - **DELIVERY-side (live stream):** extend PACE_STRATEGIES so persisted kinds (value/state/crdt/struct) can publish lawful coalescers (structural composers — crdt op-composition, value latest-wins) + allow subscribers to pace persisted delta delivery (the P6e-1b pace seam, opened to persisted). Touches field-pace.mjs + live.mjs. TODAY structurally forbidden (resolvePace synthetic pass-through, validatePaceSelection rejects `by` for `allowedBy:[]` kinds).
- **Why NOT build now (A):** NO real high-frequency persisted consumer exists — VERIFIED: space-invaders `match.mjs` is ASPIRATIONAL (entity-level tick commented out "GAP: ...API surface is not defined"; `invaders`/`projectiles` = `text({default:'[]'})` with comment "writes to DB every frame — catastrophic at 30 Hz" = a documented UNRESOLVED pain, not a running field); tick tests (`tick-engine`/`reaper`/`tick-admission`) declare `tick.hz(20/60/100)` but drive ONE-SHOT state transitions (a `status` field flip that EXITS the `while`-set after one firing — tick-as-trigger, NOT 30Hz persisted writes); the only real high-frequency case (drawing-canvas 60Hz strokes) is EPHEMERAL, already shipped by P6e-1a/b; collaborative crdt text is human-rate, already shipped by P6e-2 pass-through. AGENTS build-no-further-than-the-known-use-case: ADR #10 is the SPECIFICATION SEED, not permission to build unexercised machinery. A persisted field currently works through P6e-2 pass-through (1 envelope per event, seqSpan:[seq,seq], ordered) — no persisted field needs 20-60Hz durable coalescing today.
- **Why NOT single-defer (B):** B has the right defer decision but keeps two DISTINCT seams (reducing durable log/write pressure vs reducing live delivery pressure) bundled — risks implementing both when only one is needed, or designing one around assumptions from the other.
- **Why NOT P6e-3-style non-slice/vestige (D):** P6e-4 is NOT the same class as P6e-3. P6e-3 had dead/broken/superseded CODE to delete (orderedListDiff). P6e-4 has NO existing bad path — it's an UNBUILT ADR-backed design slot. The correct framing is NOT "this was wrong" (P6e-3) but "this is real ONLY when a consumer proves it" (P6e-4) — SPECIFIED-NOT-BUILT, CONSUMER-GATED, not deleted.
- **SPEC §7.1 reconciliation (the load-bearing design constraint for the FUTURE durability slice):** §7.1 says "resync folds the missing events through the reducers in order; the reducer fold is the source of truth and NEVER replaces the snapshot wholesale (except at initial bootstrap)." ADR #10's "coalesced-snapshot-with-sequence-span" means a single _Log row carries a snapshot + [firstSeq,lastSeq] — which REPLACES state (doesn't fold reducer outputs). Resolution: a coalesced snapshot row is LAWFUL ONLY when the field kind's reducer IS replace — i.e. value/state latest-wins (snapshot = set field to this committed value for the span) + struct IF declared latest-wins/full-state-replace. CRDT is NOT lawful by default — coalescing insert/delete ops into a final snapshot DESTROYS the per-op intent (causal ordering, tombstones, concurrent-merge behavior) the crdt reducer needs; crdt may use a future checkpoint form ONLY if the crdt field type explicitly publishes causal-frontier/version semantics. The future durability design MUST NOT say "persisted fields can snapshot-coalesce" — it must say "only field kinds that publish a lawful durable-checkpoint reducer may use snapshot+span rows," and replay treats a checkpoint row AS a reducer input (not an out-of-band replace).
- **Load-bearing FUTURE premise (lead VERIFIED):** a 30Hz persisted `.updated` writes the entity ROW every time (entity.mjs:1111-1126 `UPDATE ${table} SET ... WHERE id=:id` — one row-write per event) + `_Log` every time (pipeline.mjs:144 — one _Log INSERT per event). So `_Log` coalescing ALONE does NOT solve the 30Hz DB pain — the future durability slice may need to COALESCE THE FIELD PERSISTENCE SEAM ITSELF (the entity-row UPDATE cadence), not merely the _Log projection. The future consumer's actual bottleneck (DB row-write rate vs _Log append rate vs wire delivery rate) determines which seam(s) to coalesce.
- **Trigger to reopen P6e-4:** a REAL high-frequency persisted consumer — e.g. the space-invaders match gets an actual tick.hz(30) loop driving a persisted game-state field (`invaders`/`projectiles`/`players.x`) at 20-30Hz durable commits across hours, OR a new project surfaces one. The consumer must make clear: field kind, write rate, the actual bottleneck (DB/log writes vs live delivery vs both), and whether final-state snapshots preserve semantics. Verification MUST include an integration/project test driving RECURRING persisted mutations (NOT one-shot tick transitions).
- **P7 does NOT depend on P6e-4.** P6e-2's pass-through contract already gives every persisted event: durable `seq`, `seqSpan:[seq,seq]` (single-event span), ordered delivery, delta-native event data. That is the COMPLETE contract a client LiveList reducer/cursor needs. The P7 client SHOULD implement a span-aware cursor generically (accept a [first,last] seqSpan — treat any seq in the span as already-seen, advance to last+1) even though TODAY every span is one event wide — so the client is forward-compatible with a future P6e-4 coalesced-span WITHOUT requiring it to exist.
- **No runtime code changed for P6e-4.** `git diff` = docs/planning only (this DECISIONLOG entry + PLANS checkpoint). Proceed to P7 (client lib: LiveList/LiveChannel reducer + seq-cursor + span-aware replay + the bootstrap-before-stream ordering §7.1 mandates structurally).

## #76 — P7 client library architecture: LiveChannel (transport) + LiveList (doc-reducer) + createLiveStore (factory/dispatch/optimistic); user confirmed optimistic-apply (the AGENTS-mandated design)
- **Context + premise verification (lead VERIFIED all consult premises against code, AGENTS consult=plan-only):**
  - **WS envelope** (live.mjs:519-526): `{type:'event', entity, id, seq, seqSpan:[first,last], event, delta?}` where `event`=frozen committedEvent `{type,scope,data,seq,actionId,committedAt}` (pipeline.mjs:151). `seqSpan:[seq,seq]` today (pass-through).
  - **`subscribed` ack carries `currentSeq`** (live.mjs:123-128 comment + :336): the per-scope committed seq at subscribe time, designed EXACTLY for the snapshot-vs-live race-close ("if the server's currentSeq is ahead of the client's bootstrap cursor, the client resyncs the gap before going live"). THIS IS THE MECHANISM for Fork 8 — the consult surfaced it; lead had missed it.
  - **REST surface (serve.mjs):** NO unified `/dispatch` endpoint. 5 CRUD routes per mounted entity (app.mjs mount via `routes:(r,Entity)=>...` thunk): POST/PATCH/DELETE/GET `/:entity[/:id]` → raw row + status. `GET /snapshot/:entity/:id`→`{snapshot,seq}`; `GET /events-since/:entity/:id?cursor=N`→`{events:[...]}` one-row-per-event OR `{resync:'stale',reason:'cursor-behind-retention'}`. Field/collection sub-routes (`/docs/:id/collaborators`) are APP-AUTHORED IMPERATIVE routes (`r.post('/:id/collaborators',...)`, app.mjs:36-37), NOT framework-derived.
  - **Cursor protocol (§7.1):** dup(seq<exp)→skip; gap(seq>exp)→resync; next(seq==exp)→reduce+advance; reducer fold = source of truth, NEVER wholesale-replace (except bootstrap); stale→forced re-bootstrap (never silent truncate). Bootstrap ordering LOAD-BEARING: snapshot+cursor-set BEFORE stream-start (race = permanent event loss).
  - **`event.actionId`** on the committed envelope (pipeline.mjs:151) → available for optimistic→committed correlation (client dispatch generates actionId; committed event carries it).
  - **Auth = session cookie** (`sessionPrincipalOf`, `SESSION_COOKIE`); browser auto-sends on same-origin fetch+WS; `credentials:'include'`. CSRF guard (csrf-origin.test.mjs) requires same-origin Origin on mutations — browser sets Origin automatically; lib doesn't manage auth ("principal built server-side from session; client-supplied id is transport correlation only" — SPEC §12).
- **GPT-5.5 consult (ses_0e4c5c705) — DECISIVE, ruled all 9 forks; lead verified.** User confirmed the ONE product call (optimistic-apply, per AGENTS mandate).
- **Fork 1 — `createLiveStore({baseUrl, name})` factory** as top-level DX + export `LiveChannel`+`LiveList` as primitives. Factory owns URL derivation (snapshot/events-since/CRUD/wsBase from baseUrl+name), shares ONE LiveChannel across all subscriptions; `store.subscribe(id)→LiveList`, `store.dispatch(type,payload)→Promise<Result>` (never throws), `store.one/create/update/remove` (sugar), `store.action(type,{method,path})` (register app-imperative field routes), `store.close()`. REJECTED `new LiveList({baseUrl,name,id})` per-doc as primary (forces hand-managed shared channel, breaks the "none of it event handling" ceiling).
- **Fork 2 — client-side CRUD verb-map, NO unified `/dispatch` endpoint.** `dispatch('Doc.create'|'Doc.update'|'Doc.remove', payload)`→POST/PATCH/DELETE, `credentials:'include'`, one `decodeResult(res)` shared by BOTH call sites: (a) the dispatch fetch path + (b) the resync `fetch('/events-since')` path — so `{resync:'stale'}`, `{error}`, success rows can't drift. Field/collection verbs (`Doc.collaborators.add`) have NO framework route → `store.action()` registers them explicitly (app-imperative URLs). REJECTED server-side `/dispatch` endpoint (server change out of P7 scope; duplicates the CRUD pipeline; AGENTS singular-system / subtract-before-add).
- **Fork 3 — OPTIMISTIC APPLY IS REQUIRED (consult REVERSED lead's "no optimistic" preference; user CONFIRMED optimistic).** AGENTS.md:61-63 mandate: "`ingest`, for the client's own echoed events and for foreign live events alike. There is no second 'apply' path. Optimistic apply is a *visible placeholder*; `ingest` is what resolves it." SCOPE-FINDINGS §2.2 step 5 = "optimistic apply — apply a *visible placeholder* so the UI moves immediately"; step 7 = "ingest — fold each echoed event through its reducer exactly once." The model: dispatch (1) captures preimage, (2) applies a VISIBLE PLACEHOLDER + re-renders, (3) fires REST mutation, (4) on HTTP failure rolls back preimage + marks op failed visibly (still `dispatch` does not throw — returns failed result shape), (5) on success does nothing further — the COMMITTED event arrives over the live stream + folds through ONE `ingest` path, which REPLACES the placeholder with committed state + advances the cursor. Placeholder = transient "pending" marker on the row, correlated by `actionId` (client generates it; envelope's `event.actionId` carries it back); `ingest` clears it on the matching committed event. ONE write path (`ingest`); placeholder is NOT a parallel reducer (AGENTS one-reconciliation-path honored). REJECTED no-optimistic (contradicts binding AGENTS + SCOPE-FINDINGS; fails the "UI moves immediately" DX). Lead's Fork-3 preference was a misread of the binding docs.
- **Fork 4 — LiveChannel (transport-per-origin) vs LiveList (doc-reducer) boundary.** LiveChannel = WS transport per `baseUrl` (socket, auto-reconnect+backoff, subscribe/unsubscribe multiplex, route each envelope to registered LiveList by `(entity,id)`). LiveList = one doc-reducer (bootstrap ordering, cursor, reducer fold, render callback, resync, stale-hard-fail, optimistic reconcile). The `/me/inbox` "room" IS a LiveList on the Inbox entity subscribed by recipient id-scope (§8.3 places `_upsert` on the LiveList, confirming the reducer lives there not the channel). REJECTED LiveChannel owning per-doc reducers (fuses transport+state; re-auth/re-render leak across docs).
- **Fork 5 — span-aware cursor SHIP NOW (P6e-4 forward-compat).** On `{seqSpan:[first,last]}`: `last<exp`→dup skip; `first>exp`→gap→resync; `first≤exp≤last`→advance to `last+1` (apply the coalesced event ONCE — when `first==exp` today, apply then set cursor to `last+1`, NOT skip). Resync (`/events-since`) is point-per-row → resync fold is strictly `seq==exp` stepping, no span logic there. Costs ~5 lines now, avoids a protocol break when P6e-4 emits multi-event spans.
- **Fork 6 — the library ships a FIXED kind-aware reducer (page does NOT supply one).** `.created`→upsert row from `event.data`; `.updated`→apply `event.data` fields THEN apply `delta` per-kind (value `{set}`, state `{from,to}`, crdt op-list insert/delete-at-offset, struct `{cells:{name:{set}}}`); `.removed`→delete; 3-part store/ordered/`:appended`→apply `delta[field]` (the native `event.data`) to the sub-collection. `_upsert` (§8.3) is the row-level primitive INSIDE the reducer, not a page-supplied reducer. Load-bearing (for Slice B): the per-kind delta shapes MUST mirror `field-delta.mjs` + `field-strategy.mjs` exactly — crdt op-application (insert/delete at offset) + ordered fractional-index re-sort are non-trivial; the reducer is only correct if it inverts what the server emits. REJECTED page-supplied reducer (blows the 30-80-line ceiling; duplicates CRDT logic per app).
- **Fork 7 — resync + stale-cursor hard-fail.** Gap/stale = SEQUENCE condition, NOT socket condition → re-bootstrap = snapshot-refetch + cursor-reset + KEEP stream open (do NOT reconnect WS — the gap arrived over a healthy socket; reconnect conflates two failure axes + drops healthy subscriptions). During resync, LiveList QUEUES incoming stream events + processes them against the post-resync cursor (races the fold otherwise). WS reconnect only when the socket itself dropped (LiveChannel's concern, handled by backoff + re-subscribe → re-runs bootstrap ordering per LiveList).
- **Fork 8 — bootstrap ordering = promise-gate + queue-until-bootstrapped + USE `currentSeq` from the `subscribed` ack.** `LiveList.subscribe()` returns a promise resolving ONLY after: (1) `/snapshot` fetched, (2) cursor set to `snapshot.seq`, (3) WS subscribe sent, (4) `subscribed` ack received, (5) compare `ack.currentSeq` to cursor — if `currentSeq > cursor`, resync the race-gap BEFORE releasing queued events. Events for a not-yet-ready LiveList (cursor still being set) are QUEUED (not dropped, not thrown). Makes the race STRUCTURALLY IMPOSSIBLE — verified server designed `currentSeq` exactly for this (live.mjs:123-128). REJECTED "refuse/throw until ready" (pushes a race the framework absorbs back to the page).
- **Fork 9 — three slices in order:** Slice A = LiveChannel (WS transport: socket, reconnect+backoff, subscribe/unsubscribe multiplex, envelope-routing by `(entity,id)`) + test/live-channel.test.mjs gate. Slice B = LiveList (bootstrap w/ currentSeq, span-cursor, kind-aware reducer, resync, stale-hard-fail, render callback) + test/live-list.test.mjs gate. Slice C = createLiveStore factory + dispatch + decodeResult + optimistic reconcile (preimage/placeholder/actionId-correlate/rollback) + test/live-store.test.mjs gate. Layerable: B depends on A's envelope delivery (B injects a fake channel in tests); C depends on B's ingest. ds-v4-flash generates each slice per lead brief; lead self-reviews per user directive.
- **DX sketch (consult, ~15 lines):** `createLiveStore({baseUrl:location.origin, name:'docs'})` + `docs.action('collaborators.add',{method:'POST',path:'/:id/collaborators'})` + `const doc=docs.subscribe(DOC_ID)` + `doc.onRender(state=>{...})` + `await doc.ready` + `docs.dispatch('docs.update',{id,title})`. One factory + declarative render callback; comfortably inside the 30-80-line ceiling.
- **NEXT: Slice A (LiveChannel transport) implement.** NEW `public/workbench-client.mjs` (slice A exports `LiveChannel`); NEW `test/live-channel.test.mjs`. Design decisions durable here (#76) + in the consult brief.




## #77 — P7 Slice A DONE (LiveChannel transport) + WS graceful-close leak fixed (server-side RFC 6455 parser bug)
- [2026-07-01] **Slice A COMPLETE** (committed 28a4afc; leak fix cc996cc; orphan test 34c47d7; 773/773/0, 12.9s). NEW `public/workbench-client.mjs` exports `LiveChannel`: ONE WebSocket per channel multiplexed across `(entity,id)` subs, zero deps (Node 22+ global WebSocket). `subscribe(entity,id,onEvent)` opens socket lazily → returns `{currentSeq}` from the `subscribed` ack (Fork 8 mechanism), rejects on `error` envelope; `unsubscribe` resolves on `unsubscribed` ack or 2s timeout; auto-reconnect exponential backoff + re-subscribe-all on reconnect (server loses sub state on socket close, Fork 7); `close()` full teardown. Envelope contract matches live.mjs verbatim `{type:'event',entity,id,seq,seqSpan,event,delta?}`. NEW `test/live-channel.test.mjs` 6 e2e tests vs a real workbench server.
- [2026-07-01] **LEAK FIX (load-bearing — LiveChannel was the FIRST graceful WS client, exposed a latent SERVER bug):** undici's global WebSocket (what LiveChannel uses) closes by sending a MASKED ZERO-LENGTH close frame `88 80 <4-byte mask>` (6 bytes, no payload). `FrameParser#processBuffer` (websocket.mjs:147) looped `while (buffer.length > 0)` — for a zero-length payload the MASK state consumes the final 4 mask bytes, emptying the buffer, so the loop EXITED before the PAYLOAD state could finalize the zero-length frame. The close frame (opcode 0x8) was NEVER emitted → live.mjs `#drain` never acked → undici client stuck in readyState 2 (CLOSING) forever, never firing `close`, never releasing its socket → 4 leaked Socket handles → event-loop pinned → test file hung ~140s then errored 'Promise resolution is still pending'. Existing WS e2e tests (live-sync-e2e) use a raw-TCP client that HARD-DESTROYS its socket, never exercising the graceful close handshake, so they never hit this.
  - FIX 1 (real fix, websocket.mjs): loop condition `while (buffer.length > 0 || (state===PAYLOAD && payloadRead===payloadLen))` — lets the PAYLOAD state finalize a zero-length frame when the buffer is empty. FIX 2 (complementary, live.mjs `#drain` opcode-0x8 branch): ack the close frame (try/catch) then route through `#close()` (destroys the socket) instead of a bare `#cleanup()` that left the connection half-open (a well-behaved client waits for TCP FIN before releasing its handle). REGRESSION TEST added to websocket.test.mjs (masked zero-length close frame → 1 msg, opcode 0x8, closeCode 1005; verified FAILS without FIX 1).
- [2026-07-01] **Orphan `test/multi-hop-inherit.test.mjs` RECOVERED + committed separately (34c47d7).** A legitimate 5-test gate proving 3-hop `inherit` composition (Feed←Episode←AudioAsset←WordTimings) — chain compiles, one principalId placeholder survives, nested-EXISTS scope reaches Feed_subscribers, scoped-read admits exactly the subscribed-ancestor rows at every depth (fail-closed for stranger/anonymous), parent admits via both SQL scope + runtime mayVerb. Exercises real `inherit` composition (scope-sql recursion) not covered by inherit.test.mjs (1-hop). Its podcast narrative + `data-model-v1.md`/`council verdict` references do NOT exist in the repo — harmless aspirational context from an uncommitted exploration line lost in the crash. Kept because the test is sound + green.
- NEXT: Slice B (LiveList — bootstrap w/ currentSeq race-close, span-aware cursor, fixed kind-aware reducer mirroring field-delta.mjs/field-strategy.mjs, resync-keeps-stream, stale-hard-fail, render callback) + test/live-list.test.mjs. Then Slice C (createLiveStore factory + dispatch + decodeResult + optimistic reconcile). All per #76 forks.

## #78 — P7 Slice B: LiveList doc-reducer (bootstrap + span-cursor + kind-aware fold)
- [2026-07-01] Slice B DONE (committed c385c84, 785/785/0, +12 tests). `export class LiveList` added to public/workbench-client.mjs (LiveChannel untouched). Tracks ONE (entity,id) document. ds-v4-flash generated (ses_0e45d0459, DONE clean 785/0), lead self-reviewed + fixed one substantive bug.
- ONE FOLD PATH (`_ingest` → `_applyEvent`): both live WS envelopes AND events-since rows converge here after `_normalizeLive`/normalization. No second apply path (AGENTS one-reconciliation-path).
- BOOTSTRAP ORDERING (#76 Fork 8): `subscribe()` resolves only after (1) GET /snapshot → `_state`=snapshot, `_cursor`=seq; (2) `channel.subscribe` (live envelopes QUEUE while `_ready`=false); (3) if ack.currentSeq > cursor → `_resync()` the race-gap; (4) drain queue through `_ingest`; (5) render + resolve. Throws if called twice. Try/catch → `_readyReject`.
- SPAN-AWARE CURSOR (#76 Fork 5, P6e-4 forward-compat): exp=cursor+1. seqSpan[1]<exp → dup-skip; seqSpan[0]>exp → gap (queue + trigger `_resync`); else apply + cursor=seqSpan[1] + render. Every span is [seq,seq] today but the client is forward-compatible with future coalesced spans without requiring them.
- FIXED KIND-AWARE REDUCER (#76 Fork 6, library-owned NOT page-supplied), mirrors src/field-delta.mjs + src/field-strategy.mjs EXACTLY: `.created`→`_state`={...data}; `.updated`→assign event.data scalars (except id) THEN per-kind delta {value `{set}`; state `{from,to}`; crdt `{delete?:{at,length},insert?:{at,text}}` applied DELETE-then-INSERT; struct `{cells:{name:{set}}}`; map `{added,changed,removed}` (storeMapDiff)}; `.removed`→null. 3-part `<Entity>.<field>.<op>`: ordered inserted/moved/reordered/removed (native identity-keyed events carrying id+key), map added/changed/removed, log appended. `removed` disambiguated by data shape (data.member→map, data.id→ordered).
- RESYNC (#76 Fork 7): GET /events-since → `{resync:'stale'}`→forced re-bootstrap (fresh snapshot replaces state, cursor reset, `_ordered`={}); `{events:[...]}`→fold rows seq>cursor via `_applyEvent` BYPASSING span logic (events-since rows carry `data` but NO delta/seqSpan — reducer normalizes to `{seqSpan:[seq,seq],event:{type,data,actionId},delta:undefined}`; a `.updated` with no delta carries full scalars → plain assign is correct). Re-entrancy guarded by `_resyncing`. Stream KEPT open (not WS-reconnect); envelopes during resync QUEUE + drain after.
- **LEAD REVIEW FIX (the one substantive bug): ordered sort was NUMERIC not string.** ds-v4-flash generated `_ordered[field].sort((a,b)=>String(a.key).localeCompare(String(b.key)))`. VERIFIED WRONG against code: server stores fractional key as `key REAL NOT NULL` (ddl.mjs:108) + orders via SQLite `ORDER BY key` (entity.mjs:581) — a NUMERIC sort; the key is produced by numeric midpoint math (entity.mjs:588 `keyBetween`: `(low+high)/2`, `low+1`, `high-1`, `0`). A string/locale sort orders 2 AFTER 10 → client/server list-order divergence. Fixed to `(a,b)=>a.key-b.key`. The generated test used string keys `'0.1'/'0.2'/'0.3'` that sort identically both ways (masked the bug). Lead rewrote the ordered test to use numeric keys incl. the divergence pair 2/3/10 (numeric→beta,alpha,gamma; string→beta,gamma,alpha) — VERIFIED RED under localeCompare (11/1), green under numeric. LESSON: an ordered-list client reducer's sort must match the server's storage-column collation exactly; test with keys that DIVERGE between candidate collations or the guard is vacuous.
- NEXT: Slice C (createLiveStore factory + dispatch client-CRUD-verb-map + shared decodeResult + OPTIMISTIC-APPLY: capture preimage, visible placeholder, fire REST, rollback on fail, committed event folds via the ONE LiveList `_ingest` path clearing placeholder by actionId; dispatch never throws) + test/live-store.test.mjs. Per #76 Forks 1/2/3.

## #79 — P7 Slice C: createLiveStore (factory + dispatch + optimistic overlay) — P7 CLIENT LIB COMPLETE
- [2026-07-01] Slice C ships `createLiveStore({baseUrl,name,path})` + `decodeResult` (public/workbench-client.mjs, appended after untouched LiveChannel+LiveList). Factory owns URL derivation (snapshot/events-since/CRUD/wsBase from baseUrl+name+path), shares ONE LiveChannel across all subscriptions. API: subscribe(id)→cached LiveList; dispatch(type,payload)→Promise<Result> (NEVER throws); create/update/remove sugar; action(type,{method,path}) registers app-imperative field routes; close(); overlayFor(id) (effective rendered state); pendingCreates(); onRender(cb) (store-level).
- CLIENT CRUD VERB-MAP (NO /dispatch endpoint — #76 Fork 2): create→POST `${baseUrl}${path}` 201+row; update→PATCH `${path}/${id}` 200+row; remove→DELETE `${path}/${id}` 204-empty; all credentials:'include'. ONE shared `decodeResult(res)` (204→{ok:true}; !ok→{ok:false,error:'http '+status}; else json()) used by BOTH dispatch-fetch AND action routes so success/error/stale rows can't drift.
- **OPTIMISTIC-APPLY = Option C** (openai-gpt-5.5 consult ses_0e4460a07, lead-synthesized, AGENTS-mandated + user-confirmed prior): the optimistic overlay is a SEPARATE visible projection layer (Map opId→entry) the store renders ON TOP of LiveList.state. It NEVER writes into LiveList._state — `_ingest` remains the SOLE writer of canonical synced state + sole cursor-advancer. This satisfies AGENTS one-reconciliation-path (a visible placeholder is exactly what AGENTS permits, not a second apply path). dispatch: gen opId → capture preimage → set pending overlay entry (create={...payload}; update={id,...preimage,...payload}; remove=null) + render → fire REST → on !ok delete entry (rollback) + render + return {ok:false,status:'failed-rolled-back',opId,error}; on success entry.status='confirmed' + entry.row=returnedRow (create: entry.id=returnedRow.id) + render + return {ok:true,status:'committed',opId,id,row}. Fetch throw → delete entry + failed Result (never throws).
- **REFINEMENT (load-bearing, honored):** confirmed overlay does NOT clear on REST success — only once the matching canonical state is observed through `_ingest`. Rationale: HTTP-before-WS race — REST returns committed row at seq=N but LiveList._ingest may still be at cursor<N; clearing on REST success would flash the UI back to OLD LiveList.state until WS delivers seq=N. So `_clearConfirmedOverlays(id)` runs on the subscribed LiveList's onRender (=_ingest advanced) and deletes confirmed entries when LiveList.state matches kind (update/create→state!==null; remove→state===null). If no LiveList subscribed for an id, the confirmed overlay persists (nothing to flash to — correct).
- CREATE has no server id until 201 → optimistic entry keyed by client opId; on 201 the entry is retargeted to the real id; the later live .created for that id folds through _ingest normally (idempotent upsert). REMOVE 204 (no row/no seq) → overlay 'confirmed removed', row hidden immediately; kept until _ingest folds .removed or list teardown (never resurrect from stale canonical). Result shape: success `{ok:true,status:'committed',opId,id?,row?}`; failure `{ok:false,status:'failed-rolled-back',opId,error}`.
- **LEAD REVIEW: 1 real contract bug fixed.** ds-v4-flash's dispatch threw synchronously on a closed-store check — violates dispatch-NEVER-throws. Fixed: opId gen moved before the check; closed → returns {ok:false,status:'failed-rolled-back',opId,error:'store is closed'}. The generated test #11 asserted the WRONG contract (dispatch throws after close) — lead rewrote it to assert the non-throwing failed Result. (subscribe() still throws on closed — only dispatch has the never-throw contract.)
- **KNOWN IMPRECISION (documented, acceptable for beta):** _clearConfirmedOverlays clears a confirmed update-overlay on ANY LiveList onRender with non-null state — if an UNRELATED committed event folds first, the overlay clears one tick early (brief flashback to canonical). A precise fix needs seq/actionId correlation the client lacks: the CRUD REST responses carry NO seq/actionId (only /events-since rows + live envelopes do — see #76 fact-gathering). Not worth the round-trip complexity at beta.
- FILES: public/workbench-client.mjs (+decodeResult +createLiveStore) + NEW test/live-store.test.mjs (12 tests, FakeChannel+fakeFetch injected). Gate baseline ca5b50d=785/0 → 797/0 (+12), lead-verified raw. Committed 3a32c1e.
- **P7 CLIENT LIB COMPLETE:** Slice A LiveChannel (transport, 28a4afc) + Slice B LiveList (doc-reducer, c385c84) + Slice C createLiveStore (factory/dispatch/optimistic, 3a32c1e). The full collaborative-realtime client SDK ships in public/workbench-client.mjs (zero external deps, injectable fetch+channel for tests).

## #80 — P7 hardening: seq-correlated optimistic overlay clearing + CRUD commit headers
- [2026-07-01] **Context:** P7 Slice C (#79) intentionally kept confirmed optimistic overlays visible after REST success until LiveList observed canonical state through `_ingest`, avoiding the HTTP-before-WS flashback. The beta-accepted imprecision was shape-based clearing (`state !== null` for create/update, `state === null` for remove), which could clear an update/remove overlay on an unrelated earlier live render and flash stale canonical state. It also leaked confirmed create overlays when no LiveList subscription ever rendered the real id.
- **Decision:** REST CRUD mutation responses now expose the committed event identity via headers, and the client clears confirmed update/remove overlays only when the subscribed LiveList cursor has folded that committed sequence. `src/serve.mjs` adds `x-workbench-action-id` and `x-workbench-seq` on create/update/remove responses. The seq is the max committed seq for the mutated row's scope (`${Entity}:${id}`), not blindly the first event, so same-transaction same-row effects cannot clear an overlay before all row changes it returned have been folded. `public/workbench-client.mjs` stores `entry.confirmedSeq` from `x-workbench-seq`; `_clearConfirmedOverlays(id)` deletes only confirmed entries whose `confirmedSeq != null && list.cursor >= confirmedSeq`. This keeps `_ingest` as the sole canonical writer/cursor-advancer while making overlay retirement precise.
- **Create special case:** confirmed create overlays are retired on HTTP confirmation instead of waiting for a LiveList render. A create's durable result row is returned directly to the caller; the overlay is only a temporary placeholder. Retiring it prevents no-subscription overlay leaks after the temp op id is retargeted to the real id. Subscribed lists still fold the later `.created` event through `_ingest` normally.
- **CORS detail:** when CORS is configured and the Origin is allowlisted, `src/serve.mjs` now sets `access-control-expose-headers: x-workbench-seq, x-workbench-action-id`, because browsers otherwise hide custom response headers from frontend code. Same-origin clients were already able to read them.
- **Rejected alternatives:** clearing on REST success remains rejected because it reintroduces the HTTP-before-WS flashback; server envelope value-XOR-delta was rejected because `createClient` reducers rely on full `event.data` and the double-apply bug was in LiveList; shared replay extraction was rejected for now because `public/workbench-client.mjs` is a zero-import browser artifact and a shared `src/` module would require build machinery.
- **Tests:** `test/live-store.test.mjs` covers create overlay retirement, update overlay survival through an older live event until its confirmed seq, and remove overlay survival through an older live event until its confirmed seq. `test/http-crud.test.mjs` asserts CRUD mutation responses include action/seq headers. `test/ops-defaults.test.mjs` asserts CORS exposes the headers. Gate: `node --test` → 814/814/0.
Decision: Store `json(shape)` fields as whole-value JSON TEXT and deserialize them at HTTP/query boundaries, with structural JSON validation plus app-specific `validate` refinements rather than a new schema DSL. Reason: Scope needs object/array values now, SQLite TEXT keeps the storage seam simple and comparable through the existing field serializer, and retaining `shape` as declaration metadata leaves room for future path/index work without creating a second validation mechanism.
Decision: Parse browser form request bodies only through the shared request body spine, while keeping entity CRUD JSON-only. Reason: Imperative handlers need urlencoded and small multipart form posts for Scope-style project flows, but entity storage should not silently accept non-JSON transports, and one capped parser preserves the existing oversized-body and blob-upload behavior without a second body-reading path.
Decision: Admit inherited child rows by their compiled parent read-scope in REST read and live fan-out, including paced flush, when the child has no own `.can` body. Reason: Scope-style project documents and layers inherit membership from their parent project or canvas, and running child `mayVerb` would fail closed on a missing child `.can` even after the parent seam admitted the row, causing list, read, and live delivery to disagree.
Decision: Add inclusive range predicates to the existing value field handle and SQL lowering path. Reason: Google Photos-style timeline queries need `capturedAt.gte(from).and(capturedAt.lte(to))`, and routing through the same typed handle plus `serializeField` seam keeps dates, booleans, refs, and query API predicates comparable without introducing a second query mechanism.
Decision: Typed FK traversal from a non-role `ref` to a target map membership is part of the existing field-handle/check-registry path, not a new authorization path. Reason: Google Photos-style checks need `Photo.album.collaborators.has(principal.id)` in SQL scope and `Photo.album.collaborators.get(principal.id)?.role` in runtime `.can`; both now resolve through the active entity registry and the same declared check registry, lowering membership through the existing `existsMembership` node with an optional owner field (`Album_id = t0.album`). Ref-role fields stay raw identity values so derived role checks like `owner`/`author` keep their single FK-equality seam.
259: Decision: Runtime non-role ref handles are thenables so `await entity.targetRef` fetches the target row and exposes its scalar fields, enabling checks like `canvas.owner === principal.id` in async declared check bodies. The declared check context also exposes `entity` as a key alongside the entity-name key, so both `({ entity, principal })` and `({ Photo, principal })` destructuring styles work. The compile-time ref handle stays a plain object exposing only `is/in/isNull/gte/lte` + target map handles — no scalar field traversal in scope, which stays the existing path.
260: Decision: `projected.async({ compute })` is a stored computed field updated by a post-commit projection over the committed log. An optional `from` parameter limits recomputation to specific triggers. Each field tracks a monotonic compute counter in `_ProjectedCursor` that survives restart — a staleness indicator and progress counter, not a per-event dedup mechanism (per-scope sequences are not globally comparable).
261: Decision: `projected.inline({ compute })` is a stored computed field that runs inside the originating transaction — atomic with the mutation. Reason: Sort keys like `hotRank` from `score`/`commentCount`/`createdAt` must be transactionally consistent (no mis-ranked visible pages). The compute runs synchronously in the entity projection's apply handler; a compute failure rolls back the whole commit (fail closed).
262: Decision: `raster.crdt()` and `polyline.crdt()` are crdt-kind field constructors alongside `text.crdt()`, proof of the CRDT contract (ADR #9). The crdt strategy validates structurally per type (raster: Buffer/string, polyline: array); diff is whole-value replace for MVP (per-region/per-element merge deferred). The custom CRDT-authoring TOOLKIT remains deferred; these two instances ship as the stress-test proof.
263: Decision: `fieldCapabilities` passes `entity: row` to field `.can` bodies so they can read the field's own stored value (the photo-editor `visible` gate pattern). Field `.can` for inherit children must grant capabilities explicitly (see §photo-editor acceptance) — `defaults` carries `{ granted: false }` because inherit children have no own `.can` body. The parent's capabilities resolve through the compiled readScope and gate the parent's own auth layer; the child relies on that scope + its own field `.can` bodies. [Superseded 2026-07-03 — the "must declare explicitly" half no longer holds: `rowCapabilities` now recurses into the parent for inherit-children, so a no-`.can` child field inherits the parent's row capabilities (the field-defaults half of D1's capability-resolution move). The `entity: row` and `defaults` framing above stand; only the "silent trap" is closed. See the fable-field-can merge entry.]

264: Decision: `enum_(values)` is a thin sugar over `text({ validate })` that restricts a value to a closed set of allowed strings. Fail-closed: any value outside the set is rejected by the built-in validate. The descriptor stays `kind: 'value', type: 'text'` so the existing value strategy handles serialization and deserialization. Reason: the drawing-canvas `Shape.type` field needs constrained text values and the validate seam already exists — no new kind or strategy is warranted.
265: Decision: Boolean value fields deserialize from integer (1/0 in SQLite) to JS booleans on hydrate. The serialization path stays integer (SQLite can't bind JS booleans). Reason: `entity.visible` in field `.can` bodies and user code reads naturally as a boolean; the photo-editor `visible` gate checked `entity.visible` which was truthy for `1` and falsy for `0`, but the type-level intent is cleaner with real booleans.
266: Decision: The projected.async post-commit consumer resolves entity records at runtime from `app.entities` (a Map set by `buildKernel`) rather than building a static list at registration time. Reason: the consumer is registered during `makeRequestHandler` (called from `app.listen()`), but `buildKernel` runs later inside `app.ready` — reading entities synchronously at registration would see an empty Map. Runtime lookup also means the compute function's `findAll` / `getOrFail` on related entities works because the active DB is set before the compute call. The `id` column is included in the compute row even though it is not a declared field (it is auto-generated).

267: Decision: `app.batch(actions, { principal })` is a server-side composed mutation that runs N actions as ONE transaction = ONE composed commit (one actionId, one `now`), all-or-nothing (SPEC §11). Any sub-action authorization or post-grant denial rolls back the ENTIRE batch. Reason: a multi-entity write (e.g. create a Post and its first Comment, or a ledger transfer) needs the one-transaction atomicity guarantee the per-route HTTP handlers already provide per single action, and a batch API exposes that guarantee for server code without introducing a second pipeline. `dispatchBatch` in `src/pipeline.mjs` reuses the SAME `authorize→handler→applyEventsToTxn` path as `dispatch`, looping N handlers to concatenate their pure emitted events into ONE `applyEventsToTxn` pass bracketed by a single `BEGIN IMMEDIATE`/`COMMIT` — not a parallel write mechanism (AGENTS.md singular-system / no-second-write-path). Every action is authorized BEFORE the transaction opens (fail fast — a denied sub-action never begins the write). Dedupe is by the single batch actionId against `_Log`, mirroring `dispatch`. `app.batch` in `src/serve.mjs` wraps `kernel.dispatchBatch` once in the writeQueue (single entry — re-entering per sub-action would deadlock the mutex). The post-commit consumers fire exactly once for the whole batch. Rejected: per-action transactions with rollback-compensation (competing aborts are unsound), and exposing batch as HTTP (server code needs it; HTTP clients compose via sequential calls). Tests: `test/batch.test.mjs` (atomic multi-create, deny-rolls-back-all, actionId dedupe, cross-entity composed commit, empty no-op). Gate: `node --test` → 902/902/0.

## 2026-07-02 — batch-collapse merged into main

- **Purpose:** collapse the ~85 lines of near-verbatim duplication between durable `dispatch` and `dispatchBatch` surfaced by the thermo-nuclear code-quality review.
- **Files touched:** src/pipeline.mjs, test/batch.test.mjs
- **Commits:** cdb5d3c..b4d5da2
- **Base at merge:** main @ b14a24f
- **Conflict resolution:** NONE

268: Decision: The durable `dispatch` and `dispatchBatch` transaction brace (BEGIN IMMEDIATE → applyEventsToTxn → COMMIT → postCommitConsumers → ROLLBACK-on-403) and the `_Log` dedupe row→event mapping are extracted into shared module-level helpers (`commitEvents`, `rowToEvent`) so the two paths have ONE definition of each. Reason: the duplication was the exact "two pathways drift" seam AGENTS.md forbids — a future change to the transaction brace or the row→event shape would have to be made twice and could diverge. The `payload` argument threaded through `applyEventsToTxn` to the authorize hooks stays per-caller: `dispatch` passes its single action's `payload`, `dispatchBatch` passes the `actions` array. A full "dispatch = 1-element batch" collapse was REJECTED because the tick/schedule re-admission hooks (`admitTickedMutation`, `admitScheduledMutation` in schedule.mjs) destructure `payload` as one action's payload object — routing `dispatch` through a batch core would pass the `actions` array, breaking `const { id, ...payloadFields } = payload`. The brace is extracted but the `payload` value is parameterized: behaviour-preserving. The dead `actionId || \`dispatch-${Date.now()}\`` fallback is dropped (every internal caller supplies `actionId: randomUUID()`), and the empty-actions guard is reconciled to `actions.length === 0` on both paths. Added test: `a post-grant deny rolls back the first action mid-transaction` — the kernel's `authorize` is `() => true`, so a create deny reaches the in-txn `postHandlerAuthorize` → `mayVerb('create')` → 403 inside the open BEGIN→COMMIT; the test pins that the first action's row is undone (count 0) and no `_Log` row survives, proving the 403 is thrown inside the transaction. Gate: `node --test` → 903/903/0.

## 2026-07-02 — architecture-live-fanout merged into main

- **Purpose:** deepen the live architecture from one 607-line `live.mjs` into four focused modules (live-fanout, live-connection, live-admission, cursor).
- **Files touched:** src/live.mjs, src/pipeline.mjs, src/serve.mjs; created src/cursor.mjs, src/live-admission.mjs, src/live-connection.mjs, src/live-fanout.mjs, test/live-fanout.test.mjs.
- **Commits:** da08c82..4492405 (branch) → merged a60e5ec.
- **Base at merge:** main @ da08c82.
- **Conflict resolution:** NONE.
- **Note:** `src/live.mjs` 607→88 lines (85% reduction). Public API `createLiveServer(...) -> {emit, count, close}` preserved; one auth path through `mayRow(...,'subscribe',...)`; removed events skip reauth; field-pace/field-delta untouched. Independent reviewer: DONE. PR #1.

## 2026-07-02 — handler-ergonomics merged into main

- **Purpose:** handler ergonomics from the Scope thermo-nuclear review — `res.stream` SSE/streaming helper, hand-written TS type surface (`index.d.ts`), `createJobQueue` public export.
- **Files touched:** src/serve.mjs, src/index.mjs, package.json; created index.d.ts, test/res-stream.test.mjs.
- **Commits:** da08c82..7a5ff1a (branch) → merged a60e5ec.
- **Base at merge:** main @ da08c82 (post live-fanout merge).
- **Conflict resolution:** NONE — PR1 touched serve.mjs imports+snapshotRoute; PR2 touched the res facade in runChain; non-overlapping regions, clean merge.
- **Note:** TS-surface direction set by Opus consult (Option A: hand-written .d.ts, no compiler/migration) over full TS migration (B, singular-system violation) and helpers-only (C, incomplete). `res.raw` typed as node's `ServerResponse` (no custom RawRes alias — relocation rejected). Gates: `node --test` 913/0; `tsc --noEmit` exit 0. Independent reviewer: DONE_WITH_CONCERNS→PASS; stream error-path body-corruption + .d.ts `{now}` seam drift both fixed. PR #2.

## 2026-07-02 — arch/event-handles merged into main

- **Purpose:** type framework event handles so dotted event-name grammar lives behind one interface while `_Log.eventType` remains compatible.
- **Files touched:** src/event-handle.mjs, src/pipeline.mjs, src/entity.mjs, src/live-fanout.mjs, src/serve.mjs, src/index.mjs, test/event-handle.test.mjs.
- **Commits:** 98ba437 → merge e6d42df.
- **Base at merge:** main @ 04e867b.
- **Conflict resolution:** NONE.
- **Note:** `event(handle, reduce)` preserves a non-enumerable handle; `event(string, reduce)` remains a narrow generic compatibility adapter. Follow-up reviewer gaps were closed before merge: entity projection event types derive from handles, CRUD handlers emit handles, and blob adoption prefers `ev.handle.entity`. Gate after all Phase 1 merges: `node --test` 929/929/0.

## 2026-07-02 — arch/schedule-admission merged into main

- **Purpose:** centralize tick/schedule admission in `schedule.mjs` and delete caller-side source-shape branching.
- **Files touched:** src/schedule.mjs, src/serve.mjs, src/tick-engine.mjs, src/index.mjs, test/tick-engine.test.mjs, test/tick-admission.test.mjs, test/schedule-admission.test.mjs, test/reaper.test.mjs.
- **Commits:** c6c364a rebased to bf7a12f → merge 78932b3.
- **Base at merge:** main @ e6d42df.
- **Conflict resolution:** NONE.
- **Note:** `admitSystemMutation` discriminates by declared trigger kind, not source dot-count. Tick requires `whileSql` and skips due-check; schedule performs due-check and allows missing while. Old `admitTickedMutation`/`admitScheduledMutation` exports were retired; `_effectPrincipal` stayed in `postHandlerAuthorize`. Gate after all Phase 1 merges: `node --test` 929/929/0.

## 2026-07-02 — arch/authz-extract merged into main

- **Purpose:** extract entity load-time authorization assembly into `compileEntityAuthz` without folding the existing authorization leaves or creating a runtime facade.
- **Files touched:** src/authz.mjs, src/entity.mjs, test/authz.test.mjs.
- **Commits:** bc40804 rebased to 4d4f464 → merge 071cea6.
- **Base at merge:** main @ 78932b3.
- **Conflict resolution:** NONE.
- **Note:** `compileEntityAuthz` returns the unchanged registry/readScope/scopeAst/clauses assembly; runtime decisions remain in row-grant and the registry two-face engine remains the single auth path. Gate after all Phase 1 merges: `node --test` 929/929/0.

269: Decision: Phase 2 — the durable mutation pipeline is a named whole, not an orthogonal hook lattice (AGENTS.md "Pipeline variants are named wholes, not orthogonal flags"). The retired `applyEventsToTxn(db, events, { ...10 optional hooks/options })` interface is replaced by a named durable variant from `src/pipeline.mjs`: `durableMutationVariant({ projectionConsumers, admission, blobAdapter, effectsRegistry, postCommitConsumers })` returning `{ name:'durable.mutation', applyInTxn, afterCommit }`. `commitEvents`, durable `dispatch`, and `dispatchBatch` receive one `pipeline` variant and call `pipeline.applyInTxn(...)` inside `BEGIN IMMEDIATE`, then `pipeline.afterCommit(...)` after COMMIT. Effects recurse through the SAME variant's `applyInTxn` with incremented depth and effect principal — no second implementation. No lower-level append/project/authorize primitives are exported, so the kernel cannot build a parallel pipeline. The load-bearing in-txn order is preserved exactly and is now local to one module: `admission.beforeProjection` (in-txn, pre-mutation; scheduler/tick system admission) → append `_Log` (NOW tokens resolved to commit-time ISO) → projection consumers → `blobAdapter.adoptInTxn` (in-txn, atomic) → `admission.afterProjection` (in-txn, post-projection; create row-grant) → effects recursion. This retires the old hook names `preProjectionAuthorize` and `postHandlerAuthorize` (DECISIONLOG #63) — their phase-split semantics survive as `admission.beforeProjection` and `admission.afterProjection` on the named variant. Kernel assembly moved out of `src/serve.mjs` into a new `src/kernel.mjs` exporting only `buildKernel(app)` (entity collection, DDL, effect registry, blob adapter + finalize, live fanout, projected.async consumer, schedule/tick system admission via `admitSystemMutation`, create row-grant admission via `hasOwnCanGrant`+`mayVerb`, writeQueue, `createServer`). `serve.mjs` keeps HTTP/server/reaper/live-WS orchestration and imports `buildKernel`; no separate `src/admission.mjs` was created (user chose kernel-only split). `serve.mjs` shrinks (~306 lines removed). No second auth path and one reconciliation path preserved. Gate: `node --test` 932/932/0; independent reviewer PASS.

## 2026-07-02 — arch/kernel-variants merged into main

- **Purpose:** replace the durable pipeline hook lattice with a named durable variant and extract kernel assembly from serve.mjs.
- **Files touched:** DECISIONLOG.md, src/effect-compiler.mjs, src/index.mjs, src/kernel.mjs, src/pipeline.mjs, src/reaper.mjs, src/serve.mjs, src/tick-engine.mjs, and pipeline/kernel test harnesses.
- **Commits:** 058f234 → merge d341274.
- **Base at merge:** main @ 61c2a2f.
- **Conflict resolution:** NONE.
- **Note:** `durableMutationVariant` owns the in-transaction order and post-commit seam; `kernel.mjs` exports only `buildKernel(app)`; `serve.mjs` shrank by ~308 lines. Gate after merge: `node --test` 932/932/0.

270: Decision: Phase 3 — live delta shadow state is owned by the field-delta module, not by live fanout. `createDeltaProjector()` in `src/field-delta.mjs` owns the seed/compute/evict/clear lifecycle: created seeds the committed row and emits no delta; updated diffs the committed row against the prior committed shadow, emits `{}` when unchanged, then seeds; removed evicts and emits no delta; native store/ordered/log events normalize their already-structural event data under `delta: { [field]: event.data }`. `src/live-fanout.mjs` remains the delivery adapter: it parses/validates handles, hydrates the current row for authorization, re-authorizes subscribers, applies interest/pace, and attaches the projector result to the same event envelope. Reason: the previous pure `computeDelta` extraction had no locality — correctness depended on live fanout calling prev-get, compute, seed, and evict in the right order. The stateful projector concentrates that ordering behind one interface while preserving one reconciliation path: no extra event, no second send path, and `event.data` remains present for backward compatibility. `computeDelta` stays exported as the pure diff core for existing tests/compatibility; production live fanout no longer calls it directly.

271: Decision: Phase 4 — `projected.async` compute receives the committed database handle explicitly as `compute(row, { db })`; the post-commit projected.async consumer no longer mutates the ambient active-db binding around each compute call. Reason: the durable pipeline already hands the committed `db` to post-commit consumers, so the projected.async seam should pass that handle through directly instead of relying on a process-global swap for locality. Existing `compute(row)` declarations remain compatible because the second argument is optional in JavaScript; compute functions that need database access can now use the handle that belongs to the committed event batch. The consumer still catches compute failures, leaves the projected column unchanged, and advances `_ProjectedCursor` only after a successful write-back.

272: Decision: Phase 5 — `projected.async` post-commit projection is a module seam, not kernel implementation detail. `src/projected-async.mjs` exports `createProjectedAsyncConsumer({ entities })` and `resolveProjectedAsyncTriggerTypes`; it owns trigger selection from `from`, committed-row filtering/deserialization, `compute(row, { db })`, serialized write-back, `_ProjectedCursor` advance, and compute-failure skip semantics. `src/kernel.mjs` imports the factory and only wires the returned consumer into the named durable variant's `postCommitConsumers`. Reason: after Phase 4, the projected.async seam had a clear interface but its implementation still lived inside kernel assembly, making `kernel.mjs` a shallow mixed module. Moving the implementation concentrates projected.async behavior behind its own interface while preserving the same post-commit path and failure behavior; no second projection path is introduced.

273: Decision: Phase 6 — blob adoption/finalization is owned by a blob lifecycle module, not by kernel assembly. `src/blob-lifecycle.mjs` exports `createBlobLifecycle({ blobs, entities })`; it discovers blob fields from entity declarations, returns the reaper's `{ table, column }` references, creates the in-transaction `blobAdapter.adoptInTxn`, and creates the post-commit finalize consumer. `src/kernel.mjs` now only wires those returned parts into the durable variant and assigns `app.blobColumns` for the existing reaper path. Reason: the kernel previously encoded blob field discovery, event-to-blob-id resolution, in-transaction adoption, post-commit finalize, and reaper column metadata inline. That made the kernel a shallow mixed module and split blob lifecycle knowledge away from the blob seam. The new module concentrates lifecycle ordering behind one interface while preserving the same atomicity boundary: adopt participates in the durable transaction; finalize runs only as a post-commit consumer; rollback leaves pending blobs for the reaper.

## 2026-07-03 — fable-5-d1-inherit-authz merged into main

- **Purpose:** execute the FABLE-5-REVIEW.md structural decisions — D1 inherit-child write authz, D5 mayRow single home + denial battery, D-cleanup + DDL-into-app.ready, serve.mjs split, D2 side-table concentration + entity.mjs split, D3 client subscribe interest/pace, D4 owner()/owner.only() sugar, index.mjs public/internal shrink, D6 replace-stub diagnostic, and the effects/time simplification (typed effect handles + computed()/computed.stored() + state.auto→schedule.after lowering).
- **Files touched:** FABLE-5-REVIEW.md (new), FABLE-5-HANDOFF.md, src/{entity.mjs,entity/*,side-table-strategy.mjs,ddl.mjs,row-grant.mjs,owner.mjs,internal.mjs,index.mjs,field.mjs,field-strategy.mjs,field-delta.mjs,effect-compiler.mjs,schedule.mjs,reaper.mjs,tick-engine.mjs,scope-sql.mjs,projected-async.mjs,event-handle.mjs,http-*.mjs,serve.mjs}, public/workbench-client.mjs, package.json, app exemplars (doc/comment/gdoc/note/todo/todo-app/app/session), projects/* exemplars, and ~110 test files (import-split + migrated declarations). 162 files changed, +4513/−3715.
- **Commits:** 242898f (baseline) → c326082 (final), 15 commits; fast-forward merge.
- **Base at merge:** main @ b8bac0a.
- **Conflict resolution:** NONE — branch was strictly ahead of main (merge-base == main HEAD); fast-forward. Pre-merge local edits to FABLE-5-HANDOFF.md and an untracked stale FABLE-5-REVIEW.md on main were replaced by the reviewed branch versions per user choice.
- **Note:** All sub-goals landed behind a green suite with independent reviewer PASS on each. No second path was left alive for any sub-goal: side-table behavior resolves through one strategy table; entity.mjs is a stable wrapper over entity/{compile,query,hydrate,handles,projection}; mayRow is the single home for the own-grant skip with a denial battery run against all four transports; serve.mjs split into http-* modules; client subscribe carries fields/pace end-to-end (server admission/fanout already owned validation); owner() expands transparently to the owner ref-role; public barrel shrunk to app-facing symbols with workbench/internal for the rest; raster/polyline crdt deltas are whole-value replace with a dev diagnostic; entity effects are typed array-pair declarations (effect.anyOf branded-only, no strings/colon normalization); computed()/computed.stored() replace derived + projected.inline with no aliases; state.auto lowers to a real schedule.after via an explicit auto.from anchor with array-capable schedule verbs (one schedule path). Gate after merge: `node --test` 957/957/0; full `.mjs` syntax sweep + `git diff --check` clean.

274: Decision: removed live events skip delivery-time re-authorization deliberately. `src/live-fanout.mjs` re-authorizes every created/updated delivery via `mayRow(entityRecord, 'subscribe', authzRow, ...)`, but for `removed` events (`row === undefined`) re-auth is intentionally skipped and the event is forwarded to all current subscribers (then interest/pace still apply). Reason: the remove IS the revocation signal — the row no longer exists, so a `subscribe` capability check against a vanished row has no meaning; every client subscribed to that row must learn it is gone regardless of their post-remove capability. Re-hydrating a non-existent row to re-auth would fail-closed and silently drop the remove from clients that legitimately need to see it. This is a deliberate, documented hole in re-auth-every-delivery, not an oversight: the create/update path re-auths because a live principal's capability can be revoked between subscribe and delivery; the removed path cannot be revoked against a row that no longer exists. The code comments at `src/live-fanout.mjs` (emit path + flush path) state this; this ADR records the decision permanently.

275: Decision: reject an `async` flag on `computed.stored` — projected.async stays a named whole. The Fable review suggested folding `projected.async` into `computed.stored({ compute, async? })` so one constructor covered both stored-immediate and stored-async compute. Rejected. Reason: AGENTS.md "Pipeline variants are named wholes, not orthogonal flags." `computed.stored` computes in-transaction (synchronous, atomic with the origin event); `projected.async` computes post-commit (asynchronous, independently retried, never rolls back the origin, receives the committed `db` handle). An `async: true` boolean would be an orthogonal flag selecting a different pipeline variant (in-txn vs post-commit) inside one constructor — exactly the half-applying lattice the named-whole rule forbids. The two have different atomicity boundaries, different failure semantics, and different inputs (post-commit compute gets `{ db }`). Keeping `projected.async` as its own named constructor makes the atomicity boundary visible at the call site and prevents a half-applied configuration. `computed()` (pull, read-time, no column), `computed.stored()` (stored, in-transaction compute), and `projected.async` (stored, post-commit compute) are three named wholes, not one constructor with two booleans.

## 2026-07-03 — fable-followup merged into main

- **Purpose:** resolve the six remaining FABLE-5-REVIEW.md analysis-section items — gate on entity declaration (retire r.resource({gate})), fields:-less entity declaration, workbench/client subpath, live-fanout authz-row latch per (scope,commit), ADR for removed-event re-auth skip, and ADR rejecting the computed.stored async flag.
- **Files touched:** FABLE-5-REVIEW.md, SPEC.md, DECISIONLOG.md, package.json, src/{app.mjs,auth-entities.mjs,entity/compile.mjs,kernel.mjs,live-fanout.mjs,route-gate.mjs}, exemplars (comment/doc/gdoc/note/todo) + projects/* + ~75 test files. 99 files changed, +1582/−1411.
- **Commits:** 922e836 (single commit); fast-forward merge.
- **Base at merge:** main @ 8c3a264.
- **Conflict resolution:** NONE — branch strictly ahead (merge-base == main HEAD).
- **Note:** All six items landed behind a green suite with reviewer PASS. gate lives on the entity next to grant, compiled once via resolveRouteGate (one auth story; imperative-route gates untouched as a separate concern); fields:-less declaration with a reserved-slot guard (grant/checks/routes/create/effects/admitsEffects/schedule/gate/on) and mock-record `{fields}` objects left intact; `./client` subpath added; live-fanout latches the hydrated authz row per (scope,commit) batch and emit skips the redundant findById when the caller passes a hydrated row (direct test callers unaffected); ADR #274 documents removed-event re-auth skip (the remove is the revocation signal); ADR #275 rejects computed.stored({async?}) as an orthogonal flag (named wholes). Gate after merge: `node --test` 959/959/0; `git diff --check` clean.

## 2026-07-03 — fable-field-can merged into main

- **Purpose:** close the §6 "field `.can` strong-inherit is half-built" trap (ADR #263). `rowCapabilities` (the field-defaults source) did not handle inherit-children — an inherit-child's grant is an `inherit` directive with no runtime clause, so it returned `{granted:false}` and every no-`.can` child field was silently unreadable. D1 had fixed `mayRow` to recurse into the parent; the field-defaults half of the same move was missing.
- **Files touched:** src/row-grant.mjs (+13), test/photo-editor-acceptance.test.mjs (+23, ACCEPTANCE 7b), DECISIONLOG.md (#263 supersede annotation), FABLE-5-REVIEW.md (second-pass §6 entry). 4 files, +70/−1.
- **Commits:** 1d5879d (single commit); fast-forward merge.
- **Base at merge:** main @ f89d5ee.
- **Conflict resolution:** NONE — branch strictly ahead.
- **Note:** `rowCapabilities` now recurses into the parent for inherit-children, mirroring `mayRow`'s D1 move (load parent row via the `via` FK, delegate; no parent row fails closed). One resolution path, no second engine; the non-inherit branch is byte-identical. The child inherits the parent's capability decision verbatim (same principal threaded through), so it cannot gain capabilities the parent wouldn't grant. ADR #263 annotated: the "must declare field `.can` explicitly" half is superseded; the `entity: row` / `defaults` framing stands. Reviewer PASS on all six criteria (fail-closed no-parent, multi-hop termination, same-principal, no broader caps, decision-note accuracy). Gate after merge: `node --test` 960/960/0; `git diff --check` clean.

## 2026-07-06 — Scope convergence ratified (user decision)

- **Decision:** Scope (`~/Development/scope`) migrates **all four layers** onto workbench after its refactor v2 completes — live spine, server kernel, and client engine (the HTTP shell already runs on workbench per Scope ADR-0004). Recorded on the Scope side as `docs/adr/0005-workbench-convergence.md`; migration is per-entity strangler in stations A (live spine) → B (kernel) → C (client engine).
- **Requirements this places on workbench** (all consistent with existing AGENTS.md values; none reverse an ADR):
  1. **Configurability doctrine, stated as a product requirement:** every layer ships sensible defaults and allows deep developer customization — explicitly including authentication.
  2. **Auth parity:** workbench owns identity end-to-end for its apps. Grow to Scope parity and beyond: sessions, passkeys, and Scope's two-plane pattern (org membership = management visibility, never data access; data access = explicit per-project grant) expressible as a **generic grant/check pattern**, not a Scope feature. Scope's better-auth is retired at end state.
  3. **Full persistence ownership:** consumer apps run no second ORM. Scope's Prisma (including auth/job/infra tables) is retired at end state.
  4. **Optional UI kit:** an optional layer of state-synced UI components (buttons, inputs bound to live entity state) that apps may adopt, restyle, or replace with their own. Never required — the client library stays usable headless.
  5. **Job queue as the genericity exemplar:** workbench owns the generic substrate (post/claim/heartbeat/result/retry/reaper); Scope plugs in whisper transcription, media transcode, search indexing. The pattern-vs-plug-in split is the template for every Scope-driven feature; if a need doesn't fit the split, the feature is redesigned, not workbench bent.
  6. **Revoked principals are refused, including on retry** (user confirmation of Fork C / authorize-before-dedupe). Note: Scope's kernel already authorizes before dedupe too — the two kernels agree; no behaviour change on either side.
- **Interface contract to watch:** Scope's R8 materialiser is being designed to workbench's SPEC §7.1 bootstrap contract — `{ snapshot, cursors }` read in one transaction (see Scope `docs/refactor-v2-decisions/r8-materialiser-design.md`). The seq/cursor numbering agreed when Scope adopts the live spine (station A) becomes a load-bearing wire contract.
- **Genericity check stands:** the `projects/*` stress-test apps remain the standing proof that Scope-driven features land generic; their needs co-steer the roadmap.

## 2026-07-06 — W5 scope subscription normalization (slice 1 of station-A bridge)

- **Decision:** Introduce `subscribeScope(scope, interest?)` as the forward-looking subscribe surface, with old `{entity, id}` normalised to `{scope: "Entity:id", interest: {entity, id}}` at the decode boundary. No server fan-out change — scope-keyed fan-out deferred to slice 2. Council c01 (Opus 4.8 + GPT 5.5, cross-evaluated) adopted Option B′: unified stream-scope primitive where per-entity is the degenerate scope.
- **Reason:** The S0 wire memo found Scope uses per-project seq while workbench uses per-entity seq — structurally incompatible. The council recommended the stream-scope model as single subscription primitive covering both per-entity and room/project shapes. The owner directed: run W5 design first (with S0 as binding input), joint recommendation on cursor granularity + subscription breadth before ruling on S0. This slice delivers the wire-surface normalisation only.
- **Files:** `src/live-admission.mjs` (+`normalizeSubscribeMsg`), `src/live-connection.mjs` (scope-aware acks), `public/workbench-client.mjs` (+`subscribeScope`, `unsubscribeScope`, unified `:` key format), `test/subscribe-scope.test.mjs` (13 new tests).
- **Gate:** `node --test` 1206/1206/0 on main.

## 2026-07-07 — S0 joint recommendation: cursor granularity for station A

- **Decision:** Scope's per-project seq IS a valid coarse scope under B′. S0 memo's "INCOMPATIBLE" verdict was written under the old per-entity-only reading — the contradiction dissolves once B′'s coarse scopes are first-class. No re-key of `ProjectEventLog` needed for station A; no seq numbering change; no prefix-matching bridge. Boot payload: one cursor per project (`cursors: {"project:p1": lastSeq}`). Station A is transport swap only (SSE→WS); durable store migration waits for station B.
- **Reason:** Council c01 (Opus 4.8 + GPT 5.5, cross-evaluated) adopted B′ — unified stream-scope primitive where coarse scopes are first-class peers. The S0 joint recommendation (also Opus + GPT, both independently converged) extended B′ to the cursor question: `_Log(scope, seq)` uses a free-form scope string, so `scope = "project:p1"` with a project-wide seq is the same machinery as `scope = "Entity:id"` with a per-entity seq — just a coarser key. The `:` separator in `"project:p1"` vs `"Entity:id"` is convention, not structure.
- **Files:** `docs/convergence/S0-joint-recommendation.md`
- **Remaining:** W5 slice 2 (scope-keyed fan-out, scope snapshot/events-since routes, generalised subscribed ack) → then Scope station A adapter → station B later.
- **Gate:** N/A (spec only)

## 2026-07-07 — W5 slice 2: scope-keyed fan-out, scope-level snapshot/events-since routes

- **Decision:** Replace per-entity fan-out registry (`Map<entity, Map<id, Map<conn>>>`) with scope-keyed registry (`Map<scope, Map<conn, SubSpec>>`). `emit()` delivers events by exact `committedEvent.scope` match — one fan-out path for both per-entity (`"Entity:id"`) and coarse (`"project:p1"`) scopes. Add `GET /snapshot?scope=...` and `GET /events-since?scope=...&cursor=N` for scope-level bootstrap and gap-fill. Per-entity map retired in the same change per AGENTS.md singular-system rule.
- **Reason:** Council c01 adopted B′ — unified stream-scope primitive where per-entity is a degenerate scope. The per-entity fan-out path was the last remaining second path. Retiring it in the same change that ships coarse-scope fan-out prevents the exact dual-reconciliation drift AGENTS.md forbids. The `_Log(scope, seq)` table already supports any scope string — `readSince`/`minSeqForScope`/`readSeq` are scope-generic; only the URL router was per-entity. Legacy `addSubscription(entity, id, conn)` calls continue to work via overload heuristic (derives scope = `${entity}:${id}`).
- **Files:** `src/live-fanout.mjs` (scope-keyed registry), `src/http-framework-routes.mjs` (scope-level routes), `src/live-connection.mjs` (generic ack), `test/subscribe-scope-fanout.test.mjs` (10 new tests).
- **Gate:** `node --test` 1216/1216/0 on main.

## 2026-07-07 — W1 slice 1: membership() — named two-plane auth pattern

- **Decision:** Introduce `membership(entity, { role: { can: [read, subscribe] } })` as the first-class declaration for two-plane membership auth, concentrating ~15 lines of hand-written `map` + `checks` + `scope().can()` + `anyOf()` boilerplate into a single declaration. Also available as a declaration key (`membership: { ... }`) inline in the entity declaration. Owner role auto-detected from `ref('User', { role: 'owner' })` and receives implicit `grant(read, write, subscribe, admin)`. Compiler generates check entries with both harvest (existsMembership AST for SQL scope lowering) and run (DB query with role filtering) faces. Entity proxy gets `set` trap for in-place augmentation; record no longer fully frozen (only fields). Uses only existing primitives (map, checks, scope, grant, deny, anyOf) — no new engine.
- **Reason:** Two-plane auth was already provable through hand-written plumbing but required verbatim copy-paste per entity. The pattern needed a name and reduced ceremony before W1 could proceed to the remaining build items (passkeys, invitations, API keys, TOTP) which all depend on a clean two-plane base. The `membership:` declaration key keeps the pattern inside the entity declaration for large app definitions; the standalone `membership()` call keeps small apps concise.
- **Files:** `src/membership.mjs` (281 lines, new), `src/entity/compile.mjs` (proxy set trap, membership key, reservation), `src/index.mjs` (export), `test/membership.test.mjs` (13 tests), `test/entity.test.mjs` (2 tests updated), `projects/membership-example.mjs` (exemplar).
- **Gate:** `node --test` 1230/1230/0 on main.

## 2026-07-07 — W1 slice 2: passkey (WebAuthn) authentication

- **Decision:** Add server-side WebAuthn passkey support as a framework-owned auth battery, with credential storage (`Credential` entity), challenge lifecycle (`generateChallenge`/`challengeStore`), attestation verification (`verifyRegistration`), assertion verification (`verifyAuthentication` with signature check + counter replay protection), and 5 HTTP routes under `/auth/passkey`. Uses `node:crypto` only — zero npm dependencies. RP configuration (`name`, `id`, `origin`) is pluggable via `rpConfig()`. The `Credential` entity follows the same fail-closed `notRequestReadable` pattern as `User`/`Session`/`Inbox`.
- **Reason:** Passkeys are the WebAuthn-based passwordless login alternative Scope configured but never UI-mounted. The ceremony split: credential storage + challenge issue/verify as framework concern; UI as app concern. The authenticate route mints a Session + Set-Cookie the same way `/auth/login` does — one session path, no second auth path.
- **Files:** `src/passkey.mjs` (567 lines, new), `src/auth-entities.mjs` (+Credential entity), `src/auth-routes.mjs` (+5 routes), `src/app.mjs` (+Credential in prepareSchema), `src/kernel.mjs` (+Credential), `src/internal.mjs` (+export), `test/passkey.test.mjs` (22 tests).
- **Gate:** `node --test` 1252/1252/0 on main.

## 2026-07-07 — W3 slice 1: job progress, cancellation, scoping

- **Decision:** Add progress tracking (`progress` INT, `stage` TEXT columns + `POST /jobs/:id/progress` route with worker ownership guard), cancellation (`cancelled` status in STATES/TERMINAL + `cancelJob()` + `POST /jobs/:id/cancel`), and per-project job scoping (`scope` TEXT column + `idx__job_scope_status` index + scoped `claim()` + `?scope=` query param on `/jobs/claim`). All three are additive — no breaking changes to existing enqueue/claim/heartbeat/result API.
- **Reason:** The census found 3 BUILD items. Progress tracking lets workers report completion percentage; Scope's worker polls `/api/jobs/:id/progress`. Cancellation lets users abort queued/running jobs; Scope uses polling to detect cancelled status. Scoping isolates jobs per project; Scope uses `projectId` FK on its Job model. All three are minimum-viable additions — no new tables, no worker-side changes.
- **Files:** `src/job-queue.mjs` (+47 lines), `src/ddl.mjs` (+15 lines), `src/http-framework-routes.mjs` (+30 lines), `test/job-queue-progress.test.mjs` (21 tests).
- **Gate:** `node --test` 1273/1273/0 on main.

## 2026-07-07 — W2 slice 1: FTS (full-text search)

- **Decision:** Add `text({ indexed: 'fts' })` field type that creates an FTS5 virtual table, a `.matches(query)` scope predicate that compiles to `EXISTS (SELECT 1 FROM {table}_fts WHERE {table}_fts MATCH :q)`, and automatic lifecycle sync via the projection consumer. The FTS table follows the `<entity>_<field>_fts` naming convention with `<entity>_id UNINDEXED` column. FTS5 table alias limitation in node:sqlite: MATCH uses the full table name.
- **Reason:** Scope uses 3 raw SQL FTS5 virtual tables (segments/artefacts/codes) with manual lifecycle sync in `fts-sync.ts`. Workbench makes this a framework concern: declare the field as FTS-indexed, get DDL + query predicate + lifecycle sync for free. The `fts-strategy.mjs` side-table strategy follows the same pattern as map/log/ordered/ephemeral side tables — registered in SIDE_TABLE_STRATEGIES, picked up automatically by `generateDDL()`.
- **Files:** `src/fts-strategy.mjs` (90 lines, new), `src/field.mjs` (+8 lines), `src/ddl.mjs` (+3 lines), `src/scope-sql.mjs` (+57 lines), `src/side-table-strategy.mjs` (+4 lines), `src/authz.mjs` (+1 line), `src/membership.mjs` (+1 line), `src/entity/compile.mjs` (+5 lines), `test/fts.test.mjs` (29 tests).
- **Gate:** `node --test` 1302/1302/0 on main.

## 2026-07-07 — W1 slice 3: generic invitation flow

- **Decision:** Add `Invitation` entity with dual mode: direct user-to-user (targetUser filled) and token-based share link (targetUser null, maxUses optional). Create/accept/reject/list routes under `/auth/invitation`. On accept, inserts a row into the target entity's `{Entity}_members` side-table via `acceptInvitation()` helper. Token auto-generated from 32 random bytes (base64url). Expired invites and maxUses-exceeded link invites reject with 400.
- **Reason:** Scope has two separate invite models (`ProjectInvitation` user-to-user, `Invite` token-based link) with two separate Prisma tables and handlers. Workbench unifies them into a single entity with an optional `targetUser` discriminator — one table, one flow, two modes. Accept grants membership via direct SQL INSERT into the members map side-table (same structure `membership()` reads from), keeping the two-plane auth pipeline unchanged.
- **Files:** `src/invitation.mjs` (152 lines, new), `src/auth-entities.mjs` (+59 lines), `src/auth-routes.mjs` (+90 lines), `src/app.mjs` (+4 lines), `src/kernel.mjs` (+4 lines), `src/internal.mjs` (+2 lines), `src/index.mjs` (+3 lines), `test/invitation.test.mjs` (21 tests).
- **Gate:** `node --test` 1323/1323/0 on main.

## 2026-07-07 — W1 slice 4: API keys as first-class principal kind

- **Decision:** Generalise the job-queue bearer token pattern into a first-class `ApiKey` entity with SHA-256 hashed tokens (32 random bytes, plain text returned once at creation), a `principal({ type: 'apiKey', ... })` kind that participates in the same authorization engine, and `apiKeyPrincipalOf()` resolution in the request pipeline (tried after session cookie). Keys are optionally scoped to an entity + role. Three management routes: create, list (prefix only), revoke.
- **Reason:** Scope has `ProjectApiKey` with per-project bearer tokens. Workbench's job queue already uses bearer tokens for worker auth but as a bespoke per-worker mechanism — not a general principal kind. Making API keys a first-class principal means they participate in row-scope filtering, capability checks, and route gates through the same engine as users — no second auth path. Session takes priority (cookie wins over Bearer header); API key is the fallback.
- **Files:** `src/principal.mjs` (+apiKey type), `src/auth-entities.mjs` (+ApiKey entity with mintApiKey), `src/session.mjs` (+apiKeyPrincipalOf), `src/auth-routes.mjs` (+3 routes), `src/serve.mjs` (+Bearer resolution), `src/app.mjs`/`src/kernel.mjs`/`src/internal.mjs`/`src/index.mjs` (exports), `test/api-key.test.mjs` (30 tests).
- **Gate:** `node --test` 1353/1353/0 on main.

## 2026-07-07 — W2 slice 2: vector(dim) field type with cosine similarity

- **Decision:** Add `vector(dimensions)` field type (stores `number[]` as JSON TEXT, validates dimensions at mutation), `cosineSimilarity(a, b)` pure-JS function, `nearest(db, entityName, fieldName, queryVec, k)` standalone brute-force top-K search (same approach Scope uses in `suggest.ts` — loads all vectors, computes cosine in JS, returns top-K), and `.nearest(query, k)` as a scope predicate producing a `nearest` AST node that the query builder post-filters.
- **Reason:** Scope stores embeddings as JSON and computes cosine similarity in-memory (no SQL-level vector support). Workbench follows the same pattern — store `number[]` as JSON TEXT, validate dimensions, compute similarity in JS. The `nearest()` function and `.nearest()` predicate cover both the direct query API and the scope-predicate use case. Brute-force is fine for Scope's single-user-project scale. W2's 2 GAPs (FTS + vector) are both closed.
- **Files:** `src/field.mjs` (+vector constructor), `src/field-strategy.mjs` (+vector validation), `src/scope-sql.mjs` (+cosineSimilarity, nearest, nearest AST lowering), `src/entity/query.mjs` (+Entity.nearest method), `src/index.mjs`/`src/internal.mjs` (exports), `test/vector.test.mjs` (25 tests).
- **Gate:** `node --test` 1378/1378/0 on main.

## 2026-07-07 — W4 owner checkpoint (council c02): DOM factories + formal token contract

- **Decision:** Council c02 (Opus 4.8 + GPT 5.5, cross-evaluated) converged on Option B — DOM factory functions with a formal light-DOM styling contract — after GPT revised its initial position (Option A, Web Components) in cross-evaluation. Shadow DOM was rejected because `::part()` is an author-chosen allowlist: anything the kit author didn't name a part is unreachable without forking, contradicting the owner's "adopt, restyle, or replace" constraint. Per-framework adapters were rejected per the singular-system rule. The binding contract: each factory declares its store binding (`{ store, id, field, action }`), internally subscribes to `store.onRender()`, reads `overlayFor()`, and reflects status via `data-*` attributes — the component holds no local pending/confirmed/failed flags. Three code discoveries from the real client library: (1) `overlayFor()` skips failed entries — `dispatch()` deletes the overlay on failure; components must capture failed state from the `dispatch()` Result; the headless client needs an observable operation-status contract (W5 filing). (2) `LiveList` is a single-row `(entity,id)` document exposing `list.state[field]` as an ordered array — not an entity-wide collection. (3) `dispatch()` only overlays CRUD actions (create/update/remove) — custom action types don't get optimistic overlays. Staging: Wave 1 = ActionButton, TextInput, ListView (prove the contract); Wave 2+ = remainder. Svelte consumption: zero-logic `use:` mount action that appends/destroys the factory node — binding lives in the factory, not two paths.
- **Reason:** The owner explicitly reserved taste decisions for W4. This checkpoint settles the one-way-door decisions (technology + binding contract + styling surface) before build-out commits 25 primitives to a shape that would be expensive to unwind. The cross-evaluation was load-bearing: Opus's Shadow-DOM-restyling critique changed GPT's verdict, producing a stronger consensus than either model's solo answer.
- **Files:** `docs/convergence/W4-owner-checkpoint.md`
- **Gate:** N/A (design checkpoint — no code build-out yet).

## 2026-07-07 — W1 slice 5: TOTP 2FA + email delivery seam (W1 complete)

- **Decision:** Add TOTP two-factor authentication (RFC 6238, HMAC-SHA1, `node:crypto` only), backup codes (SHA-256 hashed, single-use consume), and a pluggable email delivery seam (`emailSeam({ transport })` — post-commit projection consumer, `noopTransport` default). W1 closes: 10 build items shipped across 5 slices, 3 defer-candidates skipped (all dead Scope features).
- **Reason:** Scope has TOTP-enabled 2FA via better-auth but no enforcement. The email seam follows AGENTS.md's out-of-band effects rule: email is a post-commit projection consumer, independently durable, never rolling back the origin. The `noopTransport` default ships with the framework; apps plug in SMTP/resend/anything.
- **Files:** `src/totp.mjs` (145 lines), `src/email-seam.mjs` (69 lines), `src/auth-entities.mjs` (+TwoFactor), `src/auth-routes.mjs` (+4 routes + login update), `src/kernel.mjs` (+emailConsumer), `src/app.mjs` (+TwoFactor DDL), `test/totp.test.mjs` (34 tests).
- **Gate:** `node --test` 1412/1412/0 on main.

## 2026-07-07 — W4 wave 1: Svelte UI kit — ActionButton, TextInput, ListView

- **Decision:** Ship the first three UI primitives as Svelte 5 components with framework-agnostic binding helpers (`bindAction`, `bindField`, `bindList` in `workbench-ui-bindings.mjs`). The binding helpers project the client store (`overlayFor()`, `onRender()`, `dispatch()`) into reactive handles that Svelte components consume via `$state`/`$effect` runes. Components own no reconciliation state — they are projections of the headless store. The kit ships with a custom Svelte loader (`test/svelte-loader.mjs`) that compiles `.svelte` on-the-fly in `node --test` using `svelte/compiler`; tests run under `--conditions=browser` with JSDOM for DOM rendering.
- **Reason:** Owner overruled council c02's Option B (DOM factories) in favour of Option C (per-framework adapters, Svelte-first) because Scope is built in Svelte. The binding helpers are the shared core — framework-agnostic JS that projects the store. Future React or vanilla JS consumers write rendering layers over the same helpers. Svelte is a dev dependency only (component authoring + test compilation); the compiled output can ship as vanilla JS for plain-JS consumers. Wave 1 proves the contract on the three hardest surfaces (action dispatch, field binding, list subscription) before multiplying across 25 primitives.
- **Files:** `public/workbench-ui-bindings.mjs` (290 lines, new), `public/workbench-ui.mjs` (11 lines, new), `public/ActionButton.svelte` (50 lines), `public/TextInput.svelte` (85 lines), `public/ListView.svelte` (70 lines), `test/workbench-ui.test.mjs` (44 tests), `test/svelte-loader.mjs` + `test/svelte-loader-hook.mjs` (loader), `package.json` (+svelte +jsdom dev deps).
- **Gate:** `node --test` 1458/1458/0 plain, `node --conditions=browser --import ./test/svelte-loader.mjs --test test/workbench-ui.test.mjs` 44/44.

## 2026-07-07 — Integration proof: convergence acceptance run

- **Decision:** Ship `projects/convergence.mjs` + `test/convergence-integration.test.mjs` as the single-acceptance-run integration proof exercising all four wave layers end-to-end. The test boots a real workbench server and exercises: W1 passkey auth (login → cookie → authed CRUD → 401 on wrong password), W3 job queue (HTTP enqueue → register worker → claim → complete/cancel), W4 UI bindings (bindAction/bindField/bindConnection surface with subscribe handles), W5 client engine (createLiveStore CRUD surface, canUndoField vocabulary). 12 new tests, zero failures.
- **Reason:** Satisfies COORDINATOR.md §8 definition of done item 2: "one `projects/*` app exercising passkey auth + job queue + client boot/optimistic/undo + UI kit in a single acceptance run." The client engine's undo capability is tested at the field-law level (`canUndoField`, `undoableFieldKinds`) since client-side undo is not yet a store-level API — the pipeline's optimistic/reducer pattern is exercised in `test/pipeline-integration.test.mjs`. The UI kit binding helpers are tested as plain JS surface contracts (subscribe handles, status lifecycle) — Svelte component rendering is exercised in `test/workbench-ui.test.mjs`.
- **Files:** `projects/convergence.mjs` (44 lines, new), `test/convergence-integration.test.mjs` (357 lines, 11 tests).
- **Gate:** `node --test` 1612/1612/0.

## 2026-07-07 — Customisation sweep: 28 override seams catalogued

- **Decision:** Document and verify every configuration seam across all six framework layers (auth, persistence, job queue, client engine, UI kit, ops+log) in `docs/convergence/census/customisation-surfaces.md`. 28 seams recorded with location, zero-config default, and test coverage proof. 3 cosmetic gaps noted (blob reaper override values, log retention reaper, createAuthClient fetchImpl override) — none are production foot-guns.
- **Reason:** Satisfies COORDINATOR.md §8 definition of done item 1: "configurability sweep documented with tested override seams." Every layer ships sensible defaults that work without configuration; every override seam is proven to exist and be testable. The sweep proves that the framework follows the singular-system rule: one config path per concern, no second path.
- **Files:** `docs/convergence/census/customisation-surfaces.md` (86 lines, new).
- **Gate:** `node --test` 1612/1612/0.

## 2026-07-07 — Station C complete: overlay vocabulary + contract lock (6 steps)

- **Decision:** Complete station C (client overlay bridge) in 6 steps. Step 1: lock contract tests (store-adapter, 19 tests). Step 2: `client-overlays.ts` pure adapter (overlayFor/overlayStatusFor/pendingCreates, 7 tests). Step 3: wire into `WorkbenchProjectStore` export so consumers use `store.overlayFor(key)`. Step 4: consumer migration proof (4 integration tests). Step 5: upstream review documented — vocabulary aligns with workbench-client overlay API but semantics differ (journal-based vs in-memory overlay Map). Step 6: transport already configurable (transport, createRealtime, realtimeSubscribe injection seams).
- **Reason:** Station C is the convergence bridge between Scope's journal engine and workbench's overlay vocabulary. Consumers use the same overlayFor/overlayStatusFor/pendingCreates names regardless of which engine backs them. When workbench absorbs Scope, the overlay primitives converge into createLiveStore and this adapter retires. Contract tests + integration tests guarantee the overlay surface doesn't drift while the bridge is live.
- **Files:** Scope — `src/lib/wb-scope/client-overlays.ts` (~160 lines), `src/lib/wb-scope/__tests__/client-overlays.test.ts` (7 tests), `src/lib/wb-scope/store.svelte.ts` (overlay wiring), `src/lib/wb-scope/__tests__/store-adapter.test.ts` (23 tests).
- **Gate:** `pnpm test` 1974/1974, `pnpm check` 0 errors.

## 2026-07-07 — W5 undo closure: substrate shipped, operation deferred to Phase S station C

- **Decision:** Workbench's undo substrate — the pipeline event-log + reducer fold (`createServer`/`createClient` in `src/pipeline.mjs`, tested in `test/pipeline-integration.test.mjs`) plus field-law vocabulary (`canUndoField`, `undoableFieldKinds` in `src/field-laws.mjs`) — is complete and shipped. This is the one-log, one-reconciliation-path substrate: a mutation appends a sequenced event; undo is replay-to-checkpoint over the same log, never a second journal. The undo *operation* (replay-to-checkpoint or inverse-event application at the client-store level, exercised as "boot → live stream → optimistic mutate → undo" per W5's own acceptance criterion) is deferred to Phase S station C, where Scope builds Scope's undo on top of the workbench substrate.
- **Reason:** W5's census rows (#6–#10: computeInverse, undo-stack, redo) were written against Scope's current shape (a separate UserUndoLog with computeInverse). Porting that would be a second reconciliation path — a direct violation of AGENTS.md "one reconciliation path." Instead, the workbench substrate (pipeline event-log + reducer fold + canUndoField vocabulary) is the undo foundation. Scope's station C builds undo operation on top — this is the intended convergence path per S-scope-migration.md §Station C. Both Opus 4.8 and GPT 5.5 converged in council c04 on this framing: the substrate is shipped, the operation is Phase S work.
- **Files:** `src/pipeline.mjs` (createServer/createClient), `src/field-laws.mjs` (canUndoField/undoableFieldKinds), `test/pipeline-integration.test.mjs` (2 tests: forward action→event→reducer fold, pipeline/CRUD coexistence), `test/field-undo-laws.test.mjs` (4 tests).
- **Gate:** `node --test` 1612/1612/0.

Decision: Park HTTP leaf / Entity-compile deepenings from the architecture review. Reason: http-route-match, http-body, http-response, and http-response-factory pass the deletion test as focused modules; further Entity splits without a clearer compiled-Entity runtime story would relocate ceremony and must not undo ADR-0003.

Decision: Collapse tick-engine and reaper into Schedule startClockTriggers; privatize discover*; fire-path tests cross that seam. Reason: ADR-0002 unified admission but left two shallow discover→dispatch loops; one interface restores locality and deletes the twin modules in the same change.

## 2026-07-10 — arch-schedule-clock-dispatch merged into main

- **Purpose:** Collapse tick-engine + reaper into Schedule startClockTriggers; private discover*; fire-path tests
- **Files touched:** DECISIONLOG.md, src/clock-runner.mjs, src/internal.mjs, src/kernel.mjs, src/reaper.mjs (deleted), src/schedule.mjs, src/tick-engine.mjs (deleted), test/reaper.test.mjs, test/schedule-discovery.test.mjs, test/tick-engine.test.mjs
- **Commits:** 81e1436
- **Base at merge:** main @ bfd333bdcafe83643eb01aff720985e8f52f6b97
- **Conflict resolution:** NONE

## 2026-07-10 — Scope handle + Live Delivery + Kernel assembly + Replay decision

Architecture-review program deepenings 2–4 (Schedule was #1, already merged).

### Scope handle (ADR-0004)

- **Decision:** Typed **Scope handle** owns committed-log scope grammar (`Entity:id`). Module `src/scope-handle.mjs` (`scopeOf`, `parseScopeKey`, `tryParseScopeKey`). Migrated all `entity:id` format/parse callers (live, kernel consumer path, projected-async, effects, HTTP resync, entity CRUD, side-table, field-delta). Peer of Event handle; distinct from Grant row-scope (`src/scope.mjs`).
- **Files:** `src/scope-handle.mjs`, `docs/adr/0004-typed-scope-handles.md`, callers under `src/`
- **Gate:** unit tests in `test/scope-handle.test.mjs`

### Live Delivery + Kernel assembly

- **Decision:** Live post-commit fan-out + per-batch row latch moves to `src/live-delivery.mjs` (`createLivePostCommitConsumer`). Kernel public seam is dispatch + durable variant + write queue; post-commit consumers are assembled from engaged seams (`engagedPostCommitConsumers`) rather than implemented in Kernel. `authorize: () => true` stays (route gate + in-txn admission own Grants). Legacy entity+id subscribe still works; both paths format/parse via Scope handle.
- **Files:** `src/live-delivery.mjs` (new), `src/kernel.mjs`, `src/live-fanout.mjs`, `src/live-admission.mjs`

### Replay decision + Seq cursor

- **Decision:** Pure `decideReplay(cursor, seqOrSpan)` + `normalizeSeqSpan` in `src/replay-decision.mjs`. `createClient.ingest` is span-aware (accepts `seqSpan` or single `seq`). LiveList `_ingest` calls the same decision (inlined under GENERATED markers for zero-import client). Folds stay separate. Parity test locks the client copy to the source predicates.
- **Files:** `src/replay-decision.mjs`, `src/pipeline.mjs`, `public/workbench-client.mjs`, `test/replay-decision*.test.mjs`
- **Gate:** `node --test` **1635/1635/0**

## 2026-07-10 — three-loop philosophy instilled; structural investigation recorded

- **Decision:** Encode first-principles structure into binding project docs:
  workbench is a **small machine** (Compile / Commit / Deliver) wearing a
  **known-app coat**; Kernel stays thin; Event/Scope/Replay grammars are
  load-bearing; transports are skins; auth product is coat on Principal+routes.
- **Files:** `AGENTS.md`, `CONTEXT.md`, `docs/architecture-map.md`, `README.md`,
  `docs/structural-investigation-2026-07-10.md` (candidates S1–S7, no code moves).
- **Next structural slice if desired:** S1 Live Delivery singular public seam,
  then S2 client fold golden fixtures. HTTP leaf deepenings remain parked.

## 2026-07-10 — S1 Live Delivery singular public seam

- **Decision:** One factory `createLiveDelivery(httpServer, opts)` returns
  `{ emit, count, close, createConsumer }`. Serve engages delivery; Kernel
  registers `app.live.createConsumer(app)` only. Free-function
  `createLivePostCommitConsumer` retired. `createLiveServer` is the same
  function (alias, not a second path). `live.mjs` is a re-export shim.
- **Reason:** Concentrates Deliver-loop composition behind one interface
  (architecture-map / structural investigation S1). Behavior unchanged —
  navigability and singular wiring.
- **Files:** `src/live-delivery.mjs`, `src/live.mjs`, `src/serve.mjs`,
  `src/kernel.mjs`, `src/internal.mjs`, `test/live-delivery-seam.test.mjs`,
  docs architecture-map + structural-investigation.

## 2026-07-10 — S2–S7 structural programme closed

- **S2 done:** Golden fold fixtures (`test/fixtures/fold-golden.mjs`,
  `test/fold-golden.test.mjs`) lock createClient vs LiveList agreement on
  lifecycle, value set, CRDT insert, and replay edges. Folds stay separate.
- **S3 done:** Auth coat packaged under `src/auth/` (entities, routes, session,
  passkey, totp, invitation, membership, index barrel). Compile authz stays
  outside the coat directory.
- **S4 parked:** No further HTTP leaf / Entity splits without runtime story.
- **S5 rejected:** Keep explicit `engagedPostCommitConsumers` list (no
  auto-discover).
- **S6 demand-gated:** `workbench-local-*` no growth without projects/* spine.
- **S7 process:** Field kinds re-defend known-app bar (AGENTS).
- **Files:** auth moves + tests + `docs/structural-investigation-2026-07-10.md`.

## 2026-07-12 — W3 slices 2+3: live job visibility + genericity proof

- **Decision:** Every state-changing mutation on a job with a non-NULL `scope` appends exactly one `_Job.created`/`_Job.updated` event to `_Log` via `appendEvents` (the canonical write surface), allocating per-scope seq with the SAME grammar as `pipeline.mjs` `nextSeq` (readSeq+1 + `_Cursor` upsert) — one seq grammar, never a second. Transitions covered: enqueued/claimed/running (first heartbeat only, via pre-read of status)/progress/cancelled/completed/retried/deadLettered/reassigned (reap switched to `UPDATE … RETURNING *`, keeping single-statement claim-race safety). NULL-scope jobs emit nothing — no stream key, invisible to live boards by construction. `onEvent(fn)` listener registry (per-listener try/catch; a throwing listener never breaks the mutation) and `list({ scope, kind, status, limit })` added to the queue API. The live fan-out gains **scope-anchored** delivery: a foreign `_Job.*` event whose `scope` equals the subscriber's scope key is delivered with the anchor entity identity, authz re-checked against the anchor row, delta projection and ephemeral-field pacing skipped (no delta in the envelope). Genericity proof (slice 3): blog-platform drives a `search-index` kind through the generic queue — `projects/blog-platform/entities.mjs` (SEARCH_INDEX_KIND, enqueueSearchIndex scope `Post:<id>`, searchIndexWorkerFn, searchPosts) with acceptance tests asserting payload is exactly `{ postId }` (no media-shaped keys) and the index row only appears after `work().once()` — the QUEUE did the work.
- **Reason:** W3 packet slices 2+3. The queue mutates `_Job` with raw SQL, bypassing the committed log — job boards subscribed to `_Log` never saw claims/progress/completions (bespoke polling would be a second live path). Progress rides the one event path (council item #1: no second progress channel). The non-media proof demonstrates nothing whisper/transcode-shaped leaked into the substrate — the only queue surface an app touches is kind + payload/result contract.
- **Files:** `src/job-queue.mjs`, `src/live-fanout.mjs`, `projects/blog-platform/entities.mjs`, `test/job-queue-events.test.mjs`, `test/live-fanout-scope-anchored.test.mjs`, `test/blog-platform-search-jobs.test.mjs`.
- **Gate:** `node --test` 1693/1693/0; eslint clean. Merged c3d6165..8aa197b.

## 2026-07-12 — W3 wiring: queue events bridged into the live fan-out (W3 done)

- **Decision:** serve.mjs bridges `app.jobs.onEvent` into the live fan-out. The queue appends its `_Job.*` events to `_Log` itself, not through the durable pipeline, so the post-commit consumer (`createLiveDelivery.createConsumer`) never sees them — durable for catch-up but never pushed live. The bridge mirrors the consumer's shape: parse the event's scope key (`tryParseScopeKey`), resolve anchor entity + row, hydrate, `app.live.emit(entity, id, row, ev, { hydrated })`. **Fail closed:** an unparseable scope, unknown entity, or missing anchor row delivers nothing live — a foreign event must never ride the fan-out's removed-row path, which skips re-auth; the event stays durable in `_Log` either way. Raw-WS e2e (`test/job-live-visibility.test.mjs`) proves: `_Job.created`/`_Job.updated` pushed to an anchor-scope subscriber with anchor identity, shared per-scope seq (1,2,3 across enqueue→claim→complete), no delta; unscoped jobs and missing-anchor events deliver nothing. Also shipped the remaining W3 done criteria: queue API documented in SPEC §9.4 (new subsection — §10–§13 ordinals are load-bearing across PLANS/DECISIONLOG/tests, so no renumbering); census closed with Section 5 mapping Scope's three kinds (whisper transcribe / media transcode / search-index) as pure plug-ins (worker + kind + payload/result contract), inputFileId/profile living in payloads, large results by blob reference, chaining by composition at the enqueue edge.
- **Reason:** Without the bridge, W3 slice 2's "live job visibility" was durable-only — a board got events on resync, not live. Bridging at serve (where `app.live` and `app.jobs` are both in scope) keeps ONE fan-out path; the alternative — teaching the queue about live delivery — would couple the substrate to a consumer.
- **Files:** `src/serve.mjs`, `test/job-live-visibility.test.mjs`, `SPEC.md` (§9.4), `docs/convergence/census/W3-jobs.md` (Section 4 closure + Section 5), `docs/convergence/LEDGER.md` (W3 → done).
- **Gate:** `node --test` 1697/1697/0; eslint clean. Merged 8aa197b..05a5ee2. W3 packet: **done**.

## 2026-07-13 — simulation lifecycle belongs to the application clock

- **Decision:** A compiled `simulation` declaration is started by `buildAndStart`, using the app-bound entity registry, database, queued dispatch, and shared clock. It is not a separately managed timer service.
- **Reason:** Declarations must become behavior through the one application lifecycle. Sharing the clock gives startup and shutdown one owner and prevents tests or callers from needing private simulation wiring.
- **Rate rule:** The shared wake-up interval follows the fastest declared simulation; each simulation's own elapsed-time guard retains its requested cadence.
- **Compiler rule:** The reserved `simulation` slot accepts only the public `simulate()` descriptor. It does not generally permit fields to occupy reserved declaration slots.

## 2026-07-13 — effect provenance does not grant row authority

- **Decision:** `admitsEffects` answers only whether a target entity accepts effects from a source. Every generated mutation must still pass the target entity's normal row grant using the internally minted effect principal.
- **Reason:** Source admission and row authorization protect different boundaries. Treating source admission as write authority let an otherwise accepted source mutate target rows the effect principal was not allowed to change.
- **Authority shape:** Effect provenance lives only at `principal.attributes.effect`; schedule/tick provenance lives only at `principal.attributes.source`. Helpers inspect their own channel, which prevents one internal subsystem from accidentally satisfying another subsystem's admission rule.
- **Atomic failure:** A denied generated mutation aborts the enclosing pipeline transaction, so neither the origin event nor any earlier generated event is committed.

## 2026-07-13 — durable-effect progress is one atomic unit

- **Decision:** For a committed event, durable job insertion and `effect.durable` cursor advancement share one database transaction in live consumption and recovery.
- **Reason:** A cursor written without all jobs loses work; jobs written without the cursor make recovery repeat partial work. The only safe unit is all derived jobs plus cursor, or none.
- **Recovery rule:** A failed transaction does not update the in-memory recovery cursor or enqueue count. A later sweep retries the same log event from durable truth.

## 2026-07-13 — the mutation pipeline owns effect depth

- **Decision:** `maxEffectDepth` is a validated `durableMutationVariant` setting and counts generated effect hops. The effect compiler has no independent recursion counter.
- **Reason:** The pipeline is the component that sees both the current hop and whether another generated mutation actually exists. Enforcing the bound there lets a terminal event at the limit succeed while an attempted extra hop fails atomically.
- **Compatibility:** The private `createEffectContext` and `checkEffectDepth` exports were removed. They had no production caller and duplicated the runtime rule.

## 2026-07-13 — state-transition effects lower into the ordinary effect pipeline

- **Decision:** A field-local transition effect compiles as an updated-event effect annotated with its exact state field and from/to pair. CRUD supplies matching transition metadata for the duration of the transaction; the effect registry and mutation pipeline remain singular.
- **Default target:** Omitting `mutate` means `self`. This makes the compact field-local declaration useful while preserving explicit cross-entity targets.
- **Durability boundary:** Transition metadata is not committed to `_Log`, so transition effects cannot declare durable delivery. Load-time rejection is safer than live/recovery disagreement.
- **Authority:** Lowering does not create a privileged path. Generated mutations pass the same source admission, target row grant, effect-depth, and atomic rollback rules as entity-level effects.

## 2026-07-13 — live synchronization has one lifecycle owner

- **Decision:** `LiveChannel` is backed by one private session state machine with a single connection state, monotonically increasing socket generation, shared connection promise, and desired-subscription registry. Raw wire messages are not queued for replay.
- **Reason:** Reconnect, unsubscribe, send failure, and close previously mutated overlapping maps and callbacks independently. That allowed stale sockets, contradictory replay, hanging promises, and missed offline changes. One lifecycle owner makes those states mutually consistent.
- **Checkpoint rule:** Every subscription acknowledgement, including a reconnect acknowledgement, is an authoritative checkpoint. `LiveList` reconciles its cursor from that checkpoint and uses a bounded buffer plus snapshot recovery when deltas cannot be trusted.
- **Teardown rule:** Closing invalidates every deferred continuation, settles pending operations, closes lists before transport, releases local-relay leadership, and is idempotent. No async completion may render or broadcast after its owner closes.
- **Mutation truthfulness:** A locally rejected write is `failed-rolled-back`; a confirmed success is `committed`; once transmission starts, losing the response is `outcome-unknown`. Uncertain optimistic data is removed because the client cannot honestly present it as committed or rolled back.
- **Testing seam:** Socket construction and lock coordination are injected. Tests drive real lifecycle transitions through those seams instead of mutating connection internals or depending on ambient browser globals.

## 2026-07-13 — each HTTP request body has exactly one bounded reader

- **Decision:** The first body read claims the request. A duplicate reader fails immediately; it never waits on a stream whose `end` event has already passed.
- **Settlement rule:** Success, size refusal, stream error, client abort, and premature close all pass through one settle-once boundary that removes every listener it installed. Oversized bodies are drained without retaining further chunks so the 413 response can flush cleanly.
- **Early refusal:** A trustworthy integer `Content-Length` above the configured cap is rejected before buffering. Chunked or missing lengths remain protected by the same byte counter.
- **Reason:** Request streams are consumable once. Treating them as reusable allowed silent hangs, while leaving callbacks attached after settlement retained request state and performed work after the result was already known.

## 2026-07-13 — subscription requests have correlated outcomes

- **Decision:** Each subscribe wire request carries a client-generated `requestId`, and the server echoes it in both `subscribed` acknowledgements and subscription errors.
- **Reason:** Scope alone is not a safe correlation key across reconnect generations, and an uncorrelated error forced the client to reject every pending subscription. Correlation makes one authorization denial local to one attempt.
- **Lifecycle rule:** A denied desired subscription is removed before its promise rejects. A later subscription to the same scope is a new request; denied intent is not replayed on reconnect.
- **Compatibility:** Uncorrelated server errors remain connection-level failures for older/custom servers and reject all pending attempts, but current Workbench peers always correlate subscription outcomes.

## 2026-07-13 — application startup is independent of HTTP

- **Decision:** `app.start()` is the singular application boot path. It owns route
  finalization, schema preparation, Kernel assembly, recovery, maintenance,
  simulations, job reaping, the shared clock, and graceful-shutdown hooks.
  `listen()` attaches HTTP and Live Delivery before calling that same start path;
  headless processes call it directly.
- **Transport rule:** Starting without HTTP makes the headless choice final for
  that application. A caller that needs HTTP later constructs a fresh app, so the
  Kernel never has to mutate its post-commit consumer set after assembly.
- **Ownership rule:** Maintenance cadence belongs to `workbench({...})`, not to
  one `listen({...})` call. The write queue is created with the application and
  closes admission before shutdown drains already-accepted writes.
- **Failure rule:** Startup and shutdown are singular, idempotent promises. Bind
  errors, route failures, raw pre-listen transport closure, hook failures, and
  process signals release acquired owners without self-await deadlocks. A second
  termination signal force-exits a stuck graceful drain.
- **Compatibility:** `app.ready` is the exact `app.start()` promise once startup
  begins. Existing synchronous `listen()` chaining and Node's synchronous bad-port
  error remain intact.

## 2026-07-13 — undo is compensating mutation metadata, not a second write path

- **Decision:** Wave 9 will build undo/redo on one transactional action executor. Actions own typed, versioned preimages and compensators; the framework persists private undo metadata and applies compensation through ordinary authorization, admission, projection, log, effect, and live-delivery machinery.
- **Rejected:** Public `Entity.undo` verbs, direct SQL restoration, client-authoritative preimages, whole-row snapshot overwrite, and nested `history.undone` wrapper events.
- **Conflict rule:** Compensation fails atomically when later work changed the same semantic state. Unrelated later edits may survive only where an action-specific compensator can prove that merge safe.
- **Ordering:** This is a Wave 9 change because it first requires migrated action families and a unified in-transaction executor. It is not part of the Wave 2 client/HTTP closure.

## 2026-07-14 — Wave 4.9: owning-stream action receipt (identity/idempotency closure)

- **Decision:** Durable `dispatch`/`dispatchBatch` dedupe moves from scanning `_Log` for rows matching `actionId` to reading a new `_ActionReceipt` row keyed `PRIMARY KEY (scope, actionId)`, where `scope` is the dispatch request's own owning scope — distinct from any individual emitted event's scope. The receipt stores `eventRefs`: a JSON array of `{scope, seq}` `_Log` references in the exact order the handler originally emitted them (the stable emission ordinal), and is written inside the same transaction as the events it references (`commitEvents`), so append and receipt are atomic. A zero-event action still receives a receipt, closing the durability gap where a no-op action had no `_Log` row to key a retry against. Requests that omit the optional public `scope` field key on `''`, so every caller that hasn't opted in shares one bucket — reproducing the pre-Wave-4.9 global-by-actionId dedupe exactly, with no behavior change for existing consumers.
- **Reason:** Wave 3.6 pinned five destination contracts as `test.todo` and named this exact direction: "Wave 4.9 will introduce an explicit owning-stream action receipt with stored result and stable emission ordinal." The prior `eventsFor(db, actionId)` dedupe had two real bugs it exposed: (1) the same `actionId` reused under two different owning scopes collided (the second dispatch would see the first's committed events and wrongly report a dedupe instead of running), and (2) a zero-event action left no `_Log` row, so a retried no-op re-ran its handler every time — not idempotent. `ORDER BY scope, seq` also could not reconstruct a multi-scope action's original emission order on replay; the receipt's stored `eventRefs` order fixes that without introducing a second event-ordering grammar (the `_Log` PK and per-scope seq allocation are unchanged).
- **Files:** `src/committed-log.mjs` (+`_ActionReceipt` DDL, `receiptFor`, `eventsFromReceipt`, `insertReceipt`), `src/pipeline.mjs` (`dispatch`/`dispatchBatch` destructure `scope`, dedupe via receipt; `commitEvents` writes the receipt atomically with the transaction), `test/identity-idempotency.test.mjs` (5 tests, `test.todo` → `test`), `test/schema-table-census.test.mjs` (pinned table list +`_ActionReceipt`).
- **Gate:** `node --test` 1951/1951/0 (4 pre-existing unrelated `test.todo`); ESLint 0 errors (244 pre-existing warnings).

## 2026-07-14 — Wave 4.10: snapshot atomicity, deleted-row history anchor, all-or-nothing historical resync

- **Decision:** Three independent fixes close the Wave 3.7 hostile-recovery destination contracts. (1) `snapshotScopeRoute` reads the scope cursor as the LAST synchronous step before responding — after `authorizeScope` and the optional async `scopeSnapshot` aggregation callback both resolve — instead of before either. (2) A new `_DeletedRowAnchor(entity, id, row, deletedAt)` table captures a row's last known state atomically with its `DELETE` (same projection-consumer transaction, `entity/projection.mjs`). `authorizeRow` gained an explicit opt-in `allowDeletedAnchor` option: when set and the live row is gone, it evaluates the SAME `mayRow` grant against the anchor's historical snapshot, returning 404 on denial (indistinguishable from never-existed). Only `eventsSinceRoute` opts in. (3) `LiveList._resync`'s events-since fold gained an all-or-nothing pre-check (`_isValidHistoricalBatch`): the fetched batch must be contiguous from `cursor+1` with every event type recognized, or none of it applies — reusing the resync's existing failure/backoff path rather than a new one.
- **Reason:** (1) A custom `scopeSnapshot` may read fresher state than the anchor row it was given (the contract test proves this with a parked promise), so pairing it with a cursor read before that resolution risks an epoch split — Contract 4 (detail). (2) Deletion is fail-closed by construction (`readScopedRow` finds nothing), so an owner's own resync of their own deleted resource's tail history 404'd even though `_Log` still holds the full history and the owner still held read+subscribe when the removal happened — Contract 1. The anchor is scoped narrowly (opt-in parameter, read-only historical path) specifically so it cannot become a second source of live row state or a second auth engine — AGENTS.md's no-second-path rule. (3) A historical batch is external data (HTTP response) arriving outside the live span/dup-gap machinery `decideReplay` already guards; partially applying a batch with an internal hole or malformed row left state and cursor in a position neither the pre-fetch nor a fully-caught-up state could produce — Contracts 2+3.
- **Files:** `src/http-framework-routes.mjs` (`snapshotScopeRoute` cursor ordering, `eventsSinceRoute` opts into the anchor), `src/deleted-row-anchor.mjs` (new: DDL + capture + read), `src/ddl.mjs` (+table), `src/entity/projection.mjs` (capture-before-delete), `src/http-crud-dispatch.mjs` (`authorizeRow` `allowDeletedAnchor` option), `public/workbench-client.mjs` (`_isValidHistoricalBatch`/`_isKnownEventType`, `_resync` all-or-nothing fold), `test/hostile-recovery.test.mjs` (4 tests, `test.todo` → `test`), `test/schema-table-census.test.mjs` (pinned list +`_DeletedRowAnchor`).
- **Gate:** `node --test` 1955/1955/0 (0 remaining `test.todo`); ESLint 0 errors (244 pre-existing warnings). Wave 4 complete.

## 2026-07-14 — Wave 5/6 scoping: council consensus on "persistence cutover" and "scope migrations"

- **Decision:** "Wave 5" is renamed in intent (not yet in file/branch names) to durable post-commit consumers — a kernel-internal generalization of the cursor-backed recovery pattern already proven by `effect.durable`/`projected.async`, with zero Scope-data involvement. "Wave 6" is renamed in intent to a Scope entity storage-parity PROOF — a workbench-repo-only test harness comparing `projects/scope-entities.mjs` against a read-only parse of Scope's `prisma/schema.prisma`, never opening `scope.db`. The actual Scope persistence cutover (deleting Prisma tables, repointing Scope's server, enabling declarative grants) remains exactly where `docs/convergence/S-scope-migration.md` already gates it — a later, separately owner-approved, Scope-repo slice. Neither council member proposed relitigating that gate.
- **Process:** the decision was not made unilaterally. Two independent council members (Claude Opus 4.8, and gpt-5.6-terra via the `codex` CLI) received the same self-contained brief with read-only tool access to both this repo and `~/Development/scope`, answered independently, then were each given the other's full answer and asked for a revised final position (COORDINATOR.md's cross-evaluation protocol, applied ad hoc since this branch isn't running the full council/subagent machinery). Both rounds converged on the same structural split before AND after cross-eval — treated as strong signal, not coincidence, since the reasoning paths were independently anchored to the same code evidence (`PLANS.md:604`'s prior deferral, `scope-entities.mjs`'s own "grants are DECLARATIVE until cutover" self-caveat, `S-scope-migration.md`'s existing gate).
- **Reason the naming itself was flagged as the risk, not just the scope:** both members independently warned that a future agent reading "Wave 6: scope migrations" out of context — without this brief — could misread it as license to touch real data. `~/Development/scope`'s database is real and in active local use; nothing in Wave 5/6 touches it.
- **Refinement from cross-eval:** live delivery is excluded from Wave 5's cursor-unification target. It re-authorizes at delivery time and the client (not a server-side durable queue) owns the reconnect/replay decision — folding it into the same recovery contract as blob/email consumers would create a second delivery/reconciliation model, which AGENTS.md's no-second-path rule forbids. Wave 5's uniformity is at the consumer descriptor/lifecycle-assembly level; recovery semantics stay a named variant per consumer kind.
- **Refinement from cross-eval:** an email "crash after send never double-sends" test is not achievable honestly for an external transport without provider-side idempotency keys. Wave 5's external-delivery contracts must state at-least-once/unknown-handoff explicitly rather than imply exactly-once.
- **Verified finding surfaced during cross-eval (not merely proposed — checked against the live tree):** `projects/scope-entities.mjs`, the file Wave 6 will use as its golden input, is already stale against the real, already-migrated `~/Development/scope/src/lib/wb-scope/entities.ts` — missing `Source.updatedAt`, and `Theme.codeIds` declared `text()` with a comment claiming workbench has no JSON field kind, when `json()` (`src/field.mjs`) already exists and the real declaration already uses it. Wave 6's first canary test is therefore expected to fail RED on the unmodified tree — evidence the harness will catch a real bug on day one, not a defect in the council's reasoning.
- **Scope of what was actually built this session (see next entry):** only the narrowest, most concretely-evidenced slice of Wave 5 — cursor-backed blob-finalize recovery, replacing `blob-store.mjs`'s reaper-based reconcile step. Log retention (already idempotent by construction; needs a regression test, not a new mechanism) and live delivery (excluded per the refinement above) are deliberately deferred to a follow-on slice. Wave 6 has not been started.

## 2026-07-14 — Wave 5.1: blob-finalize joins the `_ConsumerCursor` durable-recovery pattern

- **Decision:** `blob-lifecycle.mjs`'s post-commit finalize consumer now advances a `blob.finalize`-keyed row in the existing `_ConsumerCursor` table (no new DDL — the table already exists, shared with `effect.durable`/`projected.async`), and a new `reconcileBlobFinalize(db)` boot-time sweep — wired into `application-runtime.mjs`'s `bootApplication` alongside the other two reconcile calls — replays any scope whose `_Log` history outran its cursor. `blob-store.mjs`'s `reap()` loses its former "adopted + pending slot exists → finalize" step and `reconciled` counter in the same change: that step is now strictly redundant (cursor recovery covers every case it did, with an actual progress marker instead of a full-table rescan every tick), and AGENTS.md requires the generalized mechanism retire the special case it replaces in the same change.
- **Reason:** this was the one concretely-identified gap from the Wave 5/6 scoping council — `effect.durable` and `projected.async` were already durable/replayable; blob-finalize was the third of five `engagedPostCommitConsumers` and the only one with no cursor, relying entirely on a 10-minute interval table scan with no notion of what it had already checked.
- **Atomicity is explicitly at-least-once, not exactly-once, and the code says so:** `blobs.finalize(id)` renames bytes on the filesystem, which the wrapping SQL transaction (for the `_ConsumerCursor` write) cannot roll back. A failed or blocked cursor write can leave the file already renamed while the checkpoint stays behind. This is safe only because `finalize()` is idempotent (pre-existing contract in `blob-store.mjs`), so a reconcile replay of the same id is always a no-op. A test (`'a blocked cursor write leaves the checkpoint behind, but the idempotent finalize still converges on reconcile'`) pins this down directly instead of asserting a false stronger guarantee.
- **Batch-wide dedup preserved:** the pre-existing consumer deduped blob ids across an entire `afterCommit` batch before calling `finalize()` once per id (two events referencing the same blob in one batch — e.g. create then update — must finalize once). The first draft of the cursor-backed rewrite processed per-event instead and was caught by the existing pinned `test/blob-upload.test.mjs` fixture; the fix dedupes across the batch while still advancing each relevant event's own scope cursor.
- **Files:** `src/blob-lifecycle.mjs` (cursor-backed consumer + `reconcileBlobFinalize`), `src/kernel.mjs` (`app.reconcileBlobFinalize` wiring), `src/application-runtime.mjs` (boot-time sweep call), `src/blob-store.mjs` (`reap()` reconcile-step retirement + comment updates), `test/blob-finalize-durable.test.mjs` (new, 5 tests).
- **Gate:** `node --test` 1960/1960/0 (0 `test.todo`); ESLint 0 errors (244 pre-existing warnings, 0 new).

## 2026-07-14 — Wave 5.2: post-commit consumer kind taxonomy made explicit and honest

- **Decision:** `src/kernel.mjs`'s consumer assembly now produces `{ name, kind, consumer }` descriptors (stored as `app.postCommitConsumerDescriptors`) instead of a bare filtered array of functions, with `kind` drawn from a closed exported set `POST_COMMIT_CONSUMER_KINDS`: `durable-projection-consumer`, `live-delivery-consumer`, `best-effort-external-consumer`.
- **Reason:** the Wave 5/6 council's cross-eval refinement said live delivery must not be folded into the cursor-recovery contract blob/email/projected/effect consumers share, and that the uniformity Wave 5 should deliver is "at the consumer descriptor/lifecycle-assembly level, with recovery semantics as a named variant per consumer kind." This makes that variant visible and enumerable at the one place consumers are assembled, without changing what `durableMutationVariant` receives (still a plain function array via `.map((d) => d.consumer)`) — no pipeline contract change.
- **Honesty finding:** classifying the email consumer (`email-seam.mjs`) surfaced that it has no cursor and no reconcile sweep at all — not even the at-least-once behavior the council assumed Wave 5's tests should assert. Its kind is named `best-effort-external-consumer`, not `durable-projection-consumer`, specifically so the taxonomy doesn't overclaim a guarantee the code doesn't provide. Fixing this (giving email real durability) is explicitly out of scope for this slice — the ask was a naming/classification pass, not a new mechanism.
- **No-second-path check:** `engagedPostCommitConsumerDescriptors` itself contains no branch on `kind` — recovery mechanics stay inside each consumer's own module (`blob-lifecycle.mjs`, `projected-async.mjs`, `durable-effects.mjs`, `email-seam.mjs`), pinned by a structural test that extracts just that function's source via a balanced-brace scan and asserts no `kind ===`/`switch` on it.
- **Files:** `src/kernel.mjs` (`POST_COMMIT_CONSUMER_KINDS`, `engagedPostCommitConsumerDescriptors`, `app.postCommitConsumerDescriptors`), `src/internal.mjs` (re-export), `test/consumer-descriptor-kinds.test.mjs` (new, 3 tests).
- **Gate:** `node --test` 1966/1966/0 at the point this slice landed; ESLint 0 errors, 0 new warnings.

## 2026-07-14 — Wave 6: scope-entities.mjs golden-parity proof against Scope's real entities.ts

- **Decision:** `projects/scope-entities.mjs` is brought current against Scope's real, deployed `~/Development/scope/src/lib/wb-scope/entities.ts` (not `prisma/schema.prisma`), and a new test (`test/scope-entities-parity.test.mjs`) reads that real file at test-run time and fails RED if the mirror ever drifts again — a live proof, not a frozen snapshot.
- **Reason target changed from the council's stated comparison (Prisma) to entities.ts:** direct reading of `schema.prisma` (815 lines) against `entities.ts` (180 lines) showed the two differ by deliberate redesign in dozens of places — e.g. Prisma's `ExternalRef` has no `projectId` column at all (entities.ts adds it as a workbench-side convenience), and Prisma's `Artefact`/`MediaFile` carry review/original-hash fields the migration dropped on purpose. A raw Prisma-column diff would have reported dozens of intentional decisions as false gaps. entities.ts is real, deployed TypeScript already imported by Scope's own `schema/compatibility.ts` and `phase-S/station-B.ts` — it is the actual golden shape, and the only comparison that produces the small, meaningful diff the council itself predicted.
- **Drift found and fixed — five gaps, not the two the council named:** `Source.updatedAt` (missing), `Theme.codeIds` (`text()` should be `json()`) were the two known ones; direct comparison also found `Codebook.updatedAt` and `Collection.updatedAt` missing entirely, and `Comment` missing `resolved`/`resolvedAt`/`resolvedBy` and using `date()` where the real entity uses `text()` (ISO strings). All five closed in the same change.
- **Read-only constraint held throughout:** only `entities.ts` was read (never imported, never executed, never written to); `scope.db` and `schema.prisma` were never opened this session.
- **Files:** `projects/scope-entities.mjs` (field fixes), `test/scope-entities-parity.test.mjs` (new, 3 tests — inline-fixture parser tests + the live real-file comparison), `test/scope-entities-parity-parser.test.mjs` (new, 3 tests — a second, independently-tested narrow TS-as-text extractor, same "descriptive evidence, not a general parser" framing as `sqlite-storage-description.mjs`; accepted as a second small parser rather than forcing a shared module for ~40 lines of regex).
- **Gate:** `node --test` 1969/1969/0 (0 `test.todo`); ESLint 0 errors (244 pre-existing warnings, 0 new).

## 2026-07-14 — Wave 5.3: email delivery cursor-backed; blob-finalize cursor-monotonicity bug fixed

- **Decision:** `email-seam.mjs`'s post-commit consumer now uses the same `_ConsumerCursor` pattern as blob.finalize/projected.async/effect.durable — a per-scope cursor advances only after a successful `transport()` call, and a new `reconcileEmailDelivery(db)` boot sweep replays scopes whose `_Log` outran their cursor. Reclassified in `kernel.mjs` from `'best-effort-external-consumer'` to `'durable-projection-consumer'`.
- **Bug found and fixed in the same change, in already-shipped Wave 5.1 code:** `blobFinalizeConsumer` and `reconcileBlobFinalize` tracked only "the highest seq seen per scope," not "every event actually processed." An earlier same-scope event's failed cursor write could be silently overwritten by a later same-scope event's successful one (cursor only ever advances), permanently hiding the earlier miss from all future reconcile sweeps. Fixed by adding a per-batch/per-pass `blockedScopes` set to both functions — once a scope's cursor write fails, its remaining events in that pass are skipped (deferred to the next reconcile, replayed in order) rather than letting a later success paper over the miss. Discovered while designing the email consumer along the same pattern, before any code was written for it — applied to email from the start, then backported to blob-finalize.
- **Idempotency is NOT assumed to transfer between consumers sharing the same mechanism — pinned by a test, not just a comment:** blob-finalize's underlying work (a filesystem rename) is idempotent, so its at-least-once cursor mechanism behaves as exactly-once in practice; a transport send is not provably idempotent. `test/email-delivery-durable.test.mjs`'s fourth test proves a blocked-cursor-write-after-success scenario causes a real duplicate send on reconcile (2 transport calls, not 1) — the honest contract, not an inflated one.
- **Files:** `src/email-seam.mjs` (cursor-backed consumer + `reconcileEmailDelivery`), `src/kernel.mjs` (kind reclassification, `app.reconcileEmailDelivery` wiring, expanded taxonomy doc comment), `src/application-runtime.mjs` (boot-time email sweep call), `src/blob-lifecycle.mjs` (monotonicity fix, both functions), `test/blob-finalize-durable.test.mjs` (new regression test), `test/email-delivery-durable.test.mjs` (new, 5 tests), `test/consumer-descriptor-kinds.test.mjs` (updated kind assertion + rewritten email test).
- **Gate:** `node --test` 1975/1975/0 (0 `test.todo`); ESLint 0 errors (243 pre-existing warnings, 0 new).
