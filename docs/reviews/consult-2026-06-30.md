## Consult: 48 open questions — Opus + GPT-5.5 (2026-06-30)

**Protocol.** Round 1: both models independently answer all 48 (position + confidence + reasoning). Round 2: each receives the other's Round-1 answers, marks AGREE/DISAGREE per item, revises to a final position. Lead synthesizes. This file is the lossless per-item record; DECISIONLOG #60 holds the final-resolution register.

**Consensus metric: 37 full · 10 direction-only (confidence split or exact-value = implementer call) · 1 genuine disagreement.**

Legend per item: `[FULL]` both converged · `[SPLIT]` direction agreed, confidence/value differs (one of the 10) · `[RETRACT]` one model corrected its own R1 → converged · `[DISAGREE]` the one genuine split (#34, user-resolved). Confidences: H/M.

**Cross-check corrections of note (the value-add of mutual review):**
- **#11** GPT-5.5 retracted its R1 `now` handler arg after Opus flagged it as ADR #24's ad-hoc-clock foot-gun; `now` resolves at commit in the kernel.
- **#30** Opus retracted its R1 "materialize derived" framing; `derived` stays read-time, `projected.inline` is a separate concept.
- **#38** Opus corrected its over-cautious "defer" — unify-at-step-5 resolves the #34 deferral (the cutover IS the consumer).
- **#14** Opus caught a real constraint GPT-5.5 overlooked: VACUUM / CREATE UNIQUE INDEX can't run inside a sqlite txn → needs a separate pre-traffic non-txn phase.
- **#27** GPT-5.5 flagged Opus's "latched re-auth" wording against the no-cache-first rule (#15); resolved to cache-less-first.
- **Confidence raised M→H** on #18, #19, #20, #25, #27, #33 (settled decisions verified against ADRs, not judgment calls).

---

### Tier 1 — Active bugs / hard-rule violations

**#1 linkShare.tier crash** `[FULL]`
- Opus R1: thread `{ is, entity: row }` into `clause.can` + build compiled `linkHolder` harvest face (symbolic principal-attribute bind, threaded like PRINCIPAL_ID_TOKEN). H
- GPT-5.5 R1: pass materialized row as `entity` + extend registry to compile `linkHolder` via structured `linkShare.token` handle (scope-sql already supports sub-cell handles). H
- R2: both AGREE — real gap confirmed (row-grant.mjs:61 calls `{ is }` only; doc.mjs:163 reads `entity.linkShare.tier`). H/H.

**#2 serve.mjs raw-SQL = second mutation path** `[FULL]`
- Opus R1: delete raw INSERT/UPDATE/DELETE+`live.emit`, route all CRUD through `app.pipelineServer.dispatch`, delete `crudHandlers` if dead. H
- GPT-5.5 R1: delete raw SQL branch, route CRUD through `pipelineServer.dispatch`, keep list/read as scoped reads. H
- R2: both AGREE. H/H.

**#3 live.mjs 2nd per-scope seq Map** `[FULL]`
- Opus R1: live reads `seq` off the committed event (pipeline `_Cursor`/`nextSeq`), delete `sequences`+`dispatchedActions` Maps. H
- GPT-5.5 R1: retire live.mjs sequences/dispatchedActions; deliver committed `(scope,seq,actionId)`. H
- R2: both AGREE. H/H.

### Tier 2 — P1 blockers

**#4 TEXT-UUID id model** `[SPLIT]`
- Opus R1: land TEXT caller-owned ids (PKs + FK/member as strings) before/with step 5; ~20 seeds numeric→string. H
- GPT-5.5 R1: land TEXT UUID before step 5; mint before dispatch; migrate numeric seeds. H
- Opus R2: AGREE — drops to M (repo may tolerate a mixed-id interim; sequencing-first is cleaner but not strictly blocking).
- GPT-5.5 R2: AGREE — H.
- *Resolution: land before step 5 (entity-as-projection needs the id in the event before row materialization; INTEGER lastInsertRowid can't supply). **Verified during implementation: DDL TEXT PK + UUID minting + ~14 seed migrations are ALREADY DONE by in-flight P1 work (green 347); ~6 stragglers use numeric ids tolerated by TEXT affinity.***

**#5 app.pipelineServer cutover (step 5)** `[FULL]`
- Opus R1: build `app.pipelineServer` in `app.ready`, await in tests, no raw-SQL fallback (prior attempt failed on broken fallback, not kernel construction). H
- GPT-5.5 R1: hard cutover — build during app.ready, await in tests, remove (not fallback around) raw SQL. H
- R2: both AGREE. H/H.

**#6 collapse live seq (step 6)** `[FULL]`
- Opus R1: live = post-commit consumer reading committed event's seq + `(scope,actionId)` dedupe onto kernel `_Cursor`/`_Log`. H
- GPT-5.5 R1: live = pure consumer of committed pipeline events, no live-local seq/dedupe. H
- R2: both AGREE. H/H.

**#7 post-commit fan-out registry (step 7)** `[FULL]`
- Opus R1: one registry, live=sync in-proc, jobs/webhooks/fts=async, fired AFTER COMMIT, socket I/O never in txn; entity-row projections run in-txn (derive rows, not deliver). H
- GPT-5.5 R1: one registry, live=sync, jobs/webhooks/FTS/projected.async=async, strictly after COMMIT. H
- R2: both AGREE. H/H.

**#8 snapshot/bootstrap + resync (step 8)** `[FULL]`
- Opus R1: snapshot reads row + `Cursor.lastSeq` in ONE sqlite txn; `subscribe(since)` replays `_Log WHERE seq>since`; hard-fail on stale cursor (never silent truncate). H
- GPT-5.5 R1: atomic row+cursor read, subscribe since, resync hard-fail. H
- R2: both AGREE. H/H. (Carries eng-review Tier-1 silent-data-loss specs #1 + #2.)

**#9 writeQueue (step 9)** `[FULL]`
- Opus R1: single-writer async mutex over `BEGIN IMMEDIATE`→`COMMIT`, bounded wait ~5s → 503 (handlers may `await` mid-txn). H
- GPT-5.5 R1: single writeQueue ~5s → 503. H
- R2: both AGREE. H/H.

**#10 txn-scoped db wrapper (step 10)** `[FULL]`
- Opus R1: handler `{db}` throws on BEGIN/COMMIT/ROLLBACK/SAVEPOINT; framework owns the one txn. H
- GPT-5.5 R1: reject BEGIN/COMMIT/ROLLBACK/SAVEPOINT. H
- R2: both AGREE. H/H.

### Tier 3 — eng-review specs

**#11 handler signature** `[RETRACT → FULL]`
- Opus R1: `({ payload, principal })` emit-only (already implemented, pipeline.mjs:157); custom handlers needing reads get the read-only `{db}`. H
- GPT-5.5 R1: `{ payload, principal, db, now }` — read through wrapper, emit only. H  *(included `now`)*
- Opus R2: DISAGREE — `{ payload, principal }` + read-only `{db}`, NOT `now`; `now` resolves at commit (ADR #24). H
- GPT-5.5 R2: AGREE — retracted R1 `now`; emit-only + read-only txn-scoped `{db}`. H

**#12 projection re-enters dispatch** `[FULL]`
- Opus R1: projection writes re-enter `server.dispatch` (event→log→projection folds the row), never direct UPDATE. H
- GPT-5.5 R1: projection dispatches target action as bounded projection principal. H
- R2: both AGREE. H/H.

**#13 projection idempotency** `[FULL]`
- Opus R1: in-framework: cursor atomic with derived write (same txn); external-consumer dedupe keyed `(scope,seq)` + job-enqueue `(consumerId,eventId)`. H
- GPT-5.5 R1: per-consumer watermark keyed `(consumerId,scope,seq)`; atomic or replay-tolerant. H
- R2: both AGREE. H/H.

**#14 migrations stop-the-world** `[SPLIT]`
- Opus R1: pre-traffic under writeQueue + meta-version bump same txn; BUT VACUUM / CREATE-UNIQUE-INDEX can't run in a sqlite txn → separate pre-traffic non-txn phase. M *(caught the constraint)*
- GPT-5.5 R1: pre-traffic under writeQueue, non-rollbackable DDL split into preflight + idempotent apply + meta-bump same txn. H *(overlooked VACUUM-in-txn)*
- R2: both AGREE on direction; GPT-5.5 R2 raises the VACUUM wrinkle Opus caught → drops to M. H/M.
- *Resolution: pre-traffic under writeQueue + meta-bump same txn where possible; VACUUM / CREATE-UNIQUE-INDEX in a separate pre-traffic non-txn phase.*

**#15 re-auth no-cache-first** `[FULL]`
- Opus R1: re-run `mayVerb('subscribe')` per emit on captured row; latched cache only after profiling. H
- GPT-5.5 R1: no cache first; re-hydrate at emit. H
- R2: both AGREE. H/H.

**#16 bootstrap gate bound** `[SPLIT]` — user call
- Opus R1: max-buffered-events cap (or snapshot-fetch deadline) forcing hard-fail. M
- GPT-5.5 R1: BOTH a max buffered-event count AND a snapshot timeout. H
- Opus R2: AGREE — M (right defensive shape, not an established decision).
- GPT-5.5 R2: AGREE — H.
- *User resolved: BOTH a held-events size cap AND a wait-deadline, EITHER trips forces hard-fail/re-bootstrap. Bound = implementer tuning call (small + configurable).*

**#17 reaper mutex** `[FULL]`
- Opus R1: reaper acquires writeQueue mutex for both pending-reap and adopted-refcount-reap. H
- GPT-5.5 R1: reaper acquires mutex for both. H
- R2: both AGREE. H/H.

### Tier 4 — Deferred Phase-2 machinery

**#18 text.crdt merge** `[FULL]` — confidence raised M→H
- Opus R1: build real merge in P6, replace `phase2Merge('crdt')`; M. R2: raises M→H (ADR #27 = settled named whole).
- GPT-5.5 R1: defer to Phase 2, keep import failing loudly; H. R2: AGREE — H.
- *Resolution: built-in named whole (NOT the deferred toolkit); real per-element merge in P6.*

**#19 store map/log pipeline actions** `[FULL]` — confidence raised M→H
- Opus R1: build store apply/diff + `:appended` as pipeline actions in P6; M. R2: raises M→H (direct consequence of #2 cutover applied to side-tables).
- GPT-5.5 R1: promote store mutations into pipeline actions; H. R2: AGREE — H.

**#20 ordered fractional-index** `[FULL]` — confidence raised M→H
- Opus R1: build `ordered` named-whole in P6 (insertAt/move/reorder); M. R2: raises M→H (ADR #9/#18 named whole earned by deletion test).
- GPT-5.5 R1: implement as own contract, defer until pipeline-backed; H. R2: AGREE — H.

**#21 state runtime** `[SPLIT]`
- Opus R1: build state runtime in P6 (transition guard + edge effects + auto=sugar over schedule.after); H.
- GPT-5.5 R1: wire transitions as pipeline mutations, lower `auto` to schedule; H.
- Opus R2: AGREE — H.
- GPT-5.5 R2: AGREE — M (runtime unimplemented; depends on #22).

**#22 schedule/tick time-source** `[FULL]`
- Opus R1: build `src/schedule.mjs` time-source before state.auto/TTL. H
- GPT-5.5 R1: add schedule/tick as typed time sources feeding the one pipeline. H
- R2: both AGREE. H/H.

**#23 declarative effects wiring** `[FULL]`
- Opus R1: wire `effects { mutate, with }` in entity() as bounded in-txn reentrancy + load-time cycle/effectSource verification. H
- GPT-5.5 R1: replace `fireMapEffects` with the general effect compiler/runtime; delete the direct `mutate.create()` side path. H
- R2: both AGREE. H/H.

**#24 now token substitution** `[FULL]`
- Opus R1: resolve `now` at commit in the kernel (pipeline.mjs:161 hardcodes `new Date()`). H
- GPT-5.5 R1: resolve `now` at commit in pipeline/effect materialization; never store the token or call `new Date()` ad hoc. H
- R2: both AGREE. H/H.

**#25 presence ephemeral** `[FULL]` — confidence raised M→H (Opus); GPT-5.5 M
- Opus R1: build presence ephemeral/volatile in P6 (no persistence seam); M. R2: raises M→H (SPEC §7.2 / serve.mjs:167 excludes it from the stored table).
- GPT-5.5 R1: non-persistent live state with coalescing; H. R2: AGREE — M (exact coalescing belongs with P6 delivery).

**#26 link token autogen** `[SPLIT]` — user call
- Opus R1: build `link({ token:'autogen' })` minting using existing `tokenIntent`; M. R2: raises M→H (scope-sql lowers linkShare.token, auth-entities mints link principals).
- GPT-5.5 R1: implement minting at create/materialization; H. R2: AGREE — M (no mint step yet).
- *User resolved: SERVER AUTO-GENERATES at create (`token:'autogen'` via existing tokenIntent); apps never hand-make tokens.*

**#27 §8 live delivery** `[RETRACT → FULL]` — confidence raised M→H
- Opus R1: build §8 (field-keyed interest, two-layer pace, latched re-auth-at-emit) after the fan-out spine; M. R2: raises M→H (eng-review makes §8 a consumer of ★1+★3).
- GPT-5.5 R1: build §8 after the spine; H. R2: DISAGREE with "latched re-auth" wording → align to no-cache-first (#15).
- *Resolution: build §8 after the spine; re-auth cache-less first.*

**#28 client LiveChannel/LiveList** `[FULL]`
- Opus R1: build in P7 with snapshot→cursor→stream structurally enforced; URLs derived from declarations. H
- GPT-5.5 R1: blessed client API enforcing bootstrap ordering. H
- R2: both AGREE. H/H.

**#29 default + final required** `[FULL]`
- Opus R1: apply at the create/materialization seam (after payload/route/principal/generated-id merge; validate only rejects). H
- GPT-5.5 R1: apply at the materialization seam. H
- R2: both AGREE. H/H.

**#30 derived vs projected.inline** `[RETRACT → FULL]`
- Opus R1: keep `number({derived})` read-time by default; materialized recompute only under `projected.inline`. H *(mis-framed as a recompute knob)*
- GPT-5.5 R1: keep `derived` read-time; materialized recompute is `projected.inline`. H
- Opus R2: DISAGREE with own framing — `derived` and `projected.inline` are two distinct named concepts (read-time pull vs in-txn sort key), not a default-and-escalation; three separately-named concepts (derived / projected.inline / projected.async). H
- GPT-5.5 R2: DISAGREE — same correction: `derived` read-time, `projected.inline` is its own declaration. H

### Tier 5 — Go/no-gos

**#31 recomputeFrom(query)** `[FULL]`
- Opus R1: defer until a real app proves need + ADR #6 safety case extended (runtime query = not depth-capped). H
- GPT-5.5 R1: defer until app proves need + ADR bounds cardinality. H
- R2: both AGREE (deferred). H/H.

**#32 spatial event scoping** `[FULL]`
- Opus R1: defer; ship coordinate/index seam (ADR #9), not rtree engine. H
- GPT-5.5 R1: defer; ship seam, decide runtime when a spatial app is the spine. H
- R2: both AGREE (deferred). H/H.

**#33 CRDT-authoring toolkit** `[FULL]` — confidence raised M→H
- Opus R1: defer toolkit; ship built-in text.crdt/polyline/raster as proof of contract sufficiency; M. R2: raises M→H (SPEC §5.1 + ADR #18 verbatim defer the toolkit, keep the proofs).
- GPT-5.5 R1: defer toolkit, ship built-in proofs first; H. R2: AGREE — H.

**#34 general arithmetic beyond inc/dec** `[DISAGREE]` — user resolved
- Opus R1: PERMANENTLY EXCLUDE; new arithmetic arrives only as field-plugin operators if proven. M. R2: AGREE (exclude) — H.
- GPT-5.5 R1: exclude for now; typed operators only, new arithmetic as field-plugin operators if proven. H. R2: DISAGREE — general arithmetic is DEFERRED, not permanently excluded (SPEC §9.2 lists under "Deferred" not "Rejected"); H.
- *User resolved: DEFERRED-NOT-EXCLUDED (needs a real use-case + an ADR cardinality/safety bound before it ships). Safety bar is identical either way (both require an ADR first); posture differs only in whether DECISIONLOG reads 'deferred' vs 'excluded'.*

**#35 batch-load + rate-negotiation** `[FULL]`
- Opus R1: defer both; ship fixed-window rate-limiting + single-entity snapshot. H
- GPT-5.5 R1: defer both (optimizations, not foundation invariants). H
- R2: both AGREE (deferred). H/H.

**#36 projected.async = §9.3 projection** `[FULL]`
- Opus R1: `projected.async` IS the same projection primitive as §9.3 OOB effects (one registry entry, not a separate subsystem). H
- GPT-5.5 R1: `projected.async` = in-framework read-model case of the same post-commit projection primitive. H
- R2: both AGREE. H/H.

**#37 .scopedTo(principal)** `[FULL]`
- Opus R1: keep unbuilt until a consumer exists; unscoped trusted query API is the only path (DECISIONLOG #41). H
- GPT-5.5 R1: defer until a request-path consumer needs scoped reads. H
- R2: both AGREE (unbuilt). H/H.

**#38 unify principal** `[RETRACT → FULL]`
- Opus R1: defer until the request path consumes it (DECISIONLOG #34). H *(over-cautious)*
- GPT-5.5 R1: unify NOW at the HTTP→pipeline boundary as `{type,id,attributes}`, eliminating the bare `{id}` before CRUD dispatch is wired. H
- Opus R2: DISAGREE with own over-cautious defer — unify AT the step-5 cutover (that cutover IS the consumer whose absence justified #34's deferral; resolves, not violates). H
- GPT-5.5 R2: AGREE — unify exactly when step-5 threads the HTTP principal into `pipelineServer.dispatch`. H

**#39 log retention default** `[FULL]`
- Opus R1: retain-all default; a retention window arrives only as explicit named opt-in with hard-fail resync (#8) as its safety net. H
- GPT-5.5 R1: retain-all default; trimming opt-in + stale-cursor hard-fail preserved. H
- R2: both AGREE. H/H.

### Tier 6 — May-defer specs

**#40 writeQueue bounds** `[SPLIT]`
- Opus R1: depth (small, ~tens) + wait ceiling ~5s → 503. H.
- GPT-5.5 R1: max wait ≤5s + small finite queue depth → 503. H.
- R2: both AGREE — M (eng-review fixes the 503-on-starvation shape; exact 5s/depth = tunable initial guesses).

**#41 deleted-entity snapshot semantics** `[SPLIT]` — user call
- Opus R1: soft-delete/tombstone so the client sees a `removed` event (item vanishes, not a 404); hard-delete row after replay-horizon = retention call tied to #39. M
- GPT-5.5 R1: tombstone/delete events + "client sees it vanish"; soft-delete only when an app needs readable deleted records. M
- R2: both AGREE — M (vanish-semantics consistent; tombstone-in-log vs cascade refcount interaction unproven until blob/delete cascade lands).
- *User resolved: a 'removed' NOTICE + the item VANISHES from the live view (no 404 hole); soft-delete only if an app needs deleted items readable.*

**#42 worker↔blob access path** `[FULL]`
- Opus R1: shared-disk single-process now (node:sqlite local); defer HTTP `GET /blobs/:id` to multi-process. H
- GPT-5.5 R1: shared-disk for single-process; defer HTTP for remote workers. H
- R2: both AGREE. H/H.

**#43 projection denial = load-time error** `[FULL]`
- Opus R1: load-time error (the preferred arm), never silent runtime skip; `effectSource`/`principalFrom` admission handshake. H
- GPT-5.5 R1: load-time error, not silent skip. H
- R2: both AGREE. H/H.

**#44 blob orphan handling** `[SPLIT]`
- Opus R1: refcount sweep over blob-typed columns under writeQueue mutex; content-hash dedup (refcount>1) is opt-in and complicates the count. M
- GPT-5.5 R1: defer full content-hash refcounting, implement orphan handling (pending uploads + unreferenced adopted blobs) before blob ships. H
- Opus R2: AGREE — raises M→H (eng-review marks dedup opt-in).
- GPT-5.5 R2: AGREE — M (side-table/cascade edge cases need care).

**#45 projected field bounded-projection-principal** `[FULL]`
- Opus R1: bounded-projection-principal API with load-time denial (SPEC §5.3); same admission as #12/#43. H
- GPT-5.5 R1: bounded projection principal verified at load against the target grant; denial = load-time error. H
- R2: both AGREE. H/H.

### Tier 7 — Housekeeping

**#46 inherit string→object form** `[FULL]`
- Opus R1: fix stale `inherit('Name',{via})` comments/examples → object form `inherit(Entity,{via})` (scope-sql already rejects string-form). H
- GPT-5.5 R1: update to object form; treat string-form as rejected. H
- R2: both AGREE. H/H.

**#47 event separator** `[SPLIT]` — user call
- Opus R1: adopt ONE convention (dot, matching `Note.created`) + load-time lint rejecting the other. M
- GPT-5.5 R1: dot for entity CRUD + typed handles for field/store events; remove colon-form comments. M
- R2: both AGREE — M (CRUD uses dot; field events still comment-form with no typed handle yet).
- *User resolved: DOTS everywhere + a load-time lint rejecting colon-form (pipeline.mjs parses on '.').*

**#48 http-crud `await app.ready` + 345/346** `[FULL]`
- Opus R1: accept 345 canonical; revert the stray `await app.ready` unless step 5 makes it load-bearing (then keep). H
- GPT-5.5 R1: `await app.ready` harmless; keep once step-5 builds pipelineServer in app.ready; baseline 345. H
- R2: both AGREE. H/H.

---

*Source: four task outputs (Opus Round-1, GPT-5.5 Round-1, Opus Round-2 cross-check, GPT-5.5 Round-2 cross-check). Final-resolution register in DECISIONLOG #60; summary + consensus metric in #59.*
