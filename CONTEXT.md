# CONTEXT — retired

This glossary has been absorbed into the canonical specification.

Read **[SPEC.md](SPEC.md)** — the vocabulary it defined (action/event, the
pipeline and its stages, sequence cursor, ingest, preimage, projection, snapshot,
the grant's two halves, checks, principals, field-type contracts, coordinates,
time-driven sources, computed fields) is now defined inline where each concept is
specified, principally §6 (authorization), §7 (the mutation pipeline), §8 (live
delivery), §9 (effects), and §5 (field types).

The design values live in **[AGENTS.md](AGENTS.md)**; the decisions behind the
design live in **[DECISIONLOG.md](DECISIONLOG.md)** (ADRs #1–#15).
