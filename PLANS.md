# PLANS.md — express+ backend rebuild (all 7 priorities)

Append-only ledger. The authoritative plan + council confirmation live in
`docs/reviews/eng-review-2026-06-30.md` (1085 lines). Re-read that for full
detail. This file is the restartable execution ledger.

## Starting state
- [2026-06-30] Repo: /Users/server/Code/express+. Zero-dependency (node:http/crypto/sqlite/fs only). Test: `node --test`. **321 tests, 0 fail** (lead-verified raw). TDD iron law: every decision lands RED-GREEN (DECISIONLOG #1-#58 convention).
- [2026-06-30] DECISIONLOG has 38 entries (#1-#58, mixing design ADRs + impl decisions). Recent progress (#56-58): two-phase app assembly (resolveRoutes/listen), ambient db handle + unscoped query API (findOne/findAll/findById/getOrFail/create/delete), map handle .set/.toArray with FK population, req.<entity> auto-loading, findAll predicate query builder (.sort/.limit/.select), literal-beats-parametric routing, field-level capability authz for map handles (mayFieldOp reuses the check registry — no second auth path).
- [2026-06-30] Despite that progress, the eng-review confirmed the two load-bearing gaps still stand: (A) `pipeline.mjs` Action/Event kernel is LATENT — `serve.mjs dispatch()` does raw SQL INSERT/UPDATE/DELETE + `live.emit()` directly (a second mutation path); (B) `live.mjs` keeps its own per-scope seq Map (live.mjs:199) independent of pipeline.mjs's (pipeline.mjs:58) — scope's "two seq counters" debt. Priority #1 unifies both.

## Council-confirmed plan (2026-06-30)
- [2026-06-30] plan-eng-review wrote docs/reviews/eng-review-2026-06-30.md (architect subagent). council-review (DS Pro + GLM-5.2 + DS Flash, judged by Opus 4.8) confirmed D1-D11 architecture UNCHANGED, added 22 Authoritative Specifications (4 tiers) — see eng-review §8. No proven hard-rule violation. Modified winner (Response B / GLM-5.2, absorbing Response C's snapshot-to-subscribe catch-up gap).

## Fork resolutions (2026-06-30, lead + user decided)
- [2026-06-30] **Fork A (spec #3) → Entity-as-projection.** The materialized entity row is a PROJECTION of the committed event log: rows are derived, never directly written. Handlers EMIT EVENTS ONLY; the entity table is a projection consumer that folds committed events into rows. Retires the dual-write, satisfies one-reconciliation-path most cleanly (council preferred). Consequence: spec #6 handler signature emits events only (no `{db}` row-writing) — the projection consumer writes the row. CRUD auto-gen reducers fold into rows.
- [2026-06-30] **Fork B (spec #7) → Auto-gen CRUD action types per entity.** `entity('Note',{...})` auto-generates `Note.create/.update/.remove` action+event types at compile time with a generic append-to-row reducer. Each CRUD verb is a real pipeline action dispatched through the same `handlers[type]` lookup as custom actions. Most uniform; CRUD and custom actions identical to the kernel.
- [2026-06-30] **Fork C (spec #8) → Authorize-first.** Authorize BEFORE dedupe. A retried action by a since-revoked principal returns 403 (mutation already happened but retry refused). Matches eng-review Walk 1. REVERSES current kernel (pipeline.mjs:70-78 checks dedupe before authorize) — flag the flip.

## The 22 Authoritative Specifications ( REQUIRED before/during build)
See eng-review §8.1. Tier 1 (silent-data-loss/hard-rule, before build): #1 subscribe carries `since` + server replays Log WHERE seq>since; #2 snapshot reads row+Cursor.lastSeq in ONE txn; #3 entity-as-projection (resolved Fork A); #4 projection-principal write re-enters server.dispatch (→event→Log), NEVER direct UPDATE; #5 in-txn post-handler row-grant hook in kernel (mayVerb async, needs post-handler row, runs in txn, rolls back on deny; bindReadScope pure outside txn). Tier 2 (blocking impl): #6 handler signature; #7 CRUD→kernel mapping (resolved Fork B); #8 dedupe-vs-authorize order (resolved Fork C); #9 migrations startup pre-traffic stop-the-world. Tier 3 (correctness edges): #10 reaper takes writeQueue mutex; #11 live fan-out strictly post-commit (no socket I/O in txn); #12 bound bootstrap gate; #13 re-auth = stale captured row (land NO cache first); #14 projection idempotency independent of log dedupe (cursor atomic w/ derived write OR replay-tolerant; job-enqueue keys (consumerId,eventId); dedupe key=(scope,seq)); #15 handler txn discipline (no BEGIN/COMMIT/SAVEPOINT). Tier 4 (state, may defer mechanism): #16 writeQueue bounds (≤~5s→503); #17 deleted-entity snapshot semantics; #18 worker↔blob access path; #19 projection denial = load-time error; #20 Log table schema (scope,seq,eventType,eventData,actionId,committedAt), PK (scope,seq), index actionId; #21 blob ref-count deferral; #22 job-queue state is native persistence not log-derived.

## Execution order (7 priorities)
- P1 Durable event log + persistent seq cursors + snapshot/bootstrap endpoint + gap-resync/hard-fail + UNIFY serve.mjs CRUD onto kernel (retire raw SQL) + collapse live's 2nd seq. [in_progress]
- P2 Blob field + disk BlobStore (pending→adopt, hashing, reaper) + in-txn adopt with commit.
- P3 Projection fan-out registry over committed log (live=sync consumer; jobs/webhooks/fts=async consumers; entity-as-projection IS a consumer).
- P4 Operational defaults (rate limiting, CSRF/CSP/HSTS, request logging+metrics, env gate, shutdown registry, write-queue, migrations).
- P5 Background job-queue substrate (claim/heartbeat/result/reaper) — separate seam, projections enqueue into it.
- P6 Phase-2 field + live + effects machinery (crdt/list/log/presence/state, field-keyed interest+since+pace, effects grammar).
- P7 Client library (LiveChannel/LiveList) + framework-derived endpoint URLs + structural bootstrap ordering.

## Review gates (per priority)
- architecture-lint + reviewer (different model) per priority; cso on auth/blob/transport (P1 live auth, P2 blob, P4 CSRF/rate-limit).

## Progress
- [2026-06-30] Planning complete. Council confirmed. Forks resolved. 321 tests green. Ready to dispatch P1 implementer.
- [2026-06-30 15:00] P1 implementer started. Baseline suite: 321/0. Mandatory reads complete (PLANS, eng-review, AGENTS, DECISIONLOG #40-#58, all 10 source files). Starting RED-GREEN cycle: Step 1 — framework DDL for Log + Cursor tables.
- [2026-06-30 15:10] Steps 1-2 GREEN (327/0): generateFrameworkDDL/executeFrameworkDDL exported from ddl.mjs; app.ddl() auto-creates framework tables (Log, Cursor) alongside entity tables. 6 new tests in test/durable-log.test.mjs.
- [2026-06-30 15:20] Step 3a GREEN (334/0): durable createServer with db engagement. Durable dispatch appends to _Log/_Cursor; authorize-first (Fork C); dedupe by actionId survives restart. Ephemeral path preserved (no db). 7 new tests in test/pipeline-durable.test.mjs.
- [2026-06-30 15:25] Step 4a GREEN (338/0): auto-gen CRUD types per entity (Fork B). entity.mjs produces entity.verbs with create/created/update/updated/remove/removed action+event types. Generic reducers: created/updated fold data into state; removed marks _removed. 4 new tests in test/entity-crud-types.test.mjs.
- [2026-06-30 15:40] Step 4b GREEN (343/0): entity-as-projection (Fork A). entity.mjs exposes .projection consumer (eventTypes + apply that writes entity rows inside txn via serializeField). entity.mjs also exposes .generateDDL(). pipeline.mjs createServer accepts projections[] and runs them inside the write txn after events are logged but before commit — a projection failure rolls back the whole txn (atomic: log event + projected row). Round-trip test proves row == reducer fold. 5 new tests in test/entity-projection.test.mjs.
- [2026-06-30 15:50] Step 4c GREEN (345/0): in-txn post-handler row-grant hook (spec #5). createServer accepts `postHandlerAuthorize` — runs after projections inside txn, async-aware, rolls back on deny (returns {granted:false} without throwing). Exposed `record.insert(cells)` as trusted seeding path (bypasses readonly validation — projection consumers + trusted callers use it). 2 new tests in test/row-grant-hook.test.mjs. Two bugs fixed from resume: (1) test seeding used Note.create() which rejects readonly fields — switched to Note.insert(); (2) pipeline.mjs catch block now catches err.status===403 from postHandlerAuthorize and returns {granted:false} gracefully instead of re-throwing.</｜DSML｜parameter>

## Decision log / surprises / discoveries
See DECISIONLOG.md — the source of truth for conventions and decisions
(#56-58 show the patterns the P1 implementer must follow: ambient db,
two-phase resolveRoutes, mayFieldOp reuses the check registry, renderError
numeric-status = deliberate client error). Forks A/B/C are in §Fork
resolutions above; the starting repo state is in §Starting state.

## [2026-06-30 update] P1 step 5 — broke suite, restored to green
- [2026-06-30] Steps 1-4c GREEN at 345/0 (framework DDL, durable createServer, verbs, projection, post-handler hook). Step 5 (serve.mjs migration) was attempted by the general-prog agent and LEFT THE SUITE HANGING: it added `entity.crudHandlers` + an `app.ready` IIFE that builds `app.pipelineServer` + a `create` block with a broken raw-`body` fallback path. Lead diagnosed: durable `createServer` is pure construction (no I/O), so the hang was NOT at construction — it was the broken fallback (500s on create) + tests not awaiting `app.ready` + the httpServer.listening wait inside the IIFE.
- [2026-06-30] RESTORE: lead removed `crudHandlers` from src/entity.mjs (kept the good 4a/4b/4c additions: verbs, insert, generateDDL, projection) and `git checkout src/serve.mjs` reverted serve.mjs to HEAD (3740c6e). Suite restored to 345/0 (lead-verified raw). The 345-green P1 foundation is uncommitted in the working tree.
- [2026-06-30 16:00] RECOVERY: botched `git checkout src/ddl.mjs src/entity.mjs` reverted those two files to HEAD (3740c6e), destroying uncommitted P1 work. Restored from TDD spec: added `generateFrameworkDDL` + `executeFrameworkDDL` to ddl.mjs (Log/Cursor CREATE TABLE); added `record.verbs`, `record.projection`, `record.insert`, `record.generateDDL`, `record.crudHandlers` to entity.mjs. Suite restored to 345/0 (lead-verified raw). src/pipeline.mjs, src/app.mjs, src/index.mjs confirmed intact — NO changes needed.
- [2026-06-30] NEXT: re-attempt step 5 with a de-risked approach. The migration must (1) delete the broken raw-SQL fallback entirely — after migration there is NO raw SQL create/update/remove, all CRUD routes through `app.pipelineServer.dispatch`; (2) construct the pipeline server in `app.ready` (already designed, construction is pure); (3) ensure http-crud tests await `app.ready` before making requests so the pipeline server exists; (4) flip test/pipeline-integration.test.mjs "coexistence" assertion to unified-path. Remaining P1 steps: 6 collapse live 2nd seq, 7 post-commit fan-out, 8 snapshot/bootstrap+subscribe-since+resync/hard-fail, 9 writeQueue, 10 txn-scoped db wrapper.

## Progress (continued)
- [2026-06-30] RECONSTRUCTION COMPLETE (lead botched `git checkout src/ddl.mjs src/entity.mjs` reverted uncommitted P1 steps 1-4c; general-prog subagent ses_0eabfcc03ffeY7o1GTkc448FFp reconstructed the lost src from the 5 P1 test files). Suite restored to 345/0 green (lead-verified raw). Files modified (uncommitted): src/ddl.mjs (generateFrameworkDDL/executeFrameworkDDL), src/entity.mjs (verbs/projection/insert/generateDDL/crudHandlers). src/pipeline.mjs, src/app.mjs, src/index.mjs intact (P1 work preserved). src/serve.mjs at HEAD 3740c6e (raw-SQL dispatch — crudHandlers is currently DEAD CODE). test/http-crud.test.mjs still has agent's `await app.ready` change (harmless). Baseline = 345 (NOT 346 — the 346 was a transient +1 from an agent's extra test that got lost in the revert; 345 is the true P1 step-4c state).

## Discoveries (continued)
- [2026-06-30] ID-MODEL BLOCKER for step 5: crudHandlers mint `randomUUID()` (string) but entity DDL is still `id INTEGER PRIMARY KEY` → SQLite rejects a string PK. The TEXT-UUID id-model migration (ddl.mjs `id TEXT PRIMARY KEY` + entity.mjs insert() mint-UUID-if-absent + SELECT-by-id + ~20 test seed files `INTEGER PRIMARY KEY`→`TEXT PRIMARY KEY` + numeric seed ids → string ids) MUST land BEFORE or WITH P1 step 5 (serve.mjs routes CRUD through crudHandlers). User already chose TEXT-UUID caller-owned ids (the scope-validated pattern). This migration is the next concrete step.

## Decision Log (continued)
- [2026-06-30] Id model → TEXT UUID caller-owned ids (user decision). The durable kernel needs the id knowable BEFORE the row is written (handler emits event carrying id; projection writes row from event). INTEGER PK + lastInsertRowid is incompatible. Blast radius: ~20 test seed files. Accepted.

## Progress (step 5)
- [2026-06-30] P1 STEP 5 DONE — serve.mjs CRUD routes through the durable kernel (raw INSERT/UPDATE/DELETE retired). 347/0 green, suite exits clean (1.2s). LEAD-led (the riskiest step, 4 prior subagent failures).
  - src/serve.mjs: `buildKernel(app)` (called in app.ready) merges every mounted entity's `crudHandlers` (handlers) + `projection` (projections) into one `createServer({handlers, projections, authorize:()=>true, db})` (`authorize` passthrough — route gate is layer 1; bindReadScope+mayVerb stay in dispatch as layer 2). create/update/remove verbs now call `await kernel.dispatch({actionId, type:'${name}.${verb}', payload, principal})` and read back the raw row (post-commit) → 201/200/204 + live.emit (live.mjs unchanged — seq-collapse is step 6). Dead `serializeRow`/`ownerFieldOf`/`serializeField`/`validateMutation` removed from serve.mjs.
  - src/entity.mjs crudHandler.update FIXED: peel `id` out before `validateMutation` (id is the synthetic PK, not a declared field — validateMutation rejected it: "Note.id is not a declared field"; the original crudHandler.update was broken/untested). + bump `touch` fields into event.data (parity with old serve.mjs touch logic; projection serializes the Date). create/remove unchanged (create mints its own id; remove doesn't validate).
  - Framework-tables default: buildKernel ensures `_Log`/`_Cursor` exist via `executeFrameworkDDL` (idempotent, guarded `typeof app.db.exec === 'function'` — stub-db error-renderer tests skip it). Existing HTTP tests seed only entity tables; the kernel now auto-ensures its infra.
  - RED test: test/serve-durable-migration.test.mjs (2 cases: HTTP create/update/remove append to _Log; denied/404 mutation appends nothing).
  - KEY LEARNING (load-bearing): a hang = a failing test left an HTTP server open (assertion threw before t.after close). The migration initially broke http-crud (3 fail) + hung doc-share-e2e/field-authz/live-sync-e2e — root cause was bare-seed test dbs lacking _Log/_Cursor (kernel INSERT threw → 500 → assertion → open server → hang). Fixed by the framework-tables default.

## Progress (continued)
- [2026-06-30] P1 STEP 6 DONE — collapsed live.mjs's 2nd per-scope seq onto the kernel's (D2) + retired live's dispatchedActions dedupe onto the kernel's single dedupe (D3). live.emit(entity,id,row,committedEvent) now FORWARDS the kernel's committed event (type/scope/seq/data/actionId/committedAt) — one event shape for both live delivery and future subscribe-since replay (no second payload construction). Dropped live's `sequences` Map + `dispatchedActions` Map (genuine subtract: two Maps + the {verb,row} construction gone). serve.mjs: 3 emit call sites now pass `result.events[0]` + skip when `result.deduped` (`if (live && !result.deduped)`). Tests: live-sync-e2e assertions `.event.verb`→`.event.type` ('Note.updated'/'Note.removed'); added distinguishing RED→GREEN test 'live event carries the kernel committed seq, not a local counter' (no-sub mutation bumps kernel cursor→1; subscribe; mutate→live event seq=2 not 1, proving live reads kernel seq); removed live-sync.test.mjs lines 241-290 (2 SIMULATION tests of the old local-seq/dedupe model — never exercised real emit, asserted a now-removed model; kernel seq/dedupe covered by durable-log + new e2e). Gate: 346/0 green, suite exits clean. src/{live,serve}.mjs + test/{live-sync,live-sync-e2e}.test.mjs.

## Discoveries (continued)
- [2026-06-30] live-sync.test.mjs tests 1-6 (subscription registry + re-auth MAYVERB signature) are STANDALONE simulations (don't import live.mjs) — they document the mechanism, not the seq model; kept. Only the seq/dedupe sims (7-8) were model-coupled → removed.
- [2026-06-30] The dedupe-skip (`!result.deduped`) can't be triggered via normal HTTP — serve mints a fresh randomUUID actionId per request (serve.mjs:191). Dedupe fires on retried requests with the SAME actionId (idempotency key), which the HTTP layer doesn't expose yet. The kernel dedupe itself is tested in durable-log; serve's skip is a 1-line defensive guard.

## NEXT: P1 step 7 — snapshot/bootstrap + subscribe-since + hard-fail (D6/D7)
`GET /snapshot/:scope` returns materialized row + Cursor.lastSeq (authorized). On subscribe, server reports currentSeq; if client cursor < currentSeq, replay the gap from _Log; if cursor < oldest retained seq → HARD-FAIL {resync:'stale'} (forced re-bootstrap, never silent truncate). Then step 8 (writeQueue single-writer mutex), step 9 (txn-scoped db wrapper spec #6), step 10 (post-commit fan-out registry — live already a consumer). Then P2-P7.

## Progress (continued)
- [2026-06-30] P1 STEP 7 DONE — snapshot endpoint + subscribe-since replay + hard-fail (D6/D7). LEAD-led. 352/0 green, suite exits clean (1.25s). NEW: `GET /snapshot/:entity/:id` returns {snapshot: row, seq: Cursor.lastSeq}; `GET /events-since/:entity/:id?cursor=N` returns committed events >N in seq order; HARD-FAIL `{resync:'stale', reason:'cursor-behind-retention'}` when cursor+1 < oldest retained seq (never a silent truncate — the single non-negotiable correctness property, eng-review §3.6). Subscribe now reports `currentSeq` (kernel _Cursor per-scope) — closes the snapshot-vs-live race. Both endpoints intercepted in makeRequestHandler (framework defaults, like /events WS — NOT mounted routes), authorized via the SAME mayVerb('read') engine + readScope (viewer bar) — no second auth path. buildKernel builds `app.entities` (Map name→entity) for path-param entity resolution. createClient.ingest already folds gap→resync / duplicate→skip / next→fold (unit-tested in snapshot-resync). FIXED latent bug: `entity.insert` crashed on a caller-supplied `id` (serializeField read `.kind` of undefined descriptor) — now skips undeclared keys (id set explicitly above).
- Files: src/serve.mjs (handleResyncRoute + authorizeRead + snapshotRoute + eventsSinceRoute + buildKernel entities + live db wiring), src/live.mjs (db option + currentSeq closure + subscribed ack), src/entity.mjs (insert undeclared-key skip). Tests: test/snapshot-resync.test.mjs (5, new), test/live-sync-e2e.test.mjs (+1 subscribe-currentSeq, +nextMessage harness).

## Discoveries (continued)
- [2026-06-30] Framework endpoints (snapshot/events-since) are INTERCEPTED in makeRequestHandler before matchRoute (like static-file + /events WS) — NOT pushed into the route table. app.mjs owns the route table (the single source of routes); serve.mjs only serves it. Interception of framework transport endpoints (static/WS/snapshot) is the established pattern; entity/imperative routes stay in the table. This keeps the layering clean.
- [2026-06-30] `app.entities` (Map name→entity) is built in buildKernel (app.ready), so it's only available AFTER listen+ready. Tests that seed via `app.entities.get('Note').insert(...)` must await app.ready first. ddl() alone does NOT build app.entities.

## Decision Log (continued)
- [2026-06-30] events-since on a deleted/out-of-scope row → 404 (fail closed). events-since authorizes against the CURRENT materialized row (same as snapshot). A deleted scope's resync edge (no current row) is 404 → client re-bootstraps → snapshot 404 → client shows gone. Accepted for now (the rare deleted-row-resync edge); the load-bearing property is the stale-cursor HARD-FAIL, which is tested.
- [2026-06-30] LiveList.open structural ordering (bootstrap-then-stream, no public open-WS-first API) DEFERRED to P7 (client library). createClient.bootstrap+ingest already enforce ordering at the client-reducer level (ingest before bootstrap signals resync). P7 adds the LiveList/LiveChannel wrapper that structurally forbids WS-before-snapshot.

## Progress (continued)
- [2026-06-30] P1 COMPLETE — 352/0 green, suite exits clean (1.39s). All 6 P1 execution-order goals done: (1) durable event log ✓ [steps 1-2 framework DDL], (2) persistent seq cursors ✓ [_Cursor], (3) snapshot/bootstrap endpoint ✓ [step 7 /snapshot/:entity/:id], (4) gap-resync/hard-fail ✓ [step 7 events-since + stale-cursor HARD-FAIL], (5) serve.mjs CRUD unified onto kernel (raw SQL retired) ✓ [step 5], (6) live's 2nd seq collapsed ✓ [step 6].
REMAINING-P1-NOTES resolved/deferred: spec #6 (handler txn signature) RESOLVED by Fork A (handlers emit events only, no db; projections write in-txn and see own writes via the passed db handle). writeQueue (D9) → P4 (operational defaults; not needed for correctness today: node:sqlite is sync, the HTTP path's BEGIN→COMMIT is atomic w/ no in-txn await since postHandlerAuthorize is omitted on the HTTP path). post-commit fan-out registry (D3) → P3 (live already a post-commit consumer). txn-scoped db wrapper → not needed (spec #6 resolved above).

## Progress (P2 — BlobStore)
- [2026-06-30] P2 BLOBSTORE DONE — 364/0 green, suite exits clean (1.2s). Added BlobStore table to generateFrameworkDDL + src/blob-store.mjs (isolated module, 8 methods: safeId, upload, adopt, finalize, readRange, reap, stat, pathFor) + test/blob-store.test.mjs (6 tests: upload one-pass hash, path-containment guard, range read, adopt txn semantics, reaper orphan sweep, reaper refcount sweep). Upload streams to .pending computing md5+sha256 in ONE pass (crypto hashes fed same bytes, no double read). Adopt runs in CALLER'S txn (no implicit commit). Finalize renames .pending→final out-of-band (post-commit). Reaper: (1) reconcile adopted-with-.pending, (2) orphan sweep (stale pending), (3) refcount sweep (adopted with no entity references). Zero deps (node:fs/crypto/path/sqlite only). Files: src/ddl.mjs (+BlobStore), src/blob-store.mjs (new), test/blob-store.test.mjs (new).

## NEXT: P2 continued — Blob field wiring (entity integration)

## [2026-06-30] cso security audit — P1 auth surface (lead-dispatched GPT-5.5, lead-verified)
- [2026-06-30] Lead dispatched `/cso` (OWASP Top 10 + STRIDE) over the auth surface to `openai-gpt-5.5-expensive` (cross-family reviewer; the author surface is GLM-family). Full report: `docs/reviews/cso-2026-06-30.md`. Lead INDEPENDENTLY re-read source for every Critical/High — ALL CONFIRMED REAL (one correction noted). The auth MODEL is sound (bound params, NULL→equality fail-closed, salted scrypt, closed principal union, route-gate requireUser, snapshot/events-since reuses authorizeRead). The bugs are WIRING, all in the UNCOMMITTED P1 (serve/live/pipeline) tree.
- KEY INSIGHT: suite is green (358/0) BECAUSE C1's bypass makes live delivery "work" by skipping authz — live e2e tests assert delivery, never denial. Green hid the bug.
- FINDINGS (lead-verified, file:line): C1 CRITICAL live delivery auth bypass — `src/live.mjs:224` awaits-less `mayVerb(...)` (Promise truthy → `if(!allowed)` never fires → every subscriber gets events). Same path: `serve.mjs:772-776` doesn't pass `principalOf` to `createLiveServer` (WS→anonymous), and emit passes entity NAME string not RECORD. Fix await alone → all-deny; all three land together. RED test must assert UNAUTHORIZED subscriber is denied.
- C2 CRITICAL (latent) `list` skips mayVerb — `serve.mjs:164-167` applies only SQL readScope, not `mayVerb('read')` (unlike read at :203). Latent (no exemplar admits-then-denies-read). Fix: post-filter list via `mayVerb(entity,'list',row,principal)`.
- C3 CRITICAL (latent) `create` has NO row-grant authz — `serve.mjs:730` buildKernel wires `authorize:()=>true` + does NOT wire `postHandlerAuthorize` (the in-txn post-projection hook, spec #5, BUILT+benched in pipeline.mjs:205-223 but unwired). `result.granted` (:220) can only be true → create never 403s. CREATE-ONLY (lead correction: GPT lumped update/remove — VERIFIED FALSE; update :237 + remove :266 DO pre-check mayVerb). Latent for exemplars (doc.mjs .can grants write to owner=creator). Fix: wire postHandlerAuthorize; fix fragile verb-derivation `pipeline.mjs:210` `replace('d','')`→explicit map.
- H1 HIGH autoLoad bypasses readScope — `serve.mjs:447` uses UNSCOPED `findById` (trusted-query API) for HTTP imperative child routes → `req.<entity>` = raw row even when principal can't read it (the AGENTS.md-flagged "trusted-query-API bypass reachable from untrusted request"). Fix: route autoLoad through the same authorizeRead path as snapshot/events-since.
- H2 HIGH WS subscribe-before-authz — `live.mjs:139-145` stores sub + sends currentSeq with NO authz (activity oracle + unbounded-sub DoS). Fix: resolve entity from app.entities, bindReadScope+`await mayVerb('subscribe')` BEFORE addSubscription/currentSeq; bound per-conn sub count; deny→403/404 never currentSeq.
- M1 malformed Cookie decodeURIComponent throws pre-try → 500 (`session.mjs:23-34`); M2 sessionCookie secure:false no prod guard (`:37-45`); M3 no HSTS/CSP default (middleware.mjs:8-18, defer P4); L1 no auth-event audit logging (backlog).
- VERDICT: BLOCKED before commit of the P1 cutover. C1 = clearest live exploit; C2/C3/H1/H2 = one-auth-path violations. All in uncommitted P1 tree → fix BEFORE this work commits.

## Fix step (blocking, pre-commit) — P1 authz wiring
- [ ] C1: live `await mayVerb` + pass `principalOf` to createLiveServer + pass entity RECORD to live.emit. RED test asserting UNAUTHORIZED subscriber denied (the test gap). GREEN.
- [ ] C3: wire `postHandlerAuthorize` in buildKernel (resolve entity, in-txn load projected row, `mayVerb(entity,verb,row,principal)`, deny→roll back → existing `!result.granted`→403); `pipeline.mjs:210` verb map.
- [ ] H1: route autoLoad through authorizeRead (factor authorizeRead into a shared internal helper).
- [ ] H2: subscribe-authz + per-connection subscription bound + id length bound; deny returns 403/404, never currentSeq.
- [ ] M1 (parseCookies try/catch) + M2 (prod Secure guard) land with the above. M3/L1 → P4.
- GATE: after fixes, lead re-runs `node --test` raw (expect fail 0) + adds denial-coverage tests; then commit.

## Review gate (added 2026-06-30) — pre-commit council-review of the step-5 cutover [A]
- [2026-06-30] Before committing the uncommitted P1 serve.mjs CRUD cutover (the riskiest change in the tree: hard cutover, NO raw-SQL fallback, `app.pipelineServer = buildKernel(app)` in `app.ready`), run `/council-review` on the cutover DESIGN: (1) is `authorize:()=>true` + (post-fix) wired `postHandlerAuthorize` the right layer split, or should create authorize differently? (2) is the `pipeline.mjs:210` verb-derivation acceptable as an explicit map, or a smell? (3) validate the post-cso-fix state. council-review = DS Pro + GLM-5.2 + DS Flash scanning, Opus synthesis (credit-efficient). Run by any Task-capable plan agent; BLOCKS commit of the step-5 cutover until resolved. Runs AFTER the cso fix-step above (review the fixed state, not the buggy state).

## Progress (P2)
- [2026-06-30] P2 part 1 — BlobStore module (general-prog subagent ses_0ea4a55d1ffexFRmmLCWUYInAM, ISOLATED, bounded TDD). src/blob-store.mjs (new, 175 lines): `createBlobStore({root,db})` → `{safeId, upload, adopt, finalize, readRange, reap, stat, pathFor}`. Two-phase: upload→`<id>.pending` (md5+sha256 ONE pass)+row(status=pending); `adopt(db,id)` UPDATE→adopted IN caller's txn (no own commit — atomic with the dispatch); `finalize(id)` post-commit FS rename .pending→final; `reap({ttl,blobColumns})` reconcile (adopted+.pending→rename) + orphan sweep (stale pending) + refcount sweep (adopted+unreferenced→delete). BlobStore table+idx added to `generateFrameworkDDL`. test/blob-store.test.mjs (6). 364/0 green. Module imports ONLY node:crypto/fs/path — fully decoupled (no kernel/entity/serve imports); `reap` takes `blobColumns:[{table,column}]` as config.
- [2026-06-30] P2 part 2 — kernel blobAdopter hook + atomicity (LEAD-led). src/pipeline.mjs `createServer` gains additive `blobAdopter: {resolve(ev)->[ids], adopt(db,ids), finalize(id)}` option. In-txn adopt placed AFTER projections, BEFORE postHandlerAuthorize — so a `.can` denial → ROLLBACK undoes the adopt → blob stays pending (spec #2 atomicity). Post-commit finalize (best-effort FS rename) AFTER COMMIT, skipped on rollback; crash gap reconciled by reaper. test/blob-atomicity.test.mjs (2): denied dispatch → blob stays pending + .pending file (no final); granted → adopted + final file. 366/0 green, clean exit.
- NEXT (P2 part 3): `POST /blobs` upload route (framework-intercepted in serve.mjs like /snapshot) → store.upload → {id,md5,sha256,size} 201; buildKernel auto-wires blobAdopter from app.entities' blob fields + `app.blobs` BlobStore; a blob field KIND marker (descriptor) so buildKernel knows which fields to resolve + refcount (blobColumns). Then P3 (fan-out registry), P4 (operational defaults incl writeQueue D9), P5 (job-queue), P6 (phase-2 fields/live/effects), P7 (client library).

## Discoveries (P2)
- [2026-06-30] git status shows MORE modified files than prior summaries enumerated (src/registry.mjs +18, scope-sql.mjs +38, session.mjs +8, row-grant.mjs +2, index.mjs +3 + many test files: auth-edges, blog-acceptance, demo-smoke, framework-entities, hash-field, inherit, link-field, link-principal(untracked), session, scope-sql). VERIFIED these are EARLIER-session P1/id-migration work (registry/scope-sql/row-grant = framework additions from steps 1-4c; test files = id-migration's INTEGER PK→TEXT sweep), NOT the blob-store subagent's footprint — grep confirms ZERO blob refs in registry/row-grant/scope-sql/session; blob-store.mjs imports only node:crypto/fs/path. So the cumulative uncommitted tree = all P1 (steps 1-7) + id-migration + P2 (parts 1-2). No commits since 3740c6e.

## Progress (P2 part 3)
- [2026-06-30] P2 part 3 — user-facing blob upload + blob-field wiring (LEAD-led). 369/0 green, clean exit (1.3s).
  - src/field.mjs: added `blob()` constructor → `{ kind:'value', type:'text', blob:true }` (stored/validated as ordinary text id; the `blob:true` marker is what the adopter reads). Re-exported from index.mjs.
  - src/app.mjs: `expressPlus({db, blobs:{root}})` constructs `app.blobs = createBlobStore({root, db})` when db engaged; default root `path.join(cwd,'.blobs')` (durable across restart) + mkdirSync recursive.
  - src/serve.mjs: `readRawBody(req,limit)` (binary, 413 cap) + `handleBlobUploadRoute` (POST /blobs; anonymous→401; upload→201 {id,md5,sha256,size,mime}; BLOB_LIMIT 50mb). Intercepted in makeRequestHandler (POST block, before matchRoute, like the GET resync block). `buildKernel` auto-wires `blobAdopter` from declared blob fields (resolve by entity-name prefix of `ev.type`; adopt=app.blobs.adopt; finalize=app.blobs.finalize) + sets `app.blobColumns` (the (table,column) scan) so the reaper's refcount sweep is derivable from the same field declarations. `createServer` gets `blobAdopter`.
  - test/blob-upload.test.mjs (3): POST /blobs pending+one-pass hashes; anonymous→401; a create referencing the blob → adopted (stat) + finalized (final file, .pending gone). The rollback contract lives in blob-atomicity.test.mjs — the HTTP kernel omits postHandlerAuthorize, so a dispatched create always commits; not re-tested here.
- NEXT: P3 (post-commit fan-out registry — formalize live as a sync consumer + jobs/webhooks as async; eng-review §D3), P4 (operational defaults incl writeQueue D9 + middleware stack), P5 (job-queue substrate), P6 (phase-2 fields/live/effects), P7 (client library LiveChannel/LiveList + LiveList.open structural ordering).

## Discoveries (P2 part 3)
- [2026-06-30] The HTTP app.kernel (buildKernel) creates `createServer({authorize:()=>true, db, blobAdopter})` with NO postHandlerAuthorize — a dispatched create always commits (granted:true). So the blob-rollback contract (denied dispatch → blob stays pending) is only reachable via a CUSTOM kernel with postHandlerAuthorize (covered by test/blob-atomicity.test.mjs). The HTTP-integration test only asserts the HAPPY path (create adopts+finalizes). This is correct: the row-grant .can denial for create is omitted by design (create has no .can, today), so no HTTP path denies a commit.
- [2026-06-30] `sqlType` (ddl.mjs:25-36): value/text → TEXT (default case). So `blob()` (= value/text/blob:true) needs NO ddl change. marker-only field.
- [2026-06-30] POST /blobs intercepts in makeRequestHandler BEFORE matchRoute (framework default, not a mounted route) — same pattern as /snapshot, /events-since, /events WS. Layering: app.mjs owns the route table; serve.mjs serves framework-transport endpoints by interception.

## Progress (P3)
- [2026-06-30] P3 — post-commit fan-out registry (eng-review §D3). 372/0 green, clean exit (1.2s). LEAD-led.
  - src/pipeline.mjs: `createServer` gains `postCommitConsumers: []` option. After `db.exec('COMMIT')`, a uniform loop fires each consumer `(events, {db, actionId})`, awaited, errors CAUGHT (a post-commit fan-out failure never undoes the committed dispatch — it runs out-of-transaction; AGENTS.md: out-of-band effects are post-commit projection consumers, never a new effect primitive). Former inline blob-finalize loop RETIRED into this loop (the special-case → a registered consumer). Dead `adoptedBlobIds` removed; in-txn `blobAdopter.adopt` stays (atomic with the event — a projection, not fan-out).
  - src/serve.mjs buildKernel: registers TWO post-commit consumers — (1) blob-finalize (re-derives ids via `blobAdopter.resolve` + `blobAdopter.finalize`, reaper reconciles crashes); (2) live fan-out (re-reads the materialized row post-commit by parsing `ev.scope` = `entity:id`, calls `app.live.emit`). The 3 imperative `live.emit` call sites in `dispatch` (create/update/remove) REMOVED + `const live` — declaration absorbs the wiring; the kernel is now the single fan-out point. Dedupe-skip is automatic: a deduped dispatch doesn't COMMIT (replays stored events) → consumers don't re-fire.
  - src/live.mjs `emit`: re-authz now SKIPS when `row === undefined`. For 'removed' events the row is gone post-commit (re-read returns undefined); the remove event forwards to every current subscriber (it IS the revocation signal). For 'created'/'updated' the re-read row drives `mayVerb('subscribe',...)` as before. Header comment updated (the kernel's post-commit loop calls emit, not "the mutation path").
  - test/post-commit-fanout.test.mjs (NEW, 3): tap consumer receives committed events; deduped dispatch fires no consumers (commit is the fan-out boundary); a throwing consumer doesn't roll back the origin or block later consumers.
  - test/blob-atomicity.test.mjs: both kernels now register a `blobFinalizeFor(adopter)` post-commit consumer (inline finalize retired → a fixture must wire the consumer). Denied path: no COMMIT → consumer skipped → finalize correctly skipped.
- NEXT: P4 (operational defaults — middleware stack: rate-limit/CSRF/CSP/HSTS/req-logging/metrics/env-gate/shutdown-registry; writeQueue D9 now genuinely needed when async in-txn auth lands; migrations startup pre-traffic stop-the-world spec #9), P5 (job-queue substrate claim/heartbeat/result/reaper — a post-commit consumer enqueues), P6 (phase-2 fields/live/effects), P7 (client library).

## Discoveries (P3)
- [2026-06-30] Boot order (serve.mjs `listen`): `app.live = createLiveServer(...)` runs SYNCHRONOUSLY at line ~772, BEFORE `app.ready` (an async IIFE assigned ~line 780) which calls `buildKernel(app)` at ~line 782. So `app.live` IS available inside buildKernel — the live consumer captures it. Resolved the concern that live wouldn't exist at buildKernel time.
- [2026-06-30] live-sync.test.mjs sims (tests 1-7) DON'T import `live.emit` — they reimplement the registry + fan-out INLINE with stub conns + a stub mayVerb. So changing `live.mjs`'s emit (row-undefined-skip-reauthz) is SAFE for those sims. The only callers of live.emit are serve.mjs (now removed) + the new buildKernel live consumer.
- [2026-06-30] live-sync-e2e tests pass with the move: PATCH → consumer re-reads row (exists, owner matches) → mayVerb passes → forward update; DELETE → consumer re-reads undefined → skip re-authz → forward remove to subscribers. The "denied subscriber" sims are model-independent (inline). No test asserts "denied subscriber is denied the REMOVE event specifically" (the only behavior change: removed events always forward).
- [2026-06-30] The HTTP app.kernel (buildKernel) omits postHandlerAuthorize (create has no `.can`, today) → a dispatched create always commits → the blob-rollback + post-commit-consumer-skip-on-rollback contracts are testable only via a CUSTOM kernel (blob-atomicity test). The HTTP path exercises only the HAPPY post-commit fan-out (live + blob finalize run on commit).

## Progress (P4 — paused)
- [2026-06-30] P4 isolated modules BUILT + VERIFIED (lead-led dispatch, 2 parallel general-prog subagents):
  - `src/write-queue.mjs` + `test/write-queue.test.mjs` (9 tests). `createWriteQueue({maxDepth,maxWaitMs,now})` — single-writer async mutex; bounded wait→503; bounded depth→503. ISOLATED (zero internal imports). Passes 9/9 in isolation.
  - `src/rate-limit.mjs` + `test/rate-limit.test.mjs` (8 tests). `createRateLimiter({ip,session,now})` — fixed-window, per-IP + per-session (stricter wins), stale-bucket pruning. ISOLATED. Passes 8/8 in isolation.
  - Both currently SHELVED in `/tmp/p4hold` (kept out of the tree) to avoid a test-ordering flake (`doc.mjs + comment.mjs: parent-child FK → 403`) while the concurrent session is mid-flight.
- [2026-06-30] P4 BLOCKER surfaced → RESOLVED (user): concurrent work in the tree that the lead did NOT produce — `docs/reviews/cso-2026-06-30.md` (OWASP+STRIDE audit by lead-dispatched openai-gpt-5.5-expensive), `test/create-deny.test.mjs` (C3: make create run the row-grant — the exact create-grant gap the lead noted+deferred in P3/b7-b8), modified `doc.mjs` exemplar + `_debug.mjs`, `test/link-principal.test.mjs`, AND src edits to serve.mjs(12:20)/pipeline.mjs(12:19)/row-grant.mjs(12:15)/live.mjs(12:15) AFTER P3. User confirmed this is THEIR OTHER opencode session and will manage it. The doc/comment FK 403 + intermittent full-suite hang are that session's half-done C3 create-deny, not the lead's work. Lead's P1-P3 + the two P4 isolated modules are intact (tree 373/0 clean with concurrent-affected files present + module tests excluded; modules pass in isolation).
- LEAD HOLDING on all P4 integration (any edit to serve.mjs/pipeline.mjs/row-grant.mjs/live.mjs) + on committing, until the user's other session settles the collide-zone src. Note: the other session's cso audit covers the SAME security/ops ground as P4 (CSRF/rate-limit/headers/create-deny) — RECONCILE with that session's output, do not duplicate. Resume on user signal.

## Discoveries (P4)
- [2026-06-30] The two P4 isolated modules are pure (zero internal imports): write-queue.mjs imports only node builtins; rate-limit.mjs imports nothing. This was deliberate (bounded subagent scope, no collision). Integration (into makeRequestHandler/buildKernel) is where collision lives — deferred.
- [2026-06-30] The C3 create-deny gap is real and was the lead's known deferral: `buildKernel` wires `createServer({authorize:()=>true, db, blobAdopter, postCommitConsumers})` with NO `postHandlerAuthorize`, so a dispatched create always commits (create has no `.can` today). Closing it (create runs the row-grant) is desirable — if the user's other session lands it correctly, the lead's P4 should integrate ON TOP of it, not duplicate it.

## Progress (P4 integration — slices A + B)
- [2026-06-30] P4 slice A — CSRF origin guard (eng-review #13, foreign-only policy per user fork). src/serve.mjs: `csrfGuard(req)` + `SAFE_METHODS` + `sameOriginAsHost`; a foreign-origin/Referer (host ≠ request Host) mutation → 403 before the route gate; same-origin passes; NEITHER present → allow (non-browser client). Empirically grounded: Node `fetch` omits Origin/Referer on same-origin POST, so fail-closed would reject all bare-fetch tests. Wired in makeRequestHandler (non-GET, after ready, before resync/blob/static). test/csrf-origin.test.mjs (4: no-origin allowed, same-origin 201, foreign-origin 403, foreign-referer 403).
- [2026-06-30] P4 slice B — write-queue + opt-in rate-limit. src/serve.mjs: imports createWriteQueue+createRateLimiter; `app.writeQueue=createWriteQueue()` in buildKernel; the 3 mutation branches (create/update/remove) wrap `kernel.dispatch` in `app.writeQueue.run(...)` → 503 'service busy' on bounded wait/depth. This is now correctness-bearing: `postHandlerAuthorize` (C3) is async mid-txn, so two in-flight mutations on the one connection would race a 2nd BEGIN → 500; the writeQueue serializes. rate-limit is OPT-IN via `listen(port, {rateLimit:{ip:{windowMs,max}}})` (limits are app-specific; a forbidden request already returns before the kernel so it never held the write lock) — runs FIRST in the stack, before CSRF/gate. test/write-queue-concurrency.test.mjs (8 concurrent POSTs all 201 + 8 _Log rows). test/rate-limit-http.test.mjs (3 allowed then 429 + Retry-After). **403/0 green, clean exit 6.5s.**
- NEXT (P4): slice C (ops defaults: app-opt-in CSP/HSTS/CORS headers M3, /health + /stats, envGate fail-closed at startup, onShutdown deadline registry, requestLog/metrics; cso M1 Cookie parse→400, M2 secure:false prod guard, L1 audit log); slice D (migrations: versioned schema, startup pre-traffic inside writeQueue txn, idempotent crash re-run — spec #9/#17). Then P5-P7.

## Discoveries (P4 A+B)
- [2026-06-30] CSRF policy fork (RESOLVED, user decision): the eng-review's literal "missing Origin → 403 fail-closed" is incompatible with Node fetch (omits Origin on same-origin POST) + curl/Postman. User chose foreign-only: foreign Origin/Referer → 403; same-origin → pass; neither → allow. Breaks zero existing tests. (CsrfGuard runs only for non-GET app requests; static/WS/snapshot GETs unaffected.)
- [2026-06-30] write-queue necessity (now real): the durable kernel's `postHandlerAuthorize` is async (await mayVerb), holding BEGIN→…→COMMIT open across an await → a concurrent dispatch would BEGIN on the open connection → "cannot start a transaction within a transaction" → 500. writeQueue.run serializes the whole dispatch txn; bounded wait/depth → 503.
- [2026-06-30] rate-limit as opt-in (not default-on): limits are app-specific; forbidden requests return at the gate before the kernel (never held the write lock), so the "flood never holds the write lock" property already holds. Framework owns the mechanism (createRateLimiter); app declares the policy ({rateLimit:...}). Per eng-review §8 line 816 reconciliation.

## Progress (P4 COMPLETE — slices C + D)
- [2026-06-30] P4 slice C — ops defaults + cso M1/M2 (subagent ses_0e9e4dae2ffeV7dwZAaDRcMUIj, lead-cleaned; committed e0640bc, 424/0): `/health`(+`/stats`) public endpoint (anonymous, no auth); `envGate` (`expressPlus({requireEnv:[...]})` → fail-closed at construction); `onShutdown(name,fn,{timeoutMs})` deadline registry (extend installGracefulShutdown); opt-in CSP/HSTS/CORS headers (`listen(port,{csp,hsts,cors:{origins}})` — ALL default OFF, app-declares-policy); opt-in requestLog (stderr at 'finish', writeHead patch only when on; requestCount always runs → feeds /stats). cso M1: parseCookies try/catch → malformed % skip (no 500). cso M2: sessionCookie secure:false forbidden in production (env guard). tests: ops-defaults (15) + session-security (6).
- [2026-06-30] P4 slice D — migrations (lead-led, TDD; committed d85705f, 431/0): src/migrations.mjs (ensureMigrationTable / appliedVersion / runMigrations). Each migration runs in its OWN BEGIN IMMEDIATE txn: `up(db)` → INSERT _Migration(version,appliedAt) → COMMIT; throw → ROLLBACK (ALTER + meta-bump atomic — no half-migrated schema; pending migration re-runs idempotently on restart). Wired into app.ddl() via `expressPlus({db, migrations:[...]})` AFTER the entity-table loop; zero blast radius when undeclared. tests: migrations.test.mjs (7, incl. the distinguishing atomicity-rollback test).
- [2026-06-30] P4 COMPLETE. 431/0 green. Commits this session: 5641a9c (A+B), e0640bc (C), d85705f (D) on top of 489d000. cso review findings: C1-C3/H1/H2 FIXED (489d000); M1/M2 FIXED (e0640bc); M3 ADDRESSED opt-in (e0640bc); L1 (audit log) PARTIAL — minimal requestLog hook only, full surface deferred.
- NEXT: P5 (job-queue substrate), P6 (phase-2 fields/live/effects), P7 (client lib). Review gates per PLANS.md.

## Progress (P5 COMPLETE)
- [2026-06-30] P5 job-queue substrate (lead-led, TDD; committed 2b9bc8c, 447/0): src/job-queue.mjs `createJobQueue({db, sharedSecret, leaseMs, heartbeatGraceMs, reapIntervalMs, now})`. registerWorker(secret) constant-time compares the shared secret → per-worker bearer `<workerId>.<token>` (token stored sha256'd, never raw). authenticate(bearer) = PK lookup workerId + timingSafeEqual on token hash; fail-closed on unknown/revoked. claim(workerId) = single atomic `UPDATE...WHERE status='queued' RETURNING` (SQLite statement atomicity + node single-threaded sync DB = race-safe per eng-review failure-table #3). heartbeat flips claimed→running + extends lease for owner only. submitResult idempotent by job id (first terminal wins; retry = no-op ack, output NOT overwritten). reap() reassigns lease-expired jobs + revokes stale-heartbeat workers. startReaper() periodic sweep. DDL: `_Job`+`_Worker` framework tables (ms-epoch INTEGER timestamps) in generateFrameworkDDL. serve.mjs: framework intercept (after /blobs, before matchRoute) for POST /workers/register, /jobs/claim, /jobs/:id/heartbeat, /jobs/:id/result — bearer-auth'd, anonymous w.r.t. route gate. Reaper started in listen() + stop() in onShutdown. app.mjs: expressPlus({jobs:{...}}) builds app.jobs at construction (fail-closed: createJobQueue throws on missing secret). 16 tests: job-queue (10 substrate) + job-queue-http (6 wiring/status). Separate seam (NOT a projection); composes with P3 post-commit registry at the enqueue edge.
- NEXT: P6 (phase-2 fields/live/effects: crdt/list/log/presence/state field kinds + field-keyed interest/since/pace + effects grammar — the largest surface), P7 (client lib LiveChannel/LiveList). Review gates per PLANS.md; cso on the new worker-auth transport (bearer/secret) recommended before P6.

## Progress (P6b Part 1 COMPLETE — committed 6d19fd3, 458/458/0)
- [2026-06-30] P6b part 1 — declarative effects compiler + in-txn reentrancy core (committed 6d19fd3, +11 tests). NEW src/effect-compiler.mjs (377 lines): inc/dec operator-object constructors; validateEffectDeclaration/validateWhenPredicate (load-time; forbidden-scope regex rejects I/O refs in `when`); buildEffectsRegistry → Map<eventType,[{sourceEntity,effect}]>; detectCrossEntityCycles (DFS, real-cycle); verifyAdmissionHandshake (target must declare admitsEffects); executeEffectsForEvent/executeEffect (depth-capped recursion). pipeline.mjs: extracted applyEventsToTxn = singular in-txn core (_Log→projection→blob→postHandlerAuthorize→effects block); effect events recursively re-enter applyEventsToTxn in the SAME open txn (no nested BEGIN — single-conn can't nest); effectsExecutor wired into createServer({effects}); dispatch is async (await in tests). entity.mjs: admitsEffects declaration + per-effect validateEffectDeclaration at compile. index.mjs exports effect-compiler API. test/effects-grammar.test.mjs (11 green): CRUD-trigger create-effect E2E, set-operator, when-rejects, non-compilable-when, structural-cycle (real A↔B graph), depth-cap, operator-shape, effect-principal-shape.
  - Lead owned the Gate: subagent (ses_0e9a286acffeD2lBww95s9SKTz) burned step ceiling twice + misdiagnosed (claimed 447-unchanged; actually a DUPLICATE executeEffectsForEvent decl → SyntaxError → 145/66-fail). Lead fixed directly: removed dup stub, fixed 3 tests (await server.dispatch + async callbacks; real cycle graph for cycle test), subtracted dead detectEffectCycles.
  - Sequencing correction: fireMapEffects + direct mutate.create LEFT UNTOUCHED (consult #23 blocked on #19 = P6c); P6b purely additive on existing CRUD triggers.
  - **4 HONEST GAPS deferred to P6b Part 2**: (1) inc/dec RMW NOT executed (operator object stored as literal value, not read-modify-written; needs UPDATE-effect targeting existing row); (2) effect principal NOT minted at runtime (recursive applyEventsToTxn runs under outer principal, not principal({type:'system',attributes:{effect:<sourceEntity>})}); (3) admission handshake structural-only (admitsEffects declared-check only, not evaluated against effect principal at runtime; `admitsEffects` never called); (4) global validation pass NOT wired (detectCrossEntityCycles + verifyAdmissionHandshake exported but not called at app boot).
  - P6b Part 2 ALSO adds: `mutate: many(Target,{over:Origin.collection})` typed-FK fan-out (depth-capped), `effect.anyOf(hA,hB,hC)` compound trigger, + `append`/`push`/`insertAt`/`move` operators (grammar/resolution plumbing; full field-specific operator E2E lands P6c when ordered/log/list strategies ship).
- NEXT (in order): P6b Part 2 → P6c (crdt/store/ordered field strategies + retire fireMapEffects via #19 + now-token ADR #24) → P6d (presence/state + src/schedule.mjs) → P6e (live-advanced). Review gates after P6: architecture-lint + reviewer(diff model); cso on effect-principal admission + store-events txn + live re-auth.

## Progress (P6b — effects grammar)
- [2026-06-30] P6b part 1 — declarative effects compiler + in-txn reentrancy (committed 6d19fd3, 458/0): src/effect-compiler.mjs (inc/dec, validateEffectDeclaration, buildEffectsRegistry, detectCrossEntityCycles, verifyAdmissionHandshake, executeEffectsForEvent, depth cap). pipeline.mjs: extracted `applyEventsToTxn` singular in-txn core (append _Log → projectionConsumers → blobAdopter → postHandlerAuthorize → effects block → recursive applyEventsToTxn for effect events; effect JOINS outer open txn, does NOT re-BEGIN). Sees ADR #22 in-txn reentrancy. fireMapEffects left UNTOUCHED (blocks on P6c consult #19). test/effects-grammar.test.mjs (11). 4 honest gaps scaffolded (RMW + effect principal + admission eval + global pass).
- [2026-06-30] P6b part 2 — closed the effect-auth correctness core (committed 51053be, 466/0): effect principal `principal({type:'system',attributes:{effect:src}})` minted in executeEffect + threaded into recursive applyEventsToTxn (replaces outer user principal); `admitsEffects({...})` evaluated at runtime (falsy→403→ROLLBACK origin, ADR #6); NEW `buildEffectsGraph`+`validateEffects` wired into serve buildKernel at app.ready (cycle+admission fail-closed; no-op when no effects; framework auth entities included so unmounted-target effects resolve). serve postHandlerAuthorize admits effect events (`if(event?._effectPrincipal) return true`). DEFERRED gap #1 (inc/dec RMW) → P6c. test/effects-auth.test.mjs (8, behavioral E2E). Doc/Inbox regression fixed (Inbox.admitsEffects + FRAMEWORK_ENTITIES in validation set).
- NEXT: P6c (crdt/store/ordered field strategies, retire phase2Merge + fireMapEffects via #19, now-token ADR #24, gap #1 inc/dec RMW + many/anyOf/append/push/insertAt/move operators). Then P6d (presence/state + src/schedule.mjs), P6e (live-advanced), P7 (client lib). Review gates after P6: architecture-lint + reviewer; cso on store-events txn (P6c) + live re-auth (P6e).

## Progress (P6c — A1/B1/A2 shipped; B2 traced, deferred to fresh session)
- [2026-06-30] P6c-A1 — per-element field merge strategies (committed 4466787, 466→477). Retired phase2Merge; crdt/store/ordered/struct produce real per-element diffs (consult #18/#20).
- [2026-06-30] P6c-B1 — now-token resolved at commit, ADR #24 (committed 5cb23e0, 477→479). `NOW` Symbol substituted in applyEventsToTxn; isPlainObject guard (Date foot-gun).
- [2026-06-30] P6c-A2 — ordered `list` field + fractional-index handle + side-table DDL (committed 3bfa72b, 479→483). makeOrderedListHandle (insertAt/move/reorder/remove); keyBetween float keys. Purely additive (diff reconciliation deferred to P6e).
- [2026-06-30] P6c-B2 TRACED (not started; deferred to a fresh session). The dual-path ban (record.projection.apply `continue`s on kind==='store', entity.mjs:638) means B2 = 3 atomic units, each migrating a store kind FULLY:
  - UNIT 1 (foundation + log): thread optional `dispatch` ref through `hydrate` (entity.mjs:457/510/543/552/591 + serve.mjs:553 + live.mjs:218); `logTableDDL`; `makeLogHandle.append()` → dispatch `<Entity>.<field>.append` → project `.appended` to side-table. No prior path → no dual-path risk, no routes to rewrite, no fireMapEffects. Satisfies log-field REDs 741-744.
  - UNIT 2 (map): retire makeMapHandle direct-SQL → `.set`/`.remove` re-enter dispatch as `<Entity>.<field>.added`/`.removed`/`.roleChanged`; projection writes side-table; RETIRE fireMapEffects (entity.mjs:246) + direct mutate.create — P6b effect compiler fires [collaborators.onAdded] off the new `.added` event. Rewrite effects.test.mjs (.set outside dispatch:42) + map-handle.test.mjs. App share routes (album.mjs:147 etc.) survive (.set re-enters dispatch internally via the hydrate-threaded ref).
    - EDGE: routes that do `Entity.getOrFail(id)` INSIDE a handler (photo-editor.mjs:202) lack a dispatch ref → handle dispatch=null → fallback recreates the dual path. Resolving = framework change to how routes hydrate (deepest seam; re-read consult #19's exact resolution before driving).
  - UNIT 3 (ordered): migrate makeOrderedListHandle the same way; reconcile orderedListDiff to match-by-id + key=position (the A2 model-inconsistency; deferred to P6e delta broadcast anyway).
  Full B2 is a 3-unit stack; too large for a single context-tail landing. Base is clean 483/0 (A1/B1/A2 committed). Rerun in a fresh window.
- NEXT (fresh session): P6c-B2 unit 1 (log) → unit 2 (map + retire fireMapEffects) → unit 3 (ordered). Then P6c-C (effect operators inc/dec RMW + many/anyOf/append/push/insertAt/move — needs B2 store-event + handle machinery). Then P6d (presence/state + src/schedule.mjs), P6e (live-advanced + orderedListDiff reconciliation), P7 (client lib). Review gates after P6: architecture-lint + reviewer(diff model); cso on store-events txn (P6c-B2) + live re-auth (P6e).

## Progress (P6c-B2 UNITS 1+2 COMPLETE — committed 8ca22f0, 495/495/0)
- [2026-06-30] P6c-B2 UNIT 1 (committed 23aeed3) — log store mutations -> events. `makeLogHandle.append()` re-enters dispatch as `<Entity>.<field>.append` (own actionId, own txn) -> `:appended` event -> projection applies to the log side-table. `hydrate(row, principal, dispatch)` 3rd arg (backward-compatible). `logTableDDL` fixed (iterates descriptor.entry). `appendLogHandlers` + projection `:appended` branch.
- [2026-06-30] P6c-B2 UNIT 2 (committed 8ca22f0) — map store mutations -> events. `makeMapHandle.set/.remove` RE-ENTERS dispatch as `<Entity>.<field>.add`/`.setRole`/`.remove` (consult #19); projection applies `:added`/`:roleChanged`/`:removed` to side-table. NEW member -> `.add` -> `:added` (fires `[collaborators.onAdded]` via compiler in-txn under effect principal, ADR #6); role change -> `.setRole` -> `:roleChanged` (DECISIONLOG #57: idempotent re-share is roleChanged NOT fresh add, onAdded does NOT re-fire); same-role = no-op (no dispatch). RETIRED `fireMapEffects` (P6b left it untouched; now the compiler fires effects off the new `:added` event). READS (`has`/`get`/`toArray`) stay direct-SQL (trusted-query, DECISIONLOG #41). Handle hydrated without dispatch throws on `.set`/`.remove` (no direct-SQL fallback — dual reconciliation path forbidden).
  - serve.mjs autoLoad hydrate (~:553) threads writeQueue-wrapped dispatch ref: `req[autoLoad.key] = autoLoad.entity.hydrate(auth.row, principal, dispatchRef)` where `dispatchRef = app?.kernel ? (args)=>app.writeQueue.run(()=>app.kernel.dispatch(args)) : null`. Custom route handlers run OUTSIDE writeQueue so re-entry acquires a FREE lock (no deadlock). writeQueue.run returns the fn's result (dispatch promise propagates).
  - Effect `with` template contract = `{delta, origin}` (origin = `{id: <triggering row id>}`), NOT `{delta, entity}`. doc.mjs:201 + tests migrated. Compiler `executeEffect` was already passing `origin`.
  - postHandlerAuthorize spy signature is the WRAPPER `{entityName, verb, principal, eventType, event}` (not raw event); effect principal on `event._effectPrincipal`, `attributes.effect = <sourceEntityName>`, `id: null`.
  - Migrated 6 tests to dispatch contract (map-write x3, membership-acceptance x2, field-authz x1) + rewrote effects.test.mjs. Skip on ORDERED handle (UNIT 3) for now — A2 diff/model reconciliation deferred to P6e.
  - Bundled (concurrent-agent work swept into same commit): write-queue depth-drift regression fix (src/write-queue.mjs waiters double-decrement on timed-out waiters → depth-drifts negative → maxDepth guard weakens → starvation DoS) + src/job-queue.mjs edits + docs/reviews/models-review-2026-06-30.md (multi-model review, explicitly scoped to committed HEAD, NOT this WIP).
- NEXT: P6c-B2 UNIT 3 (ordered: makeOrderedListHandle -> dispatch-reentry; orderedListDiff reconciliation deferred to P6e). Then P6c-C (effect operators inc/dec RMW + many/anyOf/append/push/insertAt/move — needs B2 store-event machinery, now in place). Then P6d (presence/state + src/schedule.mjs), P6e (live-advanced + orderedListDiff reconciliation), P7 (client lib). Review gates after P6: architecture-lint + reviewer(diff model); cso on store-events txn (P6c-B2) + live re-auth (P6e).

## Progress (P6c-B2 COMPLETE — committed 11792a7, 500/500/0)
- [2026-06-30] P6c-B2 UNIT 3 (committed 11792a7) — ordered store mutations -> events. `makeOrderedListHandle.insertAt/.move/.reorder/.remove` RE-ENTER dispatch as `<Entity>.<field>.insert/.move/.reorder/.remove` (consult #19, UNIT 3); pure `orderedMutateHandlers` validate payload (fail closed) + mint id (insert only, `randomUUID`) + emit `:inserted`/`:moved`/`:reordered`/`:removed`; projection applies the event to the side-table (INSERT/UPDATE-key/loop-UPDATE/DELETE). Handle owns the fractional-key math (`keyBetween(low,high)`) + the ordering read (`rowsOrdered`) — same stale-read posture as map's `probeRow`; `insertAt` returns the handler-minted id via `result.events.find(:inserted).data.id`. READS (`has`/`get`/`toArray`) stay direct-SQL. `hydrate` threads `dispatch` to ordered handle (line ~613). `makeOrderedListHandle` signature `(…, dispatch)` 5-arg.
  - log/map/ordered store handles are now ONE consistent dispatch-reentry mechanism: handle dispatches intent -> handler emits -> projection applies. No direct-SQL mutation path remains (dual reconciliation path forbidden, AGENTS.md). B2 structurally complete.
  - Migrated `test/list-field.test.mjs` (4 tests) to dispatch contract (`createServer` + `executeFrameworkDDL` + `Items.hydrate({id},null,server.dispatch)`, `t.after(()=>db.close())`); kept direct-INSERT row seed + `keyOf` SELECT read-assertions.
  - Minor deviation (left as-is): `reorder` handler coerces missing `entries` to `[]` rather than fail-closing — not a hole (empty reorder = no-op; handle always passes non-empty). Map/log fail-closed posture is stricter; reconcile if a later audit asks for uniformity.
  - Bundled (concurrent-agent work swept into same commit): blob-store, serve, snapshot-resync, blob-reaper changes. NOTE the concurrent agent edited serve.mjs + added test(s) inside the commit window — pre-commit gate read 498/0, post-commit HEAD re-verify (clean tree) read 500/0. The post-commit re-verify against clean HEAD was load-bearing (commit message conservatively states 498; HEAD is actually 500/0).
  - Subagent usage (general-prog `ses_0e919a69`): dispatched the mechanism + test migration. Status DONE, returned 498/0. Lead re-ran the gate independently (500/0 against HEAD) + reviewed the entity.mjs + list-field.test.mjs diffs for honesty before committing: no direct-SQL mutation fallback reintroduced, handler pure (no db read/write), projection branches correct, `insert` validation uses `== null` so legitimate `key: 0` (first insert) passes.
  - FOLLOWUP flagged (carried from UNIT 2): serve.mjs writeQueue-wrapped dispatch re-entry deadlock case is REASONED-safe (custom routes run outside writeQueue → free lock on re-entry) but UNTESTED. Worth a dedicated concurrency test before P6e.
- REVIEW GATES (P6 closure, not yet run): architecture-lint + reviewer(diff model). cso on store-events txn (P6c-B2, all 3 units) + live re-auth (P6e).
- NEXT: P6c-C — effect operators. `inc`/`dec` (read-modify-write arithmetic, new RMW semantics in effect-compiler.mjs — current `with` is a template, not RMW) + `many`/`anyOf`/`push`/`insertAt` (effect multiplicity/positioning). Needs B2 store-event + handle machinery (now in place). Then P6d (presence/state + src/schedule.mjs), P6e (live-advanced + orderedListDiff reconciliation), P7 (client lib).

## Progress (P6c-C step 1 — committed da802fc, 506/506/0)
- [2026-06-30] P6c-C step 1 — `inc`/`dec` read-modify-write + `self` sentinel. The effect `with` template was a *literal* (markers copied verbatim into the cell — models-review Q1 CRITICAL); now an interpretation pass. `self` = frozen sentinel `{kind:'self'}` (NOT a source-entity handle). `executeEffect` rewritten (effect-compiler.mjs:182-262): new sig threads the OPEN txn `db`; `isSelf = effect.mutate?.kind==='self'`; target resolves to the ORIGIN row (self → `:updated`) vs a fresh UUID (plain → `:created`); object-form `with` interpretation detects operator markers (`{kind:'inc'|'dec', value:n}`), reads the target's current cell via in-txn `db.prepare('SELECT <field> FROM <targetName> WHERE id=?').get(readRowId)`, `current=Number(existing?.[field] ?? 0)`, emits `payload[field]= kind==='inc' ? current+value : current-value`. Create-branch reads a non-existent row → current=0 → 0+n=n (degenerate literal, NOT error; SPEC §9.1 "engine decides set-vs-create from whether the row exists"). Function-form `with` path unchanged (operators apply object-form only).
  - In-txn db threading (the genuinely-new seam): pipeline.mjs:168 `effectsExecutor(ev,{now,actionId,depth:depth+1,maxEffectDepth,db})` + closure :234 `(event,{now,actionId,depth,maxDepth,db})=>executeEffectsForEvent(...)` forwards `db`; `executeEffectsForEvent` (:268) + `executeEffect` (:285) carry it. RMW reads see prior events in the same batch + a target deny rolls back the origin (BEGIN IMMEDIATE at :340, throw skips COMMIT at :352). `db` threaded into BOTH the durable path (:350) AND the recursive path (:187).
  - `self` sidesteps cycle detection: sentinel has no `.name` → buildEffectsGraph (:418 `if effect.mutate && effect.mutate.name`) skips it → no self-edge → detectCrossEntityCycles DFS never sees it (a real A→A self-edge WOULD trip `recStack.has(neighbor)` at :341). NO detector change.
  - `validateEffectDeclaration` (:43-82) accepts `mutate.kind==='self'` (existing `.name` check retained for plain handles); `compileEntityEffects` ~:146 guards `effectGraphEntry.add(<name>)` to skip when nameless. `self` re-exported from src/index.mjs:27.
  - DESIGN DECISION (Opus consult `ses_0e90388e`, lead-verified all load-bearing claims against code): NO pluggable `TargetResolver` registry (fails deletion test = RELOCATES over a closed ADR-frozen set). ONE `executeEffect` with an internal branch on the `mutate` handle shape (self/plain/create) + a single per-row `with`-interpretation pass. "create" is the ZERO-ROW branch of the same code (CONCENTRATES — deletes the create-only special case). Operators live as an INTERPRETATION PASS that emits the FIELD's event type the reducer folds — NOT a new mutation grammar, NOT a re-entered `.push` dispatch (no such intent exists — field-strategy.mjs strategies own `{validate,apply,diff}` over whole values). Singular-system rule: `many()` will be MIGRATED onto this same pass, never a parallel path. Documented `## Product stage` in PROJECT AGENTS.md (beta product / long-term-best / consult-expensive-model for direction before defaulting to minimal path).
  - Tests (test/effects-grammar.test.mjs:293-494): self inc seed=5→7; self dec seed=10→7; inc-on-create degenerate 0+4=4 + `:created`; depth-cap self-recursion `Counter.updated→Counter.updated` backstop (throws depth-limit, no hang); db-threaded doc test. Old `inc`/`dec` tests were STUBS (asserted marker objects only); no prior RMW-execution behavior existed.
  - Subagent (general-prog `ses_0e8fdea7`): prescriptive brief, status DONE 506/0. Lead re-gated independently against current tree (HEAD had moved `cb7f265`→`4529172` via concurrent "rate-limit session key" commit DURING the session — 506 gate ran against the live tree incl that commit, so valid) — matches claim, no drift. Review of effect-compiler.mjs + pipeline.mjs + test diffs honest.
  - FOLLOWUP (found this work): pipeline.mjs :168 passes `maxEffectDepth` but closure :234 reads `maxDepth` → `maxDepth` always defaults to 8. Trivial fix, kept out of scope. (Plus carried: serve.mjs writeQueue-wrapped dispatch deadlock case UNTESTED.)
- ROLLED INTO step 2 below.

## Progress (P6c-C step 2 — committed 3454007, 509/509/0)
- [2026-06-30] P6c-C step 2 — `many(Target, { over })` fan-out as the 2nd target-identity branch feeding the SAME with-interpretation pass (step 1's mandate realized: set-vs-create now keyed on ACTUAL row existence, not `isSelf`). `many()` constructor returns frozen `{kind:'many', target, overField}` exposing `.target.name` so the `Blog→Inbox` graph edge lands (cycle detection + admission handshake apply to fan-out targets; fan-out does NOT bypass admission). `over` = the field DESCRIPTOR object itself (e.g. `const subscribers=map(ref('User'))`), identity-matched against `entityRecord.fields` at load time → field NAME. No bare string (no-magic-strings). Owning entity = the ORIGIN at runtime (the `self` trick — no forward-ref to the not-yet-bound `Blog` handle at declaration time).
  - `executeEffect` final shape (effect-compiler.mjs): uniform loop over resolved target rows; `kind=effect.mutate?.kind` (`'self'|'many'|undefined`); `realTarget = kind==='many' ? effect.mutate.target : (kind==='self'?null:effect.mutate)`; admission `if(kind!=='self' && realTarget?.admitsEffects)`. Target rows: many→`resolveManyMembers`, self→`[{id:originId}]`, create→`[{id:randomUUID()}]`. Per-row existence probe `SELECT 1 AS hit FROM <target> WHERE id=?` → `:created`(miss)/`:updated`(hit). function-form `with({delta,origin,member})` called once per member (`member` mirrors `origin`); object-form stays closed/verbatim (NOT taught to interpolate `member` — would be a second `with` contract, singular-system violation). Events carry `_effectSource`/`_effectPrincipal`/`_parentActionId`.
  - `resolveManyMembers` reads the in-txn membership side-table reusing `membershipTable`/`membershipOwnerCol`/`MEMBER_COLUMN` (scope-sql.mjs:33-35) — NO new table, one membership path. The `overFieldName` is identity-resolved at registry-build time in `buildEffectsRegistry` (effect-compiler.mjs:418) — NOT in `compileEntityEffects` (which is never called at runtime boot; serve.mjs:959 `buildEffectsRegistry` + :967 `validateEffects` is the live path). `overFieldName` threaded: `executeEffectsForEvent`(:386)→`executeEffect`(:256)→`resolveManyMembers`(:221).
  - DESIGN DECISION (Opus consult `ses_0e8ee7b4`, narrow-grammar only — mechanism shape settled by step-1 consult, NOT re-litigated; lead-verified all load-bearing claims): `over`=field-descriptor-by-identity (no-magic-strings); member binding=extend function-form to `({delta,origin,member})`; set-vs-create=ACTUAL existence probe (REMOVES step-1's `isSelf`-keyed assumption — net subtraction). Q3 policy fork (create-only vs upsert-per-member) Opus flagged back; LEAD-DECIDED CREATE-ONLY (grounded in singular-system: one target-id path; named-wholes: no `keyed:` flag; blog PAIN-POINTS needs fan-out-CREATE; no real consumer needs upsert — "build for known use cases, no further"). Upsert = future named whole (`upsert(Target,{over,keyed})`) if a real app needs it.
  - PRE-IMPL FINDING: NO working `many` consumer exists. match.mjs/album.mjs/photo-editor.mjs effects are ALL ASPIRATIONAL (bare `players`/`delta.member` in object-form `with` THROWS ReferenceError at declaration — object-form is copied verbatim, gets no args). blog-platform is a GAP-doc, not a running app. The `over`-reach + member-binding grammar was genuinely UN-DESIGNED — no working example existed before this step.
  - Tests (test/effects-grammar.test.mjs): 3 new many tests w/ real `buildEffectsRegistry([User,Inbox])` harness + `executeFrameworkDDL` — 3 Inboxes from c3-added-twice fan-out; 2 Inboxes from two origins each fanning to c1; sentinel shape. Per-file 19/19/0.
  - Subagent (general-prog `ses_0e8eb3c0`): prescriptive brief, returned MAX_STEPS w/ 3 many tests FAILING (Inboxes=0) — correctly diagnosed `_resolvedOver` undefined (wired identity-resolve into `compileEntityEffects`, which is NEVER CALLED at runtime boot → dead path). Per the resume-don't-retry rule, lead applied the surgical fix directly (5 edits + 1 cleanup in effect-compiler.mjs: moved overField identity-resolution into the LIVE `buildEffectsRegistry` path + threaded `overFieldName` down) — smaller than re-briefing. Lead re-gated independently: 509/509/0, no regressions (existence-generalization preserved step-1 behavior). Diff reviewed honest.
  - FOLLOWUPS (unchanged from step 1): (1) pipeline.mjs :168/:351 `maxEffectDepth`/`maxDepth` naming mismatch (maxDepth always 8); (2) serve.mjs writeQueue-wrapped dispatch deadlock case UNTESTED (carried from B2).
- ROLLED INTO step 3 below.

## Progress (P6c-C step 3 — committed 810b2b1, 516/516/0)
- [2026-06-30] P6c-C step 3 — `effect.anyOf(handleA, handleB, handleC)` compound fan-IN trigger (SPEC §9.2 line 474). ONE effect declaration fires when ANY of N statically-known, cycle-detectable trigger handles fire. Orthogonal to `executeEffect` + the cycle algorithm — purely a registration-time multiplexing (N registry Map slots → same `{sourceEntity, effect}` entry; `executeEffectsForEvent` does flat `registry.get(event.type)` → fires once per event, untouched).
  - Grammar (Approach C — Symbol key + `Reflect.ownKeys`; Opus consult `ses_0e8d9264`, lead-verified ALL load-bearing claims against source): `effect.anyOf(...triggers)` returns a fresh `Symbol('effect.anyOf')`; the N original handles stored in a module-level `Map<symbol, readonly handle[]>` (symbols are valid Map keys). Used as computed key `effects:{ [effect.anyOf(a,b,c)]: effect }` — a Symbol key survives JS object-literal coercion UNCOERCED (unlike objects, which `ToPropertyKey` collapses to strings). At registry build, read handles by symbol identity, fan out. No magic strings, no detection heuristic, no parallel declaration shape (CONCENTRATES — one trigger→eventType resolver `resolveTriggerEventType` shared by the string-key + symbol-key paths).
  - **THE LOAD-BEARING FIX (lead-verified the hazard is REAL)**: `Object.entries` (buildEffectsRegistry) AND `Object.values` (buildEffectsGraph) BOTH skip symbol keys per ES spec. Without changing BOTH to `Reflect.ownKeys`, a symbol-keyed anyOf effect would (a) never register at `buildEffectsRegistry` AND (b) escape the admission + cycle graph at `buildEffectsGraph` → **fail-OPEN**. Both sites now use `Reflect.ownKeys`; the cycle ALGORITHM (source→target entity DFS, trigger-blind) is UNCHANGED — only input completeness. The admission-coverage test (#2) FAILS if the :558 fix is reverted (proves the fix).
  - `effect` namespace object with ONLY `anyOf` (SPEC §9.1/9.2 keep `inc`/`dec`/`self`/`many` BARE — different kinds, NO two-spelling drift). Opus recommended migrating the bare exports onto `effect`; lead OVERRIDES per SPEC — NO migration, zero blast radius.
  - entity.mjs :218 — second validation pass over `Object.getOwnPropertySymbols(validatedEffects)` so anyOf effects are shape-validated (without it they'd skip validation; `Object.entries` skips symbols). Spread at :200 already preserves symbols (verified); freeze at :221 preserves them.
  - anyOf accepts CRUD/string + plain-object handles; REJECTS map-collection handles (`'map:onAdded'`/`'map:onRemoved'`) with a load-time error (lead decision, option b) — the documented consumer (photo-editor `RasterLayer.updated`) uses CRUD triggers; entity-scoping a nested map handle needs top-level re-keying that doesn't reach inside anyOf; no consumer needs it. Fail-closed. If a real consumer emerges, that's a future named whole.
  - Dedupe resolved event-type strings (`Set`) so `anyOf(X.updated, X.updated)` fires once.
  - Companion to step-1 Opus consult (`ses_0e90388e`, mechanism shape) + step-2 (`ses_0e8ee7b4`, over/member-binding grammar). This consult scoped to the NARROW compound-trigger-key-grammar question only (mechanism shape NOT re-litigated). Research (explore-flash `ses_0e8dbe3f`) mapped the trigger-handle system: every existing handle (CRUD string / `{toString→'map:onAdded'}` / `state.transition`) coerces to a STRING key by the time the entity compiler sees the `effects:{}` object; `map:onAdded` is resolvable because the compiler holds the field DESCRIPTOR — anyOf's triggers are arbitrary user handles, can't be re-derived, must be CARRIED → Symbol+backing-Map.
  - Off-limits (verified UNTOUCHED in diff): `executeEffect` (target-resolution + RMW with-interpretation), `detectCrossEntityCycles` algorithm, `many`/`self`/`inc`/`dec` mechanics.
  - Tests (+7, 509→516): fan-IN on either event; admission coverage (the :558 fix load-bearing test); self-recursion depth cap (anyOf whose mutate re-triggers, bounded by maxDepth); dedupe; sentinel shape; zero-trigger rejection; map-handle rejection.
  - Subagent (general-prog `ses_0e8f4a6a`): prescriptive brief, status DONE 516/0. Lead re-gated independently (516/516/0, 7069ms — matches claim, no drift) + reviewed diff honest (off-limits surfaces clean, the :558 fix real, entity.mjs symbol pass exact). Per-file effects-grammar 26/26.
- **P6c-C COMPLETE** (steps 1+2+3): `inc`/`dec` RMW + `self` + `many(Target,{over})` fan-out + `effect.anyOf(...)` compound trigger. All three target-identity branches (self/many/create) feed ONE with-interpretation pass; one trigger→eventType resolver serves string + symbol trigger keys. NO direct-SQL mutation, NO second reconciliation path, NO second auth path.
- FOLLOWUPS (DONE): (1) `maxEffectDepth`/`maxDepth` naming — fixed `fe53982` (pipeline.mjs:234-236 closure reads `maxEffectDepth` + forwards `maxDepth: maxEffectDepth`; behavior-preserving at default 8 — `createServer` doesn't expose the cap + `applyEventsToTxn` isn't exported, so non-default verified by reasoning + 516-green guard, no bespoke test, no config knob per "build for known use cases, no further"); (2) serve.mjs writeQueue-wrapped dispatch deadlock case — tested `4fed756` (test/dispatch-reentry.test.mjs, 2 tests: single-request no-deadlock + held-lock serialization; 2000ms AbortController guards; side-table SELECT is self-verifying proof the chain fired). Gate 516→518/0 (lead re-verified, matches subagent claim; Test 2 hardened by lead with timeout guard).

## Progress (P6d — started; Spine A step 1 committed 93ca092, 535/535/0)
- [2026-06-30] P6d = time-driven sources stage (ADR #10, SPEC §10). ALL unbuilt before this work: `src/schedule.mjs` did not exist; `presence`/`state` field kinds import-surface only (no strategy/DDL/runtime); `effectSource(handle)`/`principalFrom(handle)` did not exist (only `_effectSource` internal metadata). Direction set via Opus consult `ses_0e8b30414` (lead-verified ALL load-bearing claims against code) + explore-flash research `ses_0e8bebc4`. KEY CATCH (Opus): the `_Job`/job-queue substrate (`src/job-queue.mjs` `enqueue`/`claim`/`heartbeat`/`reap`/`startReaper`, atomic single-stmt claim, `_Job` DDL ddl.mjs:179-189) is REAL + substantial + PRE-DATES ADR #10/SPEC §10 → schedule rides it, not a new clock. DECISIONLOG #59/#60 (user-ratified Opus+GPT-5.5 consult) pins sequencing: #22 build src/schedule.mjs BEFORE state.auto/TTL; #21 state runtime separate; #25 presence=ephemeral separate; #23 effects (done).
  - DIRECTION (Opus, lead-verified): NO unifying primitive beneath schedule+tick — both nouns feed the EXISTING `dispatch` sink (one reconciliation path). `schedule.at/after` rides the `_Job` reaper (a deadline IS a job); `tick.hz/every` rides in-process `setInterval`+`unref`+`stop` (same shape as startReaper). Named wholes, NOT orthogonal flags. `ephemeral` is NOT a new strategy kind — it's the FORMALIZATION of presence's implicit ephemerality; `presence(cells)` RETIRES to a thin wrapper over `ephemeral` (same change, AGENTS rule 1); STAYS ABSENT from STRATEGIES (absent seam IS the ephemerality). `while` discovery REUSES scope-sql.mjs compiler (harvest/compileReadScope/NonCompilableError) + 2 NEW tick-specific structural guards (empty-while forbidden for row-set; non-compilable while only w/ paired indexed source). `effectSource`/`principalFrom` = GENERALIZE the effect-principal minting (effect-compiler.mjs:282) — NOT a new principal kind (union stays closed system-typed); `effectSource(handle)` = a CHECK inside the target's existing admission body, NOT a parallel slot. One auth path.
  - USER-RESOLVED FORK: drawing-canvas 60Hz throttle/coalescer (SPEC §8.2 pace) DEFERRED to P6e. P6d ships ephemeral SHAPE + raw per-connection broadcast (BLOCKER #1 unblocked); firehose (SHOULD-FIX #1) stays unthrottled until P6e (DECISIONLOG #27).
  - Spine A step 1 (committed `93ca092`, 535/0, +17 tests): NEW `src/schedule.mjs` — `schedule.at(field)`/`schedule.after(field,delay)` frozen triggers (field=descriptor, identity-matched later like `many`'s over; delay parses '7d'/'12h'/'30m'/'90s'/number, fail-closed). `entity()` accepts `schedule:` slot, key=verb-to-fire, value=trigger; validated (verb-name non-empty, kind, field-is-object, delay-finite) + frozen `record.schedule`=null-when-empty. `index.mjs` re-exports `schedule`. Import-surface ONLY — no firing/dispatch/reaper/while/tick (steps 2-5). Subagent `ses_0e8a15e5` DONE: lead re-gated 535/535/0 (matches, no drift), reverted unauthorized PLANS edit, diff reviewed honest.
  - BUILD ORDER (each behind green tests, mapped to consumer): **Spine A (time-source)** — (1) schedule.at/after constructors [DONE 93ca092]; (2) while/when discovery compiler (reuse harvest/compileReadScope + 2 guards); (3) effectSource/principalFrom admission; (4) wire schedule→dispatch via _Job reaper (reaper does NOT fire today — startReaper only reaps dead workers; `_Job` has NO dueAt column → genuine new work: add a due-job sweep that claims+fires); (5) tick.hz/every interval engine. **Spine B (drawing-canvas BLOCKER, parallel)** — (6) presence→ephemeral({...}) generalize + retire; (7) ephemeral per-connection broadcast in live.mjs. **Spine C (convergence, depends on A)** — (8) state STRATEGIES+DDL+transition enforcement (fail-closed); (9) state.auto/TTL desugar into schedule.after.
  - Risks (Opus): one reconciliation path (time-source fires dispatch ONLY); no second auth path; fail-closed on while guards (load-time, not runtime full-scan); `now` resolves at commit (ADR #24, NOW token pipeline.mjs:67 — time-source must NOT stamp wall-clock in handler, emits NOW token; inject clock for tests, now seam job-queue.mjs:54); ephemeral must NOT engage persistence seam (no STRATEGIES entry, no DDL — if a test sees ephemeral producing a _Log row, the seam leaked); tick interval unref()+stop() on onShutdown; state transition fail-closed on illegal moves.
- ROLLED INTO Spine A step 2 below.

## Progress (P6d Spine A step 2 — committed 81a04b6, 544/544/0)
- [2026-06-30] P6d Spine A step 2 — schedule `while` discovery compiler. `schedule.at(field, {while})` / `schedule.after(field, delay, {while})` gain an OPTIONAL `while` scope predicate (options object, last positional arg), authored in the EXACT same shape as a `scope(...)` predicate (`({is, fields}) => AST`). The while compiles at entity-load through the EXISTING `compileReadScope()` (scope-sql.mjs:399) — the SAME path the grant read-scope uses (entity.mjs:170) — producing `whileSql`/`whileParams`/`whileAst` stored on `record.schedule[verb]` (mirror of `scopeAst` retained at entity.mjs:183). The date `field` is the BUILT-IN indexed discovery source (step 4 will compose `WHERE <field> <= :now AND <whileSql>`); step 2 compiles ONLY the while fragment.
  - CONCENTRATION: the genuine new code is minimal — the REUSE of compileReadScope (one compiler, singular system) IS the point. No second compiler, no scope-sql.mjs change (import only, already imported at entity.mjs:21).
  - GUARDS (lead-decided, SPEC §10 applied as a fail-closed SUBSET): **strict-compile** — a non-compilable while is a LOAD-TIME error (`NonCompilableError` propagates), NOT a silent runtime guard. This catches author typos (unknown fields) fail-closed. The SPEC §10 runtime-guard fallback, the `when` lifecycle guard, and the "empty while forbidden for row-set ticks" guard are DEFERRED to step 5 (tick), where they are the SPEC-motivating case (a tick has no date field → while IS the discovery → needs the fallback + empty-while guard). For schedule.at/after the date field IS the indexed source, so strict-compile is the fail-closed subset all real consumers satisfy (blog/library narrowing references comparable fields that DO compile). SPEC §10's fallback is PERMISSIONAL ("allowed only as"), not mandatory → strict-compile is SPEC-compliant + fail-closed-preferred.
  - `when` REJECTED at load (`trigger.when` → `Error('not yet supported (ships with state runtime, Spine C8)')`): `when` needs the `state` field runtime (Spine C8); accepting it unenforced would be a silent no-op foot-gun (fail-closed forbids).
  - Tests (+9, 535→544): at/after carry while fn; while-not-a-fn throws at construction; backward-compat (no options → while undefined); entity compiles while → whileSql (contains field), whileParams (populated), whileAst (object); no-while → whileSql undefined; non-compilable while (nonexistent field) → load-time NonCompilableError w/ where-context; `when` rejection; comparable-field while compiles. Real entity harness (grant `scope(()=>everyone()).can(()=>grant(read))`, text status + date publishedAt fields). Per-file 26/26.
  - Subagent (general-prog `ses_0e89746b2`): prescriptive brief, status DONE 544/0 with raw gate output shown. Lead re-gated independently (NEVER trusts subagent claim — B2 caught stale-count drift): 544/544/0/7233ms, matches claim EXACTLY, no drift. Diff reviewed honest (compileReadScope reuse mirrors grant :170; `when` rejected before while compile; NonCompilableError propagates strict; validated entry fresh-frozen w/ whileSql/whileParams/whileAst; 3 files only, no PLANS/concurrent). Commit msg hang on first attempt was spurious (no git hooks installed — only `.sample`, no hooksPath); retried clean.
- NEXT: Spine A step 3 — `effectSource(handle)`/`principalFrom(handle)` admission (generalize the effect-principal minting at effect-compiler.mjs:282 into a reusable `principalFrom(handle)`; `effectSource(handle)` = a CHECK inside the target's existing admission body reading `principal.attributes` source tag, NOT a parallel slot; route through admitsEffects/grant registry; consumer: doc.mjs:73 ghost). Then step 4 (reaper due-job sweep), step 5 (tick interval). Spine B (ephemeral, drawing-canvas BLOCKER) parallel. Spine C (state runtime + auto/TTL sugar) depends on A. THEN P6e (live-advanced + orderedListDiff + pace/coalescer §8), P7 (client lib).

## Progress (P6d Spine B6 — committed b5b113d, 600/600/0)
- [2026-06-30] P6d Spine B6 — `ephemeral(cells)` general NON-PERSISTING field kind; `presence` RETIRED to a thin wrapper over it (same change, AGENTS rule "a new general mechanism retires the special-case it generalizes, in the same change"). `ephemeral(cells)` (src/field.mjs) returns `{kind:'ephemeral', type:'ephemeral', cells: frozen}` — author-declared live sub-cells, richer than presence's boolean toggles (the drawing-canvas in-progress-stroke BLOCKER). `presence(cells)` is now `return ephemeral(cells)` — ONE non-persisting kind, NOT two parallel kinds. `makeDescriptor.can` unchanged (access-fn chain). ddl.mjs `presenceTableDDL`→`ephemeralTableDDL`; dispatch collapsed `kind==='presence'||kind==='ephemeral'` → `kind==='ephemeral'` ONLY (one branch, no parallel path). NO main-table column for ephemeral (blanket skipped in mainTableDDL alongside map/log/state/store). field-strategy.mjs STRATEGIES UNCHANGED — `presence` AND `ephemeral` BOTH throw on `resolveStrategy` (the absent-seam guarantee holds; if an ephemeral field ever produces a `_Log` row, the seam LEAKED = bug). index.mjs re-exports `ephemeral` alongside `presence`.
  - DECISION (lead-owned, override): subagent `ses_0e86e4e6ff` (general-prog) delivered 600/0 but chose "keep presence.kind='presence' for backward-compat" → TWO parallel constructors (presence body duplicated ephemeral's makeDescriptor), ddl OR-branch on both kinds — the EXACT parallel-path smell the consult (#25 / ADR `## Product stage`) forbade. Brief was internally contradictory ("preserve exact descriptor" vs "retire to one kind"); subagent resolved it toward backward-compat-of-an-internal-label. Lead verified the only `kind==='presence'` dependents were 4 test assertions + the ddl OR-branch — NO project/external API depends on the kind string (grep confined to tests+ddl). So retirement was clean + is the genuine "migrate behind-green-tests" the rule demands. Lead surgical fix (no re-dispatch): `presence(cells)→return ephemeral(cells)`; ddl single-branch; presence-field + ephemeral-field tests migrated to expect kind:'ephemeral' + parity (presence(...) ≡ ephemeral(...) at kind/type/cells; deepEqual chokes on the `can` method descriptor attaches, so compared fields not whole-object); stale error-regex `/presence field/`→`/ephemeral field/` (scope-sql.mjs:228 emits `${descriptor.kind} field`, now 'ephemeral'). Re-gated 600/0 lead-verified raw.
  - Deletion-test: PASS — presence constructor GONE (not kept beside ephemeral); one kind, one ddl branch, one side-table fn. Net +16 lines (comment-heavy).
  - TDD: test/ephemeral-field.test.mjs (10) + test/presence-field.test.mjs (6, migrated). Per-file 16/16.
  - OFF-LIMITS held: pipeline.mjs/serve.mjs/live.mjs/schedule.mjs/principal.mjs/effect-compiler.mjs/entity.mjs untouched (per-connection broadcast is Spine B7, later).
  - Spine B7 (ephemeral broadcast in live.mjs) still NOT briefable — needs lead read of live.mjs subscription model first (may overlap P6e pace/coalescer).
- NEXT: verify Spine A step 4a (`discoverDueSchedules` — subagent claimed 590/0, UNVERIFIED, working-tree has its 4 dirty files: entity.mjs/schedule.mjs/schedule-field.test.mjs + untracked schedule-discovery.test.mjs; MUST re-gate + diff-review before commit). Then lead decides step 4b admission wiring (where a scheduled system-principal dispatch is admitted — mirror effect gate `admitsEffects` on the dispatch path, fail-closed; the `authorize:()=>true` at serve.mjs:1069 runs UNGATED for direct kernel.dispatch — the "no second auth path" hard rule). Then A5 (tick.hz/every). Spine C8 (state runtime) depends on A. THEN P6e, P7.

## Progress (P6d Spine A step 4a — committed d81fdab, 600/600/0)
- [2026-06-30] P6d Spine A step 4a — `with` payload grammar + CRUD-verb-only validation + field identity-resolve + PURE `discoverDueSchedules(db,entities,now)`. Entity schedule lane (src/entity.mjs) gains load-time validation: (1) CRUD-verb restriction — `schedule` verb must be create|update|remove (fail-closed; pipeline.mjs:325 `handlers[type]` throws for unknown types, so NO custom verbs are dispatchable — confirmed no custom-verb mechanism exists); (2) field identity-resolve — `trigger.field` descriptor matched to a declared field NAME by object identity (same pattern `many`'s `over` uses — singular system); (3) field-kind gate — trigger field MUST be `kind:'value'` AND `type:'date'|'number'` (load-time fail-closed the field is comparable via `<=`; `date` serializes to epoch-ms INT per field-strategy.mjs:136-143 — direct integer SQL comparison); (4) `with` payload validation (object-literal OR `({row})=>obj`).
  - `discoverDueSchedules(db,entities,now)` (src/schedule.mjs:56) — PURE read-only discovery (does NOT dispatch/write/mutate). Iterates entity `record.schedule` triggers; builds `SELECT id FROM <name> AS t0 WHERE t0.<field> <= :now` (schedule.at) OR `CAST(t0.<field> AS INTEGER) + :delay <= :now` (schedule.after); appends `AND (${trigger.whileSql})` + merges whileParams (the t0 alias matches scope-sql's convention so the while fragment composes); resolves `with` per row — FUNCTION form refetches full row (`SELECT * FROM <name> WHERE id=:id`) for `{row}`, OBJECT-LITERAL form shallow-copies. Returns `[{entity,verb,rowId,payload}]`. Epoch-ms INT contract validated: tests pass `Date.now()` (epoch-ms int) as `now` + seed date fields via the real entity (field-strategy serializes Date→epoch-ms), so the `<=` is int-vs-int end-to-end.
  - VERIFICATION (lead re-gated independently — NEVER trusts subagent claim): subagent `ses_...` claimed 590/0; ACTUAL with B6 present = 600/0 (B6 committed +16 tests = ephemeral 10 + presence 6; 4a adds 31 in schedule-discovery.test.mjs; 4a's schedule-field.test.mjs edits are modifications not additions). 31/31 direct on the new file. Diff reviewed HONEST: pure read-only (the per-row SELECT * is an N+1 read, acceptable — no mutation; optimize later if hot); t0 alias consistent with whileSql; field-kind gate is the load-bearing fail-closed (prevents map/log/ephemeral as schedule triggers); CRUD restriction matches handlers[type] reality (no custom verbs).
  - NOT YET WIRED — discoverDueSchedules is UNUSED at runtime (step 4b wires it into the reaper sweep). No caller until 4b.
  - TDD: test/schedule-discovery.test.mjs (31 — at past-due/future exclusion/after+delay/with-function-recv-row/while-clause). Per-file 31/31.
- NEXT (lead-decided, NOT delegated): step 4b admission wiring — a reaper-fired `dispatch` runs through `authorize:()=>true` (serve.mjs:1069) UNGATED today; wiring admission for a scheduled system-principal dispatch is the "no second auth path" hard rule. Lead decides WHERE: (a) a dispatch `authorize` hook mirroring the effect gate `admitsEffects`, OR (b) a reaper pre-call admission check. Then dispatch step 4b: reaper sweep loop → discoverDueSchedules → kernel.dispatch under scheduler principal → thread admission. Then A5 (tick.hz/every — bundles tick-specific guards: runtime while fallback, empty-while, when). Spine C8 (state runtime) depends on A. THEN P6e, P7.

## Progress (P6d Spine A step 4b — wiring committed THIS commit, 614/614/0)
- [2026-06-30] P6d A4b (2/2) wiring — scheduler admission wired through a NEW `preProjectionAuthorize` dispatch-phase hook. `src/pipeline.mjs`: `applyEventsToTxn` gains `preProjectionAuthorize` (called IN-TXN per emitted event, before _Log-append + projections + blob-adopt; derives entityName/verb from `e.type` via VERB_FROM_EVENT; throw 403 on deny → zero footprint); threaded through `createServer` opts + durable `dispatch` + the recursive effect re-entry (effects carry `effectPrincipal` so the scheduler branch is a no-op there). `src/serve.mjs`: the scheduler system-principal branch moved OUT of `postHandlerAuthorize` into a NEW `preProjectionAuthorize` callback (returns true for non-scheduler principals; runs `admitScheduledMutation` for `type==='system' && attributes.source`). `postHandlerAuthorize` keeps create row-grant + `_effectPrincipal` short-circuit (post-projection, unchanged).
  - WHY TWO HOOK PHASES (the bug the subagent caught + the lead's correct synthesis): `postHandlerAuthorize` runs AFTER projections (post-mutation row). Scheduler admission re-checks `while`/due/`with` — those must see the PRE-mutation row (as it stood at discovery, NOT after this dispatch's own projection applied the payload). Create row-grant needs the POST-mutation row (authorize against the freshly-created row). OPPOSITE visibility → cannot share one hook position. `preProjectionAuthorize` = pre-mutation (scheduler); `postHandlerAuthorize` = post-mutation (create grant). The subagent's "move the whole hook earlier" would have broken create auth.
  - `admitScheduledMutation` (src/schedule.mjs) payload-compare FIX: strips `_rowId` ({id}) from the dispatched payload before JSON.stringify — comparing against the declared `with` (which never includes `id`; the `update` handler requires `payload.id` as the target pointer, not a schedule-written field). Without this, every bound scheduler dispatch would falsely deny (write-anything guard mismatches on the structural id). The earlier e2e harness MASKED this by stripping `id` in the hook; real serve.mjs wiring would have denied everything in production.
  - e2e harness (test/schedule-admission.test.mjs) mirrors serve.mjs: scheduler branch in `preProjectionAuthorize`; 4 e2e — admitted-on-due+while (row.status → 'published'), denied-undeclared-payload (`status:'hijacked'` vs declared `'published'`), denied-wrong-source, denied-while-fails (seed 'archived'). 14/14 on the file.
- NEXT: A5 tick.hz/every interval engine + tick-specific guards (runtime while fallback, empty-while, when). Then Spine C8 (state runtime, depends on A). Then the REAPER sweep (discoverDueSchedules → kernel.dispatch under scheduler principal through the now-wired admission) — discovers candidates; dispatch's preProjectionAuthorize re-admits in-txn (TOCTOU-safe; stale candidates deny as normal skips). Then P6e, P7.
- [2026-06-30] DECISIONLOG #63 — ordering + payload-compare correction.

## Progress (P6d A5 Phase 1 — IN PROGRESS, delegate ds-v4-flash)
- [2026-06-30] P6d A5 Phase 1 — `tick.hz(n)`/`tick.every(duration)` interval trigger IMPORT-SURFACE + admission (NO engine yet — Phase 2). Row-set tick only (singleton deferred). DECISIONS (user-resolved grill): (1) row-set tick — fires `update` against every row matching `while` per interval; empty `while` FORBIDDEN at load-time (the "run on ALL rows forever" foot-gun guard); (2) NEW `admitTickedMutation` gate — sister to `admitScheduledMutation`, SKIPS due-check (tick is due-by-firing), keeps source-binding + while + payload-compare; (3) engine lifecycle mirrors `startReaper` (Phase 2). CONCENTRATION: tick reuses the existing `compileReadScope` while compiler (strict, singular system), the existing `principalFrom(source)` mint (system principal, attributes.source — NO new principal kind), the existing `dispatch` sink (one reconciliation path). NEW code = tick constructors (frozen, kind 'tick.hz'/'tick.every', carry hertz/intervalMs + while + with, NO field/delay) + entity lane validation (kind accept, hertz/interval finite+positive, empty-while forbidden, while strict-compile, `when` rejected, NO field-kind gate) + `admitTickedMutation` (sister gate, no due) + `tickSource(entityName,verb)` (no fieldName — `${entityName}.${verb}`).
  - Generation model: ds-v4-flash (user directive). Lead reviews self (lead synthesis owns the decision; risky-slice reviewer still applies for Phase 2 engine+wiring). Gate baseline HEAD fbd3676 = 614/0. Target after Phase 1: 614 + N new tick tests, 0 fail.
- [2026-06-30] COMMITTED `65181dc` — 646/646/0 (614 baseline + 32 new tick tests). Lead self-reviewed (user directive: ds-v4-flash generates, lead reviews self — Phase 1 is import-surface+admission, lower-risk than Phase 2's engine; risky-slice reviewer still applies to Phase 2). VERIFIED VERBATIM: `admitTickedMutation` fail-closed + KIND-GATED (`trigger.kind!=='tick.hz'&&!=='tick.every'` deny at schedule.mjs:288) so a `schedule.at` trigger CANNOT be admitted via the tick path (no due-check bypass fraud — the auth-critical concern); `discoverTickedRows` pure read-only (defensive `!whileSql` skip + N+1 function-with refetch matches discoverDueSchedules pattern); tick constructors frozen + validated; `tickSource=${entityName}.${verb}` (no fieldName — derived id); entity lane `isDeadline`/`isTick` branching correct (field identity/kind/delay kept ONLY on deadline path; empty-while + hertz/intervalMs on tick path; `when` rejection in BOTH branches). Fix: ds-v4-flash hit MAX_STEPS leaving the DEADLINE `when`-rejection check dropped during the lane refactor (1 real regression of existing behavior — schedule-field.test.mjs:288). Per resume-interrupted-subagents discipline: did NOT re-dispatch from scratch (diagnosis + fix were already in the report); lead applied the 1-line surgical fix directly (when-first in deadline branch), re-gated 646/0. Honest test edit: schedule-field.test.mjs bogus-kind regex WIDENED to include tick kinds (legit error-string change, not a weakened assertion). Gate baseline HEAD fbd3676=614/0 → 65181dc=646/0. Off-limits held (pipeline/serve/live/principal/effect-compiler/ddl/field/scope-sql untouched; PLANS lead-owned).
- NEXT: A5 Phase 2 — tick-engine.mjs (setInterval+unref+stop, mirror startReaper: job-queue.mjs:189-199) running discoverTickedRows each interval → kernel.dispatch under principalFrom(tickSource(...)) through the ALREADY-WIRED preProjectionAuthorize (serve.mjs must add a tick-source branch calling admitTickedMutation alongside the scheduler-source branch). Start in serve.mjs listen() if any entity has ticks; onShutdown hook (mirror blob-reaper serve.mjs:1192-1201 / job-queue reaper serve.mjs:1176-1178). e2e: a tick entity fires dispatch at interval; row-set discovered + admitted + projected; TOCTOU deny (row deleted mid-path); undeclared payload deny; while-fails deny. Risky slice (live timer + system-principal dispatch) — full delegated workflow + decision notes + reviewer.

## Progress (P6d A5 Phase 2 — IN PROGRESS, delegate ds-v4-flash + lead self-review)
- [2026-06-30] P6d A5 Phase 2 — tick ENGINE + wiring. NEW `src/tick-engine.mjs`: `startTickEngine({db,entities,dispatch,now})` mirrors `startReaper` (job-queue.mjs:189-199) — `setInterval`+`unref()`+idempotent-start+`stop`. Computes the single fastest sweep interval from all tick triggers (`tick.hz(n)`→`1000/n` ms; `tick.every(d)`→`intervalMs`); returns a no-op `{stop(){}}` if no tick triggers exist (no empty-config timer). Each sweep: `discoverTickedRows(db,entityList,now())` → per row, `dispatch({actionId:randomUUID(), type:\`${entityName}.${verb}\`, principal:principalFrom(tickSource(entityName,verb)), payload:{id:rowId,...payload}})` wrapped in PER-ROW try/catch (one row's deny/throw → stderr + CONTINUE — never aborts the sweep; outer try/catch guards discovery-phase throws too). `entities` normalized Map→array (`discoverTickedRows` iterates `for...of`).
  - `src/serve.mjs`: (a) `preProjectionAuthorize` gains a TICK-SOURCE branch BEFORE the existing scheduler branch — `principal.attributes.source===tickSource(entityName,verb)` (exact 2-part `${e}.${v}` match; a scheduler source is always 3 parts `${e}.${v}.${fieldName}`, so it falls through) → `admitTickedMutation`. (b) Tick engine started inside `app.ready` AFTER `app.kernel=buildKernel(app)` (NOT synchronously in `listen()` — `app.kernel` is undefined until the async ready IIFE resolves; an earlier start would hand the engine `dispatch:undefined`).
  - BUGS CAUGHT + FIXED BY LEAD SELF-REVIEW (3 real, 1 test-setup): (1) dispatch call MISSING `actionId` → pipeline.mjs:351 `ERR_INVALID_ARG_TYPE` on `undefined` (pre-staged `randomUUID` import by ds-v4-flash, wired by lead); (2) SERVE.MJS PRODUCTION BUG — tick-engine startup was placed at serve.mjs:1229 (synchronous, BEFORE `app.kernel` assigned at :1268 inside `app.ready`) → `dispatch:app.kernel?.dispatch` was `undefined` in production; e2e tests masked it (they pass `server.dispatch` directly). Lead moved startup into `app.ready` post-buildKernel. (3) TYPE-KEY INVERSION — engine dispatched `type:\`${e}.updated\`` (event noun) but the handler registry (entity.mjs:1060) keys on `type:\`${e}.update\`` (CRUD action verb); dispatch threw "no handler for 'BlogTick.updated'". LEAD BRIEF HAD THE CONVENTION INVERTED (confused the EMITTED event-noun with the CONSUMED action-type). Fixed to `\`${entityName}.${verb}\``. (4) test 2 flawed setup — `BadBlog={...Blog,schedule:{...rawTick}}` swapped in an UNCOMPILED trigger (no `whileSql`) → `discoverTickedRows` skipped it → `dispatchCount` stayed 0. Fixed to use the REAL compiled Blog + `throwingDispatch` (the per-row throw is what the test exercises).
  - DIAGNOSIS ACCURACY: ds-v4-flash ses_0e80f9ae9 returned DONE_WITH_CONCERNS — honest + precisely diagnosed the actionId issue + flagged the serve.mjs `dispatch:app.kernel?.dispatch` finding. But it MISSED (a) the production ordering bug (claimed `app.kernel.dispatch` was the handle at :1223; actually assigned at :1268 inside async ready), (b) the `.updated`/`.update` type-key inversion (it had the SAME inverted belief my brief gave it), (c) the test-2 uncompiled-trigger flaw. Lead self-review (per user directive "review yourself") caught all three + fixed surgically (resume-interrupted-subagents discipline: NOT re-dispatched — fixes were precise + small).
  - USER DIRECTIVE applied: "generate code with ds-v4-flash via pioneer and review yourself." Lead is the reviewer for this risky slice (no separate reviewer subagent). Risky-slice reviewer STILL would apply if directive retracted.
  - Off-limits HELD (pipeline/live/principal/effect-compiler/schedule.mjs/entity.mjs/field/ddl/scope-sql untouched — reuse only). Gate baseline HEAD 65181dc=646/0 → 656/0 (10 new tick-engine tests), 0 fail, lead-verified raw (/tmp/a5p2-lead.txt). Files: NEW src/tick-engine.mjs (110 lines) + src/serve.mjs + src/index.mjs (exports startTickEngine) + NEW test/tick-engine.test.mjs (10 tests).
- NEXT: Spine C8 (state runtime — depends on A, now that the time-source harness ships). Then REAPER sweep (discoverDueSchedules → kernel.dispatch under scheduler principal through the wired preProjectionAuthorize scheduler branch). Then P6e (live-advanced + orderedListDiff + pace/coalescer §8.2). Then P7 (client lib).

## Progress (P6d Spine C8 — committed 02e67b3, 664/664/0)
- [2026-06-30] P6d Spine C8 — `state` field RUNTIME (storage + structural validation + transition guard). The `state({values,transitions,effects,auto})` constructor (field.mjs:254) existed at the import surface but had NO runtime: no strategy entry (resolveStrategy threw), no DDL column (fell through mainTableDDL), no transition enforcement. Shipped runtime CORE only:
  - field-strategy.mjs: STRATEGIES.state entry — `validate`=membership in declared `values` domain (returns reason string `expected one of [draft, shared, archived]`); `apply`=replace; `diff`=`{from,to}` or null; NO `serialize` (value is a domain string, passthrough like crdt/store/ordered). resolveStrategy error msg updated to honest full kind list (was stale, listed only value/store/crdt/ordered — also missing hash/struct).
  - ddl.mjs: `'state'` added to the main-table-column branch (TEXT column). Stale comment fixed (state removed from the 'NOT stored' list). No side-table.
  - entity.mjs: update CRUD handler (:1071) gains a transition guard — for each state field in the payload, pre-read current row via `record.findById(id)` (the ambient `getActiveDb()` handle), compare current→next against `descriptor.transitions`, throw `ValidationError` status:400 on illegal move (`${name}.${fieldName}: illegal transition ${currentValue} -> ${next}`). Runs AFTER `validateMutation` (structural first → invalid target reports as domain error, not transition error; clearer diagnostic). No-op (current===next) skipped. Fail-closed: missing db (`requires durable database`), missing row / null current (`no current state`).
  - ZERO-FOOTPRINT (verified): the guard throws BEFORE the event is emitted → `applyEventsToTxn` never runs → nothing appended to _Log, no projection. Test asserts log-count unchanged + row unchanged on illegal move.
  - SCOPE HELD (deferred per user grill): `state.effects` (in-txn effect keyed by `state.transition(from,to)` handle — wires into effect-compiler, separate slice) + `state.auto` ({when,after,to} desugar to schedule.after — separate slice) NOT wired. Transition-handle strings (for the deferred effects map) NOT used by the guard — guard reads the `transitions` graph directly.
- PROCESS: Step 1 research delegated to `explore-flash` (ses_0e7ed3ec5) — mapped STRATEGIES/validateMutation/DDL/handler precisely (the fact sheet was accurate; its only miss was not knowing about the ambient db). Step 2 grill (3 forks resolved by user: update-handler pre-read guard; core-only; create=value-in-domain). GPT-5.5 consult (ses_0e7e4a70b) for direction. Step 6 implementation delegated to `ds-v4-flash` (ses_0e7e11b4d, per user directive) — hit step limit, returned EMPTY (no report), but the 4-file edit landed; lead re-gated + self-reviewed (per user directive 'ds-v4-flash generates, lead reviews self'). Step 7 verify: lead self-reviewed (user directive overrides default reviewer delegation). 664/0 lead-verified raw (/tmp/c8.txt).
- LEAD SYNTHESIS (load-bearing): GPT-5.5 flagged 'CRUD handler lacks db → must widen pipeline.mjs handler context' as the load-bearing risk + a TOCTOU concurrency concern ('handler runs outside BEGIN IMMEDIATE'). Lead VERIFIED both premises against the code:
  - WRONG for this codebase — the entity has an AMBIENT db handle via `getActiveDb()` (src/db.mjs:19, set once at app construction app.mjs:308 `if(db) setActiveDb(db)`), and `record.findById` (entity.mjs:822) already uses it throughout (entity.mjs:783 comment: 'The db handle is ambient (getActiveDb)' — established DECISIONLOG #41 convention, NOT a second path). So the guard pre-reads via record.findById with ZERO pipeline.mjs changes — no handler-context widening, no 5+ module blast radius. The slice stayed 4 files (NOT risky-tier).
  - TOCTOU: handler runs outside BEGIN IMMEDIATE, but app writes serialize through `writeQueue.run` (serve.mjs:240/267/296) — no concurrent WRITE dispatch can mutate the row between the pre-read and this dispatch's txn-open. Reads (HTTP GET) don't mutate. So the write-path TOCTOU is closed by writeQueue serialization. (GPT-5.5's 'move handler into txn' long-term fix is NOT needed for C8.) LESSON: a consult's load-bearing premise about the framework's db-access pattern must be verified against the actual code — GPT-5.5 assumed the standard 'pass db through context' pattern and didn't know about the ambient handle.
- NEXT: REAPER sweep — discoverDueSchedules(db,entities,now) → kernel.dispatch under principalFrom(schedulerSource(e,v,fieldName)) through the wired preProjectionAuthorize scheduler branch. Mirrors startTickEngine shape (setInterval+unref+stop, per-row try/catch stderr+continue). The reaper rides _Job (a deadline IS a job) per PLANS build-order. Then P6e (live-advanced + orderedListDiff + pace/coalescer §8.2 — resolves the drawing-canvas BLOCKER). Then P7 (client lib).
- [2026-06-30] DECISIONLOG #66 — state-runtime responsibility split + ambient-db seam (handler pre-reads via record.findById, NOT pipeline widening).

## REAPER sweep — DONE (pre-commit, lead self-review complete)
- [2026-06-30] Shape decision (GPT-5.5 consult ses_0e7bc9216f, user accepted): Shape C — direct dispatch mirroring `startTickEngine`, NOT _Job. The deadline is RE-DERIVED from committed entity state every sweep via `discoverDueSchedules`, so a deadline is never a stored thing that can be "lost" (crash mid-dispatch → next sweep re-discovers the still-due row); `preProjectionAuthorize` already re-admits in-txn (TOCTOU-safe, zero-footprint denial). _Job (ddl.mjs:179, a remote-worker claim/lease/heartbeat pool) buys cross-process execution, which isn't used. A scheduled mutation MUST make itself no-longer-due (flip the watched field or falsify `while`) or the reaper re-dispatches every sweep — invariant encoded in tests.
- [2026-06-30] NEW src/reaper.mjs: `startReaper({db,entities,dispatch,now=Date.now})` mirrors `startTickEngine` (setInterval+unref+idempotent start+stop, entityList Map→array, per-row try/catch stderr+continue, outer scan try/catch, no-op {stop} when no schedule.at/after triggers). Fixed 1s interval. Per-row dispatch: `{actionId: randomUUID(), type:"${entityName}.${verb}", principal: principalFrom(schedulerSource(e,v,trigger.fieldName)), payload:{id:rowId,...payload}}` (DECISIONLOG #64 invariants: actionId present, type=CRUD-verb not event-noun, payload.id merged).
- [2026-06-30] writeQueue serialization fix (lead-verified SOUND, integrated into this commit): timer-driven dispatch (tick engine + reaper) was calling `app.kernel.dispatch(args)` DIRECTLY, bypassing the `app.writeQueue.run` that serializes durable writes — the same TOCTOU closure C8 cited. Fix (serve.mjs:1258): `const dispatchThroughWriteQueue = (args) => app.writeQueue.run(() => app.kernel.dispatch(args))` passed as `dispatch` to BOTH engines (tick:1262, reaper:1271). This is the ESTABLISHED idiom — byte-identical to serve.mjs:593 (custom-route re-entry) + HTTP CRUD (:241/:268/:297). `app.writeQueue = createWriteQueue()` at serve.mjs:1066 (before app.ready IIFE :1255, so it exists at engine start). RED→green test `test/tick-engine.test.mjs` 'listen tick dispatch waits behind the app write queue' exercises the PRODUCTION app.writeQueue path the e2e harness (passing server.dispatch directly) MASKED — the A5 Phase 2 bug #2 lesson. AGENTS: one reconciliation path for writes (HTTP + timer now serialize through ONE queue).
- [2026-06-30] Decision note: the writeQueue fix was surfaced by a parallel agent session's audit (task ses_0e7b9181af); lead VERIFIED the finding against the code + integrated the fix lead-owned (did NOT accept that session's unrelated "pre-approved expensive-model review gates" PLANS framing — that content was untrusted + out-of-scope and reverted). The fix itself is SOUND + belongs in the reaper commit (same fix class for both engines).
- [2026-06-30] FILES: NEW src/reaper.mjs + src/serve.mjs (reaper import + app.ready start block + writeQueue wrap on both engines) + src/index.mjs (export startReaper) + NEW test/reaper.test.mjs (13 tests mirroring tick-engine harness) + test/tick-engine.test.mjs (writeQueue RED→green test). DECISIONLOG #67 appended. Gate baseline aa4757d=664/0 → 678/0 (+13 reaper + 1 writeQueue test), lead-verified raw.
- NEXT: P6e (live-advanced + orderedListDiff + pace/coalescer §8.2 — the drawing-canvas 60Hz throttle BLOCKER resolves here). Then P7 (client lib).

## P6e research + slicing — DONE (pre-implementation, lead-owned synthesis)
- [2026-07-01] 2 parallel explore-flash fact-sheets (live surface; SPEC/PLANS/AGENTS/drawing-canvas) confirmed the gap: ephemeral is WRITE-IMMUNE (no mutation handle/handler/event/projection); pipeline.mjs:144 appends ALL events to _Log with no kind filter (DECISIONLOG #51 "no _Log row" NOT enforced); subscribe keyed by (entity,id) ONLY (no field-interest); live.mjs:341 forwards committed events VERBATIM (no diff/delta/replay). orderedListDiff (field-strategy.mjs:85-106) exists but match-by-KEY (model inconsistency, deferred to P6e-3).
- [2026-07-01] GPT-5.5 consult (ses_0e79c66b9f) chose Shape C: P6e-1 `field-paced-live` for ephemeral as first slice (folds Spine B7 in). Lead split P6e-1 into 1a + 1b (user-confirmed): 1a builds the ephemeral event pathway only (mutation→committed-event→raw fan-out, the path 1b retires); 1b adds pace/coalescer + field-interest + retires 1a's raw broadcast in the SAME change (AGENTS: general mechanism retires the special-case). Named variants (NOT flags): raw | field-paced-live | durable-field-delta-live | durable-coalesced-log. P6e-2=crdtTextDiff/storeMapDiff/structDiff live projection; P6e-3=ordered-list-identity-reconcile (key→id); P6e-4=durable-coalesced-log (ADR #10 replay-visible spans).

## P6e-1a ephemeral event pathway — DONE (committed 7f9430c, lead self-reviewed, 685/685/0)
- [2026-07-01] Lead implemented directly (3 consecutive subagent step-limit empties on P6e-1a design-consult + implement dispatches — resume-discipline: working tree was CLEAN so empties = genuinely no work; lead stops dispatching, implements the mechanical mirror directly). The 7 design decisions owned by lead synthesis from b8 consult + verified facts + AGENTS (GPT-5.5 consult ses_0e773f06a returned empty step-limit).
- [2026-07-01] Foundation that P6e-1b retires into the paced path: `makeEphemeralHandle` (entity.mjs) exposes `.set(cells)` RE-ENTERS dispatch as `<Entity>.<field>.set` (own actionId/txn, mirrors makeLogHandle byte-faithful — `membershipTable`/`membershipOwnerCol`, `requireDispatch` guard, `mayFieldOp` auth-before-mutation, dispatch re-entry, `result.granted`→403 on deny) + `.get()` self-scoped side-table read. `ephemeralMutateHandlers` spreads into crudHandlers (emit `<Entity>.<field>.set` carrying `{owner, client, cells}`; one emission type, whole-cells snapshot replace). Projection apply `INSERT OR REPLACE INTO <Entity>_<field> (<Entity>_id, client_id, cells)` (latest-wins). eventTypes registered. Hydrate loop attaches handle for ephemeralFields. `ephemeralTableDDL` gained `cells TEXT` column.
- [2026-07-01] Auth: reuses `mayFieldOp(record, field, write, ...)` — the SAME field-auth engine (no second path). Test entity uses `grant: () => [scope(() => everyone()).can(() => grant(read, write, subscribe))]` (function-returning-array with own `.can` — `rowCapabilities` requires `typeof grant === 'function'`; bare `grant(read,write,subscribe)` denies everyone). Lesson caught at test-time: `principal({id:'alice'})` omits `type` → UnknownPrincipalTypeError (closed union) → use `principal({type:'user', id:'alice'})`.
- [2026-07-01] DECISIONLOG #68 (refines #51: ephemerality = no STRATEGIES/serialize handle, NOT no-_Log; ephemeral events DO append to _Log — drives projection + raw fan-out; 1b coalesces DELIVERY not commit). Cross-client visibility is the known 1a limitation 1b fixes (subscribe keyed by entity:id, fan-out verbatim incl. `client` field) — accepted for the explicit-to-be-retired raw path. id-strip (#62) does NOT apply (user-principal field-mutation, payload has no `id` PK pointer, bypasses scheduler/tick admission gates).
- [2026-07-01] FILES: src/entity.mjs (ephemeralFields distillation + makeEphemeralHandle + hydrate loop slot + eventTypes + projection apply branch + ephemeralMutateHandlers) + src/ddl.mjs (cells TEXT column) + NEW test/ephemeral-live.test.mjs (7 tests: descriptor sanity, hydrate attaches handle, write-grant admits+projects, latest-wins replace, client_id isolation, _Log-append verifies, fail-closed no-dispatch). Gate baseline 7e0d193=678/0 → 685/0 (+7), lead-verified raw.
- NEXT: P6e-1b `field-paced-live` — `subscribe(Entity, id, {fields, pace})` surface + field-interest normalization (restructure live.mjs's 3-map registry or add a filter layer) + field-strategy pace contract (coalescer/seq-span-reducer/profiles as DATA after re-auth+interest; `by` NOT a closure — declarative field/cell/client/stroke handle) + per-subscriber buffer/timer (re-auth BEFORE buffer; re-auth at flush if perms change mid-window) + deliver ephemeral through paced pipeline + RETIRE 1a's raw verbatim broadcast (one mechanism, AGENTS). Then P6e-2/3/4, then P7 (client lib).

## P6e-1b field-paced-live — DESIGN DONE + Slice A DONE
- [2026-07-01] GPT-5.5 consult ses_0e5c492aaf returned DECISIVE plan on all 5 forks (NOT empty — the tight pre-resolved brief worked). Lead verified every load-bearing premise against code (HP-2: serve.mjs:1041 postCommitConsumer parses ev.scope on ':' → entity:id, SELECTs MAIN doc row, re-auth against canvas doc row; HP-4: field-strategy.mjs:346 resolveStrategy error listed ephemeral as valid while throws; DECISIONLOG #15: re-auth NO-CACHE-FIRST is the decided baseline not a compromise). Resolution = DECISIONLOG #69.
- [2026-07-01] Fork resolutions (DECISIONLOG #69 full text): F1 subscribe `{entity,id,fields?,pace?}` + opaque-cells-for-1b (cells TEXT is unindexed blob, per-coordinate narrowing deferred to P6e-3); F2 `byEntity→Map<entity,Map<id,Map<conn,SubSpec>>>` SubSpec={fields,pace,latch}, buffer SEPARATE runtime state (not in SubSpec), connSubs unchanged + disconnect purges buffers; F3 SEPARATE `PACE_STRATEGIES` registry keyed by KIND (not in STRATEGIES — keeps persist/pace seams orthogonal, ephemeral has pace-without-persist); F4a per-(conn,scope,field) buffer, field from ev.eventType free, scope=re-auth unit; F4b unconditional re-run mayVerb at flush with hydrated row cached PER FLUSH-TICK (strictly safer than latch+dirty-set — a missed dirty-mark is a security hole); F4c coalesced `{seq:lastSeq, seqSpan:[first,last]}`, reduceSpan is kind's published fn, loss-safe for ephemeral (INSERT OR REPLACE latest); F5 5B ONE path, no-pace≡`pass-through` named profile (flush-on-receive+identity+inline re-auth), DELETE raw branch (5A rejected — a branch IS a path, drifts at re-auth seam). Risks: buffer key includes scope; re-auth+interest IN coalesce OUT; disconnect purges buffers+timers; commit/projection unchanged; `removed` events bypass coalescing.
- [2026-07-01] **Slice A DONE** (pace registry module, ds-v4-flash generated + lead self-reviewed, 709/709/0): NEW src/field-pace.mjs `PACE_STRATEGIES` registry (frozen, ephemeral entry: coalescers.latest-wins=(_acc,e)=>e, reduceSpan→{seq:last,seqSpan:[first,last]}, profiles pass-through/15fps{window:66,by:latest-wins}, bounds{allowedBy:['latest-wins'],maxWindow:1000}) + `resolvePace(kind)` (synthetic pass-through for absent kinds — absence = only verbatim lawful, NOT error) + `validatePaceSelection(kind,pace)` (subscribe-time fail-closed: rejects closures, profile/coalesce bounds, window≤maxWindow, 400-shaped throws). field-strategy.mjs reconcile: `resolveStrategy('ephemeral')` now throws specific "do not persist — pace seam not persist seam" + unknown-kind error no longer lists ephemeral (persist/pace seam split). NEW test/field-pace.test.mjs (24 tests). Lead caught + fixed 2 minor: unknown-profile throw 400-shaped consistency + SPEC §8.2 reference. Baseline 685→709 (+24).
- NEXT: Slice B — live.mjs surgery: Step 2 subscribe surface + interest validation (wire `{entity,id,fields?,pace?}`, validate field keys declared + ephemeral opt-in `true`-only + pace selection via validatePaceSelection, reject closures/non-indexed coords) + SubSpec storage; Step 3 `byEntity` registry reshape (Map<entity,Map<id,Map<conn,SubSpec>>>) + add/remove/removeAll + disconnect-purges-buffers; Step 4 ONE paced emit path (interest gate IN → re-auth per-flush-tick-cached row → enqueue per-(conn,scope,field) buffer → coalescer on flush OUT → send {seq:lastSeq,seqSpan}) + DELETE raw verbatim branch, pass-through=window=0 flush-on-receive identity inline-re-auth; Step 5 `removed` events bypass coalescing + full gate. ds-v4-flash generates, lead self-reviews. Then P6e-2/3/4, then P7 (client lib).
- [2026-07-01] **Slice B1 DONE** (subscribe interest + registry reshape, ds-v4-flash generated + lead self-reviewed, committed 4f06081, 720/720/0): Decomposed B into B1 (interest/registry, retires 1a firehose) + B2 (pace/coalescing, adds capability — no retire from B1, so each slice retires/adds cleanly per AGENTS). B1 scope: subscribe gains `fields` param (NO pace yet — `pace` param rejected with `'pace not yet supported'`, forward-compat); `byEntity` reshaped `Map<entity,Map<id,Map<conn,SubSpec>>>` where SubSpec={fields,latch:true}; emit interest-filters EPHEMERAL events ONLY (3-part `<Entity>.<field>.set` w/ field.kind==='ephemeral' → deliver only to subs whose SubSpec.fields[field]===true; re-auth FIRST then interest-filter per SPEC §8 ordering; all else incl `removed` pass-through to all subs). Existing fan-out unchanged → existing tests green (no existing test subscribes to ephemeral). Lead caught minor nit (dead line-214 closure-check unreachable — line 209 already rejects non-objects) — left it (not load-bearing, would churn for nothing). NEW test/live-interest.test.mjs (11 tests, raw-WS harness mirrors live-authz). ds-v4-flash return was TRUNCATED (step-limit during final reporting, no status/counts) — but tree had M src/live.mjs + ?? test/live-interest.test.mjs + lead-independent gate 720/0 (709+11) → work had landed (resume-discipline: incomplete return ≠ no work, check tree + re-gate).
- NEXT: Slice B2 — the pace/coalescing slice (heaviest, retires raw-for-paced in SAME change per AGENTS retire-rule). subscribe gains `pace` param (validated via validatePaceSelection → resolved {window,by}) stored in SubSpec; ONE paced emit path: per-(conn,scope,field) buffer + per-flush-tick hydrated-row cache + coalescer-on-flush (P6e-1b B2 work is pure live-delivery coalescing; no commit/projection changes). pass-through profile=window=0 flush-on-receive identity coalescer inline-re-auth; 15fps=window=66 by=latest-wins coalesces batch. DELETE raw-for-paced (pass-through IS the no-pace path). coalesced event `{seq:lastSeq, seqSpan:[firstSeq,lastSeq]}` (reduceSpan from PACE_STRATEGIES). `removed` bypass coalescing (revocation signal — flush immediately, never sit in stroke buffer). disconnect purges pending buffers+timers. SAFETY CAVEAT (DECISIONLOG #69 F5): if pass-through+paced can't share re-auth-row cache without 1-tick delay on pass-through, STOP + re-consult (sole fact resurrecting 5A). Then P6e-2/3/4, then P7 (client lib).
- [2026-07-01] **Slice B2 DONE** (pace/coalescing emit path, ds-v4-flash generated + lead self-reviewed + lead-fixed-2-test-bugs, committed f436878, 732/732/0): ONE paced emit path replaces raw verbatim-for-paced (retire-in-same-change per AGENTS). NEW `paceBuffers` Map (separate from SubSpec per F2). subscribe gains `pace` (validatePaceSelection('ephemeral',pace)→{window,by} in SubSpec.pace; one pace per sub applied to all interested ephemeral fields). emit: pass-through (window=0) sends immediately inline w/ `seqSpan:[seq,seq]`; paced (window>0) enqueues per-(conn,scope,field) buffer (`bufKey=${conn.id}|${scope}|${field}` — scope is re-auth unit F4a) + setTimeout(flushPacedBuffer, window). flushPacedBuffer: clears buf+timer FIRST, re-auth mayVerb fail-closed (throw/!allowed→drop silently NOT leak), coalesce via PACE_STRATEGIES.ephemeral.coalescers[by] (latest-wins=events.reduce→last), reduceSpan→{seq:last,seqSpan:[first,last]}, send ONE coalesced envelope. `removed` bypass AUTOMATIC (2-part→ephemeralField null→pass-through, never buffered). disconnect/close purge buffers+timers. Lead verified all 5 risks hold + F5 safety caveat satisfied (pass-through sends synchronously inline, no 1-tick delay vs paced). NON-BLOCKING refinement noted (authzRow refreshed per-enqueue not per-flush-tick per #69 F4b full flavor — sound for 60Hz streams, ~82ms revocation latency; tighten only if insufficient). NEW test/live-pace.test.mjs (11 tests). ds-v4-flash returned `BLOCKED` w/ 729/2 (substantial work landed) — the 2 failures were TEST bugs NOT production: (a) B1's obsolete `pace not yet supported` assertion that B2 supersedes (lead updated B1 test to B2-acceptance + invalid-pace-rejection); (b) revocation test's raw-createLiveServer db missing executeFrameworkDDL → currentSeq queried nonexistent _Cursor → threw → outer catch → 'forbidden' at subscribe (lead fixed: compiled Canvas + setActiveDb + executeFrameworkDDL). Resume-discipline reaffirmed: BLOCKED/truncated return with dirty tree = recoverable work; check tree + gate independently BEFORE re-dispatching. DECISIONLOG #70.
- **P6e-1b COMPLETE.** NEXT: P6e-2 (durable-field-delta-live — crdtTextDiff/storeMapDiff/structDiff live projection; the diffs EXIST in field-strategy.mjs but aren't connected to the live stream — P6e-2 connects them so persisted high-frequency fields deliver deltas not whole-state). Then P6e-3 (ordered-list-identity-reconcile — orderedListDiff matches by KEY not identity; P6e-3 reconciles key→id), P6e-4 (durable-coalesced-log — ADR #10 replay-visible spans for high-frequency persisted fields). Then P7 (client lib).

## P6e-2 research + GPT-5.5 consult — DONE (plan + 5 forks resolved, lead-verified)
- [2026-07-01] Lead gathered verbatim facts DIRECTLY (faster than explore-flash per 1b discipline): committed event carries NEW state only, frozen before projection (pipeline.mjs:151); `.updated` apply does NOT read prev (entity.mjs:1111-1126); postCommitConsumer re-reads NEW row, prev gone post-UPDATE (serve.mjs:1051); every kind has a `diff(prev,next,descriptor)` (field-strategy.mjs STRATEGIES); snapshot/bootstrap ALREADY handled (live.mjs:322 currentSeq + REST snapshot — out of scope); 1b's paced path coalesces EPHEMERAL only, persisted events pass-through (window=0).
- [2026-07-01] GPT-5.5 consult (ses_0e5380901) returned DECISIVE + corrected two load-bearing premises (lead VERIFIED both): (1) store/ordered do NOT ride `.updated` — they emit native delta events (`.added`/`.inserted`/etc.) whose `data` IS the delta → delta COMPUTATION needed only for value/state/crdt/struct; hash EXCLUDED (re-salt churn); store/ordered delta-native (normalize envelope key only). (2) cold shadow MUST NOT emit whole-state (double-delivers vs REST snapshot = dual-reconciliation-path trap) → cold→set-from-empty delta via the existing diff fns; shadow is committed-state never client-state.
- [2026-07-01] 5 forks RESOLVED (DECISIONLOG #71): F1=delivery-layer prev-shadow (no pipeline change; (A) pipeline-restructure REJECTED); F2=delta delivery-only never in _Log (derivable from shadow, O(1); reconnect=REST-snapshot+currentSeq not _Log-replay); F3=ONE envelope per committed event carrying `delta:{[field]:deltaObj}` map (one seq=one envelope=one dedupe; diverges from 1b's per-(conn,scope,field) buffers which are for coalescing); F4=pass-through only for P6e-2 (field-pace.mjs already forbids coalescing non-ephemeral kinds; P6e-4 stays separate — do NOT merge); F5=carry `delta` ALONGSIDE `event.data` (one-path backward-compat; `mode`-flag rejected as second path); `created` omits delta; `removed` evicts shadow.
- [2026-07-01] Plan: Slice A = NEW src/field-delta.mjs `computeDelta` pure module + tests (isolated). Slice B = prev-shadow store + wire into emit for `.updated` (Steps 2-3) + normalize store/ordered envelope (Step 4) + retire/exclude (hash skip, created omit, removed evict) (Step 5). ds-v4-flash generates, lead self-reviews per user directive.
- [2026-07-01] **Slice A DONE** (committed 24eaa47, ds-v4-flash generated + lead self-reviewed, 748/748/0, +16 from 732): NEW src/field-delta.mjs `computeDelta(entityRecord, prevRow, nextRow, changedFieldNames) → {[field]: deltaObj}`. Pure: no I/O, no arg mutation. DIFF_ELIGIBLE=`['value','state','crdt','struct']` only (the `.updated`-borne kinds); excludes hash (re-salt churn), store/ordered (delta-native via own side-table events), ephemeral (paced, 1b), log/map/blob. Skips non-eligible BEFORE resolveStrategy (never hits the ephemeral throw). Drops null deltas. Passes descriptor as 3rd arg uniformly (struct uses it via structDiff; others ignore). Cold prev normalizes `prevRow ?? {}` → diff fns return set-from-empty deltas. Defensive: unknown field name in changedFieldNames skipped. index.mjs re-exports `computeDelta`. 16 tests (value/state/crdt/struct deltas; cold-prev set-from-empty; hash+store+ordered+map exclusion; null-drop; changedFieldNames filter; defensive unknown-field). Lead-verified raw gate 748/0.
- [2026-07-01] **Pre-existing gap discovered (NOT P6e-2's to fix, flags Slice B verification):** struct fields updated via `.update` do NOT persist. entity.mjs:1119 the `.updated` projection apply does `if (descriptor.kind === 'struct') continue;` — skips writing the flattened `<field>__<cell>` columns. struct persists ONLY on create (entity.mjs:913 `insert` low-level core flattens via `flattenStruct` + the `.created` projection :1096 flattens too). So a struct field sent in `.update(id, {structField: {...}})` broadcasts in event.data (handler builds `data={...rest,id}` without filtering struct) but the cell columns aren't updated — re-read row shows the OLD create-time struct. Impact on Slice B: computeDelta's struct branch is DORMANT-but-correct (re-read shows old struct → `struct.diff(old,old)` → null → dropped; confirmed by test/field-delta.test.mjs line 27-33: unchanged struct → `{}`). Slice B wires all 4 eligible kinds over the re-hydrated row; struct naturally dormant until struct persistence on `.updated` is fixed (separate pre-existing bug, may surface in drawing-canvas link-field updates). Slice B MUST verify struct persistence as a side-question (write a test: does `.update` with a struct field persist?) + flag if broken.
- NEXT: Slice B (decomposed B1 + B2 like 1b). **B1 = Steps 2-3 (the P6e-2 driver: shadow + `.updated` delta wiring for value/state/crdt + struct dormant)** — prev-shadow store owned by live server (`Map<scope,Map<field,value>>` API getPrev/seed/evict; evict on `removed` live.mjs:415 + disconnect purge mirror pace-buffer purge live.mjs:383-389); wire into emit for `.updated` only (read prev cold→empty, nextRow=hydrated findById row already fetched for authzRow, changedFieldNames=Object.keys(committedEvent.data) minus 'id', compute delta, shadow.seed, attach `delta` to pass-through envelope); `removed`→evict+no delta; `created`→omit delta (cold→set-from-empty is natural seed but #71 F5 omits). **B2 = Steps 4-5 (store/ordered native-event normalization + pass-through invariance)** — normalize 3-part store/ordered events: present committedEvent.data under `delta` keyed by field parsed from event type; confirm persisted-coalesce-request 400s + delta envelopes carry seqSpan:[seq,seq]. Then P6e-3 (ordered-list-identity-reconcile key→id), P6e-4 (durable-coalesced-log ADR #10), then P7 (client lib).

## P6e-2 Slice A — DONE (committed 24eaa47 + docs 19e8dd5, 748/0, +16)
- [2026-07-01] NEW src/field-delta.mjs (53 lines) pure `computeDelta(entityRecord, prevRow, nextRow, changedFieldNames) → {[field]:deltaObj}`. DIFF_ELIGIBLE=Set(['value','state','crdt','struct']) (only the `.updated`-borne kinds); skips non-eligible BEFORE resolveStrategy (never hits the ephemeral throw). Imports resolveStrategy (doesn't re-declare STRATEGIES). Drops null deltas. Passes descriptor as 3rd arg uniformly. Cold prev `prevRow ?? {}` → set-from-empty. index.mjs re-exports. test/field-delta.test.mjs (16 tests: no-change→{}; value {set}; state {from,to}; crdt insert-op; struct {cells}; cold-prev set-from-empty; hash EXCLUDED; store/ordered/map EXCLUDED; changedFieldNames honored; unknown-field skip; null-delta dropped; multi-field). ds-v4-flash generated (DONE clean 748/0); lead Gate-Function-verified (tree + re-gate independently). All 8 brief rules verified.

## P6e-2 Slice B1 — DONE (committed <this>, 756/0, +8)
- [2026-07-01] live.mjs wiring (Steps 2-3). Shadow store: closure-level `prevState = new Map()` (~:76, mirrors paceBuffers ~:73, SEPARATE from SubSpec — #69 F2). Helpers prevGet/prevSeed/prevEvict. Per-SCOPE NOT per-conn (committed state shared; NOT purged on disconnect — other subs may share scope; evicted on removed + cleared on close). Delta block (~:449-472): computed ONCE per emit AFTER authzRow resolved + BEFORE sub loop. `removed`→prevEvict. 2-part `.updated`→prev=prevGet, changed=keys(data) minus id, delta=computeDelta(prev, authzRow, changed), prevSeed(authzRow). 2-part `.created`→prevSeed only (no delta). 3-part/ephemeral/store/ordered→delta undefined (B2 normalizes store/ordered). Envelope (~:511-519): pass-through builds envelope, `if (delta!==undefined) envelope.delta = delta`. Paced (window>0) UNTOUCHED (ephemeral delta===undefined by construction). close() prevState.clear().
- [2026-07-01] **5+1 test-construction bugs lead caught + fixed (NOT production):** (1) `subscribe` helper name-collided with imported `subscribe` grant-capability → renamed `wsSubscribe` (decl + 8 call sites). (2-4) tests 3/6 (state/multi-field) expected warm-shadow diffs `{from:'draft'}` but never emitted `.created` → cold `prev` → `from:undefined` DROPPED by JSON → client got `{to:'published'}`. Fix: emit `.created` (seq=1) to seed shadow before `.updated` (seq=2) (mirror production). (5) test 4 `emitRemoved` passed row=undefined but didn't DELETE the DB row → re-INSERT failed UNIQUE. Fix: DELETE after emitRemoved (mirror production: .remove handler DELETEs). (6) test 8 emitted `.updated{title:'b2'}` but never UPDATEd DB → authzRow re-read `'b1'` → delta `{set:'b1'}`. Fix: UPDATE DB before emit (mirror production: .update applies mutation BEFORE post-commit re-read). LESSON: raw-WS direct-emit harness MUST replicate the FULL production post-commit sequence (mutation applied to DB → re-read hydrated → emit); emit helpers re-read from DB so the DB MUST reflect committed state BEFORE emit.
- [2026-07-01] Lead self-review verified all 5 #71 risks hold: (1) ONE seq=ONE envelope (delta computed once; test 6 multi-key single-envelope) ✓; (2) shadow=committed-state (prevSeed uses authzRow) ✓; (3) removed evicts shadow (prevEvict :461 + close :564; test 4 delete-then-recreate→set-from-empty) ✓; (4) crdt/struct full prev (authzRow=hydrated re-read) ✓; (5) NO commit/projection change (only live.mjs) ✓; (6) hash excluded ✓; (7) store/ordered untouched (3-part skip delta block — B2) ✓; (8) pass-through invariance (.updated 2-part non-ephemeral → window=0) ✓. struct-persistence side-question: dormant-but-correct on 2nd+ update (re-read old struct → diff null); 1st-update-after-cold-shadow → set-from-empty delta reflecting old persisted struct (manifestation of pre-existing entity.mjs:1119 gap, NOT P6e-2's fix). No test exercises struct .update. No test asserting broken struct-persistence added (anti-pattern — the fix will add it).
- NEXT: P6e-2 B2 (Steps 4-5) — normalize store/ordered native events (present committedEvent.data under `delta` keyed by field parsed from 3-part event type) + confirm pass-through invariance (persisted coalesce request 400s; delta envelopes carry seqSpan:[seq,seq]). Then P6e-3 (ordered-list-identity-reconcile key→id), P6e-4 (durable-coalesced-log ADR #10), then P7 (client lib).

## P6e-2 B2 + P6e-2 COMPLETE — DONE (committed + docs, 763/763/0, +7 from 756)
- [2026-07-01] ONE else-if branch added to B1's delta block (live.mjs ~:471-477): `else if (parts?.length===3 && ephemeralField===null) { delta = { [parts[1]]: committedEvent.data }; }`. store/ordered native events (`.added`/`.roleChanged`/`.removed`/`.inserted`/`.moved`/`.reordered`/`.appended`) are DELTA-NATIVE — their `event.data` IS the structural delta (no diff computed, #71 risk #7). `ephemeralField===null` excludes paced ephemeral `.set`. The existing pass-through envelope (~:511-519 `if(delta!==undefined) envelope.delta=delta`) picks it up — NO envelope/paced/pipeline/entity/ddl change. `log()` is kind 'store' (field.mjs:151) → its `.appended` normalized too. ONE seq=ONE envelope (delta map). Pass-through invariance: `ephemeralField===null` → window=0 → seqSpan:[seq,seq] inline. ds-v4-flash generated (DONE 763/0, ACCURATE — lead re-gated: diff=exactly 6 specified lines, no overreach).
- [2026-07-01] NEW test/live-delta-native.test.mjs (7 tests): map .added/.roleChanged/.removed + ordered .inserted/.moved delta shape+event.data+seqSpan; pass-through invariance (pace subscriber gets non-ephemeral inline seqSpan:[1,1]+delta); backward-compat (both delta AND event). Entity Doc{title, collaborators:map(ref('User'),{role}), items:list(text())}. 0 test-construction bugs (B2 simpler: no shadow/no prev/direct emit). Lead self-review: all 8 #71 risks ✓.
- [2026-07-01] **P6e-2 COMPLETE (`durable-field-delta-live`).** Slice A (field-delta module, 24eaa47) + B1 (shadow+`.updated` delta, 7e5cd6a) + B2 (store/ordered normalization). Baseline 732→748→756→763. The collaborative-crdt-text driver + store/ordered delta-native both live. struct-`.updated`-persistence gap flagged (pre-existing entity.mjs:1119, NOT P6e-2's fix). P6e-4 (durable-coalesced-log ADR #10) is the SEPARATE persisted-delta-coalescing slice — do NOT merge (#71 F4). DECISIONLOG #73.
- NEXT: P6e-3 (ordered-list-identity-reconcile — orderedListDiff matches by KEY not identity; reconcile key→id), then P6e-4 (durable-coalesced-log ADR #10 replay-visible spans for persisted deltas), then P7 (client lib).

## P6e-3 `ordered-list-identity-reconcile` — DONE (RESOLVED AS VESTIGIAL: deleted orderedListDiff; 761/761/0, net -2 tests)
- [2026-07-01] Lead fact-gathering surfaced a genuine architecture fork (AGENTS hard-gate): `orderedListDiff` was (a) broken-by-design (matched by volatile `key` not stable `id`), (b) DEAD CODE (only caller field-delta.mjs:45 excluded by DIFF_ELIGIBLE — never reached; zero production callers; only 2 unit tests asserted the broken behavior), (c) superseded by the delta-native per-op events (P6e-1b B2 normalized store/ordered native events under `delta:{[field]:event.data}`). Lead's load-bearing observation: orderedListDiff's INPUT SHAPE (`{key,...item}` objects) was itself wrong vs the side-table's `(id,key,item)` rows — a whole-list snapshot diff is a category error for a fractional-index keyspace.
- [2026-07-01] GPT-5.5 consult (ses_0e4e6c7bb) ruled **B — VESTIGIAL**: delete `orderedListDiff`; ordered's "diff machinery" (SPEC §5.3) = the native per-op identity-keyed events (`.inserted`/`.moved`/`.reordered`/`.removed` carrying `id`) + the operation API + the side-table projection. A separate snapshot diff would be a forbidden SECOND reconciliation path (AGENTS one-path). Option A (fix key→id) REJECTED (preserves unused shape); Option C (defer, leave in place) REJECTED (broken contract-shaped artifact is a latent trap). Lead VERIFIED the consult's one flagged premise (no public export exposes orderedListDiff — index.mjs exports only resolveStrategy/validateMutation/ValidationError).
- [2026-07-01] **Deletion (lead implemented directly — mechanical, no subagent dispatch):** DELETED `orderedListDiff` function (field-strategy.mjs) + REMOVED the `diff` property from the ordered strategy (now `{validate, apply}` — absent `.diff` is the structural declaration of "ordered is per-op, not snapshot"; NOT a throwing fn — declaration absorbs imperative wiring) + DELETED 2 field-merge.test.mjs ordered-diff tests (asserting broken-by-key behavior) + REWROTE field-strategy.test.mjs shape-assertion (value/store/crdt assert {validate,apply,diff}; ordered asserts {validate,apply} + diff===undefined — the assertion of absence IS the positive contract) + UPDATED 4 comments. Only `strategy.diff(` caller (field-delta.mjs:45) is guarded by DIFF_ELIGIBLE → never reaches ordered's absent `.diff`. Net -2 tests (763→761), -~25 lines. Suite 761/0, lead-verified raw.
- [2026-07-01] **P6e-3 REMOVED from roadmap as a standalone slice** (it was a vestige). P6e-4 is the prospective consumer that decides IF ordered snapshot-reconciliation is ever needed (replay unit, coalescing semantics, inverse semantics, two-snapshot authority model) — ONLY if P6e-4 proves a real two-snapshot consumer is a snapshot reconciler introduced, named as snapshot reconciliation NOT as ordered's primary diff contract. DECISIONLOG #74.
- NEXT: P6e-4 (durable-coalesced-log ADR #10 replay-visible spans for persisted deltas — the slice that decides ordered's coalescing/replay shape + whether snapshot-reconciliation is ever needed), then P7 (client lib).

## P6e-4 DEFERRED (split + consumer-gated) — DONE (DECISIONLOG #75; docs only, no runtime code; 761/0)
- [2026-07-01] Lead gathered verbatim facts DIRECTLY (ADR #10 = DECISIONLOG #10 + SPEC §10:526-529 "coalesced-snapshot-with-sequence-span for high-frequency persisted fields"; SPEC §7.1 sequence-cursor replay; SPEC §8.2 two-layer; pipeline.mjs:144-152 _Log append = 1 row/event verbatim, pre-projection-freeze; field-pace.mjs PACE_STRATEGIES ephemeral-ONLY + resolvePace synthetic pass-through for absent kinds + validatePaceSelection rejects `by` for `allowedBy:[]` kinds → persisted fields CANNOT be paced today; P6e-1a/b ephemeral pace DELIVERY shipped; P6e-2 persisted deltas pass-through seqSpan:[seq,seq] shipped).
- [2026-07-01] GPT-5.5 consult (ses_0e4d42a5d) ruled **C — SPLIT + DEFER both sub-slices until a real high-frequency persisted consumer exists; proceed to P7.** Lead VERIFIED all 5 load-bearing premises against code. The two concerns #71 bundled are DISTINCT SEAMS: (1) DURABILITY-side (commit/_Log snapshot+seqSpan coalescing — touches pipeline.mjs:144-152 + field persistence strategy + replay/resync); (2) DELIVERY-side (extend PACE_STRATEGIES for persisted kinds with structural coalescers — crdt op-composition, value latest-wins — touches field-pace.mjs + live.mjs). NOT build-now (A): NO real high-frequency persisted consumer — space-invaders match.mjs ASPIRATIONAL (tick commented "GAP"; invaders/projectiles = text() "catastrophic at 30Hz" comment = documented unresolved pain NOT running field); tick tests ONE-SHOT state transitions (row exits while-set after firing) NOT 30Hz persisted; only real high-freq case (drawing strokes) EPHEMERAL (shipped); crdt text human-rate (shipped). AGENTS build-no-further. NOT single-defer (B): keeps distinct seams bundled. NOT P6e-3-style non-slice (D): P6e-4 is UNBUILT ADR-backed slot (no dead/wrong code to delete — NOT "this was wrong" but "real ONLY when a consumer proves it" = SPECIFIED-NOT-BUILT CONSUMER-GATED).
- [2026-07-01] **SPEC §7.1 reconciliation (load-bearing design constraint for FUTURE durability slice):** snapshot+seqSpan _Log row REPLACES state (doesn't fold reducer outputs) — §7.1 says "resync folds missing events through reducers in order; NEVER replaces the snapshot wholesale (except bootstrap)." LAWFUL ONLY when the kind's reducer IS replace — value/state latest-wins (+ struct IF declared latest-wins). CRDT NOT lawful by default (coalescing insert/delete ops destroys per-op intent: causal ordering/tombstones/concurrent-merge the crdt reducer needs); crdt may use a future checkpoint ONLY if it publishes causal-frontier/version semantics. Future design MUST say "only kinds that publish a lawful durable-checkpoint reducer may use snapshot+span rows," + replay treats a checkpoint as reducer input not out-of-band replace.
- [2026-07-01] **Load-bearing FUTURE premise (lead VERIFIED):** a 30Hz persisted `.updated` writes the entity ROW every time (entity.mjs:1111-1126 `UPDATE...WHERE id` — 1 row-write/event) + `_Log` every time (pipeline.mjs:144 — 1 INSERT/event). So `_Log` coalescing ALONE does NOT solve the 30Hz DB pain — future durability slice may need to coalesce THE FIELD PERSISTENCE SEAM ITSELF (entity-row UPDATE cadence), not merely the _Log projection. The future consumer's actual bottleneck (DB row-write vs _Log append vs wire) determines which seam(s) to coalesce.
- [2026-07-01] **Trigger to reopen:** a REAL high-frequency persisted consumer — space-invaders tick.hz(30) loop driving persisted game-state at 20-30Hz durable commits across hours, OR a new project. Consumer must make clear: field kind, write rate, actual bottleneck (DB/log vs delivery vs both), whether final-state snapshots preserve semantics. Verification MUST include a recurring-persisted-mutation integration test (NOT one-shot tick transitions).
- [2026-07-01] **P7 does NOT depend on P6e-4.** P6e-2 pass-through = durable `seq` + `seqSpan:[seq,seq]` (single-event span) + ordered delivery + delta-native — the COMPLETE contract a client LiveList reducer/cursor needs. P7 SHOULD implement a span-aware cursor GENERALLY (accept [first,last] — treat any seq in span as seen, advance to last+1) even though TODAY every span is one event wide → client is forward-compatible with future P6e-4 coalesced-span WITHOUT requiring it to exist. No runtime code changed for P6e-4 (`git diff` = docs only). **P6e LIVE-ADVANCED ARC EFFECTIVELY COMPLETE** (P6a/b/c/d shipped; P6e-1a/b + P6e-2 + P6e-3 RESOLVED + P6e-4 DEFERRED-consumer-gated). DECISIONLOG #75.
- NEXT: P7 (client lib — LiveList/LiveChannel reducer + seq-cursor + span-aware replay + bootstrap-before-stream ordering §7.1 mandates structurally). NOT STARTED — needs fact-gathering (SPEC §7.1, §8.1/§8.2, §12 LiveList bootstrap, the existing live envelope contract + any existing client code in projects/*) before the design.

## P7 client lib — DESIGN DONE (DECISIONLOG #76; user confirmed optimistic-apply per AGENTS mandate)
- [2026-07-01] Lead fact-gathering (verbatim DIRECT reads — the 1b/P6e-2/P6e-3/P6e-4 technique; explore-flash empty-returned on 1a). Verified: WS envelope `{type:'event',entity,id,seq,seqSpan:[first,last],event,delta?}` (live.mjs:519-526; event=frozen committedEvent with actionId pipeline.mjs:151); `subscribed` ack carries `currentSeq` (live.mjs:123-128 + :336 — designed for the snapshot-vs-live race-close, the consult surfaced it, lead had missed); REST = 5 CRUD routes per entity (app.mjs mount via `routes:(r,Entity)=>...`), NO `/dispatch` endpoint, field sub-routes (`/docs/:id/collaborators`) are APP-IMPERATIVE (app.mjs:36-37); `/snapshot`→`{snapshot,seq}`, `/events-since?cursor=N`→`{events}` OR `{resync:'stale',reason:'cursor-behind-retention'}`; §7.1 cursor protocol (dup/gap/next; reducer fold = source of truth never wholesale-replace except bootstrap; stale→forced re-bootstrap; bootstrap-before-stream LOAD-BEARING); auth = session cookie (`sessionPrincipalOf`), browser auto-sends same-origin Origin on fetch+WS, `credentials:'include'`, lib doesn't manage auth. No existing client lib anywhere (public/ has only files.html); P7 greenfield.
- [2026-07-01] GPT-5.5 consult (ses_0e4c5c705) ruled all 9 forks; lead VERIFIED every premise against code; user CONFIRMED the one product call (optimistic-apply, the AGENTS-mandated design — consult REVERSED lead's "no optimistic" Fork-3 misread). 9 forks: (1) `createLiveStore({baseUrl,name})` factory + export LiveChannel/LiveList primitives; (2) client-side CRUD verb-map (no `/dispatch`), one `decodeResult` shared by dispatch-fetch + resync-fetch, `store.action()` for app-imperative field routes; (3) OPTIMISTIC apply (AGENTS.md:61-63 + SCOPE-FINDINGS §2.2 — dispatch captures preimage, applies visible placeholder, fires REST, rollback on failure, committed event folds via ONE `ingest` path clearing the placeholder by actionId); (4) LiveChannel=transport-per-origin / LiveList=doc-reducer (room = LiveList on the entity); (5) span-aware cursor ship now (P6e-4 forward-compat, ~5 lines); (6) fixed kind-aware reducer ships in lib (NOT page-supplied); (7) resync=snapshot-refetch+cursor-reset+KEEP stream (not WS-reconnect), queue stream events during resync fold; (8) bootstrap=promise-gate+queue-until-ready+USE `currentSeq` from subscribed ack to resync the race-gap before releasing; (9) 3 slices: A=LiveChannel, B=LiveList, C=createLiveStore+dispatch+optimistic.
- [2026-07-01] DECISIONLOG #76 (full text). NEXT: Slice A (LiveChannel transport) — NEW public/express-plus-client.mjs (LiveChannel) + test/live-channel.test.mjs. ds-v4-flash generates per lead brief, lead self-reviews.

## P7 Slice A `LiveChannel` transport — DONE (committed 28a4afc; leak fix cc996cc; orphan 34c47d7; 773/773/0)
- [2026-07-01] NEW `public/express-plus-client.mjs` (303 lines) — `LiveChannel` class: ONE WebSocket per origin, multiplexed across (entity,id) subscriptions, uses Node global `WebSocket` (undici, Node 22+), zero deps. Protocol matches src/live.mjs verbatim (`subscribe`/`unsubscribe`/`subscribed`/`unsubscribed`/`event`/`error`). `subscribe(entity,id,onEvent)→Promise<{currentSeq}>` (resolves on `subscribed` ack, rejects on `error` envelope); `unsubscribe` resolves on ack or 2s unref timeout; `close()` tears down (idempotent, no further reconnect/delivery). Lazy socket-open on first subscribe; exponential-backoff auto-reconnect (base 200ms, max 5s) that re-subscribes all active subs on reconnect (server loses state on drop); outbox buffers sends before socket open; watchdog interval (100ms, unref) detects sockets stuck in CLOSING that never fire 'close' and forces reconnect. All timers unref'd. NEW test/live-channel.test.mjs (6 e2e tests: subscribe-ack+currentSeq; server-mutation→envelope; unsubscribe-stops-delivery; multiplex-isolation; reconnect-resumes; close-cleanup).
- [2026-07-01] **LATENT SERVER BUG FIXED (leak fix cc996cc, RFC-6455 parser):** LiveChannel was the FIRST graceful WS client (existing e2e tests hard-destroy a raw-TCP socket, never exercising the close handshake). undici sends a masked ZERO-LENGTH close frame (`88 80 <4-byte mask>` = 6 bytes) on `close()`. src/websocket.mjs `FrameParser.#processBuffer` looped `while(buffer.length>0)` — a zero-length payload's mask bytes drain the buffer to 0, so the loop EXITED before the PAYLOAD state finalized the empty frame → opcode 0x8 never emitted → server never acked → undici stuck readyState 2 (CLOSING) forever → 4 leaked sockets → `node --test` file wrapper hung ~140s. Fix: loop condition now `while(buffer.length>0 || (state===PAYLOAD && payloadRead===payloadLen))` (finalizes zero-length frame). Complementary fix (src/live.mjs #drain opcode-0x8 branch): wrap ack-frame write in try/catch + route through `#close()` (destroys socket) instead of bare `#cleanup()`. Regression test added to test/websocket.test.mjs (verified RED without the parser fix). LESSON: a graceful-close handshake path is only exercised by a well-behaved client; hard-destroy test clients mask half-open-close server bugs.
- [2026-07-01] **Orphan recovered (34c47d7):** test/multi-hop-inherit.test.mjs (uncommitted, lost in crash) — a legit 5-test gate proving 3-hop `inherit` composition (Feed←Episode←AudioAsset←WordTimings) compiles + scopes correctly (nested EXISTS to Feed_subscribers, fail-closed for stranger/anonymous, admits through BOTH scope-SQL and runtime mayVerb). Exercises real scope-sql.mjs `lower2` recursion that inherit.test.mjs (1-hop) doesn't. References a `data-model-v1.md`/'council verdict' that exist nowhere (harmless aspirational podcast-platform comments). Committed separately with honest orphan-provenance message.
- [2026-07-01] Gate: 761 (HEAD) → 773/773/0, 0 cancelled, 12.9s, lead-verified raw. DECISIONLOG #77.
- NEXT: Slice B (`LiveList` doc-reducer) — bootstrap-before-stream (snapshot→cursor→WS-subscribe→ack→race-gap-resync→release queued events); span-aware cursor (dup-skip/gap-resync/apply+advance-to-last+1); FIXED kind-aware reducer in-lib (`.created`→upsert, `.updated`→data-fields+per-kind delta {value.set/state.from-to/crdt-op-list/struct.cells}, `.removed`→delete, 3-part store/ordered→delta[field]); resync=snapshot-refetch+cursor-reset+KEEP stream (queue during fold); stale→forced re-bootstrap. Delta shapes mirror src/field-delta.mjs + src/field-strategy.mjs EXACTLY. FAKE channel in tests. NEW test/live-list.test.mjs. Then Slice C (createLiveStore + dispatch + optimistic-apply). ds-v4-flash generates, lead self-reviews.

## P7 Slice B `LiveList` doc-reducer — DONE (committed c385c84; 785/785/0, +12)
- [2026-07-01] `export class LiveList` in public/express-plus-client.mjs (LiveChannel untouched). ds-v4-flash generated (ses_0e45d0459 DONE 785/0), lead self-reviewed + fixed the numeric-sort bug. ONE fold path (`_ingest`→`_applyEvent`; live envelopes + events-since rows converge). Bootstrap: snapshot→cursor→WS-subscribe(queue-while-!ready)→race-gap-resync(if ack.currentSeq>cursor)→drain queue→render→resolve. Span cursor: dup-skip/gap-resync/apply+advance-to-seqSpan[1]. Kind-aware reducer mirrors field-delta.mjs+field-strategy.mjs (created/updated+per-kind-delta/removed + 3-part ordered/map/log native ops). Resync: stale→re-bootstrap, events→fold-bypass-span, keeps stream open, queues during fold. onRender(cb) + `ready` promise. FakeChannel+fakeFetch in tests (no real server).
- [2026-07-01] **Lead review fix (substantive):** ordered sort was `localeCompare` (string) — WRONG. Server key column is `key REAL` (ddl.mjs:108) sorted by SQLite `ORDER BY key` (numeric); keyBetween produces numbers (entity.mjs:588). String sort orders 2 after 10 → divergence. Fixed to `a.key-b.key`. Test rewritten to numeric keys incl. divergence pair 2/3/10 (RED under localeCompare, verified). DECISIONLOG #78.
- NEXT: Slice C (`createLiveStore({baseUrl,name})` factory + dispatch client-CRUD-verb-map [NO /dispatch endpoint: POST/PATCH/DELETE] + shared decodeResult + OPTIMISTIC-APPLY [preimage→visible placeholder→REST→rollback-on-fail→committed event folds via ONE LiveList _ingest clearing placeholder by actionId; dispatch never throws, returns Result]; store.subscribe(id)→LiveList, store.one/create/update/remove sugar, store.action(type,{method,path}), store.close()) + test/live-store.test.mjs. Per #76 Forks 1/2/3. ds-v4-flash generates, lead self-reviews.

## P7 Slice C `createLiveStore` — DONE (committed 3a32c1e; 797/797/0, +12) — **P7 CLIENT LIB COMPLETE**
- [2026-07-01] `createLiveStore({baseUrl,name,path})` + `decodeResult` in public/express-plus-client.mjs (LiveChannel+LiveList untouched). ds-v4-flash generated (ses_0e443a335 DONE 797/0), lead self-reviewed + fixed 1 contract bug. Factory shares ONE LiveChannel across subs; URL derivation from baseUrl+name+path. API: subscribe(id)→cached LiveList; dispatch(type,payload)→Promise<Result> (NEVER throws); create/update/remove sugar; action(type,{method,path}); close(); overlayFor(id); pendingCreates(); onRender(cb). Client CRUD verb-map (NO /dispatch): create→POST 201+row, update→PATCH 200+row, remove→DELETE 204; credentials:'include'. ONE shared decodeResult for dispatch + action routes.
- [2026-07-01] **Optimistic-apply = Option C** (gpt-5.5 consult ses_0e4460a07): overlay is a SEPARATE visible projection layer (Map opId→entry) rendered ON TOP of LiveList.state; NEVER writes LiveList._state — `_ingest` stays the SOLE canonical writer + cursor-advancer (AGENTS one-reconciliation-path: visible placeholder, not a 2nd apply path). Confirmed overlay does NOT clear on REST success — only when the matching canonical state is observed through `_ingest` (avoids the HTTP-before-WS flashback). create keyed by client opId, retargeted to real id on 201; remove 204→confirmed-removed kept until _ingest folds .removed. Result: `{ok:true,status:'committed',opId,id?,row?}` | `{ok:false,status:'failed-rolled-back',opId,error}`.
- [2026-07-01] **Lead review fix:** dispatch threw synchronously on closed-store — violated never-throw. Fixed to return failed Result; rewrote the generated test that asserted the wrong (throwing) contract. **Known imprecision (documented, beta-acceptable):** _clearConfirmedOverlays clears on ANY LiveList onRender with matching-kind state → an unrelated committed event can clear an update-overlay one tick early (brief flashback). Precise fix needs seq/actionId correlation the CRUD REST responses don't carry. DECISIONLOG #79.
- **P7 COMPLETE:** Slice A LiveChannel (28a4afc) + Slice B LiveList (c385c84) + Slice C createLiveStore (3a32c1e). Full collaborative-realtime client SDK in public/express-plus-client.mjs (zero deps, injectable fetch+channel).
- NEXT: consult the P-series roadmap for what follows P7 (P8+). The live-advanced arc (P6) + client lib (P7) are shipped. Candidate next work: a real end-to-end app in projects/* exercising the full stack (server + client SDK) as an integration gate; OR the deferred P6e-4 durable-coalesced-log IF a real high-frequency persisted consumer emerges (DECISIONLOG #75 trigger).

## P7 hardening — overlay clear precision + CRUD commit headers (DONE; DECISIONLOG #80; 814/814/0)
- [2026-07-01] Fixed the #79 known imprecision instead of leaving it as beta debt. `src/serve.mjs` now emits `x-express-plus-action-id` and `x-express-plus-seq` on create/update/remove responses. The seq is the max committed event seq for the mutated row scope, not just the first event, so overlay retirement waits for the returned row's whole committed scope to be folded. CORS allowlisted responses expose those custom headers with `access-control-expose-headers`.
- [2026-07-01] `public/express-plus-client.mjs` stores `confirmedSeq` on optimistic overlay entries. Confirmed update/remove overlays clear only after the corresponding LiveList cursor reaches `confirmedSeq`; older live renders no longer clear them early. Confirmed create overlays are retired on HTTP confirmation to avoid no-subscription overlay leaks; the returned row remains available in the success Result and subscribed lists still fold the later `.created` event through `_ingest`.
- [2026-07-01] Tests added/updated: `test/live-store.test.mjs` covers create retirement, update older-event survival, remove older-event survival; `test/http-crud.test.mjs` covers CRUD action/seq response headers; `test/ops-defaults.test.mjs` covers CORS exposing the headers. Targeted gate: `node --test test/live-store.test.mjs test/http-crud.test.mjs test/ops-defaults.test.mjs` → 36/36/0. Full gate: `node --test` → 814/814/0, 0 fail.
- NEXT: commit this hardening slice, then choose next roadmap item. Candidate next work remains a real projects/* integration gate for the shipped server+client stack, unless a real high-frequency persisted consumer appears and reopens P6e-4 per DECISIONLOG #75.

## Scope-support Slice 1 `json(shape)` fields — DONE (worktree branch; 821/821/0)
- [2026-07-01] Added `json(shape = null, options = {})` as a value-kind field descriptor exported from `src/index.mjs`. The slice is whole-value only: fields validate that assigned JS values are JSON-compatible, serialize to SQLite `TEXT` with `JSON.stringify`, and deserialize back to objects/arrays for HTTP/query-facing rows. The optional `shape` is retained as declaration metadata for future path/index/schema work; app-specific checks stay in the existing `validate` option instead of introducing a schema DSL.
- [2026-07-01] `src/entity.mjs` now exposes `record.deserializeRow(row)` for stored value fields and uses it during trusted query hydration; `src/serve.mjs` applies it to CRUD list/read/mutation response rows and live snapshots before auth/output so `.can` and transport code see public JSON values, not raw storage strings. Whole-json scope equality uses the existing `serializeField` path, so writes and predicates bind the same stored representation.
- [2026-07-01] Tests added/updated: `test/field-type.test.mjs` covers the descriptor; `test/field-strategy.test.mjs` covers structural validation, serialization, and deserialization; `test/json-field.test.mjs` covers CRUD roundtrip/TEXT storage, scope equality, trusted query hydration, and snapshot output. Focused gate: `node --test test/json-field.test.mjs test/field-strategy.test.mjs test/field-type.test.mjs` → 34/34/0. Full gate: `node --test` → 821/821/0.
- NEXT: commit the json slice after review, then continue scope-support with request body parsing / multipart upload support, followed by project membership acceptance / child inherit proof.

## Scope-support Slice 2 request body parsing — DONE (worktree branch; 826/826/0)
- [2026-07-01] Replaced the server's JSON-only body reader with one shared capped body reader plus content-type-aware parsing. JSON remains the default for empty/JSON requests; `application/x-www-form-urlencoded` parses repeated fields into arrays; `multipart/form-data` parses small buffered text fields and file parts into `req.body` for imperative handlers. Multipart stays under the existing 1mb request-body cap and is intended for browser forms, not large binary transfer; `/blobs` keeps the raw body route and its separate upload limit.
- [2026-07-01] Entity CRUD and job queue endpoints call the same parser in JSON-only mode, so form posts do not flow into entity validation/storage by accident. Imperative routes use the wider browser-form mode after the route gate has admitted the request. Form assignment uses own properties for special field names like `__proto__`, preventing prototype mutation while preserving the submitted field as data.
- [2026-07-01] The multipart parser is deliberately small and split-based for this beta form-post seam: it handles ordinary browser multipart bodies under the cap, not every RFC edge case or file contents containing the boundary delimiter. Handlers that need large or adversarial binary upload should use `/blobs` instead.
- [2026-07-01] Tests added/updated: `test/imperative-route.test.mjs` covers JSON, urlencoded fields, repeated fields, multipart text/file parts, `__proto__` field safety, and unsupported content-type 415s; `test/http-crud.test.mjs` covers entity CRUD rejecting urlencoded forms with no stored row; existing middleware/blob/CRUD tests cover oversized body handling, malformed JSON handling, blob upload behavior, and entity CRUD still working. Focused gate: `node --test test/imperative-route.test.mjs test/middleware.test.mjs test/blob-upload.test.mjs test/http-crud.test.mjs` → 33/33/0. Full gate: `node --test` → 826/826/0.
- NEXT: commit the request body parsing slice after independent review, then continue scope-support with project membership acceptance / child inherit proof.

## Scope-support Slice 3 project membership child inherit proof — DONE (worktree branch; 830/830/0)
- [2026-07-01] Added a Scope-shaped `Project` + `ProjectDocument` acceptance proof. `Project` owns membership through an `owner` ref plus `members: map(ref('User'), { role:['viewer','editor'] })`; `ProjectDocument` declares `grant(inherit(Project, { via:'project' }))`. The test seeds one shared project and one private project, proves a member lists/reads only the shared child document, proves a stranger sees no child rows and gets 404, and proves a member receives live child events through the inherited project membership.
- [2026-07-01] Fixed the inconsistent inherited-child REST read path. List, snapshot/events-since, and subscribe already authorize inherit/no-own-`.can` children by the compiled read-scope alone; direct `GET /documents/:id` selected through that same read-scope but then unconditionally ran child `mayVerb('read')`, which fails closed because inherited children intentionally have no own `.can`. REST read now mirrors the other read surfaces: run runtime `mayVerb` only when the entity owns a `.can` body.
- [2026-07-01] Fixed live re-authorization to match subscribe-time authorization for inherited children. Subscribe-time already admits through the bound read-scope and skips `mayVerb('subscribe')` for no-own-`.can` children; fan-out and paced flush now use the same `hasOwnCanGrant` guard so an inherited child subscription is not accepted and then silently starved when events are delivered.
- [2026-07-01] This slice intentionally does not propagate parent write capabilities to child update/remove, and does not redesign the pre-existing child-create seam. Inherited child writes still need a separate parent-capability design; widening write auth here could let read-only project members mutate child rows. The proved seam is read/list/snapshot/live delivery consistency for project-style membership inheritance.
- [2026-07-01] Tests added/updated: new `test/project-membership-inherit.test.mjs` covers membership-backed child list, read, non-member denial, and live event delivery. Focused gate: `node --test test/project-membership-inherit.test.mjs test/membership-acceptance.test.mjs test/membership-scope.test.mjs test/inherit.test.mjs test/multi-hop-inherit.test.mjs test/http-crud.test.mjs test/live-authz.test.mjs` → 32/32/0. Full gate: `node --test` → 830/830/0.
- NEXT: commit this auth alignment slice after independent review, then continue Scope-support by choosing the next real `projects/*` stress-test gap instead of adding another duplicate auth proof.

## Scope-support Slice 4 value range predicates — DONE (worktree branch; 834/834/0)
- [2026-07-01] Added inclusive `.gte(value)` and `.lte(value)` predicates to the existing value field handle in `src/scope-sql.mjs`. They lower through the same closed AST and parameterized SQL path as `.is()` and `.in()`, and serialize values through `serializeField`, so `date()` predicates bind epoch milliseconds just like writes do. No `.gt/.lt` operators were added because the known Google Photos timeline use case only needs inclusive bounds.
- [2026-07-01] The new range ops are available both in grant scopes through `fields.<name>.gte/lte(...)` and in trusted query API predicates through `Entity.<field>.gte/lte(...)`. Non-value fields, map whole-values, struct whole-values, and missing fields keep fail-closed behavior by exposing throwing `.gte/.lte` methods alongside the existing throwing `.is/.in/.isNull` methods.
- [2026-07-01] Tests added/updated: `test/scope-sql.test.mjs` covers date serialization into `>=`/`<=` SQL params, undefined range bounds lowering to FALSE, and fail-closed range ops on CRDT fields; `test/query-api.test.mjs` covers the route-shaped timeline query `Event.startsAt.gte(from).and(Event.startsAt.lte(to))` with sorted results. Focused gate: `node --test test/scope-sql.test.mjs test/query-api.test.mjs` → 23/23/0. Full gate: `node --test` → 834/834/0.
- NEXT: commit this range predicate slice after independent review, then choose the next `projects/*` gap. Likely larger candidates are Google Photos cross-entity typed-FK traversal (`Photo.album.collaborators...`) or photo-editor projected/raster/list seams; do not fold those into this predicate slice.

## Scope-support Slice 5 typed-FK membership traversal — DONE (worktree branch; 837/837/0)
- [2026-07-01] Implemented Google Photos-style declared-check traversal from a non-role ref to a target map membership: `Photo.album.collaborators.has(principal.id)` now compiles through the existing field handle/check registry to the existing `existsMembership` lowering, correlated as `Album_id = t0.album` instead of only `Entity_id = t0.id`. Runtime declared checks use the same active entity registry to make `Photo.album.collaborators.get(principal.id)?.role` available to `.can` bodies.
- [2026-07-01] Ref-role fields remain raw identity values in runtime checks so existing derived role checks (`owner`, `author`) keep their FK-equality behavior. This slice only exposes target map membership through non-role refs; arbitrary target scalar traversal and hydrated-row ref population remain future seams.
- [2026-07-01] Tests added/updated: `test/membership-scope.test.mjs` covers SQL lowering for `Photo.album.collaborators.has(...)` and pins ref-role fields as raw identity handles with no target-map traversal; `test/membership-acceptance.test.mjs` covers SQL scope and runtime `.can` agreement for album member/editor grants, including null and dangling album FKs denying in both layers. Focused gate: `node --test test/membership-scope.test.mjs test/membership-acceptance.test.mjs test/check-registry.test.mjs` → 17/17/0. Full gate: `node --test` → 837/837/0.

## Scope-support Slice 6 runtime ref scalar traversal — DONE (worktree branch; 840/840/0)
- [2026-07-01] Made runtime non-role ref handles thenables: `await entity.canvas` fetches the target row and exposes target scalar fields alongside map handles, enabling photo-editor patterns like `canvas.owner === principal.id` and `canvas.collaborators.get(principal.id)?.role === 'editor'` in async declared check bodies. Sync map access (`entity.canvas.collaborators.has(...)`) continues to work without `await` as before.
- [2026-07-01] Added `entity` key to the declared check context so both `({ entity, principal })` and `({ Photo, principal })` destructuring styles work. The `entity` key carries `runtimeSelf` — the same object the entity-name key carries.
- [2026-07-01] Compile-time ref handles are unchanged: no scalar field traversal in scope. The thenable is runtime-only; scope predicates cannot `await`.
- [2026-07-01] Tests added: `test/check-registry.test.mjs` covers thenable resolution to target scalar fields with E2E mayVerb verification, and `entity` key availability in check context; `test/membership-acceptance.test.mjs` covers photo-editor-style layerOwner/layerEditor checks with null-FK fallback. Focused gate: `node --test test/membership-scope.test.mjs test/membership-acceptance.test.mjs test/check-registry.test.mjs` → 20/20/0. Full gate: `node --test` → 840/840/0.
- [2026-07-01] Remaining gaps: Google Photos timeline route placeholder still says `.gte/.lte (planned)` even though the predicates shipped; hydrated-row ref population (field `.can` and handler code doing `const canvas = await row.canvas`) remains a broader future seam.

## Scope-support Slice 7 Google Photos acceptance test — DONE (worktree branch; 852/852/0)
- [2026-07-01] Added a comprehensive E2E acceptance test proving the full Google Photos album+photo authorization spine works against a real node:sqlite DB and real HTTP transport. No new framework code — the test exercises all shipped scope-support slices together.
- [2026-07-01] The test declares Album (with owner ref + collaborators map) and Photo (with album ref + owner ref), seeds data with shared/private albums, collaborators at different roles, and edge cases (orphan photos, dangling FKs), then asserts BOTH auth layers agree on every verdict: SQL scope correctly filters by album membership via typed-FK traversal, runtime mayVerb grants the correct capabilities per album role, null/dangling FKs fail closed, removing a collaborator revokes both layers, HTTP list/read correctly enforces the scope filter, and async declared checks using thenable ref traversal work for target scalar fields.
- [2026-07-01] New file: `test/google-photos-acceptance.test.mjs` (12 tests). Focused gate: `node --test test/google-photos-acceptance.test.mjs` → 12/12/0. Full gate: `node --test` → 852/852/0.
- [2026-07-01] Also committed: stale Google Photos project file comments updated (timeline route now uses shipped `.gte/.lte`, album listing compound query comment reflects shipped `.and()`).

## projected.async — stored computed field (post-commit projection) — DONE (worktree branch; 861/861/0)
- [2026-07-01] Implemented `projected.async({ compute })` as a stored computed field updated by a post-commit projection consumer (ADR #12, SPEC §5.3). The field compiles to a TEXT column (JSON-serialized), passes validation as readonly (client cannot set it), and the compute function runs after each create/update commit on the parent entity — reading the committed row, running the async compute, and writing the result back. Compute failures leave the column unchanged and never roll back the origin commit.
- [2026-07-01] `src/field.mjs`: `projected.async({ compute })` returns a `kind:'projected', mode:'async'` descriptor. `src/field-strategy.mjs`: `projected` strategy with validate/apply/diff/serialize/deserialize (JSON TEXT storage). `src/ddl.mjs`: projected columns included in main table DDL. `src/entity.mjs`: projected fields skipped in insert (computed not user-set), stored in `projectedAsyncFields` on entity record, deserialized from JSON like value fields. `src/serve.mjs`: post-commit consumer iterates created/updated events, refetches affected rows, runs compute, writes results.
- [2026-07-01] New file: `test/projected-async.test.mjs` (9 tests) covering constructor validation, strategy shape, JSON serialization, DDL generation, readonly rejection, post-commit compute after create/update, compute-failure-no-overwrite. Focused gate: 9/9/0. Full gate: 861/861/0.
- [2026-07-01] DEFERRED: `from` trigger filtering (only recompute on specific events), sequence watermarking + staleness contract, projected.inline (in-transaction cheap compute), staleness indicators on read. The MVP recomputes after every create/update of the parent entity.
