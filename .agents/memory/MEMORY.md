# Memory index

- [Memories live in-repo](README.md) — this dir (.agents/memory/) is the canonical store for ALL agents; Claude's memory space symlinks here; format + etiquette in README.md
- [Commit autonomously](commit-autonomously.md) — standing permission 2026-07-17: commit verified work without asking; stage explicitly by path, never `git add -A`/`commit -a` when other sessions share the tree; pushing still needs a nod
